# Phase 2.2: execute-task workflow + ralph-loop full integration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in `2.2-CONTEXT.md` — this log preserves alternatives considered.

**Date:** 2026-05-15
**Phase:** 2.2 — execute-task workflow + ralph-loop full integration
**Areas discussed:** SDK reconcile path / per-phase model tier scope / transparency CI gate flip range / ADR strategy
**Mode:** interactive (4 gray areas presented, all decided in one round — research refresh + intel + Phase 2.1 sister review 已 resolve 大量 HOW;剩下的是真灰色地带)
**External inputs:** `.planning/intel/omc-comparison.md`(§ 0 SSOT 引用纪律 + 第 4 条 per-phase model tier)+ `.planning/research/v0.2.0-execute-task-ralph.md`(HIGH conf)

---

## SDK reconcile path（gray area 1）

| Option | Description | Selected |
|--------|-------------|----------|
| 14 字段块 = factory-internal,SDK 4 字段块作物理 input + dual-signal completion | factory 产 14 字段 AgentDefinition;query() unpack 4 字段;其余 10 字段拼 prompt;completion = structured output PRIMARY + `<promise>` FALLBACK | ✓ |
| 卷回 contract 到 SDK 4 字段 | 报废 v1.1 14 字段;违反 phase 1.4 W-5 V1 BLOCKER 能力面 | |
| 推后 — 先 spike | spike 4 字段能否拼接补 10 字段能力 | |

**User's choice:** 14 字段块 = factory-internal,SDK 4 字段块作物理 input + dual-signal completion
**Notes:** research refresh § 1 实证 SDK AgentDefinition 真实只 4 字段(`description`/`prompt`/`tools?`/`model?`)。harnessed contract v1.1 14 字段保留作 factory 内部类型;调 SDK query() 前 unpack 4 字段作物理 input,其余 10 字段(initialPrompt/criticalSystemReminder_EXPERIMENTAL/memory/permissionMode 等)拼接进 prompt 上下文。completion 验证升级为 dual-signal:PRIMARY = SDK `outputFormat: { type: 'json_schema' }` `structured_output` field(消除 F33 summarize-risk),FALLBACK = `<promise>COMPLETE</promise>` extract(Phase 1.5/2.1 ship 的 promiseExtract.ts)。Phase 2.2 plan-phase 启动前先 spike `outputFormat` + `agents`-map 组合(research § 4 open question)+ `npm view @anthropic-ai/claude-agent-sdk version` 复核 pin(research valid until ~2026-06-15)。

---

## Per-phase model tier scope（gray area 2）

| Option | Description | Selected |
|--------|-------------|----------|
| intel 建议表 + override 逃生口 | phases schema 加 model 必填;默认 4-phase 表(opus-sonnet/sonnet/sonnet-haiku/haiku);`--model-tier inherit` CLI flag override | ✓ |
| 只加 optional `model:` 字段不预设表 | optional 字段;不预设默认;intel actionable 未全落地 | |
| 推后 Phase 2.3 评估 | Phase 2.2 只 ralph-loop,model tier 推 2.3 多 phase 示例足后再做 | |

**User's choice:** intel 建议表 + override 逃生口
**Notes:** 完整落地 intel actionable 第 4 条。phases schema 加 `model:` 必填字段;默认 4-phase 表(01-clarify=opus/sonnet,02-code=sonnet,03-test=sonnet/haiku,04-deliver=haiku — 04 是省 token 关键点);`--model-tier inherit` CLI flag 作 override 逃生口(用户场景多样)。namespace 与 GSD `/gsd-set-profile` 独立(GSD 管 GSD agent,harnessed 管 spawn 的 subagent)。agentFactory 读 `phase.model` 填 `AgentDefinition.model`(12→14 字段 contract 本就含 `model` — 零新引擎,intel "zero new engine" 理念符合)。

---

## Transparency CI gate flip + freshness ext（gray area 3,Wave 0 必办）

| Option | Description | Selected |
|--------|-------------|----------|
| 全史迁移 + freshness ext 一起做 | flip ENFORCE=true;迁全部 PLAN-CHECK/AUDIT/VERIFICATION;扩展 gate 覆盖 README/PROJECT-SPEC 状态节 freshness | ✓ |
| 只迁 phase 1.5+ verdict 文档、freshness ext 推 phase 2.3 | 历史 phase 1.1-1.4 archived 不动;freshness ext 推 Phase 2.3 避免超范围 | |
| 只 flip ENFORCE,不预动历史文档 | 只拦截新 commit;历史文档谁改谁修 | |

**User's choice:** 全史迁移 + freshness ext 一起做
**Notes:** 一次性根治 — Wave 0 任务批沿袭 Phase 2.1 Wave 0 模式,在 SDK/model tier 实装 wave 之前跑保 CI 不 red 后续 wave。三步:(a) flip `scripts/check-transparency-verdicts.mjs` 顶部 `ENFORCE = true`;(b) 全 `.planning/**/PLAN-CHECK.md` + `*-AUDIT.md` + `VERIFICATION.md` 加 marker 行(`Verdict:` / `状态:` / `Closure:` 行首 + N/N + miss: 声明),包括 phase 1.1-1.5 archived 文档(git tracked,gate 仍扫);(c) 扩展 CI gate 覆盖 README/PROJECT-SPEC 状态节 freshness check(reviewer item 21 洞察 — T1.9 把 SSOT 引用纪律提项目级但门面文档自身 stale 是同类反模式)。具体 freshness check 形态由 plan-phase 决(候选:scan 状态节是否含当前 ROADMAP 最新已 ship phase 编号)。

---

## ADR strategy（gray area 4）

| Option | Description | Selected |
|--------|-------------|----------|
| 独立 1 个 ADR 覆盖全部 | SDK + ralph-wiggum + dual-signal + contract v1.2 + model tier + Wave 0 transparency 都进同 ADR errata | ✓ |
| 拆 2 个 ADR | A = SDK/ralph-wiggum/completion;B = model tier schema errata | |
| discuss 不预设 — plan-phase 决拆分 | 视 SDK reconcile + model tier 决策动态拆 | |

**User's choice:** 独立 1 个 ADR 覆盖全部(编号 plan-phase 实占)
**Notes:** 沿袭 phase 1.4 ADR 0008 多决策合并 errata 模式(H1a perf transparency + M1 yaml path migration + R6 engineering category 推 phase 1.5 — 三决策同 ADR)。phase 2.2 ADR 同样多决策合并:SDK 引入 + ralph-wiggum keep-vs-switch + dual-signal completion + contract v1.2 reconcile + per-phase model tier schema errata + transparency CI gate flip + freshness ext。**ADR 编号由 Phase 2.2 plan-phase 实占**(沿袭 intel § 0 SSOT 引用纪律 + Phase 2.1 CONTEXT D-08 暂记 0011 仅参考,以 plan-phase 实际 git 顺序为准)。

## Claude's Discretion

- SDK reconcile spike 具体形态(`outputFormat` + `agents`-map 是否单 sample spike vs 直 30-sample)— planner/researcher 视 research valid 期评估
- transparency CI gate freshness ext 具体扫描形态 — planner 定(候选:简单 grep current ROADMAP latest phase vs 结构化 marker)
- `--model-tier inherit` 具体 CLI flag 命名 + override 优先级 — planner 定
- `model` 字段在 phases.yaml schema 位置(顶层 vs phase entry 内嵌)— planner 定
- 30 子任务 ralph-loop COMPLETE 测试样本选取标准 — planner 沿袭 phase 1.4 SAMPLES.md R3 frozen rationale

## Deferred Ideas

- intel 第 2 条 `resolved_routing` 快照冻结 → Phase 2.3+(真实 drift 出现后)
- intel 第 3 条 handoff 四字段 checkpoint 模板 → v0.3.0 checkpoint 完整版
- intel 愿景:动态 model routing → v0.3+ 评估
- design/content/testing extension category MVP → Phase 2.3
- doctor 完整版 + audit + ralph-loop Win 兼容 → Phase 2.4
- checkpoint 完整版 + harnessed resume → v0.3.0
- karpathy-skills behavior-rule 注入 + CLAUDE.md merge → Phase 2.3
- `--force` flag → phase 2.1+ 真实需求出现时
- gstack 前缀探测 + workflow 变量插值 → v0.3.0 P3.2

---

## Discuss-phase delta — 2026-05-15

> **Trigger**: intel file `.planning/intel/omc-comparison.md` 扩展到 3-project (OMC + omo + ECC) + 11 actionable + 2 vision after original discuss-phase. User 优先评估 5 ⭐⭐+ items (CD-5 / CD-2 已 covered as D-04 / CD-6 / EE-4 / CD-4)。delta 评估范围:仅锁决策 + scope,实施细节由各 Wave executor 自定。

### CD-5 schemaVersion 单一兼容门（delta gray area 5,⭐⭐⭐ 最高价值,源 ECC)

| Option | Description | Selected |
|--------|-------------|----------|
| FULL — 全 7 surface schemaVersion + consumer branch + unknown graceful + nested 新字段 | Wave 2 SDK reconcile 同步一次性引入 7 surface(routing snapshot / handoff doc / phases.yaml / manifest state / installer state / route decision log / checkpoint);consumer 必须 branch-on-version;未知 enum graceful degrade;新字段 nested | ✓ |
| MINIMAL — 仅 routing snapshot + phases.yaml 加 schemaVersion | 仅 2 surface 入门;其余推后 | |
| DEFER — 推 Phase 2.3 一并 evaluate | 等 extension category 后再统一定 schema 演进策略 | |

**User's choice:** FULL — 全 7 surface schemaVersion + consumer branch + 3 rules(unknown graceful / nested 新字段 / consumer branch)
**Notes:** intel CD-5 ⭐⭐⭐ — 影响所有跨 phase artifact 演进策略,**越晚做迁移成本越高**(ECC 实证)。harnessed 当前每次 schema errata(ADR 0003/0005/0007/0010)靠 ADR 编号追溯版本,无统一 `schemaVersion` 字段约定 → 跨边界 payload 演进时 consumer 不知道 branch on 什么。Wave 2 SDK contract v1.2 reconcile 同步引入(7 surface 一次性 typed)。命名约定 `harnessed.<surface>.v1`(e.g. `harnessed.routing-snapshot.v1`、`harnessed.phases.v1`)。**纯学不 vendor** — 引擎层算法结构,不引入 ECC 外部代码。ADR 0011 加 ### 7. SchemaVersion 章节;ASSUMPTIONS § B 加 B-32(7 surface 列表 + 3 rules)。实施细节(TypeBox schema vs convention-only doc)由 Wave 2 executor 决。

### CD-6 provenance gate（delta gray area 6,⭐⭐,源 ECC)

| Option | Description | Selected |
|--------|-------------|----------|
| BEFORE-W4 hard gate — provenance.schema.json + composition/installer enforce | Wave 4 ralph-loop integration 启动前必上;runtime artifact 必须 sibling `.provenance.json`;否则 hard fail | ✓ |
| WARN-ONLY — gate scan + warning,不 hard fail | 不阻塞 commit;开发者自觉补 provenance | |
| DEFER — 推 Phase 2.4 一起做 audit | 等 doctor 完整版有 audit 时一起加 | |

**User's choice:** BEFORE-W4 hard gate — ~5KB provenance.schema.json + 4 字段(source/created_at/confidence/author)+ hard fail enforcement
**Notes:** intel CD-6 ⭐⭐ — ECC `docs/SKILL-PLACEMENT-POLICY.md` 实证 "Curated vs Learned/Imported/Evolved" 产源分层 + provenance gate 彻底隔离。**为什么 BEFORE-W4 是 hard gate**:Wave 4 ralph-loop 真实 spawn 一启动即产 runtime artifact 洪流(routing 决策日志 / handoff doc / failed-route 记录 / checkpoint 修复 / session learnings)— 之前不上 gate = 之后追溯 curated 装配上游 vs runtime 生成物污染极痛(ECC 实证)。**WARN-ONLY 不够**:开发者会忘记 → 污染累积;hard fail 才是真隔离。**DEFER 不可**:Wave 4 一启动就晚了。具体形态(strict required vs partial JSON schema)由 Wave 4 executor (T4.0) 决;ADR 0011 加 ### 8. Provenance gate 章节;ASSUMPTIONS § B 加 B-33(schema 定义)+ B-34(enforce path)。

### EE-4 plan 4 维量化阈值（delta gray area 7,⭐⭐,源 omo)

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 2.2 实装 4 维 schema + gsd-plan-checker 改造 | 本 phase scope 扩展;schema errata ADR | |
| DEFER-2.4 — Phase 2.4 doctor 完整版 absorb OR 独立 Phase 2.5 | 与 Phase 2.2 execute-task 主线 orthogonal;本 phase 不动 ADR/代码 | ✓ |
| DEFER-v0.3+ — 推 v0.3.0 一并评估 | 推得更远;失去时效 | |

**User's choice:** DEFER-2.4 — Phase 2.4 doctor 完整版 absorb OR 独立 phase 2.5
**Notes:** intel EE-4 ⭐⭐ — omo `src/agents/metis.ts` + `momus.ts` 双 agent 链:Metis 前置 gap 分析 + Momus 后置 4 维硬标准(100% file refs verified / ≥80% reference sources / ≥90% concrete acceptance / 0 business-logic assumption)+ 无限 REJECT loop。harnessed `gsd-plan-checker` verdict 是模糊三档(APPROVED / APPROVED WITH CONDITIONS / NEEDS REVISION),omo 量化阈值消除灰色地带。**为什么 DEFER-2.4**:与 Phase 2.2 execute-task 主线 orthogonal(gsd-plan-checker schema 改造,需起 schema errata ADR,编号由对应 phase plan-phase 分配)— 强行塞 Phase 2.2 会扩 scope 超过 KICKOFF F1-F8 边界。**关键约束**:**不 vendor Momus 的希腊神话 prompt**(omo 叙事强耦合)— 只借 4 维 schema 格式。Phase 2.2 CONTEXT § Deferred 加 EE-4 项(不分配 D 编号,defer 项 convention)。

### CD-4 Task Session 复用（delta gray area 8,⭐⭐,源 omo)

| Option | Description | Selected |
|--------|-------------|----------|
| PIGGY-W1 — Wave 1 SDK spike 加 SC4 verify session resume API;能则 Wave 4 集成 task_session_id,不能则 deferred | Wave 1 spike 边际成本接近零(本就跑 SDK probe);conditional branch 清晰 | ✓ |
| FULL-W2 — 不 verify,直接 phase manifest schema 加 task_session_id + Wave 4 实装 | 风险:SDK 不暴露则白干 | |
| DEFER-v0.3 — 推 checkpoint 完整版一起做 | 失去 Phase 2.2 ralph-loop 真实闭合的现成机会 | |

**User's choice:** PIGGY-W1 — Wave 1 SDK spike 加 SC4 verify;conditional on SDK capability
**Notes:** intel CD-4 ⭐⭐ — omo `BoulderState.task_sessions[task_key] = { session_id, agent, category, ... }` 同一 top-level task 多次 iteration 必须复用同一 subagent session(in-memory → 跨进程持久化)。harnessed execute-task workflow phase 03 (test) 失败回炉到 phase 02 (code) 时,复用同 session 比开新 session 上下文好得多。**为什么 PIGGY-W1**:Wave 1 SDK spike (T1.1/T1.2) 本就要跑 SDK probe,加 1 项 SC4(verify `@anthropic-ai/claude-agent-sdk` 是否暴露 session resume API)边际成本接近零。**Conditional branch**:① SC4 pass → phase manifest 加 `task_session_id?: string` optional + Wave 4 executor resume 一起做;② SC4 fail → CD-4 加入 deferred(v0.3.0 checkpoint 完整版),Phase 2.2 不实装、不动 schema。ADR 0011 加 ### 9. Task Session 复用 章节(decision: conditional on SDK verify outcome);ASSUMPTIONS § B 加 B-35(verify branch)+ B-36(conditional schema)。
