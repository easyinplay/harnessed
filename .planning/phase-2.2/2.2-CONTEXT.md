# Phase 2.2: execute-task workflow + ralph-loop full integration - Context

**Gathered:** 2026-05-15
**Status:** Ready for planning
**Discuss mode:** interactive (4 gray areas, all decided)

<domain>
## Phase Boundary

把 v0.1.0 的 routing engine v1(main-process-driven query→arbitrate→install missing→factory→spawn→ralph-loop→verbatim COMPLETE 闭合)+ Phase 2.1 的 6 install method runtime-ready 升级为**真实 spawn subagent + ralph-loop 完整闭合**。实装 execute-task workflow 主线 4 phase chain(brainstorming → karpathy 心法 always-on → mattpocock 招式按需召唤 → conditional TDD → ralph-loop 交付 COMPLETE),每子任务 verbatim COMPLETE 回流主流程。

**Wave 0 必办**:从 Phase 2.1 deferred 继承的 transparency CI gate `ENFORCE` flip 为 `true` + 全史 verdict 文档迁移到结构化 marker + reviewer 洞察的 CI gate freshness check 扩展(README/PROJECT-SPEC 状态节)— 一次性根治。

**不在本 phase**:
- design/content/testing extension category MVP — Phase 2.3
- doctor 完整版 + audit + ralph-loop Win 兼容(jq/Git Bash 探测/upstream_health weekly cron)— Phase 2.4
- checkpoint 完整版(harnessed resume + compact 协议)— v0.3.0
- 动态 model routing(intel 第 4 条愿景候选,signal→rules→scorer 三段式)— v0.3+ 评估
- resolved_routing 快照冻结(intel 第 2 条候选)— 推 Phase 2.3 evaluation(真实 mid-session drift 出现后,karpathy YAGNI)
- handoff 四字段 checkpoint 模板(intel 第 3 条)— 推 v0.3.0 checkpoint 完整版

</domain>

<decisions>
## Implementation Decisions

### SDK reconcile path（gray area 1)
- **D-01:** **14 字段 = factory-internal,SDK 4 字段作 query() 物理 input + dual-signal completion**。`agentFactory()` 产 14 字段 `AgentDefinition`(类型保持 harnessed contract);调 SDK `query()` 前 unpack 4 字段(`description` / `prompt` / `tools` / `model`)作物理 input;其余 10 字段(`initialPrompt` / `criticalSystemReminder_EXPERIMENTAL` / `memory` / `permissionMode` 等)拼接进 prompt 上下文(通过 SystemPrompt template inject)。
- **D-02:** completion 验证 = **dual-signal**:
  - **PRIMARY** = SDK `outputFormat: { type: 'json_schema' }` → typed `structured_output` field on `SDKResultMessage`(结构性消除 phase 1.4 F33 summarize-risk;research § 4 实证)
  - **FALLBACK** = `<promise>COMPLETE</promise>` XML wrapper extract(Phase 1.5/2.1 已 ship 的 `promiseExtract.ts`)
  - Phase 2.2 spike 验证 `outputFormat` + `agents`-map 组合可行性(research § 4 open question)
- **D-03:** SDK reconcile 流程:Phase 2.2 plan-phase 启动前 **first action** = `npm view @anthropic-ai/claude-agent-sdk version`(确认仍 0.2.141 vs 已升,research valid until ~2026-06-15)+ 对照 installed `.d.ts` 重验 14→4 字段映射;产生的 contract v1.2 reconcile 走 ADR errata(本 phase ADR — gray area 4 D-09 锁单一 ADR 覆盖)。

### Per-phase model tier（gray area 2)
- **D-04:** **intel 第 4 条建议表 + override 逃生口**(intel actionable 完整落地):
  - `workflows/<name>/phases.yaml` schema 加 `model:` **必填字段**(execute-task 主流程,phase 静态标 model)
  - 4 phase 默认表(intel 建议):
    | phase | 上游 | model |
    |-------|------|-------|
    | 01-clarify | superpowers brainstorming | `opus` / `sonnet`(任务复杂度) |
    | 02-code | karpathy 心法 always-on | `sonnet` |
    | 03-test | superpowers TDD | `sonnet` / `haiku` |
    | 04-deliver | ralph-loop | **`haiku`**(迭代验收循环省 token) |
  - `--model-tier inherit` CLI flag override 逃生口(`harnessed execute-task <name> --model-tier inherit` → 不读 phase.model,继承调用方 model)— v0.2 静态标 model 跑通后,如有动态 model routing 升级需求(intel 愿景候选)再 v0.3+ 加 dynamic resolver
- **D-05:** 不与 GSD `/gsd-set-profile` 冲突:**GSD profile** 管 GSD orchestration agent;**harnessed model tier** 管 harnessed spawn 的 execute-task subagent。两者 namespace 独立,文档明示。
- **D-06:** **agentFactory 读 `phase.model`** 填入 `AgentDefinition.model`(12→14 字段时已含 `model` — 零新引擎,intel 第 4 条"zero new engine"理念符合)

### Transparency CI gate flip + freshness ext（gray area 3,Wave 0 必办)
- **D-07:** **全史迁移 + freshness ext 一起做**(一次性根治,Wave 0 任务批):
  - **a)** `scripts/check-transparency-verdicts.mjs` 顶部 const `ENFORCE = true`(Phase 2.1 T1.7 W3 lock 解除)
  - **b)** **全史 verdict 文档迁移**:`.planning/**/PLAN-CHECK.md` + `.planning/**/*-AUDIT.md` + `.planning/**/VERIFICATION.md` 全部检查 + 修复 → 任何 closure verdict 必须写成 marker 行(`Verdict:` / `状态:` / `Closure:` 行首 + `\d+/\d+` ratio + `miss:` 声明)。包括 phase 1.1-1.5 历史文档(虽然 archived 但 git tracked,gate 仍扫)。
  - **c)** **reviewer 洞察的 freshness ext**:扩展 CI gate 覆盖 README/PROJECT-SPEC 状态节 freshness check(reviewer item 21 — T1.9 把 SSOT 引用纪律提项目级但门面文档自身 stale,同一反模式未被原 gate 覆盖)。具体形态由 Phase 2.2 plan-phase 决定(候选:scan README/PROJECT-SPEC 状态节 + 必须出现当前 ROADMAP 最新已 ship phase 编号 → 若缺则 fail)。
- **D-08:** Wave 0 任务批 = transparency flip + 全史迁移 + freshness ext(沿袭 Phase 2.1 Wave 0 模式,在 SDK/model tier 实装 wave 之前跑,保 CI 不 red 后续 wave)。

### ADR strategy（gray area 4)
- **D-09:** **独立 1 个 ADR 覆盖全部** Phase 2.2 决策:
  - SDK 引入 + ralph-wiggum keep-vs-switch
  - dual-signal completion(structured output + `<promise>` fallback)
  - contract v1.2 reconcile(14 字段 factory-internal + 4 字段 SDK input)
  - per-phase model tier schema errata(phases schema `model:` 字段)
  - transparency CI gate flip + freshness ext(Wave 0 章节)
- **D-10:** **ADR 编号由 Phase 2.2 plan-phase 实占**(沿袭 intel § 0 SSOT 引用纪律 + Phase 2.1 CONTEXT D-08 暂记 0011 仅参考,以 plan-phase 实际 git mv / new file 顺序为准)。本 CONTEXT.md / 后续 KICKOFF/PLAN 一律写"ADR errata(编号 plan-phase 分配)"。

### Carry-forward（research refresh + Phase 2.1 sister review 锁定,不重新讨论)
- **D-11:** **SDK `@anthropic-ai/claude-agent-sdk` 0.2.141 INTRODUCE NOW** — research refresh D1.5-5 closed;真实 spawn subagent + completion 验证 dual-signal 都被它 hard-block;phase-1.5 deferral 触发条件已满足
- **D-12:** **不切换** ralph-wiggum 官方 plugin(R6 closed)— 官方 plugin 是 interactive-TUI-Stop-hook-only,无 headless,jq 依赖撞 Windows 红旗;**自实装 `ralphLoopWrap` 是永久架构**(非 stopgap)
- **D-13:** **intel § 0 SSOT 引用纪律**(项目级,Phase 2.1 T1.9 CONTRIBUTING.md 已固化)— ADR 编号不预占;phase 编号语义锚定;取用前比对 ROADMAP/docs/adr/ 实际编号
- **D-14:** execute-task workflow 4 phase 模型(CLAUDE.md WORKFLOWS § 2 + ROADMAP v0.2.0 Phase 2.2 验收 1):brainstorming → karpathy 心法 always-on → mattpocock 招式按需召唤 → conditional TDD → ralph-loop 交付 COMPLETE
- **D-15:** A7 守恒持续:ADR 0001-0010 main body 0 diff;本 phase 新 ADR 走 errata 路径(不动旧 ADR);ship 时 baseline tag +1(10→11);CI A7 step iter 1-10 → 1-11

### Claude's Discretion
- SDK reconcile spike 的具体形态(D-03):`outputFormat` + `agents`-map 组合是否需要单 sample spike vs 直接做 30-sample acceptance(planner/researcher 视 research valid 期评估)
- transparency CI gate freshness ext(D-07c)具体扫描形态:简单 grep 当前 ROADMAP latest-shipped phase 是否出现 vs 更结构化 marker convention(planner 决,reviewer 倾向后者)
- per-phase model tier `--model-tier inherit`(D-04)具体 CLI flag 命名 + override 优先级(planner 定)
- `model` 字段在 phases.yaml schema 的位置(顶层 vs phase entry 内嵌)— planner 定
- 30 子任务 ralph-loop COMPLETE 测试样本(ROADMAP Phase 2.2 验收 1)选取标准 — planner 沿袭 phase 1.4 SAMPLES.md selection rationale(R3 frozen)

### Folded Todos
None — 无 pending todos 跨 phase reference 进 Phase 2.2 scope。
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 2.2 核心实装依据
- `.planning/research/v0.2.0-execute-task-ralph.md` — **HIGH conf 主依据** — SDK 0.2.141 INTRODUCE 决议 + ralph-wiggum keep-vs-switch + dual-signal completion 设计 + AgentDefinition 4-vs-14 字段 gap 实证
- `.planning/intel/omc-comparison.md` — **per-phase model tier 主依据**(第 4 条 — phases schema 加 `model:` 字段 + intel 建议表)+ § 0 SSOT 引用纪律
- `.planning/phase-2.1/2.1-CONTEXT.md` — D-08 ADR 拆分约定(0010 = Phase 2.1 schema errata;0011 暂记 Phase 2.2 仅参考,以 plan-phase 实占为准)

### Phase 2.2 Wave 0 必办来源
- `.planning/phase-2.1/task_plan.md` T1.7 — transparency CI gate W3 lock(`ENFORCE = false` round 1,Phase 2.2 flip)
- `docs/TRANSPARENCY-VERDICT-CHECKLIST.md` — marker 约定(Phase 2.1 T1.7 ship)
- `scripts/check-transparency-verdicts.mjs` — CI gate 实装(Phase 2.1 T1.7 ship)
- `.planning/STATE.md` item 19 + item 21 — Wave 0 必办(flip)+ reviewer 洞察 freshness ext 候选

### v0.1.0 ship 接口契约(Phase 2.2 直接消费 — frozen)
- `src/routing/engine.ts` 199L — main-process-driven routing engine(Pattern N — 待升级接 SDK `query()` + ralph-loop wrap)
- `src/routing/agentDefinition.ts` 191L — `AgentDefinition` v1.1 14 字段(待 SDK reconcile → contract v1.2)
- `src/routing/systemPrompt.ts` 53L — verbatim COMPLETE marker(待 dual-signal 升级 — structured output schema inject)
- `src/routing/lib/ralphLoop.ts` 42L — `ralphLoopWrap` ≤50L wedge(D1.4-3 lock,自实装永久架构 — D-12)
- `src/routing/lib/promiseExtract.ts` 32L — `<promise>COMPLETE</promise>` XML wrapper extract(dual-signal FALLBACK)
- `src/routing/decisionRules.ts` — arbitrate(decision_rules.yaml v2 routing,phase 1.5 ship)
- `src/installers/index.ts` — 6 install method dispatch table(Phase 2.1 ship — execute-task workflow `harnessed install` 调用)
- `docs/AGENT-DEFINITION-FACTORY-CONTRACT.md` 217L+ — contract v1.1 14 字段 main body(A7 守恒 不动,reconcile 走 ADR errata)
- `docs/INSTALLER-CONTRACT.md` — ADR 0004 6 契约(execute-task 调 installer 时遵守)

### Schema 资源
- `routing/decision_rules.yaml` v2 `mattpocock_phases:` 段 — 4 phase × 21 unique skills × 23 trigger entry(Phase 1.5 ship — execute-task workflow phase routing 消费)
- `src/manifest/schema/spec.ts` — TypeBox manifest schema(待加 `phases:` workflow schema + `model:` 字段)

### 里程碑上下文
- `.planning/ROADMAP.md` § v0.2.0 Phase 2.2 — Goal + 必含项 7 项 + 验收标准 5 项 + 关键风险
- `.planning/v0.1.0-MILESTONE-AUDIT.md` — R6.1-R6.5 execute-task requirements coverage status
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **Routing engine (Phase 1.4 ship + 1.5 升级)**:`src/routing/engine.ts` 199L Pattern N 主流程编排,Phase 2.2 升级路径明确(SDK `query()` 接入 + ralph-loop wrap 主流程调用)
- **AgentDefinition factory (Phase 1.4 ship + 1.5 升级 + 2.1 H3 errata)**:`agentDefinition.ts` 191L 14 字段;Phase 2.2 D-01 工作 = factory 产 14 字段类型不变 + query() 调用前 unpack 4 字段
- **ralph-loop wedge (Phase 1.4 ship + 1.5 split)**:`ralphLoop.ts` 42L `ralphLoopWrap` 永久架构(D-12);Phase 2.2 升级 = 接入 SDK spawn + dual-signal isComplete
- **Promise extract (Phase 1.5)**:`promiseExtract.ts` 32L `<promise>([^<]+)</promise>` regex — dual-signal FALLBACK
- **System prompt (Phase 1.4/1.5 ship)**:`systemPrompt.ts` 53L `<promise>COMPLETE</promise>` XML wrapper — Phase 2.2 升级加 structured output schema 注入(dual-signal PRIMARY 之 prompt 侧约束)
- **Decision rules (Phase 1.3-1.5 ship)**:`decisionRules.ts` arbitrate + `decision_rules.yaml` v2 含 `mattpocock_phases:` schema — execute-task workflow 4 phase routing 直接消费
- **6 installer (Phase 2.1 ship)**:dispatch table 全 runtime-ready — execute-task `harnessed install <name>` 调用链验证完整
- **transparency infra (Phase 2.1 T1.7 ship)**:`docs/TRANSPARENCY-VERDICT-CHECKLIST.md` + `scripts/check-transparency-verdicts.mjs` + CI step — Phase 2.2 Wave 0 仅需 flip ENFORCE + 全史迁移 + freshness ext

### Established Patterns
- **A7 conservation + errata-only ADR path** — Phase 1.1-2.1 9+ phase 一致践行,Phase 2.2 单一 ADR(D-09)走 errata,A7 baseline tag iterate 1-10 → 1-11
- **TypeBox schema (`@sinclair/typebox`)** — 所有 schema 改动用 `Type.*`,沿袭 phase 1.5/2.1 ADR 0009/0010 errata 注释块 fence 模式
- **Karpathy hard limits** — engine ≤200L,自实装 ralphLoopWrap ≤50L,systemPrompt ≤80L 等;Phase 2.2 升级时严守(可能需要 spillover 到 lib/)
- **Anti-stall executor protocol** — gsd-* agent 易 stall;Phase 2.2 plan-phase Wave A/B/C 沿袭 phase 1.5/2.1 anti-stall 约束(read files limited + immediate Write)
- **Sister review tiering** — Phase 2.2 ship 后预期 sister review(reviewer 已建立连续追溯,phase 1.4 T1 / 1.5 H1+M1 / 2.1 H1+H2 全闭环 — Phase 2.2 sister review 是连续性追溯)

### Integration Points
- **execute-task workflow 主流程入口** — CLI 注册新 register fn `registerExecuteTask`(沿袭 phase 1.4 `registerResearch` 9th register fn 模式)
- **agentFactory → SDK `query()`** — `src/routing/engine.ts` 内 `runRouting` 末尾 spawn 逻辑替换为 SDK 真实调用
- **ralph-loop full integration** — `ralphLoopWrap` 现以 stub spawn fn 接入;Phase 2.2 替换为 SDK `query()` callback
- **phases.yaml workflow schema** — 新增(workflow 层 schema,与 manifest schema spec.ts 并列);所在位置 planner 决(可能 `src/workflow/schema/phases.ts` 新文件 vs 折叠进 manifest spec)
- **CI A7 step** — ci.yml iter 1-10 → 1-11(Phase 2.2 ship 加新 ADR 时)
- **CI transparency gate flip** — `scripts/check-transparency-verdicts.mjs` `ENFORCE = false` → `true`(Wave 0 一次性根治)
- **CI freshness check** — 新 step(或扩展现有 transparency gate)扫 README/PROJECT-SPEC 状态节(D-07c)
</code_context>

<specifics>
## Specific Ideas

- **dual-signal completion 的 PRIMARY** = SDK `outputFormat: { type: 'json_schema' }` `structured_output` field(research § 4 实证)— structurally 消除 phase 1.4 F33 summarize-risk;**FALLBACK** = `<promise>COMPLETE</promise>` 兼容(degraded 但 functional);phase 2.2 spike `outputFormat` + `agents`-map 组合
- **per-phase model tier 默认表的 model 选择**(intel 第 4 条)— 04-deliver=haiku 是关键省 token 点(迭代验收循环);01-clarify=opus/sonnet 任务复杂度决定
- **transparency 全史迁移**(D-07b)涉及历史 archived 文档(phase 1.1-1.5 PLAN-CHECK/VERIFICATION 等)— 迁移粒度:每文档加 1 行 `Verdict: PASSED/SHIPPED (N/N, miss: none)` 即可,不重写 prose
- **ADR 单独 1 个**(D-09)— 沿袭 phase 1.4 ADR 0008 多决策合并 errata 模式(H1a perf + M1 yaml path + R6 engineering category 三决策同 ADR);phase 2.2 ADR 同样多决策合并
</specifics>

<deferred>
## Deferred Ideas

- **intel 第 2 条 `resolved_routing` 快照冻结** → Phase 2.3+ 真实 mid-session drift 出现后再评估;karpathy YAGNI 不提前做(arbitrate 当前每次重读 decision_rules.yaml,短 session 无 drift)
- **intel 第 3 条 handoff 四字段 checkpoint 模板** → v0.3.0 checkpoint 完整版(Decided / Rejected / Risks / Files 进 `.harnessed/checkpoints/` 模板)
- **intel 愿景:动态 model routing** → v0.3+ 评估(signal→rules→scorer 三段式;static model tier 跑通后再考虑升级)
- **design/content/testing extension category MVP** → Phase 2.3(本 phase 只做 execute-task 主线,不做 extension 实装)
- **doctor 完整版 + audit + ralph-loop Win 兼容**(jq/Git Bash 探测/upstream_health weekly cron/audit log/git origin URL 校验) → Phase 2.4
- **checkpoint 完整版(`harnessed resume` + compact 协议)** → v0.3.0 P3.1
- **karpathy-skills behavior-rule 注入 + CLAUDE.md merge** → Phase 2.3(extension category 时一起做)
- **`--force` flag for install idempotent_check** → phase 2.1+ deferred,evaluate when真实需求出现
- **gstack 前缀探测 + workflow 变量插值** → v0.3.0 P3.2

### Reviewed Todos (not folded)
None — 无 pending todos cross-reference 到本 phase。

</deferred>

---

*Phase: 2.2 — execute-task workflow + ralph-loop full integration*
*Context gathered: 2026-05-15*
