# Phase 4.3: R8.1 audit log + R8.4 ADR backfill + v1.0-RC + v0.4.0 close — Pattern Map

**Mapped:** 2026-05-19
**Files analyzed:** 22 NEW / MODIFY targets (Wave 0 carry + Wave 1 R8.1 NEW infra + Wave 2 R8.4 + CHANGELOG + milestone close)
**Analogs found:** 21 / 22 (CHANGELOG.md = first instance, partial analog only)
**Style:** sister Phase 4.2 PATTERNS.md (§ 1 table + § 2 concrete excerpts per critical file + § 3 cross-cutting + § 4 no-analog list)

---

## § 1 NEW / MODIFY Targets → Existing Analog Mapping

| # | Target | Role | Data Flow | Closest Analog | Match Quality |
|---|--------|------|-----------|----------------|---------------|
| **Wave 0 — carry-forward absorb** |||||
| 1 | `.planning/STATE.md` W0.1 D2 cadence iter 4 trim (Phase 4.2 narrative → RETROSPECTIVE.md) | docs / process | declarative | `.planning/STATE.md` current + Phase 4.2 W0.1 D2 iter 3 archive pattern (sister `## § ARCHIVED FROM STATE — Phase 4.0+4.1` section header cadence) | exact |
| 2 | `.planning/RETROSPECTIVE.md` receive Phase 4.2 D2 archive + Phase 4.3 retro entry | docs / process | declarative | `.planning/RETROSPECTIVE.md` existing `## § ARCHIVED FROM STATE` sections + Phase 4.2 W2 T2.2 7-section retro format | exact |
| 3 | `scripts/check-state-archive-stale.mjs` SIZE_LIMIT 200→175 flip (conditional) | CI gate / utility | batch (1-line config flip) | `scripts/check-state-archive-stale.mjs` L12 `SIZE_LIMIT = 200` current value + Phase 4.1 W0.5 / Phase 4.2 W0.2 conditional flip DEFER precedent | exact |
| **Wave 0 — W0 backlog extracts (#BF/#BG/#BH/#BI/#BJ/#BK)** |||||
| 4 | `src/installers/lib/runClaudeArgs.ts` NEW (#BF — runArgs 3-installer duplicate extract) | utility / lib | transform | `src/checkpoint/engineHook.ts` (49L PRIMARY helper extract pattern — single-responsibility extract from larger module) + `src/cli/lib/check-token-budget.ts` (48L 4th PRIMARY helper family member) | role-match |
| 5 | `src/installers/lib/err.ts` NEW (#BG — err() 8-file duplicate extract) | utility / lib | transform | `src/checkpoint/engineHook.ts` (49L; same PRIMARY helper extract ≤50L discipline) | role-match |
| **Wave 1 — R8.1 NEW infrastructure** |||||
| 6 | `src/audit/log.ts` NEW — JSONL append-only writer + AuditRecordSchema + TypeBox validation (≤80L) | service / module | file-I/O (append-only write) | `src/checkpoint/state.ts` (77L; writeCurrentWorkflow async mkdir + writeFile pattern; Value.Check validate-then-write discipline; `@sinclair/typebox/value` import) | role-match |
| 7 | `src/audit/hook.ts` NEW — thin engine integration wrapper emitAudit() (≤50L) | middleware / bridge | request-response | `src/checkpoint/engineHook.ts` (49L; PRIMARY helper extract pattern; interface def + 2 exported async functions; single-responsibility engine bridge) | exact |
| 8 | `src/routing/engine.ts` MODIFY — add import + emitAudit calls (success + 3 error paths); surgical 2L shrink L120-122 first | controller / orchestrator | request-response | `src/routing/engine.ts` itself (200L; existing import block L11-34; activatePhase/completePhase hook integration L164+L189 as insertion cadence analog) | self-modify |
| 9 | `tests/audit/log.test.ts` NEW — 7 fixture cells (schema + append + 11-field + sha1 + excerpt + outcome) | test | file-I/O mock | `tests/checkpoint/state.test.ts` (98L; vi.mock node:fs/promises pattern; Map-based fsState in-memory; beforeEach clear + afterEach clearAllMocks; describe/it structure) | exact |
| 10 | `tests/audit/hook.test.ts` NEW — hook integration tests | test | request-response mock | `tests/checkpoint/state.test.ts` (vi.mock pattern) + `tests/checkpoint/sdk-wire.test.ts` (engine integration test structure) | role-match |
| 11 | `docs/adr/0018-routing-audit-log.md` NEW — PRIMARY ADR 9-section errata format | docs (ADR) | declarative | `docs/adr/0017-routing-hit-rate-token-budget.md` (204L; 9-section errata format: Status + Context + Decisions + A7 Conservation + Consequences + Compliance + Errata-path + Adoption-confirmation + References) | exact |
| **Wave 2 — R8.4 ADR backfill + CHANGELOG + milestone close** |||||
| 12 | `docs/adr/0019-state-dual-ssot-collapse-pattern.md` NEW (backfill Phase 3.3 D-04) | docs (ADR backfill) | declarative | `docs/adr/0017-routing-hit-rate-token-budget.md` (9-section format reference) — but use SIMPLIFIED 5-section format (status + context + decision + consequences + references) per RESEARCH § 3.3 | role-match |
| 13 | `docs/adr/0020-hybrid-2clock-disambiguation.md` NEW (backfill Phase 4.2 D-04) | docs (ADR backfill) | declarative | Same simplified 5-section format as 0019; source = `.planning/phase-4.2/4.2-CONTEXT.md` D-04 | role-match |
| 14 | `docs/adr/README.md` MODIFY — add index entries 0018-0020 (+ 0009-0017 gap catchup optional) | docs (index) | declarative | `docs/adr/README.md` existing table rows (pipe-delimited `| [NNNN](./NNNN-title.md) | Title | Accepted | date |` format) | exact |
| 15 | `CHANGELOG.md` NEW — Keep-a-Changelog format init for v0.4.0 (~30L) | docs (changelog) | declarative | No exact project analog (first CHANGELOG); partial: `docs/adr/0017-routing-hit-rate-token-budget.md` header pattern (status + date convention) | partial |
| 16 | `.github/workflows/ci.yml` MODIFY — A7 step iter 0017 → 0018 (both loops + step name + echo L82-107) | CI config | batch | `.github/workflows/ci.yml` L82-107 current A7 step (both `for n in` loops + step name literal + echo final line — 4 surgical edits) | self-modify |
| 17 | `.planning/STATE.md` W2.5 Phase 4.3 SHIPPED narrative + 当前位置 update | docs / process | declarative | `.planning/STATE.md` Phase 4.2 SHIPPED entry pattern (sister Phase 4.2 W2 T2.1 mechanical replicate with content swap) | exact |
| 18 | `.planning/ROADMAP.md` Phase 4.3 ✅ SHIPPED + v0.4.0 3/3 ARCHIVED close | docs / process | declarative | `.planning/ROADMAP.md` Phase 4.2 ✅ SHIPPED entry + v0.3.0 L130 SHIPPED ARCHIVED milestone close marker pattern | exact |
| 19 | `.planning/milestones/v0.4.0-ROADMAP.md` NEW — v0.4.0 chapter snapshot | docs (milestone archive) | declarative | `.planning/milestones/v0.3.0-ROADMAP.md` (header format: `# Milestone vX.Y.Z: [name]` + `**Status:** 🎯 SHIPPED & ARCHIVED` + `**Phases:** ...` + `**Git range:** ...` + `**Milestone tags:** ...` + `**Audit:** ...` → `## Overview` + `## Phases`) | exact |
| 20 | `.planning/milestones/v0.4.0-REQUIREMENTS.md` NEW — v0.4.0 requirements snapshot | docs (milestone archive) | declarative | `.planning/milestones/v0.3.0-REQUIREMENTS.md` (sister snapshot format — copy v0.4.0 chapter from REQUIREMENTS.md, mark frozen) | exact |
| 21 | `.planning/milestones/v0.4.0-MILESTONE-AUDIT.md` NEW — audit with R-5 mitigation section (#BO) | docs (milestone audit) | declarative | `.planning/milestones/v0.3.0-MILESTONE-AUDIT.md` (YAML front-matter + 6 § body sections + § 6 Verdict; v0.4.0 adds § 7 R-5 mitigation "Cadence patterns" section at bottom) | exact |

---

## § 2 Per-Target Concrete Excerpts (critical Wave 1 files + shared patterns)

### 2.1 `src/audit/log.ts` (service / file-I/O, ≤80L)

**Analog:** `src/checkpoint/state.ts` (77L) + `src/manifest/schema/aliases.v1.ts` (34L)

**Imports pattern** — from `src/checkpoint/state.ts` lines 1-9 + `src/manifest/schema/aliases.v1.ts` lines 10-11:
```typescript
import { appendFileSync, mkdirSync } from 'node:fs'
import { dirname } from 'node:path'
import { createHash } from 'node:crypto'
import { type Static, Type } from '@sinclair/typebox'
import { Value } from '@sinclair/typebox/value'
```

**TypeBox schema pattern** — from `src/manifest/schema/aliases.v1.ts` lines 13-30:
```typescript
// Pattern: Type.Object({ fields }, { additionalProperties: false }) + Static<typeof X>
export const AliasEntryV1 = Type.Object(
  {
    redirect: Type.String({ minLength: 1 }),
    removal_date: Type.Optional(Type.String({ pattern: '^\\d{4}-\\d{2}-\\d{2}$' })),
  },
  { additionalProperties: false },
)
export type AliasEntryV1Type = Static<typeof AliasEntryV1>
```

**Apply to `src/audit/log.ts`** — AuditRecordSchema using same pattern:
```typescript
export const AuditRecordSchema = Type.Object(
  {
    ts: Type.String(),
    phase: Type.String(),
    task_excerpt: Type.String(),
    task_sha1: Type.String(),
    matched_rule_id: Type.Union([Type.String(), Type.Null()]),
    primary_expert: Type.Union([Type.String(), Type.Null()]),
    secondary_expert: Type.Union([Type.String(), Type.Null()]),
    category: Type.String(),
    route_layer: Type.String(),
    outcome: Type.String(),
    session_id: Type.Union([Type.String(), Type.Null()]),
    iter_count: Type.Union([Type.Number(), Type.Null()]),
  },
  { additionalProperties: false },
)
export type AuditRecord = Static<typeof AuditRecordSchema>
```

**mkdir + write pattern** — from `src/checkpoint/state.ts` lines 44-51:
```typescript
export async function writeCurrentWorkflow(s: CurrentWorkflowV1Type): Promise<void> {
  if (!Value.Check(CurrentWorkflowV1, s)) {
    const errs = [...Value.Errors(CurrentWorkflowV1, s)].map((e) => e.message).join('; ')
    throw new WorkflowStateError(`current-workflow schema validation failed: ${errs}`)
  }
  await mkdir(dirname(STATE_PATH), { recursive: true })
  await writeFile(STATE_PATH, JSON.stringify(s, null, 2), 'utf8')
}
```

**Adapted for `src/audit/log.ts`** — sync append (RESEARCH § 2.3 rationale: atomic O_APPEND for small writes):
```typescript
const AUDIT_PATH = '.harnessed/audit.log'

export function emitAuditRecord(record: AuditRecord): void {
  mkdirSync(dirname(AUDIT_PATH), { recursive: true })
  appendFileSync(AUDIT_PATH, JSON.stringify(record) + '\n')
}

export function buildAuditRecord(/* params */): AuditRecord {
  const task_sha1 = createHash('sha1').update(task.task).digest('hex')
  const task_excerpt = task.task.slice(0, 200)
  // ... construct and return all 11 fields
}
```

**Key difference from state.ts:** Use `appendFileSync` (sync, O_APPEND atomic) instead of async `writeFile`; use `mkdirSync` instead of async `mkdir` — logging path, no need to await.

---

### 2.2 `src/audit/hook.ts` (middleware / bridge, ≤50L)

**Analog:** `src/checkpoint/engineHook.ts` (49L) — exact structural match

**Full analog** — `src/checkpoint/engineHook.ts` lines 1-49:
```typescript
// Header comment: phase ref + single-responsibility bridge + analog note
import { SCHEMA_VERSIONS } from '../types/schemaVersion.js'
import { activate as stateActivate, complete as stateComplete } from './state.js'
import { writeCheckpoint } from './template.js'

export interface EngineCheckpointHookCtx {
  phaseId: string
  sessionId?: string
  status: 'active' | 'complete'
  lastTask?: string
  keyDecisions?: string[]
  canonicalRefs?: string[]
}

export async function activatePhase(phaseId: string): Promise<{ checkpointPath: string }> {
  const checkpointPath = `.harnessed/checkpoints/${phaseId}.json`
  await stateActivate(phaseId, checkpointPath)
  return { checkpointPath }
}

export async function completePhase(ctx: EngineCheckpointHookCtx): Promise<void> {
  if (ctx.phaseId === 'unknown') {
    console.error('[harnessed] WARN engineHook: ...')
  }
  writeCheckpoint({ /* ... ctx fields ... */ })
  await stateComplete()
}
```

**Apply to `src/audit/hook.ts`** — mirror structure:
```typescript
// src/audit/hook.ts — Phase 4.3 W1 T1.2 (sister engineHook.ts ≤50L extract pattern).
// Extracted thin wrapper keeps engine.ts ≤200L Karpathy hard limit clean.
// Single responsibility: bridge engine.ts outcome → audit log emit.
import type { ArbitrateResult, TaskContext } from '../routing/agentDefinition.js'
import type { Rule } from '../routing/decisionRules.js'
import { buildAuditRecord, emitAuditRecord } from './log.js'

export interface AuditHookCtx {
  outcome: 'complete' | 'max-iter' | 'verbatim-fail' | 'spawn-err' | 'install-err' | 'arbitrate-err'
  routeLayer: 'L1-keyword' | 'L2-semantic-stub' | 'L3-fallback'
  sessionId?: string
  iterCount: null  // Phase 4.3: null (ralphLoopWrap returns string only; defer v0.5+)
}

export function emitAudit(
  task: TaskContext,
  decision: ArbitrateResult,
  matched: Rule | null,
  ctx: AuditHookCtx,
): void {
  const record = buildAuditRecord(task, decision, matched, ctx)
  emitAuditRecord(record)
}
```

**Discipline:** ≤50L. Sync (not async) since `emitAuditRecord` is sync. No try-catch in hook — logging path fail-soft (console.error on error, do not throw).

---

### 2.3 `src/routing/engine.ts` MODIFY — surgical 2L shrink + import + calls

**Self-modify analog:** `engine.ts` lines 11-34 (import block) + lines 162-200 (hook call pattern)

**Current import block** (lines 11-13 — insertion point for new import):
```typescript
import { homedir } from 'node:os'
import { join } from 'node:path'
import { activatePhase, completePhase } from '../checkpoint/engineHook.js'
// NEW: import { emitAudit } from '../audit/hook.js'  ← insert after line 13
```

**Surgical 2L shrink target** (lines 120-122 — noop comment to remove pre-import addition):
```typescript
// CURRENT (lines 119-122):
    const semantic = await semanticMatch(task.task, opts.semanticThreshold)
    void semantic.rule // v0.2+ feeds a matched rule into install + factory.
    // Step 1c — L3 fallback_supervisor LLM (phase 1.4 三层兜底 contract).

// AFTER shrink (collapse void noop line — saves 1L; move comment inline saves 1L):
    const semantic = await semanticMatch(task.task, opts.semanticThreshold)
    // v0.2+ swaps stub body; void semantic.rule feeds matched rule install+factory.
    // Step 1c — L3 fallback_supervisor LLM (phase 1.4 三层兜底 contract).
```

**Hook insertion pattern** — mirroring `activatePhase`/`completePhase` at lines 164+189:
```typescript
// EXISTING pattern (lines 164 + 188-190):
  await activatePhase(phaseId)
  // ...
  const result = await ralphLoopWrap(wrappedSpawn, maxIter)
  await completePhase({ phaseId, sessionId: capturedSessionId, status: 'complete' })
  return { ok: true, result, matchedRule: matched }

// NEW: add emitAudit call BEFORE return (success path):
  await completePhase({ phaseId, sessionId: capturedSessionId, status: 'complete' })
  emitAudit(task, decision, matched, { outcome: 'complete', routeLayer, sessionId: capturedSessionId, iterCount: null })
  return { ok: true, result, matchedRule: matched }
```

**Error path pattern** (lines 191-199 — add emitAudit before each return):
```typescript
  } catch (error) {
    if (error instanceof MaxIterationsExceededError) {
      emitAudit(task, decision, matched, { outcome: 'max-iter', routeLayer, sessionId: capturedSessionId, iterCount: null })
      return { aborted: true, reason: error.message }
    }
    if (error instanceof VerbatimCompleteFailError) {
      emitAudit(task, decision, matched, { outcome: 'verbatim-fail', routeLayer, sessionId: capturedSessionId, iterCount: null })
      return { ok: false, phase: 'verbatim', error }
    }
    emitAudit(task, decision, matched, { outcome: 'spawn-err', routeLayer, sessionId: capturedSessionId, iterCount: null })
    return { ok: false, phase: 'spawn', error: error as Error }
  }
```

**routeLayer determination** — add before Step 4 spawn block:
```typescript
// Determine route_layer for audit (L1 = matched rule; L3 = fallback; L2 = stub future)
const routeLayer: 'L1-keyword' | 'L3-fallback' = matched ? 'L1-keyword' : 'L3-fallback'
```

**Budget constraint:** After 2L shrink (200L → 198L), new additions: 1 import line + 1 routeLayer line + 4 emitAudit calls (1L each inline) = +6L → 198L + 6L = 204L. **This still exceeds 200L.** Planner must verify exact delta and ensure shrink target is sufficient OR use single-arg wrapper to consolidate.

**Alternative consolidation:** Pass `routeLayer` + `sessionId` via a single `auditCtx` object set at line 163, then each `emitAudit` call is truly 1 line. Count: shrink -2L + import +1L + auditCtx declaration +1L + routeLayer assignment +1L + 4 emitAudit calls +4L = net +5L → 203L. Planner must find 3 additional lines to shrink OR confirm 1-line `emitAudit` wrapper that bundles all 4 paths via a helper inside hook.ts.

---

### 2.4 `tests/audit/log.test.ts` (test / file-I/O mock, 7 fixtures)

**Analog:** `tests/checkpoint/state.test.ts` (98L) — exact structural match

**Full mock + structure pattern** from `tests/checkpoint/state.test.ts` lines 1-37:
```typescript
// Phase X.Y TN.M — module.test.ts: N fixtures (N-M). vi.mock pattern.
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'

const fsState = new Map<string, string>()  // in-memory FS for mock
const mkdirCalls: string[] = []

vi.mock('node:fs/promises', () => ({
  readFile: async (p: string) => {
    const v = fsState.get(p)
    if (v === undefined) throw new Error(`ENOENT: ${p}`)
    return v
  },
  writeFile: async (p: string, data: string) => void fsState.set(p, data),
  mkdir: async (p: string) => void mkdirCalls.push(p),
}))

import { /* functions under test */ } from '../../src/checkpoint/state.js'

beforeEach(() => {
  fsState.clear()
  mkdirCalls.length = 0
})
afterEach(() => vi.clearAllMocks())
```

**Adapt for `tests/audit/log.test.ts`** — mock `node:fs` (sync) instead of `node:fs/promises`:
```typescript
// tests/audit/log.test.ts — Phase 4.3 W1 T1.4 (7 fixtures). vi.mock node:fs
// sister tests/checkpoint/state.test.ts vi.mock node:fs/promises pattern.
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'

const appendedLines: string[] = []
const mkdirSyncCalls: string[] = []

vi.mock('node:fs', () => ({
  appendFileSync: (_p: string, data: string) => void appendedLines.push(data),
  mkdirSync: (_p: string) => void mkdirSyncCalls.push(_p),
}))

import { emitAuditRecord, buildAuditRecord, AuditRecordSchema } from '../../src/audit/log.js'
import { Value } from '@sinclair/typebox/value'

beforeEach(() => {
  appendedLines.length = 0
  mkdirSyncCalls.length = 0
})
afterEach(() => vi.clearAllMocks())
```

**7 fixture cells** (per RESEARCH § 2.8):
```typescript
describe('audit/log — JSONL append-only (Phase 4.3 W1 T1.4)', () => {
  it('1. emitAudit writes valid JSONL line (Value.Check passes)')
  it('2. emitAudit appends — second call adds second line, first unchanged')
  it('3. all 11 fields present in emitted record')
  it('4. task_sha1 is 40-char hex (node:crypto sha1)')
  it('5. task_excerpt truncated at 200 chars')
  it('6. outcome "complete" and "spawn-err" values map correctly')
  it('7. mkdirSync called with dirname(.harnessed/audit.log)')
})
```

---

### 2.5 `docs/adr/0018-routing-audit-log.md` — 9-section format

**Analog:** `docs/adr/0017-routing-hit-rate-token-budget.md` (204L) — exact format

**9-section structure** from ADR 0017 (verified lines 1-60):
```markdown
# ADR NNNN: [title]

## Status
**Accepted (phase X.Y WN — date)** — [one-sentence ship summary]

## Context
[background + constraints + prior phase context]

## Decisions
### 1. D-01 ...
### 2. D-02 ...

## A7 Conservation
[ADR 0001-{N-1} 守恒 statement + bash verify command]

## Consequences
[capability delta table + negative consequences + mitigation]

## Compliance
[F1-F8 8/8 acceptance bar evidence table]

## Errata-path note
[freeze statement + forward reference]

## Adoption-confirmation
[milestone ship evidence + deferred items]

## References
[internal + external links]
```

**A7 Conservation section** — copy verbatim pattern from ADR 0017 lines 17-19 (adapt numbers):
```markdown
## A7 Conservation

phase 4.3 沿袭 ADR 0003/.../0017 errata 风格 — **不动 ADR 0001-0017 main body** (A7 守恒). baseline tag iterate 17 → 18 (Wave 2 ship 时 push `adr-0018-accepted` tag; ci.yml A7 step iter `1-0017` → `1-0018` per Phase 4.3 W2 T2.7 explicit literal fix sister Phase 3.4 W2 T2.7 + Phase 3.3+3.2+3.1 pattern). 本 ADR 0018 起 phase 4.3 ship 时刻 frozen; v0.5+ 演化走 ADR 0019+ errata. Note: ADR 0019/0020 backfill = NEW sequential numbers (NOT retro-inject 0014.x); backfill 到 Phase 4.3 ship 时刻同时 frozen.
```

---

### 2.6 `docs/adr/README.md` MODIFY — index entry format

**Self-modify analog:** existing table rows in `docs/adr/README.md` lines 42-49:
```markdown
| [0001](./0001-manifest-schema-v1.md) | Manifest Schema v1 (see ADR 0005 ...) | Accepted | 2026-05-11 |
```

**New entries to append** (3 rows, after existing 0008 entry):
```markdown
| [0018](./0018-routing-audit-log.md) | Routing Audit Log (JSONL append-only + 11-field schema + forward-only from Phase 4.3) | Accepted | 2026-05-19 |
| [0019](./0019-state-dual-ssot-collapse-pattern.md) | STATE dual-SoT COLLAPSE Pattern (5-recurrence terminus — Phase 3.3 D-04 backfill) | Accepted | 2026-05-19 |
| [0020](./0020-hybrid-2clock-disambiguation.md) | HYBRID 2-clock Disambiguation (internal ship vs external organic co-maintainer — Phase 4.2 D-04 backfill) | Accepted | 2026-05-19 |
```

**Gap catchup** (planner discretion per RESEARCH § 3.6 Q3): ADR 0009-0017 exist on disk but NOT in index table. Low-effort bonus: add rows 0009-0017 between 0008 and 0018. NOT required by D-03 scope but closes the index gap.

---

### 2.7 `.github/workflows/ci.yml` MODIFY — A7 step iter 0017 → 0018

**Self-modify analog:** `.github/workflows/ci.yml` lines 82-107 (current A7 step)

**4 surgical edits** (RESEARCH § 3.7 exact locations):
```yaml
# Line 82 — step name:
- name: A7 acceptance bar — ADR 0001-0017 main body 守恒
# → change to:
- name: A7 acceptance bar — ADR 0001-0018 main body 守恒

# Line 85 — first for loop (add 0018 at end):
          for n in 0001 0002 0003 0004 0005 0006 0007 0008 0009 0010 0011 0012 0013 0014 0015 0016 0017; do
# → add 0018:
          for n in 0001 0002 0003 0004 0005 0006 0007 0008 0009 0010 0011 0012 0013 0014 0015 0016 0017 0018; do

# Line 96 — second for loop (same addition):
          for n in 0001 ... 0017; do
# → add 0018

# Line 107 — echo:
          echo "A7 ✅ ADR 0001-0017 main body unchanged"
# → change to:
          echo "A7 ✅ ADR 0001-0018 main body unchanged"
```

**Ordering constraint (RESEARCH § 4.4):** ci.yml A7 update MUST be committed and pushed BEFORE `adr-0018-accepted` tag is pushed to origin. Otherwise A7 gate will attempt to check ADR 0018 before it's in the baseline loop and produce incorrect behavior on next CI run.

---

### 2.8 `.planning/milestones/v0.4.0-MILESTONE-AUDIT.md` NEW

**Analog:** `.planning/milestones/v0.3.0-MILESTONE-AUDIT.md` (verified lines 1-139)

**YAML front-matter structure** (lines 1-33 of analog):
```yaml
---
milestone: 0.4.0
audited: 2026-05-19
status: passed
status_history:
  - "2026-05-19 initial: passed — 3/3 phase × acceptance bar 8/8 全 verified；..."
scores:
  requirements: N/N
  phases: 3/3
  integration: N/N
  flows: N/N
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "4.3"
    items: [...]
---
```

**Markdown body sections** (from analog structure):
```markdown
# v0.4.0 Milestone Audit — PASSED

## § 0.5 Line Budget Deviations Accepted
## § 1 Per-Phase Status         ← 3 phases (4.1 + 4.2 + 4.3)
## § 2 Cross-Phase Integration
## § 3 E2E Flows
## § 4 Requirements Coverage    ← R8.1 + R8.2 + R8.3 + R8.4 + R8.5
## § 5 v0.4.0 vs v0.3.0 对比
## § 6 Verdict                  ← triple tag + next milestone
## § 7 Cadence Patterns (R-5 mitigation rationale) ← NEW v0.4.0 only (#BO carry)
```

**§ 7 R-5 mitigation section** (#BO carry-forward — bottom "Cadence patterns" position per CONTEXT recommendation):
```markdown
## § 7 Cadence Patterns — R-5 Publish-Only vs Architectural Phase Disambiguation

v0.4.0 中 Phase 4.1 + 4.2 是 **R-5 publish-only** phases (无 src/ architectural change; 仅 docs/community infra publish). Phase 4.3 是 **ARCHITECTURAL** phase (NEW src/audit/ module + ADR 0018 + ci.yml A7 iter + triple tag — full ship cadence). 这不是 "v0.4.0 决策力下降"; 而是 M-01 PhaseClass 刻意 tier 区分: R-5 phases 专注 external visibility (co-maintainer onboarding / stale-bot / Sponsors), architectural phases 专注 NEW src/ capability (audit log R8.1). 读者应 compare Phase 4.3 单 phase architectural delta vs v0.3.0 4-phase parallel, NOT v0.4.0 整 milestone 3-phase 对比.
```

---

## § 3 Cross-Cutting Shared Patterns

### 3.1 Karpathy ≤200L Hard Limit

**Applies to:** `src/audit/log.ts` (≤80L target), `src/audit/hook.ts` (≤50L target), `src/routing/engine.ts` (≤200L hard — surgical shrink prerequisite)

**Enforcement:** `wc -l <file>` verify after each edit; engine.ts requires 2L shrink BEFORE audit import addition.

**Shrink target (engine.ts lines 120-122):**
```typescript
// REMOVE: void semantic.rule // v0.2+ feeds a matched rule into install + factory.
// Keep only the Step 1c comment (consolidate onto semantic= line comment or preceding line)
```

### 3.2 TypeBox Validation Pattern

**Source:** `src/manifest/schema/aliases.v1.ts` (lines 10-33) + `src/checkpoint/state.ts` (lines 8, 45-47)

**Apply to:** `src/audit/log.ts` (AuditRecordSchema definition) + `tests/audit/log.test.ts` (Value.Check assertion)

```typescript
// Schema definition (log.ts):
import { type Static, Type } from '@sinclair/typebox'
export const AuditRecordSchema = Type.Object({ ... }, { additionalProperties: false })
export type AuditRecord = Static<typeof AuditRecordSchema>

// Test assertion (log.test.ts):
import { Value } from '@sinclair/typebox/value'
expect(Value.Check(AuditRecordSchema, record)).toBe(true)
```

### 3.3 node: prefix import convention

**Source:** `src/checkpoint/state.ts` lines 6-7 + `src/routing/engine.ts` lines 11-12

```typescript
import { mkdir, readFile, writeFile } from 'node:fs/promises'
import { dirname } from 'node:path'
import { homedir } from 'node:os'
import { createHash } from 'node:crypto'  // ← audit log needs this
```

**Apply to:** All new `src/audit/*.ts` files — always use `node:` prefix for built-in modules.

### 3.4 vi.mock + fsState in-memory FS pattern

**Source:** `tests/checkpoint/state.test.ts` lines 6-17

```typescript
const fsState = new Map<string, string>()
vi.mock('node:fs/promises', () => ({
  readFile: async (p: string) => {
    const v = fsState.get(p)
    if (v === undefined) throw new Error(`ENOENT: ${p}`)
    return v
  },
  writeFile: async (p: string, data: string) => void fsState.set(p, data),
  mkdir: async (p: string) => void mkdirCalls.push(p),
}))
```

**Adapt for sync `node:fs`** in `tests/audit/log.test.ts`:
```typescript
vi.mock('node:fs', () => ({
  appendFileSync: (_p: string, data: string) => void appendedLines.push(data),
  mkdirSync: (_p: string, _opts?: unknown) => void mkdirSyncCalls.push(String(_p)),
}))
```

### 3.5 PRIMARY helper extract ≤50L discipline

**Source:** `src/checkpoint/engineHook.ts` (49L) — established by Phase 3.1 W3 T3.2

**Pattern:** Single-responsibility bridge module. Comment header = phase ref + single-responsibility + analog note. Export interface + 1-2 exported functions. No direct business logic — delegates to imported module. Keeps caller (engine.ts) ≤200L.

**Apply to:** `src/audit/hook.ts` — same discipline; ≤50L hard target.

### 3.6 A7 守恒 — ADR main body freeze

**Source:** `.github/workflows/ci.yml` lines 82-107 + `docs/adr/0017-routing-hit-rate-token-budget.md` § A7 Conservation

**Rule:** Never edit `docs/adr/0001-0017` main body after their `adr-NNNN-accepted` tag. `docs/adr/README.md` is NOT covered by A7 gate (not a numbered glob `NNNN-*.md`). NEW files (0018-0020) not in A7 gate until `adr-0018-accepted` tag is pushed.

**Verify before tagging:**
```bash
git diff docs/adr/000[1-9]-*.md docs/adr/001[0-7]-*.md | wc -l  # must be 0
```

### 3.7 Biome preempt before commit

**Source:** project memory `feedback_biome-preempt.md` (3 CI-red recurrences)

**Apply to:** ALL TS/JS file commits in Wave 0 (#BF/#BG extracts) and Wave 1 (`src/audit/*.ts` + `tests/audit/*.ts` + `src/routing/engine.ts`):
```bash
corepack pnpm exec biome check --write
```

Run BEFORE `git add` + `git commit` — not after.

### 3.8 Single-SoT D2 cadence iter 4 archive

**Source:** `.planning/STATE.md` + `.planning/RETROSPECTIVE.md` existing archive sections

**Section header for iter 4** (per RESEARCH § 7 Q4 + Phase 4.2 cadence):
```
## § ARCHIVED FROM STATE — Phase 4.2 (Phase 4.3 W0.1 D2 cadence iter 4, 2026-05-19)
```

Note: Phase 4.2 = single-phase archive (sister Phase 4.1 solo archive precedent — Phase 4.0 does not exist per filesystem verify).

---

## § 4 No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `CHANGELOG.md` | docs (changelog) | declarative | First CHANGELOG in project history; no project analog; use Keep-a-Changelog industry standard format (`## [0.4.0] - 2026-05-19` + Added/Changed/Fixed sections per RESEARCH § 4.1) |

**CHANGELOG.md template** (from RESEARCH § 4.1 — industry standard, no codebase analog):
```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.4.0] - 2026-05-19

### Added
- Routing audit log (`.harnessed/audit.log`) — JSONL append-only, 11-field schema (R8.1)
- ADR 0018: routing audit log architecture
- ADR 0019: STATE dual-SoT collapse pattern (backfill Phase 3.3)
- ADR 0020: HYBRID 2-clock disambiguation (backfill Phase 4.2)
- CHANGELOG.md (this file)
- v0.4.0 milestone archive triplet

### Changed
- ci.yml A7 step: ADR 0001-0017 → ADR 0001-0018 integrity gate

[0.4.0]: https://github.com/easyinplay/harnessed/releases/tag/v0.4.0
```

---

## § 5 Path Dependencies (Wave ordering constraints)

| Constraint | Reason |
|-----------|--------|
| W0.1 STATE trim BEFORE W0.2 SIZE_LIMIT flip | Need post-trim line count to evaluate ≤165L threshold |
| engine.ts 2L shrink (L120-122) BEFORE import + emitAudit additions | engine.ts currently at 200L exactly — cannot add even 1L without shrink first |
| `src/audit/log.ts` BEFORE `src/audit/hook.ts` | hook.ts imports from log.ts |
| `src/audit/hook.ts` BEFORE `src/routing/engine.ts` MODIFY | engine.ts imports from hook.ts |
| ci.yml A7 step iter (0017 → 0018) BEFORE `adr-0018-accepted` tag push | A7 gate must include 0018 in loop before tag references it as baseline |
| ADR 0018 creation BEFORE `adr-0018-accepted` tag | Tag marks the ADR as the frozen baseline |
| Triple tag: `adr-0018-accepted` → `v0.4.0-alpha.3-audit` → `🎯 v0.4.0` (in that order) | Sister v0.3.0 close cadence; ADR baseline first, phase baseline second, milestone close last |

---

## Metadata

**Analog search scope:** `src/checkpoint/`, `src/manifest/schema/`, `src/routing/`, `tests/checkpoint/`, `docs/adr/`, `.planning/milestones/`, `.github/workflows/`
**Files scanned:** 22 target files + 11 analog files read
**Pattern extraction date:** 2026-05-19
