# Phase 3.3: aliases.yaml + deprecation marker + known-good 版本组合 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 3.3-CONTEXT.md.

**Date:** 2026-05-17
**Phase:** 3.3 — aliases.yaml + deprecation marker + known-good 版本组合
**Areas discussed:** aliases.yaml schema 形态 / deprecation marker 可见性策略 / known-good 版本锁格式 / H3 STATE.md dual-SSOT 根治 fix path
**Mode:** interactive (4-question batch AskUserQuestion, 1 call) — sister Phase 3.1+3.2 cadence

---

## Aliases — aliases.yaml schema 形态

| Option | Description | Selected |
|--------|-------------|----------|
| RICH | 含 reason + since_version + deprecation_date + removal_date? TypeBox struct; doctor warning 可解释 why+when. | ✓ |
| FLAT | 简单 `old: new` 映射. 失 deprecation metadata. | |
| TIERED | string OR object union. Karpathy YAGNI 违反; 双 schema 复杂化. | |

**User's choice:** RICH (Recommended)
**Notes:** R7.5 验收"deprecation marker 显示提示"含蓄要求 metadata. doctor `--json` 沿袭 Phase 2.4 D-01 CI-friendly 输出 RICH 直接展开.

---

## DeprecationVis — deprecation marker 可见性策略

| Option | Description | Selected |
|--------|-------------|----------|
| DOCTOR-ONLY-WARN | doctor 加 7th check 显示 deprecation warning + install 静默重定向. Karpathy YAGNI 中庸. | ✓ |
| INSTALL-WARN | install 时也警告但 continue. 增 CI 噪声 + 重复. | |
| INSTALL-THROW | install throw 需 --force-deprecated flag. 太严苛, R7.5 验收"install 通过"语义冲突. | |

**User's choice:** DOCTOR-ONLY-WARN (Recommended)
**Notes:** 把 deprecation 提醒放健康检查 (人主动查) — install path keep clean redirect.

---

## KnownGood — known-good 版本锁格式

| Option | Description | Selected |
|--------|-------------|----------|
| YAML manifest | `versions/<harnessed-ver>-known-good.yaml` sister manifests/ pattern; TypeBox 复用 13th surface candidate. | ✓ |
| JSON npm-lock style | `versions/<harnessed-ver>-known-good.json`. 项目未用 npm-lock + yaml convention 不一致. | |
| Embed in manifest yaml | `known_good_versions:[]` field. 跨 manifest agg 难, 不符 R7.6 "harnessed 版本冻结一组" 语义. | |

**User's choice:** YAML manifest (Recommended)
**Notes:** 项目 manifests/ 全 yaml convention; TypeBox + branchOnSchemaVersion 复用; 人可读 + git diff 友好.

---

## H3 path — STATE.md dual-SSOT 5th recurrence 根治 fix path

| Option | Description | Selected |
|--------|-------------|----------|
| (b) COLLAPSE | 删 STATE.md L4 frontmatter Status, "当前位置"块 作 single SSOT. Karpathy YAGNI; sister T2 推荐. | ✓ |
| (a) EXTEND | freshness gate 扩 scope 加 "当前位置" pattern check. 保 dual SSOT 但 over-engineering. | |
| (c) COLLAPSE INVERSE | 删 "当前位置" block, 仅保 L4 frontmatter Status. 损 human-readable + dashboard 可能 break. | |

**User's choice:** (b) COLLAPSE (Recommended per sister T2 + Karpathy YAGNI)
**Notes:** 5-recurrence pattern terminus (README L9 / README L44 / PROJECT-SPEC / STATE freshness scope / STATE 当前位置块). single SoT 原则 + Wave 0 first task.

---

## Claude's Discretion (留 planner)

- `harnessed install --known-good` 解析时机 (boot OR pre-resolution; 推 lazy)
- install path resolveAlias 注入 location (pre-manifest-lookup 推荐)
- doctor 7th check warning format multi-deprecation aggregation (table 推荐)
- versions/ dir bootstrap (1 file with empty upstreams 推 vs .gitkeep)
- dashboard.mjs "当前位置"块 parse impact spike Wave 0 评估
- W0.2 dashboard-sse fix path 3 candidate (a/b/c, 推 (a) random port)

## Deferred Ideas (写 CONTEXT.md `<deferred>` 节)

- 路由命中率 / plan-feature 真接外部 → Phase 3.4
- EE-4 BLOCKER auto-spawn / userSpawn capture → Phase 3.4+
- known-good 真 seed → Phase 3.4 dogfood 填充
- FLAT / TIERED / INSTALL-WARN / INSTALL-THROW / JSON-lock / Embed-known-good / EXTEND-gate / COLLAPSE-INVERSE 路径全 evaluated rejected

---

*Discussion: 4 questions, 1 batch AskUserQuestion call, all 4 Recommended options accepted by user (decisive cadence consistent with established workflow, sister Phase 3.1+3.2 同 pattern, 3-phase 连续)*
