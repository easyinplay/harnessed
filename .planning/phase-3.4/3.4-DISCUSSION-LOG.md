# Phase 3.4: 路由命中率 ≥ 85% 验收 + token budget 监控 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 3.4-CONTEXT.md.

**Date:** 2026-05-17
**Phase:** 3.4 — 路由命中率 ≥ 85% 验收 + token budget 监控 (v0.3.0 close phase)
**Areas discussed:** 30 真实任务样本 sourcing / 路由命中率验算法 / token budget approx 算法 / token budget 阈值告警 strategy
**Mode:** interactive (4-question batch AskUserQuestion, 1 call) — sister Phase 3.1+3.2+3.3 cadence 4-phase 连续

---

## Sample — 30 真实任务样本 sourcing

| Option | Description | Selected |
|--------|-------------|----------|
| REAL HISTORICAL | 挖 .planning/ + git log 重制历史 task; dogfood real scope; ~1h mining. | ✓ |
| SYNTHETIC SPEC | 手设 30 task spec; 快出但不代表 real (Phase 2.3 sister 受 limitation). | |
| MIXED | 20 real + 10 synthetic edge case; 平衡但拆分设计费 over-engineering. | |

**User's choice:** REAL HISTORICAL (Recommended)
**Notes:** dogfood real value > 1h mining effort; sister Phase 2.3 W4 100% synthetic 已 acknowledge limitation; 本 phase 推真实使用场景验证.

---

## HitAlgo — 路由命中率验算法

| Option | Description | Selected |
|--------|-------------|----------|
| RUN ENGINE | per-sample 调 routing.dispatch + 比 actual vs expected; sister Phase 2.3 W4 100% pattern直接复刻. | ✓ |
| DRY-RUN HARNESS | 仅 routing.match 查表不走 SDK; Phase 1.4 R1 已验 dry layer, 本 phase 推深入. | |
| FULL E2E | per-sample 真 SDK spawn; cost + cross-OS slow; sister Phase 3.3 dogfood synthetic 等价 isomorphic. | |

**User's choice:** RUN ENGINE (Recommended)
**Notes:** sister Phase 2.3 W4 30-sample harness 100% 模板复用 (本次 REAL input + 同 harness); ~30 sample × 100ms = ~3s test runtime.

---

## TokenBudget — token budget approx 算法

| Option | Description | Selected |
|--------|-------------|----------|
| BUFFER /4 | `Buffer.byteLength(str, 'utf8') / 4`; sister Phase 3.1 D-01 enforceBudget 已用 + zero-dep + Karpathy YAGNI. | ✓ |
| TIKTOKEN | npm dep + 精准但 bundle 肥 + cross-OS 不保证; 违 zero-dep. | |
| STRING LENGTH | `str.length / 4` 不考虑 UTF-8 byte; 中文 underestimate. | |

**User's choice:** BUFFER /4 HEURISTIC (Recommended)
**Notes:** sister Phase 3.1 D-01 sister 1 precedent + acceptable variance verified; Karpathy YAGNI strict zero-dep.

---

## ThresholdAlert — token budget 阈值告警 strategy

| Option | Description | Selected |
|--------|-------------|----------|
| DOCTOR WARN | doctor 8th check `checkTokenBudget` status warn; sister doctor 7-check pattern延袭 + helper PRIMARY extract. | ✓ |
| CI FAIL | CI step exit 1; 太严苛; 与 Phase 3.3 D-02 DOCTOR-ONLY-WARN install 沉默 inconsistent. | |
| SILENT LOG | 仅写 `.harnessed/token-budget.log`; 反 Karpathy 透明 fail-loud. | |

**User's choice:** DOCTOR WARN (Recommended)
**Notes:** 一致 doctor 8-check UX; PRIMARY helper extract sister probe-gstack + check-deprecations + check-token-budget = 3 helper family.

---

## Claude's Discretion (留 planner)

- doctor.ts 195L → ~200L borderline (pre-flight wc -l verify; split helper if real >200L)
- BUFFER /4 variance accepted (中文 underestimate, sister Phase 3.1 precedent)
- SAMPLES.md ≤200L 行预算 (30 row × 5-7L)
- routing.dispatch real call vs mock (推 production decision_rules.yaml real)
- W0.4 path traversal regex spike outcome (absorb OR defer 由 spike 决)
- versions/0.3.0-known-good.yaml W0.3 真 entries 选 ~6-10 (planner cross-ref package.json + manifests)
- v0.3.0 close milestone tag sister v0.2.0 close 模板 (Phase 3.4 W2 ship integrate)

## Deferred Ideas (写 CONTEXT.md `<deferred>` 节)

- plan-feature 真接 gsd-* spawn / EE-4 auto-spawn / userSpawn capture → Phase 4.0+
- dogfooding benchmark 公开 → v0.4 R8.1
- co-maintainer 招募窗口 → v0.4 R8.2
- SYNTHETIC SPEC / MIXED / DRY-RUN / FULL E2E / TIKTOKEN / STRING LENGTH / CI FAIL / SILENT LOG / "cleanup" 命名 路径全 evaluated rejected (or paranoid 反)

---

*Discussion: 4 questions, 1 batch AskUserQuestion call, all 4 Recommended options accepted by user (decisive cadence consistent with established workflow, sister Phase 3.1+3.2+3.3 同 pattern, 4-phase 连续 — v0.3.0 close phase)*
