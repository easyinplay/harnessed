# Phase 3.4: 路由命中率 ≥ 85% 验收 + token budget 监控 — KICKOFF

**Phase**: 3.4 (v0.3.0 milestone 第 4 phase, 4 phase 中第 4 — **v0.3.0 close phase**)
**Started**: 2026-05-17
**Status**: discuss-phase 完成 → 待 plan-phase 启动

---

## 1. ROADMAP / REQUIREMENTS 锚定

**ROADMAP § v0.3.0 Phase 拆分 L167-170**:

> **Phase 3.4：路由命中率 ≥ 85% 验收 + token budget 监控**
> - 30 样本（Haiku/Sonnet/Opus 各 ≥ 8）；description 总 token 近似算法 + 阈值告警
> - 验收：实测命中率达标 + token 监控仪表落地

**REQUIREMENTS R7.6 + R1 dogfooding scope**: Phase 3.3 R7.6 known-good infra ready, Phase 3.4 fills real seed; R1 routing engine v1 (Phase 1.4) 实测验收。

---

## 2. Carry-forward from prior phases (locked, 不重新讨论)

| 来源 | 决策 | 本 phase 应用 |
|------|------|---------------|
| **Phase 3.1 D-01 TEMPLATE 摘要** | `Buffer.byteLength / 4` heuristic (zero-dep + cross-OS + deterministic) | D-03 token budget approx 沿袭同算法 (Karpathy YAGNI sister 1 precedent) |
| **Phase 2.4 D-01 doctor MIN 5-check** + **Phase 3.2+3.3 6th+7th 累积扩** | `--json` flag + 三档 status enum (pass/warn/fail) + helper PRIMARY extract pattern | D-04 doctor 8th check `checkTokenBudget` 沿袭 (sister probe-gstack / check-deprecations 100% reuse) |
| **Phase 1.4 R1 routing engine v1** | `routing.dispatch(task)` + decision_rules.yaml + 30 sample harness 100% (Phase 2.3 W4) | D-02 RUN ENGINE per-sample run + actual vs expected compare (sister Phase 2.3 30/30 routing harness pattern延袭) |
| **Phase 2.2 CD-5 schemaVersion 13-surface single 兼容门** | `branchOnSchemaVersion<T>()` helper | 本 phase 不加 surface (no NEW schema), 仅 consume |
| **Phase 3.3 sister H1+H2 absorb + STRATEGIC task lock** | STATE.md role + archive cadence institutionalize 4 D-decisions (D1-D4) | W0 first task per user STRATEGIC option lock (paranoid 命名 architectural framing) |
| **Karpathy 4 心法 always-on** | YAGNI / Surgical / Simplicity / Goal-Driven | BUFFER /4 sister 1 precedent + DOCTOR WARN sister 7-check pattern + REAL HISTORICAL dogfood + RUN ENGINE per-sample dispatch (no SDK call, dry routing layer) |

---

## 3. v0.3.0 milestone close 在本 phase 的位置

v0.3.0 = plan-feature workflow + checkpoint (4 phase):
- Phase 3.1 ✅ SHIPPED (checkpoint 引擎 + harnessed resume + compact 协议)
- Phase 3.2 ✅ SHIPPED (gstack 前缀探测 + workflow 变量插值 + plan-feature reference 实装)
- Phase 3.3 ✅ SHIPPED (aliases.yaml + deprecation marker + known-good 版本组合)
- **Phase 3.4** (本 phase): 路由命中率 ≥ 85% 验收 + token budget 监控 — **v0.3.0 final close phase**

**Phase 3.4 ship 后 v0.3.0 close**: archive (`.planning/milestones/v0.3.0-{ROADMAP,REQUIREMENTS,MILESTONE-AUDIT}.md`) + audit 验收报告 + milestone tag (`v0.3.0-alpha.4-routing` + 🎯 `v0.3.0` 大 tag) triple push — sister v0.2.0 close pattern延袭。

---

## 4. v0.3.0 W0 backlog (Phase 3.3 ship sister STRATEGIC task + DEFERRED 兑现 — 必修)

从 STATE.md + Phase 3.3 deferred-items.md 拉入, 5-phase 连续 "deferred-items → next phase W0 一次根治" cadence 延袭 (Phase 2.3/2.4/3.1/3.2/3.3 → Phase 3.4 = 6th phase):

1. **W0.1 STRATEGIC task (必修 first task per user 4 D-decision lock)**: **"STATE.md role + archive cadence institutionalize"** (paranoid 命名 architectural framing NOT cleanup — sister D-04 COLLAPSE 命名思路延袭):
   - D1 STATE role = single-SoT (删 view-of-three 复杂度, dashboard.mjs render 单源更顺)
   - D2 archive cadence 进 ship T6.N step (每 phase ship 自动 trim prev-prev-phase narrative → RETROSPECTIVE)
   - D3 `scripts/check-state-archive-stale.mjs` 3 rules (≤150 行 / 关键决议总结 ≤1 节 / W-N errata 禁字面) — warn-only round 1 sister Phase 2.1 transparency gate cadence延袭, flip ENFORCE Phase 3.5 OR v0.4.0
   - D4 ship-time T6.N integrate (executor 不 SCOPE BOUNDARY 阻挡 archive — gate make GC visible)
   - Acceptance: STATE.md 行数 ≤150 + 1 gate script + warn-only round 1 verified
   - Estimate: ~30 min (sister D-04 W0.1 15min STATE COLLAPSE × 2 = 30min for institutionalize scope)

2. **W0.2 DEFERRED #AD 兑现** (1-line surgical fix): `src/cli/install.ts` `harnessedVer = '0.3.0'` hardcoded TODO → read from `package.json` (Phase 3.3 W1 T1.8 left TODO comment marker). Acceptance: `grep -q "harnessedVer.*package\\.json" src/cli/install.ts` exit 0.

3. **W0.3 DEFERRED #AC 兑现 (R7.6 real seed)**: `versions/0.3.0-known-good.yaml` 填 actual e2e-verified upstream entries (claude-agent-sdk@0.3.142 + gstack + GSD + superpowers + key MCP + key skills) — replace `upstreams: []` empty with ~6-10 real pinned entries. Acceptance: `versions/0.3.0-known-good.yaml` `upstreams` 长度 ≥ 5 + e2e_verified_at != 'TBD'.

4. **W0.4 DEFERRED #AE 兑现 OR defer**: path traversal regex spike — 30min spike 评估 attack vector reality. 若 surface real attack vector → 加 1-line install path validate; 若 not → defer Phase 4.0 with rationale doc (sister Phase 3.2 DEFERRED #2 dashboard-sse defer pattern 但已 RESOLVED).

5. **W0.5 30 sample sourcing pre-flight** (W1 主线 RUN ENGINE harness 前置): mining `.planning/{phase-X.Y,intel,STATE.md}` + `git log --since=2026-05-12 --until=2026-05-17 --pretty=format:"%h %s"` 重制 30 task scenarios (10 Haiku trivial + 10 Sonnet medium + 10 Opus complex). Acceptance: `.planning/phase-3.4/SAMPLES.md` NEW 30-row matrix + expected routing decision per row.

---

## 5. 不在本 phase

- **plan-feature 真接外部 gsd-* spawn** → Phase 4.0+ dogfood (v0.3.0 close 后)
- **EE-4 BLOCKER auto-spawn rerun** → Phase 4.0+ evaluate (Phase 2.4 down-scope DEFERRED)
- **userSpawn session_id capture** → Phase 4.0+ if real demand (Phase 3.1 DEFERRED #2 carry)
- **dogfooding benchmark 公开 (`docs/benchmarks/v0.4.md`)** → v0.4 R8.1
- **co-maintainer 招募窗口** → v0.4 R8.2

---

## 6. discuss-phase 4 gray area 决策 (详 CONTEXT.md)

| ID | 主题 | Locked |
|----|------|--------|
| D-01 | 30 真实任务样本 sourcing | REAL HISTORICAL (挖 .planning/ + git log 重制历史 task) |
| D-02 | 路由命中率验算法 | RUN ENGINE (per-sample dispatch + actual vs expected compare) |
| D-03 | token budget approx 算法 | BUFFER /4 HEURISTIC (`Buffer.byteLength(str, 'utf8') / 4`, sister Phase 3.1 D-01) |
| D-04 | token budget 阈值告警 strategy | DOCTOR WARN (doctor 8th check `checkTokenBudget` + helper PRIMARY extract sister probe-gstack pattern) |

---

*Phase 3.4 — 路由命中率 ≥ 85% 验收 + token budget 监控 + v0.3.0 milestone close*
*Discuss-phase: 2026-05-17*
