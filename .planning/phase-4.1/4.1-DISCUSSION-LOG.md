# Phase 4.1: dogfooding benchmark 数据采集 + 公开格式定义 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 4.1-CONTEXT.md.

**Date:** 2026-05-18 (session crossed day boundary; Phase 3.4 ship + sister H1 absorb 全部 2026-05-17, discuss-phase 4 questions + CONTEXT ship 2026-05-18)
**Phase:** 4.1 — dogfooding benchmark 数据采集 + 公开格式定义 (v0.4.0 milestone 1st phase, sister Phase 3.1+3.2+3.3+3.4 cadence延袭 5-phase 连续)
**Areas discussed:** 30 任务 sample sourcing / benchmark 公开 disclosure granularity / 上游升级 e2e recording format / benchmark drift CI cadence
**Mode:** interactive (4-question batch AskUserQuestion, 1 call) — sister Phase 3.1+3.2+3.3+3.4 cadence 5-phase 连续

---

## SampleSrc — 30 任务 sourcing for benchmark

| Option | Description | Selected |
|--------|-------------|----------|
| REUSE Phase 3.4 SAMPLES.md | 0 day overhead + 30/30 = 100% Phase 3.4 W1 验证 + sister R3 frozen lock 一致; 直接复制过用为 docs/benchmarks/v0.4.md 公开源数据. | ✓ |
| EXPAND new mining | 反"美化诱惑" (sister L222) 额外挖 5-10 miss case fixture + recovery path; 30→~40 row; 额外 1-2 hour mining overhead. | |
| MIX 30 base + 5-10 miss cases | 30 SAMPLES.md 原型 + 5-10 显式 miss case fixture (failure recovery demo); 拆设计费 + Karpathy YAGNI 疑似 over-engineering. | |

**User's choice:** REUSE Phase 3.4 SAMPLES.md (Recommended)
**Notes:** sister Phase 3.4 R3 frozen lock 30-row REAL HISTORICAL 一次 mining once-only; benchmark consumer = `docs/benchmarks/v0.4.md` 直接读 SAMPLES.md (single SoT 防 drift); EXPAND/MIX 评估为 over-engineering — 反"美化诱惑" 风险通过 D-02 FULL disclosure 自然兜底.

---

## DisclosureLevel — benchmark 公开 disclosure granularity

| Option | Description | Selected |
|--------|-------------|----------|
| FULL per-task | 原始 prompt + 决策 + 命中正误 + miss reason + recovery path 一份公开; sister Karpathy transparency + 反 ROADMAP L222 "数据美化诱惑" 风险. | ✓ |
| AGGREGATE only | stats only + per-tier breakdown; 不贴 raw prompt; 简洁但违 "原始数据公开任何人可复现" R8.1 验收. | |
| HYBRID FULL per-task + aggregate header | FULL 逐任务明细 + 顶部 aggregate summary section (per-tier % 总表); 三者兼顾但 +1 section design. | |

**User's choice:** FULL per-task (Recommended)
**Notes:** R9.2 透明度 + R8.1 "原始数据公开任何人可复现" hard requirement; HYBRID +1 section overhead 微 over-engineering — FULL only 已经隐含 aggregate (footer 30/30 + per-tier ratio 一行总结); sneak-block守门 = raw prompt NOT paraphrased + actual decision_rules.yaml routing path (rule_id + tier + skill) MUST publish.

---

## e2eRecording — 上游升级 e2e recording format

| Option | Description | Selected |
|--------|-------------|----------|
| TEXT LOG plain .log | plain `.log` text + diff/console output; grep-able / reproducible / 0 依赖 (Karpathy zero-dep precedent); cross-OS portable. | ✓ |
| ASCIINEMA .cast | terminal session recording; 小 bag + replay-able 但 需 asciinema npm dep + binary 格式 git diff hostile. | |
| SCREEN VIDEO mp4/gif | 视觉证据但 bloat (binary; 1MB+ per cast) + 难 grep + git diff hostile. | |

**User's choice:** TEXT LOG plain .log (Recommended)
**Notes:** sister Karpathy zero-dep precedent (Phase 3.1 D-01 enforceBudget; Phase 3.3 manifest 13-surface YAML lazy load 全 zero-dep); text grep-able + cross-OS portable + git diff friendly; asciinema/mp4 评估 rejected (npm dep / binary bloat); sneak-block守门 = 4-section text log (pre-upgrade state + upgrade command + diff output + post-upgrade state).

---

## BenchmarkCI — drift detection cadence

| Option | Description | Selected |
|--------|-------------|----------|
| MANUAL on-demand | 0 infra 成本; benchmark = static publish + manual re-run when SAMPLES expand. Karpathy YAGNI + sister Phase 3.4 routing harness 已 per-PR gates via `pnpm test` (post-554b82b hotfix). | ✓ |
| WEEKLY nightly CI cron | sister v0.4 weekly e2e pattern per R9.4 upstream cadence 一致; +1 GitHub Action workflow file + 多一召难处理 stale benchmark drift. | |
| PER-PR full benchmark | 过度 (slow + 已被 routing.test.ts gates) + +60s per PR runtime; YAGNI. | |

**User's choice:** MANUAL on-demand (Recommended)
**Notes:** Karpathy YAGNI; sister Phase 3.4 `tests/routing/phase-3.4-routing-hit-rate.test.ts` 已 per-PR gate via `pnpm test` (post-554b82b CI hotfix 保障); benchmark 公开 = static publish artifact (`docs/benchmarks/v0.4.md`), 不是 live CI dashboard; 30 samples REUSE Phase 3.4 SAMPLES (frozen, no drift源) — WEEKLY cron evaluated rejected (stale drift 难处理); planner SHOULD add `docs/CONTRIBUTING-BENCHMARK.md` ≤30L manual re-run instructions OR fold into existing CONTRIBUTING.md.

---

## Claude's Discretion (留 planner)

- `docs/benchmarks/v0.4.md` 文件 size budget (~300-350L 估算 30 row × ~10L; suggest ≤400L hard cap)
- benchmark schema column count expansion (7→8-9 col adding task_description_human_readable / actual_command_run / manual_review_verdict / recovery_path; planner verify forward-compat 与 SAMPLES.md frozen 30-row)
- W0.3 D2 cadence iter 2 task scope (mechanical sister Phase 3.4 W2 T2.2 verbatim reuse 推荐 vs. cadence formalization checklist doc YAGNI defer)
- benchmark MANUAL re-run instructions placement (CONTRIBUTING-BENCHMARK.md NEW ≤30L vs. fold into existing CONTRIBUTING.md vs. inline in docs/benchmarks/v0.4.md header)
- e2e upgrade target manifest selection (which 上游 to demo upgrade: ctx7 / gstack / tavily-mcp; planner choose 1-2 representative + record 3-5 step text log per manifest)

## Deferred Ideas (写 CONTEXT.md `<deferred>` 节)

- plan-feature 真接 gsd-* spawn dogfood → Phase 4.2 or v0.5
- co-maintainer 招募 (R8.2) / stale-bot (R8.3) / GitHub Sponsors (R8.5) → Phase 4.2
- 路由透明度日志 audit.log (R9.2) / 公开 ADR 全集 (R8.4) / v1.0-RC 收尾 → Phase 4.3
- 跨 harness pre-v0.5 / 云端 registry v1.0+ / 可视化 dashboard / 动态 eval / wrap 上游 API / NEW workflow type → 全部 v1.0 前拒绝清单 (per ROADMAP L228-235)
- benchmark CI cron / per-PR full benchmark / asciinema cast / mp4 video / EXPAND new mining / MIX 30+miss cases — 全 evaluated rejected (or paranoid 反)

---

*Discussion: 4 questions, 1 batch AskUserQuestion call, all 4 Recommended options accepted by user (decisive cadence consistent with established workflow, sister Phase 3.1+3.2+3.3+3.4 同 pattern, 5-phase 连续 — v0.4.0 milestone 1st phase)*
