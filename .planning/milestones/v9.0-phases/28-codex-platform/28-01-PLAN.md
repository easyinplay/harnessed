---
phase: 28-codex-platform
plan: 01
type: tdd
wave: 1
depends_on: ["26-platform-descriptor-seam", "27-config-resolvers"]
files_modified:
  - src/installers/lib/platform.ts
  - src/cli/lib/enableAgentTeamsInSettings.ts
  - src/cli/lib/enableUserLangInSettings.ts
  - src/cli/lib/capabilityResolver.ts
  - src/installers/lib/readClaudeConfig.ts
  - src/cli/setup.ts
  - src/installers/lib/idempotent.ts
  - tests/installers/platform.test.ts
  - tests/installers/platform-codex.test.ts
autonomous: true
requirements:
  - "v9.0 Phase C acceptance (28-CONTEXT.md Acceptance 1-6) + architecture §3b detection + §4 Phase C (pivoted to Codex per discuss D-28a/D-28b)"
user_setup: []

verified_refs:
  - "platform.ts: PlatformDescriptor 8 fields + claudeDescriptor(home)/detectPlatform(home) + 5 resolvers (Phase 26/27, Read-confirmed); detectPlatform currently 2 branches (override / claude)"
  - "setup.ts:93 registerSetup(program: Command) commander; .option chain L99-115; getSkillsDir() L120 + getCommandsDir() L194 already resolver-backed (Read-confirmed)"
  - "idempotent.ts:121 ['.claude','.agents'] dual-probe via join(homedir(), base, 'skills', skillName, 'SKILL.md') (Read-confirmed)"
  - "enableAgentTeamsInSettings.ts / enableUserLangInSettings.ts: thin callers of settingsWriter.mergeSettingsEnvKey post-Phase-27 (Read-confirmed)"
  - "capabilityResolver.ts:82-84 readInstalledPlugins → getPluginsRegistry(homedirOverride); readClaudeConfig isPluginRegistered → getPluginsRegistry() (Phase 27, Read-confirmed)"
  - "HARNESSED_PLATFORM / .platform pin: NEW (grep-confirmed absent — only unrelated process.platform OS checks exist)"
  - "tests/installers/platform-codex.test.ts (NEW)"

must_haves:
  truths:
    - "PlatformDescriptor: pluginsRegistry: string | null; + supportsEnvKeyWrite: boolean (additive). claude descriptor value unchanged (supportsEnvKeyWrite=true, pluginsRegistry=string)"
    - "codexDescriptor(home=homedir()): homeDir ~/.codex; stateRoot ~/.codex/harnessed; settingsPath=mcpConfigPath ~/.codex/config.toml; skillsDir ~/.agents/skills (NOT ~/.codex/skills); commandsDir ~/.codex/prompts; pluginsRegistry null; supportsEnvKeyWrite false"
    - "detectPlatform precedence: HARNESSED_ROOT_OVERRIDE (claude+stateRoot, FIRST) > HARNESSED_PLATFORM=<id> > .platform pin (claude stateRoot) > auto-probe (claude home exists→claude else codex home→codex) > fallback claude. No env/pin + claude present = byte-identical claudeDescriptor"
    - "getPluginsRegistry returns string|null; capabilityResolver + readClaudeConfig tolerate null (→ no plugins / empty)"
    - "enableAgentTeams/enableUserLang gate on detectPlatform().supportsEnvKeyWrite — no-op + inform when false; claude path unchanged"
    - "setup --platform <id> (claude|codex): set process.env.HARNESSED_PLATFORM + persist .platform pin; getSkillsDir/getCommandsDir/getHarnessedRoot route to codex"
    - "idempotent.ts:121 probe set = deduped [claudeDescriptor().skillsDir, codexDescriptor().skillsDir] (descriptor-derived, same ~/.claude/skills + ~/.agents/skills)"
    - "full existing suite green UNCHANGED (claude-default regression proof); tsc 0; biome clean"
  artifacts:
    - path: "src/installers/lib/platform.ts"
      provides: "codexDescriptor + capability-aware descriptor + detectPlatform precedence"
      contains: "codexDescriptor"
    - path: "tests/installers/platform-codex.test.ts"
      provides: "codex descriptor + detection precedence + dual-platform unit matrix"
      contains: "codexDescriptor"
  key_links:
    - from: "detectPlatform() second platform"
      to: "codexDescriptor() verified Codex paths"
      via: "HARNESSED_PLATFORM / .platform pin / claude-first auto-probe (D3)"
      pattern: "codexDescriptor"
---

<objective>
把 v9.0 抽象用一个真实完整第二 harness（Codex）验证:capability-aware PlatformDescriptor + 验证过路径的 codexDescriptor + detectPlatform 多级 detection + setup --platform codex + capability-absent 写入面 + dual-platform 测试。claude default 零行为变化(全现有套件不变绿=回归证明)。TDD red-first。

约束(28-CONTEXT.md D1-D6 + discuss D-28a/D-28b):Codex 不是 .agents(.agents 只是 skills 约定);写入面 capability-absent 不写 TOML writer;claude-first detection 零 blast;skillsDir 对 codex 是共享 ~/.agents/skills 非派生;settings===mcp 同 config.toml 且写 unsupported;pluginsRegistry codex null。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/28-codex-platform/28-CONTEXT.md
@.planning/v9.0-cross-harness-ARCHITECTURE.md
@src/installers/lib/platform.ts
@src/cli/setup.ts
@src/installers/lib/idempotent.ts
</context>

<key_facts>
verified (直接用,勿重新假设):
- platform.ts (Phase 26/27): PlatformDescriptor 8 string 字段 + claudeDescriptor(home=homedir()) + detectPlatform(home=homedir()) [当前 2 branch: override / claude] + 5 resolver getSettingsPath/getSkillsDir/getCommandsDir/getPluginsRegistry/getMcpConfigPath(home?).
- Codex 真实 layout (host C:\Users\easyi\.codex + docs, 2026-06-24): config.toml(TOML, settings+[mcp_servers.*] 同文件) / skills 读共享 ~/.agents/skills / prompts/ = commands / 无 installed_plugins.json(inline [marketplaces.*]) / CODEX_HOME=~/.codex(lowercase canonical).
- ~/.agents/ = skills-only 约定(无 settings/commands/plugins/mcp);其 skills 路径 = codex 的 skillsDir。
- setup.ts:93 registerSetup commander;getSkillsDir() L120 + getCommandsDir() L194 已 resolver-backed → detection 选 codex 后自动落 codex 路径。
- idempotent.ts:121 for-loop ['.claude','.agents'] → join(homedir(), base, 'skills', skillName, 'SKILL.md')。
- 2 env-key writer 现 = settingsWriter.mergeSettingsEnvKey thin caller (Phase 27)。
- ESM (.js import 后缀)。基线: Phase 27 后 1369 tests green。
- HARNESSED_PLATFORM / .platform = NEW (grep-confirmed absent)。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1 (Wave 1): capability-aware descriptor + codexDescriptor + detectPlatform precedence (D1/D2/D3)</name>
  <files>src/installers/lib/platform.ts, tests/installers/platform-codex.test.ts, tests/installers/platform.test.ts</files>
  <red>
NEW `tests/installers/platform-codex.test.ts`:
1. `codexDescriptor('/home/x')` → homeDir '/home/x/.codex'; skillsDir '/home/x/.agents/skills' (NOT '/home/x/.codex/skills'); settingsPath===mcpConfigPath '/home/x/.codex/config.toml'; commandsDir '/home/x/.codex/prompts'; pluginsRegistry null; supportsEnvKeyWrite false; stateRoot '/home/x/.codex/harnessed'.
2. `detectPlatform` precedence (env-isolated via vi.stubEnv / per-test home tmp):
   - HARNESSED_ROOT_OVERRIDE set → id 'claude', stateRoot=override (FIRST, unchanged).
   - HARNESSED_PLATFORM='codex' (no override) → id 'codex' (codexDescriptor).
   - HARNESSED_PLATFORM unset + `.platform` pin file at claude stateRoot containing 'codex' → id 'codex'.
   - no env/pin + claude home dir exists → id 'claude' (byte-identical claudeDescriptor).
   - no env/pin + claude home absent + codex home exists → id 'codex'.
3. claude descriptor unchanged: `claudeDescriptor().supportsEnvKeyWrite===true`; `pluginsRegistry` is a string.
追加 `tests/installers/platform.test.ts`: assert existing 12 tests still green after the type widening (pluginsRegistry string for claude).
  </red>
  <green>
- `PlatformDescriptor`: `pluginsRegistry: string | null` + add `supportsEnvKeyWrite: boolean`.
- `claudeDescriptor(home)`: add `supportsEnvKeyWrite: true` (pluginsRegistry stays the string). All existing values byte-identical.
- NEW `codexDescriptor(home: string = homedir())`: `{ id:'codex', homeDir: join(home,'.codex'), stateRoot: join(home,'.codex','harnessed'), settingsPath: join(home,'.codex','config.toml'), skillsDir: join(home,'.agents','skills'), commandsDir: join(home,'.codex','prompts'), pluginsRegistry: null, mcpConfigPath: join(home,'.codex','config.toml'), supportsEnvKeyWrite: false }`. Update `id` union if needed (already `'claude'|'agents'|'cursor'|'gemini'|'copilot'` — add `'codex'`).
- `detectPlatform(home)`: precedence (anti-stale order):
  1. HARNESSED_ROOT_OVERRIDE non-empty → `{...claudeDescriptor(home), stateRoot: override}` (FIRST, unchanged).
  2. `process.env.HARNESSED_PLATFORM` → map 'codex'→codexDescriptor(home), 'claude'→claudeDescriptor(home) (unknown → ignore, fall through).
  3. `.platform` pin: read `join(claudeDescriptor(home).stateRoot, '.platform')` (well-known incumbent location) — if it holds a known id, return that descriptor. (sync `readFileSync` in a try/catch; absent → fall through.)
  4. auto-probe: `existsSync(claudeDescriptor(home).homeDir)` → claude (INCUMBENT wins); else `existsSync(codexDescriptor(home).homeDir)` → codex.
  5. fallback → claudeDescriptor(home).
- Resolver return types: `getPluginsRegistry(home?): string | null`. Other 4 resolvers unchanged.
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/installers/platform-codex.test.ts tests/installers/platform.test.ts 2>&1 | tail -12</automated>
  </verify>
  <done>codexDescriptor verified paths; detectPlatform 5-level precedence claude-first; claude descriptor byte-identical + supportsEnvKeyWrite added; getPluginsRegistry string|null. Phase 26/27 tests unchanged green.</done>
</task>

<task type="tdd">
  <name>Task 2: capability-aware resolvers + writers gated + setup --platform + idempotent probe-set (D4/D5/D6)</name>
  <files>src/cli/lib/enableAgentTeamsInSettings.ts, src/cli/lib/enableUserLangInSettings.ts, src/cli/lib/capabilityResolver.ts, src/installers/lib/readClaudeConfig.ts, src/cli/setup.ts, src/installers/lib/idempotent.ts, tests/installers/platform-codex.test.ts</files>
  <red>
追加 `platform-codex.test.ts` (env-isolated):
1. `getPluginsRegistry()` under HARNESSED_PLATFORM='codex' → null; capabilityResolver `readInstalledPlugins()` + readClaudeConfig `isPluginRegistered()` tolerate null (return [] / false, no throw).
2. `enableAgentTeamsInSettings()` under HARNESSED_PLATFORM='codex' (supportsEnvKeyWrite false) → no settings.json write; returns an informative not-supported outcome (NOT a JSON write into config.toml). enableUserLangInSettings same. Under claude (default) → unchanged behavior (existing tests green).
3. idempotent probe-set: the npx-skill-installer branch probes `claudeDescriptor().skillsDir` AND `codexDescriptor().skillsDir` (= ~/.claude/skills + ~/.agents/skills), deduped — assert both paths attempted (or via a small exported helper `harnessSkillsDirs()`).
4. setup `--platform codex` (integration-lite, tmp HOME): writes `.platform` pin; getSkillsDir() resolves ~/.agents/skills; getCommandsDir() resolves ~/.codex/prompts.
  </red>
  <green>
- `getPluginsRegistry` callers tolerate null: `capabilityResolver.readInstalledPlugins` — if registry path null → return `[]` before readFile. `readClaudeConfig.isPluginRegistered` — if null → skip the registry check (other sources still consulted).
- `enableAgentTeamsInSettings` + `enableUserLangInSettings`: early guard `if (!detectPlatform().supportsEnvKeyWrite) return <not-supported outcome mapped to existing union (e.g. a 'skipped'/'warn' variant + message)>;` BEFORE the mergeSettingsEnvKey call. Public signatures + claude behavior unchanged.
- NEW exported helper in `platform.ts`: `harnessSkillsDirs(home?): string[]` = deduped `[claudeDescriptor(home).skillsDir, codexDescriptor(home).skillsDir]`. `idempotent.ts:121` loop → iterate `harnessSkillsDirs()` joining `skillName,'SKILL.md'` (replaces the hardcoded `['.claude','.agents']` + manual join). Same paths, descriptor-derived (D6).
- `setup.ts`: add `.option('--platform <id>', 'target harness platform (claude | codex); default: auto-detect')`. In the action, BEFORE `getSkillsDir()`: if `raw.platform`, validate ∈ {claude,codex} (else error+exit), set `process.env.HARNESSED_PLATFORM = raw.platform`, and persist pin via `writeFile(join(detectPlatform().stateRoot,'.platform'), raw.platform)` (mkdir -p first). Existing resolver calls then route correctly.
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/installers/platform-codex.test.ts "tests/cli/enable*" tests/cli/setup.test.ts 2>&1 | tail -14</automated>
  </verify>
  <done>null-tolerant getPluginsRegistry callers; 2 writers gated off for codex (inform, no corrupt); setup --platform validates+pins+routes; idempotent probe descriptor-derived. claude path unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: dual-platform integration + regression gate (biome + tsc + zero claude-default change)</name>
  <files>（验证 + SUMMARY；可加 tests/integration/dual-platform.test.ts if a focused integration case adds value）</files>
  <action>
1. (optional) NEW `tests/integration/dual-platform.test.ts`: under tmp HOMEs, assert claude-pinned vs codex-pinned setup resolve EVERY descriptor field correctly (skills/commands/state/settings/mcp/plugins) — the cross-platform proof in one place.
2. `corepack pnpm exec biome check --write src/ tests/` → clean.
3. `corepack pnpm exec tsc --noEmit` → 0 (verify the `string | null` widening didn't break any getPluginsRegistry consumer).
4. `corepack pnpm test` 全套 green 0 fail. **回归证明**: 全部 claude-default 测试不变绿;新增 codex/detection/dual-platform 测试通过。
5. `git diff --stat` = 计划内文件 (不含 README.md / 未跟踪 noise)。
  </action>
  <verify>
    <automated>corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3</automated>
  </verify>
  <done>biome clean; tsc 0; 全测试 green (claude-default 不变 = 回归证明 + 新增 codex 通过); diff 计划内。证据进 SUMMARY。</done>
</task>

</tasks>

<verification>
phase 级验收 (= 28-CONTEXT.md Acceptance 1-6):
1. codexDescriptor verified paths (skillsDir 共享 ~/.agents/skills, pluginsRegistry null, supportsEnvKeyWrite false)。
2. detectPlatform precedence override-first / HARNESSED_PLATFORM / .platform pin / claude-first probe / fallback;claude default byte-identical。
3. capability-aware: getPluginsRegistry null-tolerant callers;2 writer codex no-op+inform。
4. setup --platform codex 路由 skills/commands/state + 持久化 pin。
5. dual-platform 每字段正确;idempotent probe descriptor-derived。
6. 全 claude-default 套件不变绿 (回归证明);tsc 0;biome clean。
</verification>

<success_criteria>
- v9.0 抽象用真实完整第二 harness (Codex) 验证 — descriptor 容忍真实异构 (TOML config / 共享 skills / 无 plugin registry / settings===mcp / 写 capability-absent)。
- claude default 零行为变化 (byte-identical);codex 仅显式 opt-in (HARNESSED_PLATFORM / setup --platform / pin);claude-first 零 blast。
- 不写 TOML writer (capability-absent, D-28b);不碰 .agents-as-platform / Cursor / 状态迁移。
- KARPATHY-minimal; TDD red→green; 全 gate green = seam 接纳第二平台的证明。
</success_criteria>

<output>
完成后建 `.planning/phases/28-codex-platform/28-01-SUMMARY.md`: capability-aware descriptor (字段 before/after) + codexDescriptor 路径表 + detectPlatform precedence 实现 + 2 writer gated 证据 + setup --platform + idempotent 泛化 (before/after) + dual-platform 测试计数 + 全 claude-default 不变绿 (回归证明计数) + biome/tsc + git diff --stat + v9.0 milestone-audit 衔接指针 (Phase C 完 = v9.0 3/3, 下一步 audit + close)。
</output>
