---
phase: 19-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/comparison.md
  - README.md
autonomous: true
requirements:
  - "v7.0 Phase 19 acceptance (19-CONTEXT.md D1–D4) — docs-only, no IDs"
user_setup: []

must_haves:
  truths:
    - "docs/comparison.md is a factual harnessed-vs-comet-vs-Trellis comparison with the downloads-are-noise + 2-stars caveats and 2026-06-13 snapshot dates"
    - "README links to docs/comparison.md; the install→first-command path is crisp"
    - "no over-claiming; every cited number carries its snapshot date"
    - "docs-only: zero src/ or tests/ change; full test gate stays green"
  artifacts:
    - path: "docs/comparison.md"
      provides: "Honest 3-way positioning + each tool's strengths + harnessed's real strengths and gaps"
      contains: "downloads"
    - path: "README.md"
      provides: "A How-it-compares pointer to docs/comparison.md"
      contains: "comparison.md"
---

<objective>
让项目对第一个真实用户可读(今 2 star)+ 发一篇诚实定位文(复用 2026-06-13 分析,无 vanity-metric 美化)。docs-only,零代码零测试,无 TDD。LAST phase of v7.0。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/19-adoption/19-CONTEXT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/comparison.md — 诚实 3-way 对比文</name>
  <files>docs/comparison.md</files>
  <action>
写 `docs/comparison.md`(复用 2026-06-13 分析):
- snapshot 表:月下载(harnessed 8627 / comet 6810 / Trellis 13247)+ star(2 / 1207 / 10221)+ download÷star 比;**显式标 2026-06-13 snapshot + downloads≠adoption caveat**(harnessed 下载是 CI/dogfood 噪声,4313:1 vs comet 5.6:1 / Trellis 1.3:1)。
- 三者定位(comet vendor OpenSpec+Superpowers / Trellis 自成 framework / harnessed no-vendor 编排)。
- 各自强项 + harnessed 真强项(no-vendor composition、TS+typed+tested 工程严谨、audit/provenance 供应链)+ 真 gap(采用近零、planning 曾增生已 Phase 13 治、CONTEXT-inject 实战少触发等)。
- 诚实收口:harnessed 立意干净但需真实采用验证;no-vendor + learning loop(v7.0 16-17)是最大增量故事。
**禁 over-claim**:每个数字带 snapshot 日期;不吹"最好"。
  </action>
  <verify>
    <automated>test -f docs/comparison.md && grep -qiE "2026-06-13|snapshot" docs/comparison.md && grep -qiE "noise|caveat|≠ adoption|not.*adoption" docs/comparison.md && echo "comparison-honest=PASS"</automated>
  </verify>
  <done>docs/comparison.md 存在,含 snapshot 日期 + downloads-noise caveat + 3-way 定位 + harnessed 强项与 gap,无 over-claim。</done>
</task>

<task type="auto">
  <name>Task 2: README 加 How-it-compares 链接 + 路径核</name>
  <files>README.md</files>
  <action>
README 加一行/小段 "How it compares" 指向 `docs/comparison.md`(放在头部介绍后或 Quick Start 附近,最小侵入,不重写现有 Quick Start)。核 install→首命令路径(`npm install -g harnessed && harnessed setup` → `/auto`)清晰。仅英文 README.md(非英语翻译 Phase 13 best-effort,不手同步)。
  </action>
  <verify>
    <automated>grep -qiE "comparison\.md|how it compares|对比" README.md && echo "readme-link=PASS"</automated>
  </verify>
  <done>README 有指向 docs/comparison.md 的链接;install→首命令路径清晰;现有 Quick Start 未重写。</done>
</task>

<task type="auto">
  <name>Task 3: verify gate — docs-only + 链接 + 全测试不受影响</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. `git diff --stat` 仅 docs/comparison.md + README.md(+ .planning),零 src/tests。
2. 链接核:docs/comparison.md 存在且 README 链接路径正确。
3. `corepack pnpm test` 全套仍 green(docs-only 不应影响,兜底确认)。
4. doc-discipline:对比文事实准确、带 snapshot 日期、无 over-claim。
  </action>
  <verify>
    <automated>cd /d/GitCode/harnessed && git diff --stat -- src tests | grep -q . && echo "NON-DOC-CHANGE" || echo "docs-only=PASS"; corepack pnpm test 2>&1 | grep -iE "Tests " | tail -1</automated>
  </verify>
  <done>diff 仅 docs + README;链接 resolve;全测试 green;事实准确无 over-claim。证据进 SUMMARY。</done>
</task>

</tasks>

<verification>
phase 级验收(= 19-CONTEXT.md Acceptance):
1. docs/comparison.md 诚实 3-way + caveats + snapshot 日期。
2. README 链接 + 路径清晰。
3. 事实准确无 over-claim;链接 resolve。
4. docs-only diff;全测试 green。
</verification>

<success_criteria>
- 一篇诚实对比文(复用 6-13 分析,downloads-noise + 2-star caveat 显式)+ README How-it-compares 链接。
- docs-only,零代码;v7.0 LAST phase 收尾。
</success_criteria>

<output>
完成后建 `.planning/phases/19-adoption/19-01-SUMMARY.md`:对比文要点、README 链接、docs-only diff、全测试 green、事实准确确认、`git diff --stat`。
</output>
