---
phase: 17-spec-injection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/checkpoint/injectState.ts
  - bin/harnessed-inject-state.mjs
  - tests/checkpoint/injectState.test.ts
autonomous: true
requirements:
  - "v7.0 Phase 17 acceptance (17-CONTEXT.md D1–D8) — no numeric REQUIREMENTS.md IDs mapped"
user_setup: []

must_haves:
  truths:
    - "parseLearnings round-trips the Phase-16 markdown into {phase, subs} entries; header-only file → []"
    - "filterRelevantLearnings keeps phase-match OR sub-match entries, recency-ordered; most-recent fallback when none match"
    - "selectWithinBudget never exceeds the token budget (Buffer/4, default 1500, HARNESSED_INJECT_BUDGET override); recent-first"
    - "buildInjection composes <workflow-state> + <project-context>; workflow-state only when no learnings/CONTEXT; '' when no workflow"
    - "the bin is repo-aware: reads workflows.json[repoKey(cwd)] (legacy current-workflow.json fallback) — not the global singleton blindly"
    - "the bin stays self-contained plain JS (no project imports, no subprocess, no LLM); the parity test guards drift vs the TS builder"
    - "fail-soft: any error → emit nothing extra; full gate green; biome clean"
  artifacts:
    - path: "src/checkpoint/injectState.ts"
      provides: "parseLearnings/filterRelevantLearnings/selectWithinBudget/buildProjectContextBlock/findPhaseContextExcerpt/buildInjection"
      contains: "filterRelevantLearnings"
    - path: "bin/harnessed-inject-state.mjs"
      provides: "repo-aware per-turn hook emitting <workflow-state> + relevance-filtered <project-context>"
      contains: "repoKey"
    - path: "tests/checkpoint/injectState.test.ts"
      provides: "pure parse/filter/select/build coverage + extended repo-aware bin parity"
      contains: "project-context"
  key_links:
    - from: "bin harnessed-inject-state.mjs (UserPromptSubmit hook)"
      to: "workflows.json[repoKey(cwd)] + <repoRoot>/.planning/LEARNINGS.md + phase CONTEXT.md"
      via: "repo-aware relevance-filtered injection (closes the 16→17 loop)"
      pattern: "project-context"
---

<objective>
扩 G4 per-turn inject hook:除 `<workflow-state>` 外,注入 relevance-FILTERED 项目知识(repo 的 phase/sub-相关 `.planning/LEARNINGS.md` 条目 + 当前 phase CONTEXT.md 摘录),token-bounded。闭合 16→17 learning loop(capture→inject)。顺带修 Phase 15 遗留:bin 改 repo-aware(按 repoKey(cwd) 读 store,非盲读全局 singleton)。

约束(17-CONTEXT.md D1–D8):relevance-FILTER(phase/sub 匹配 + recency,无 LLM,无语义);源 = learnings + 当前 phase CONTEXT(静态文件/CLAUDE.md 排除);token 预算默认 1500 可 env override;bin 自包含 plain JS + parity test 兜;fail-soft;TDD red-first。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/17-spec-injection/17-CONTEXT.md
@src/checkpoint/injectState.ts
@bin/harnessed-inject-state.mjs
@tests/checkpoint/injectState.test.ts
@src/checkpoint/learnings.ts
@src/checkpoint/workflowStore.ts
</context>

<key_facts>
planner 已验证（直接用）：

- `injectState.ts`:纯 `buildWorkflowStateBlock(wf): string`（保留不动）。
- `bin/harnessed-inject-state.mjs`:自包含 plain JS,读 `current-workflow.json` 直发块。**parity test 已存在** `tests/checkpoint/injectState.test.ts` `describe('bin/...mjs parity')` —— execFileSync 跑 .mjs + HARNESSED_ROOT_OVERRIDE 比对 builder。扩展它。
- Phase 16 LEARNINGS.md 条目格式:`### <at> — phase <phase>` / `- <kind>: <sub> (×N)` / `- lesson: <prose>`,空格分隔块。CONTEXT 文件 `.planning/phases/<N>/<N>-CONTEXT.md`。
- Phase 15:`workflows.json` = `{schemaVersion, workflows: {<repoKey>: envelope}}`;`repoKey(cwd)` = 向上找 `.git`。bin 要 plain-JS 复制 repoKey walk-up + 读 workflows.json[repoKey](缺则 fallback current-workflow.json)。
- token:`Buffer.byteLength(JSON.stringify|str)/4`（sister）。
- 测试 setup:HARNESSED_ROOT_OVERRIDE + tmpdir + chdir(repo)（sister state-store/learnings 测试）。基线 1265 green。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1: 纯 parseLearnings + filterRelevantLearnings + selectWithinBudget</name>
  <files>src/checkpoint/injectState.ts, tests/checkpoint/injectState.test.ts</files>
  <red>
失败测试:
1. `parseLearnings(md)`:Phase-16 格式 md(含 header + 2 entry)→ 2 个 `{raw, phase, subs[]}`(phase 从 `phase X` 抽,subs 从 `- kind: sub` 行抽);header-only / 空 → []。
2. `filterRelevantLearnings(entries, {phase:'P', ledgerSubs:['s1']})`:保留 `entry.phase==='P'` 或 `entry.subs ∩ ['s1']≠∅`;按 recency(输入顺序末尾=新)排,新在前;**都不匹配 → 返回最近 1 条**(fallback)。
3. `selectWithinBudget(entries, budget)`:贪心从新到旧累加 `tok(raw)`,不超 budget;budget 极小 → 至多 0/1 条不超额。
当前无这些函数 → red。
  </red>
  <green>
`injectState.ts` 续(纯):
- `export interface LearningEntry { raw: string; phase: string; subs: string[] }`
- `parseLearnings(md)`:`md.split(/^### /m).slice(1)` 每块 → raw=`### `+块;phase=match `phase (\S+)`;subs=每行 `^- (?:looped|rejected|failed): (\S+)` 的 sub。无 entry→[]。
- `filterRelevantLearnings(entries, ctx)`:`const rel = entries.filter(e => e.phase===ctx.phase || e.subs.some(s=>ctx.ledgerSubs.includes(s)))`;`const ordered = [...rel].reverse()`(新在前,假设输入 append 序);`if(ordered.length===0 && entries.length>0) return [entries[entries.length-1]]`;return ordered。
- `selectWithinBudget(entries, budget)`:`const tok = s=>Math.ceil(Buffer.byteLength(s,'utf8')/4)`;贪心累加 raw,`acc+tok<=budget` 收,否则停;return 收集。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/injectState.test.ts -t "parseLearnings|filterRelevant|selectWithinBudget" 2>&1 | tail -15</automated>
  </verify>
  <done>parseLearnings 解析 + header-only→[];filterRelevant phase/sub 匹配 + fallback;selectWithinBudget 不超额。纯。</done>
</task>

<task type="tdd">
  <name>Task 2: buildProjectContextBlock + findPhaseContextExcerpt + buildInjection</name>
  <files>src/checkpoint/injectState.ts, tests/checkpoint/injectState.test.ts</files>
  <red>
失败测试:
1. `buildProjectContextBlock({learnings:[...], contextExcerpt:'...'})`:发 `<project-context>` 块含 learnings raw + CONTEXT 摘录;两者皆空 → `''`。
2. `findPhaseContextExcerpt(repoRoot, phase, budget)`(tmp repo):`.planning/phases/13-x/13-CONTEXT.md` 存在且 phase 含 `13` → 返回 bounded 摘录(含 Goal);无匹配 → null。
3. `buildInjection(repoRoot, wf, learningsMd, budget)`:wf + 相关 learnings → workflow-state + project-context 两块;wf 但无 learnings/CONTEXT → 仅 workflow-state;wf=null → ''。
  </red>
  <green>
`injectState.ts` 续:
- `buildProjectContextBlock({learnings, contextExcerpt})`:`const parts=[]; for(l of learnings) parts.push(l.raw.trim()); if(contextExcerpt) parts.push(contextExcerpt.trim()); if(parts.length===0) return ''; return ['<project-context>', ...parts, '</project-context>'].join('\n')`。
- `findPhaseContextExcerpt(repoRoot, phase, budget)`(impure):glob `<repoRoot>/.planning/phases/*/`-`-CONTEXT.md`(用 readdirSync);选 dir 名 token 出现在 `phase` 里的;读文件,截 Goal+Locked-decisions 段(简单:取前 `budget` token 内的 `## Goal`..下个 `## ` 或前 N 行);无匹配/读失败 → null。
- `buildInjection(repoRoot, wf, learningsMd, budget=DEFAULT)`:`const ws = buildWorkflowStateBlock(wf); if(!wf) return ''; const ledgerSubs=(wf.sub_progress??[]).map(e=>e.sub); const entries=parseLearnings(learningsMd); const rel=selectWithinBudget(filterRelevantLearnings(entries,{phase:wf.phase,ledgerSubs}), budget); const ctx=findPhaseContextExcerpt(repoRoot, wf.phase, Math.max(0, budget - rel.reduce(...tok))); const pc=buildProjectContextBlock({learnings:rel, contextExcerpt:ctx??undefined}); return pc ? `${ws}\n${pc}` : ws`。export `DEFAULT_INJECT_BUDGET=1500`。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/injectState.test.ts 2>&1 | tail -18</automated>
  </verify>
  <done>buildProjectContextBlock 块/空;findPhaseContextExcerpt 匹配 bounded/null;buildInjection 两块/仅 ws/'' 三态。</done>
</task>

<task type="tdd">
  <name>Task 3: bin 重写 repo-aware + relevance-filtered injection + 扩 parity test</name>
  <files>bin/harnessed-inject-state.mjs, tests/checkpoint/injectState.test.ts</files>
  <red>
扩 parity test(`describe('bin ... parity')`):在 override root 写 `workflows.json = {schemaVersion, workflows:{[repoKey]: envelope-with-ledger}}` + repo 下 `.planning/LEARNINGS.md`(含 1 条 phase-相关 entry)+ chdir(repo);execFileSync 跑 .mjs;断言 stdout 含 `<workflow-state>` + `<project-context>` + 该 learning 的 sub。再断言:仅 legacy `current-workflow.json`(无 workflows.json)时 .mjs 仍出 workflow-state(fallback)。
  </red>
  <green>
重写 `bin/harnessed-inject-state.mjs`(plain JS,自包含):
- `repoKey(cwd)`:`existsSync(join(dir,'.git'))` 向上走,fallback resolve(cwd)。
- root 解析同现状(HARNESSED_ROOT_OVERRIDE | homedir/.claude/harnessed)。
- 读 workflow:`try workflows.json → wf = parsed.workflows[repoKey()]`;缺/无该 key → fallback `JSON.parse(current-workflow.json)`;再缺 → 无 wf。
- 无 wf → exit 0 静默(sister)。
- workflow-state 块(同现有逻辑:phase/status/next/BREAK-LOOP)。
- learnings:`readFileSync(join(repoKey(),'.planning','LEARNINGS.md'))` → plain-JS parse(`split(/^### /m)`)+ filter(phase===wf.phase || subs∩ledgerSubs)+ recency reverse + fallback most-recent + budget(`HARNESSED_INJECT_BUDGET` || 1500,Buffer/4 贪心)→ 若有,发 `<project-context>` 块(learnings raw)。CONTEXT 摘录:best-effort 同 TS(读匹配 phase 的 CONTEXT.md Goal 段),无则略。
- 全程 try/catch fail-soft(任何读失败 → 那部分不发)。
- **逻辑须与 TS buildInjection 等价**(parity test 守)。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/injectState.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>bin repo-aware(workflows.json[repoKey] + legacy fallback)+ 发 workflow-state + relevance-filtered project-context;parity test 绿(.mjs ≡ TS buildInjection);fail-soft。</done>
</task>

<task type="auto">
  <name>Task 4: verify gate — 全测试 + biome + 无 repo 污染 + diff scope</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. `corepack pnpm exec biome check --write` 目标 → `biome check` clean。
2. `corepack pnpm exec tsc --noEmit` exit 0。
3. `corepack pnpm test` 全套 green 0 fail(≥1265 + 新增 injectState 测试)。
4. **污染检查**(`test -f` 非 git-grep,RTK 假阳教训):真 repo `.planning/LEARNINGS.md` 不应被测试创建。注:Phase 17 只 READ learnings,不写,所以本不该污染;仍 `test -f` 确认。
5. `git diff --stat` 仅 3 目标文件。
  </action>
  <verify>
    <automated>cd /d/GitCode/harnessed && corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3 && (test -f .planning/LEARNINGS.md && echo "POLLUTION" || echo "no-pollution=PASS")</automated>
  </verify>
  <done>biome clean;tsc 0;全测试 green;无真 repo 污染;diff 仅目标文件。证据进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-17-01 | Tampering (logic drift) | bin plain-JS 复制 ≠ TS buildInjection | mitigate | 扩展已有 parity test:execFileSync 跑真 .mjs 比对 TS;fixture 覆盖 workflow-state + learnings |
| T-17-02 | DoS (hot-path slow) | per-turn hook 变慢 | mitigate | 自包含 plain JS、无 subprocess、无 LLM、无重 import;只 fs 读 + 轻量 parse;budget 限制注入体积 |
| T-17-03 | Info Disclosure (over-inject) | 注入超 token 预算 / 注入无关 repo | mitigate | selectWithinBudget 硬限;repo-aware(repoKey(cwd))只读当前 repo;D2 排除静态文件 |
| T-17-04 | Tampering (wrong repo) | bin 盲读全局 singleton 注入错 repo state | mitigate | D4 repo-aware:workflows.json[repoKey(cwd)] 优先,legacy 仅 fallback;parity test cwd 隔离 |
| T-17-05 | Availability (hook crash blocks prompt) | 读失败抛错中断 UserPromptSubmit | mitigate | 全程 try/catch fail-soft → 出错只少注入,绝不抛(sister G4) |
</threat_model>

<verification>
phase 级验收(= 17-CONTEXT.md Acceptance):
1. parseLearnings 解析 + header-only→[]。
2. filterRelevantLearnings phase/sub 匹配 + recency + fallback。
3. selectWithinBudget 不超额 recent-first。
4. buildProjectContextBlock 块/空。
5. buildInjection 两块/仅 ws/'' 三态。
6. bin repo-aware + parity:.mjs 出 workflow-state + relevant learnings,≡ TS buildInjection;legacy fallback。
7. 全 gate green;无污染;biome clean。
</verification>

<success_criteria>
- per-turn hook 注入 relevance-filtered learnings + 当前 phase CONTEXT 摘录,token-bounded,闭合 16→17 loop。
- bin repo-aware(修 Phase 15 遗留)、自包含 plain JS、parity 守、fail-soft。
- 确定性 filter(无 LLM)、静态文件排除。
- TDD red→green;全 gate green;KARPATHY-minimal。
</success_criteria>

<output>
完成后建 `.planning/phases/17-spec-injection/17-01-SUMMARY.md`:RED→GREEN、parse/filter/select 证据、buildInjection 三态、bin repo-aware parity 输出、fail-soft、无污染、biome clean、tsc 0、全测试计数、`git diff --stat`。诚实记:CONTEXT-match 常 miss(phase 字段≠phase dir),learnings 是可靠半。
</output>
