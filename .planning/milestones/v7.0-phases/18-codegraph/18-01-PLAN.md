---
phase: 18-codegraph
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - manifests/optional/codegraph.yaml
  - src/cli/lib/check-codegraph.ts
  - src/cli/lib/doctor-registry.ts
  - workflows/capabilities.yaml
  - tests/cli/check-codegraph.test.ts
  - tests/cli/doctor.test.ts
autonomous: true
requirements:
  - "v7.0 Phase 18 acceptance (18-CONTEXT.md D1–D6) — no numeric REQUIREMENTS.md IDs mapped"
user_setup: []

must_haves:
  truths:
    - "manifests/optional/codegraph.yaml validates (validateManifestFile.ok); MIT; cli-npm; describes codegraph's own install/verify/uninstall"
    - "the manifest is opt-in: NOT globbed by install-base (lives in manifests/optional/, not tools/skill-packs)"
    - "checkCodeGraph always returns status 'pass': present (.codegraph/ exists) → detected; absent → optional-not-configured + enable hint"
    - "doctor registry has 13 checks; harnessed doctor summary stays 'pass'"
    - "capabilities.yaml has a conforming codegraph tool entry; check-workflow-schema stays green"
    - "harnessed runs NO codegraph install and vendors NO codegraph code (catalog + detect only)"
    - "full gate green; biome clean"
  artifacts:
    - path: "manifests/optional/codegraph.yaml"
      provides: "opt-in cli-npm manifest cataloging CodeGraph (MIT, self-installed)"
      contains: "codegraph"
    - path: "src/cli/lib/check-codegraph.ts"
      provides: "always-pass doctor detect of .codegraph/ + enable hint"
      contains: "checkCodeGraph"
  key_links:
    - from: "doctor-registry.ts CHECKS"
      to: "checkCodeGraph (.codegraph/ detection)"
      via: "13th doctor check (opt-in, always-pass)"
      pattern: "checkCodeGraph"
---

<objective>
把 CodeGraph 编目为 opt-in 语义索引组件:validated manifest(provenance)+ doctor 探测 + capability 条目。harnessed **不跑 install**(CodeGraph 自带 installer 自装)、**不 vendor**(no-vendor 立意到底)。manifest 放 `manifests/optional/`(install-base 不 glob → 永不进 base);doctor 探测 `.codegraph/` always-pass(opt-in 缺失非健康问题)。

约束(18-CONTEXT.md D1–D6):catalog+detect-only、opt-in(optional dir)、cli-npm + MIT、doctor always-pass、capability 条目合规、TDD red-first。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/18-codegraph/18-CONTEXT.md
@manifests/tools/ctx7.yaml
@manifests/tools/chrome-devtools-mcp.yaml
@src/cli/lib/doctor-registry.ts
@src/cli/lib/check-token-budget.ts
@workflows/capabilities.yaml
</context>

<key_facts>
planner 已验证（直接用）：

- CodeGraph = npm `@colbymchenry/codegraph`, **MIT**, TypeScript, 100% local。install:`npm i -g @colbymchenry/codegraph` → `codegraph install`(接 MCP)→ `codegraph init`(per-project `.codegraph/`)。verify `codegraph status`;uninstall `codegraph uninstall`;shortcut `npx @colbymchenry/codegraph`。
- cli-npm manifest shape = `manifests/tools/ctx7.yaml`(type:cli-npm/component_type:cli-binary/category/install_type:npm/install{method:npm-cli,cmd,npm_version,idempotent_check}/verify/uninstall/upstream_health/signed_by/platforms)。license 白名单 MIT ✓。
- `install-base` glob `manifests/tools` + `manifests/skill-packs` only → `manifests/optional/` 不进 base。`install <name>` 只 resolve tools+skill-packs → optional 不可被 harnessed 装(intended)。
- doctor `CheckResult {name, status:'pass'|'warn'|'fail', message, fix?}`;`CHECKS` array in `doctor-registry.ts`(12);`warn≠fail`(exit 0);`tests/cli/doctor.test.ts` 三处 length 断言 12 + summary 'pass'。
- capabilities tool 条目(sister chrome-devtools-mcp):`impl / cmd / since / category / description / fires_when`。`check-workflow-schema` 校验。
- `validateManifestFile(src, path)` 校验 manifest(sister install-base 用法)。
- 基线 1280 green。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1: checkCodeGraph doctor 探测(always-pass)</name>
  <files>src/cli/lib/check-codegraph.ts, tests/cli/check-codegraph.test.ts</files>
  <red>
失败测试(tmp dir + 注入 cwd):
1. `.codegraph/` 存在的 dir → `checkCodeGraph(dir)` 返回 `{name:'codegraph', status:'pass', message contains 'detected'}`。
2. 无 `.codegraph/` 的 dir → `status:'pass'`(opt-in 缺失非 fail)+ message 含 'optional' + 'npx @colbymchenry/codegraph' enable hint。
3. **永远 pass**(两种情况 status 都 'pass')。
当前无 check-codegraph.ts → red。
  </red>
  <green>
`check-codegraph.ts`(NEW):`import { existsSync } from 'node:fs'; import { join } from 'node:path'; import type { CheckResult } from './doctor-registry.js'`。
`export function checkCodeGraph(cwd: string = process.cwd()): CheckResult`:`const present = existsSync(join(cwd, '.codegraph')); return present ? {name:'codegraph', status:'pass', message:'CodeGraph index detected (.codegraph/)'} : {name:'codegraph', status:'pass', message:'CodeGraph not configured (optional semantic index)', fix:'npx @colbymchenry/codegraph to enable — MIT, 100% local'}`。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/check-codegraph.test.ts 2>&1 | tail -12</automated>
  </verify>
  <done>checkCodeGraph present→pass detected / absent→pass optional+hint;两种都 pass。</done>
</task>

<task type="tdd">
  <name>Task 2: manifests/optional/codegraph.yaml + 校验测试</name>
  <files>manifests/optional/codegraph.yaml, tests/cli/check-codegraph.test.ts</files>
  <red>
失败测试:`validateManifestFile(readFileSync('manifests/optional/codegraph.yaml'), path).ok === true`;且 metadata.name === 'codegraph'、license 'MIT'。当前文件不存在 → red。
  </red>
  <green>
`manifests/optional/codegraph.yaml`(NEW,镜像 ctx7 cli-npm shape):
- metadata: name `codegraph`, display_name `CodeGraph`, description(≤120 char)`Pre-indexed code knowledge graph (semantic index) for Claude Code — fewer tokens/tool-calls. Self-installed, opt-in.`, upstream{source:codegraph, homepage:https://colbymchenry.github.io/codegraph/, repository:https://github.com/colbymchenry/codegraph.git, license:MIT, notice:`CodeGraph by colbymchenry (MIT, 100% local). Optional semantic index — harnessed catalogs + detects only; CodeGraph self-installs via its own CLI (codegraph install).`}
- spec: type cli-npm, component_type cli-binary, category search(或 engineering — 选 schema 允许的;sister ctx7 用 search), install_type npm, install{method:npm-cli, cmd:`npm i -g @colbymchenry/codegraph && codegraph install --yes`, npm_version `^1.0.0`, idempotent_check:`command -v codegraph`}, verify{cmd:`codegraph status`, timeout_ms 10000, expected_exit_code 0}, uninstall{cmd:`codegraph uninstall --yes`}, upstream_health{stability stable, last_check `2026-06-13`, last_known_good_version `1.0.0`, fallback_action warn}, signed_by easyinplay, platforms[linux,darwin,win32]。
**注意**:category / 各 enum 值须在 schema 允许集内(执行时若 validateManifestFile fail,按报错调字段;category 取 ctx7 同款 search)。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/check-codegraph.test.ts -t "manifest" 2>&1 | tail -12</automated>
  </verify>
  <done>codegraph.yaml validateManifestFile.ok、name codegraph、MIT;在 manifests/optional/(不进 base)。</done>
</task>

<task type="tdd">
  <name>Task 3: 注册 doctor check(12→13)+ capabilities 条目</name>
  <files>src/cli/lib/doctor-registry.ts, tests/cli/doctor.test.ts, workflows/capabilities.yaml</files>
  <red>
失败测试:`CHECKS.length === 13`(doctor.test.ts 现断言 12)。先把 doctor.test.ts 三处 `12`→`13`(+ length 13)。运行 → 因 registry 仍 12 → red。
  </red>
  <green>
- `doctor-registry.ts`:`CHECKS` array append `async () => { const { checkCodeGraph } = await import('./check-codegraph.js'); return checkCodeGraph(process.cwd()) }`(sister lazy-import pattern)。
- `tests/cli/doctor.test.ts`:三处 `toBe(12)`/`toHaveLength(12)` → 13。summary 'pass' 断言不变(checkCodeGraph always-pass 保 green)。
- `workflows/capabilities.yaml`:加 codegraph tool 条目(sister chrome-devtools-mcp):
  ```
    codegraph:
      impl: mcp
      cmd: codegraph_explore
      since: v7.0
      category: tool-mcp
      description: 语义代码索引 (symbol/call-graph query; opt-in, self-installed; fewer tool-calls)
      fires_when:
        - subtask.type in ['code_navigation', 'symbol_lookup', 'call_graph']
  ```
  (若 check-workflow-schema 对 fires_when 表达式或字段有约束报错,按报错最小调整)。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/doctor.test.ts tests/scripts/check-workflow-schema.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>CHECKS 13、doctor summary 'pass'、doctor.test length 13;capabilities codegraph 条目 check-workflow-schema green。</done>
</task>

<task type="auto">
  <name>Task 4: verify gate — 全测试 + biome + opt-in 确认</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. `corepack pnpm exec biome check --write` 目标 → `biome check` clean。
2. `corepack pnpm exec tsc --noEmit` exit 0。
3. `corepack pnpm test` 全套 green 0 fail（≥1280 + 新增）。
4. **opt-in 确认**:`grep -rn "codegraph" src/cli/install-base.ts` 应无(install-base 不引 optional);`harnessed doctor` summary 'pass'(checkCodeGraph 不 fail)。
5. **no-vendor 确认**:无 codegraph 上游代码被拷进 repo(只 manifest yaml + doctor check + capability 条目)。
6. `git diff --stat` 仅目标文件。
  </action>
  <verify>
    <automated>cd /d/GitCode/harnessed && corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3</automated>
  </verify>
  <done>biome clean;tsc 0;全测试 green;opt-in(不进 base)+ no-vendor 确认;diff 仅目标文件。证据进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-18-01 | Tampering (unwanted install) | codegraph 混入 base / 被 harnessed 自动装 | mitigate | manifest 放 manifests/optional/(install-base 不 glob);install <name> 不 resolve optional;Task 4 grep 确认 |
| T-18-02 | Availability (doctor false-fail) | opt-in 工具缺失让 doctor 报 fail | mitigate | checkCodeGraph always-pass(absent 也 pass);doctor summary 'pass' 测试守 |
| T-18-03 | Supply chain (bad license) | 非白名单 license 混入 | mitigate | CodeGraph MIT(白名单);validateManifestFile schema 校验守门 |
| T-18-04 | Tampering | 误把 codegraph 上游代码 vendor 进 repo | mitigate | D1 catalog-only:仅 manifest 描述 + 探测;Task 4 no-vendor 确认 |
</threat_model>

<verification>
phase 级验收(= 18-CONTEXT.md Acceptance):
1. codegraph.yaml validateManifestFile.ok + MIT + 描述 install/verify/uninstall。
2. 不进 base + 不可 harnessed install。
3. checkCodeGraph present→pass detected / absent→pass optional+hint(always-pass)。
4. doctor 13 checks + summary 'pass' + length 断言更新。
5. capabilities codegraph 条目 + check-workflow-schema green。
6. 全 gate green;biome clean;no-vendor。
</verification>

<success_criteria>
- CodeGraph 编目为 opt-in manifest(manifests/optional/,MIT,描述自装)+ doctor 探测(always-pass)+ capability 条目;harnessed 不装不 vendor。
- 永不进 base、doctor 不因缺失 fail。
- TDD red→green;全 gate green;KARPATHY-minimal;no-vendor 立意守恒。
</success_criteria>

<output>
完成后建 `.planning/phases/18-codegraph/18-01-SUMMARY.md`:RED→GREEN、manifest validate 证据、checkCodeGraph present/absent、doctor 13 + summary pass、capabilities 条目 schema green、opt-in + no-vendor 确认、biome clean、tsc 0、全测试计数、`git diff --stat`。
</output>
