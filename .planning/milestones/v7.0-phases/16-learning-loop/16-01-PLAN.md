---
phase: 16-learning-loop
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/checkpoint/learnings.ts
  - src/cli/checkpoint.ts
  - src/cli/learn.ts
  - src/cli.ts
  - tests/checkpoint/learnings.test.ts
autonomous: true
requirements:
  - "v7.0 Phase 16 acceptance (16-CONTEXT.md D1–D6) — no numeric REQUIREMENTS.md IDs mapped"
user_setup: []

must_haves:
  truths:
    - "extractLearnings returns looped(fail_count>0)/rejected/failed signals from the final ledger; empty array for a clean ledger"
    - "appendLearning creates <repoRoot>/.planning/LEARNINGS.md on first write and APPENDS (never truncates) thereafter"
    - "captureWorkflowLearnings is a no-op (no file created) when the ledger has no failure signals (D4 — protects the real repo + test suite from pollution)"
    - "the write target is repoKey(cwd) repo root, never the global harnessed root"
    - "checkpoint complete auto-captures into the repo LEARNINGS.md only when the completed workflow ledger has failure signals"
    - "harnessed learn \"<lesson>\" appends a prose learning to the current repo's LEARNINGS.md"
    - "append-only: existing LEARNINGS.md content is never rewritten/deleted; full gate green; biome clean"
  artifacts:
    - path: "src/checkpoint/learnings.ts"
      provides: "extractLearnings/formatLearningEntry (pure) + learningsPath/appendLearning/captureWorkflowLearnings (impure, repo-targeted)"
      contains: "extractLearnings"
    - path: "src/cli/learn.ts"
      provides: "harnessed learn <lesson> — append a prose learning to the repo"
      contains: "registerLearn"
    - path: "tests/checkpoint/learnings.test.ts"
      provides: "pure extraction + append + no-empty-write + auto-capture TDD coverage"
      contains: "no-op"
  key_links:
    - from: "checkpoint complete (allResolved)"
      to: "captureWorkflowLearnings → <repoRoot>/.planning/LEARNINGS.md"
      via: "auto-capture half of the hybrid loop (D3)"
      pattern: "captureWorkflowLearnings"
---

<objective>
闭合 cross-session learning loop:workflow 完成时把 learnings（机械的 failed/loop/rejected 信号 + 可选 agent prose）APPEND 进 repo 的 git-可共享 `.planning/LEARNINGS.md`，下次 session 更聪明（Trellis update-spec 对位，no-vendor）。append-only 不改现有文档(D1);写 in-repo `<repoRoot>/.planning/LEARNINGS.md`(D2,repoKey 复用 Phase 15);hybrid 抽取(D3:自动机械 + `harnessed learn` prose);**无信号不写**(D4,防污染真 repo + 测试套件);TDD red-first。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/16-learning-loop/16-CONTEXT.md
@src/checkpoint/workflowStore.ts
@src/checkpoint/schema/currentWorkflow.v1.ts
@src/checkpoint/breakLoop.ts
@src/cli/checkpoint.ts
</context>

<key_facts>
planner 已验证（直接用）：

- `SubProgressEntry` = `sub, status(pending|done|failed|skipped|rejected), gate_fired, reason?, evidence_status?, evidence?, fail_count?`。机械信号:`fail_count>0`=looped(G6),`status==='rejected'`=abandoned(G7-lite),`status==='failed'`=failed。
- `repoKey(cwd=process.cwd())` from `workflowStore.ts`(Phase 15)= 向上找 `.git` 的 repo root。learnings 写 `join(repoKey(), '.planning', 'LEARNINGS.md')` —— **repo 工作树,非 `~/.claude/harnessed` 全局根**。
- `checkpoint.ts` complete 分支:`const allResolved = nextPending(afterMark?.sub_progress ?? []) === null`(L220)→ `completePhase({...transitionWorkflowComplete: allResolved})`(L221)→ Phase 14 auto-compact(L228)→ `process.exit(0)`。auto-capture 插在 completePhase 后、当 `allResolved` 且有信号时,用 `afterMark.sub_progress`。
- 时间戳:src 里可用 `new Date().toISOString()`(sister state.ts activate)。
- 测试 setup:HARNESSED_ROOT_OVERRIDE + tmpdir + `process.chdir(tmpRepo)`(sister `tests/checkpoint/state-store.test.ts`)。基线 1256 green。
- CLI 注册 sister:`registerCompact`(22nd)/`registerWorkflows`(23rd);learn = 24th。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1: 纯函数 extractLearnings + formatLearningEntry</name>
  <files>src/checkpoint/learnings.ts, tests/checkpoint/learnings.test.ts</files>
  <red>
失败测试:
1. `extractLearnings(ledger)`:含 `{sub:'a',status:'done',fail_count:3}`(looped)、`{sub:'b',status:'rejected'}`、`{sub:'c',status:'failed'}`、`{sub:'d',status:'done'}`(clean)→ 返回 3 个 signal(looped a / rejected b / failed c),`d` 不在内。每 signal 含 `{sub, kind, detail?}`(kind ∈ looped|rejected|failed;looped 带 fail_count)。
2. clean ledger(全 done/pending/skipped,无 fail_count)→ `[]`。
3. `formatLearningEntry({phase:'task', signals, prose:'lesson X', at:'2026-...'})`→ markdown block:含日期、phase、每个 signal 一行、prose 行。prose 省略时不出 prose 行。
当前无 `learnings.ts` → red。
  </red>
  <green>
`learnings.ts`(NEW)纯部分:
- `export interface LearningSignal { sub: string; kind: 'looped'|'rejected'|'failed'; fail_count?: number }`
- `extractLearnings(ledger: SubProgressEntryType[]): LearningSignal[]` —— map/filter:`(e.fail_count??0)>0`→looped(带 fail_count);`status==='rejected'`→rejected;`status==='failed'`→failed。一个 sub 可能既 failed 又 looped → 优先 looped(fail_count>0 already implies repeated fail);用 if/else 链每 sub 至多一条,looped 优先,其次 rejected,其次 failed。clean → []。
- `formatLearningEntry({phase, signals, prose, at}): string` —— 返回 `### ${at} — phase ${phase}\n` + 每 signal `- ${kind}: ${sub}${fail_count?` (×${fail_count})`:''}\n` + (prose?`- lesson: ${prose}\n`:'') + `\n`。纯。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/learnings.test.ts -t "extractLearnings|formatLearningEntry" 2>&1 | tail -15</automated>
  </verify>
  <done>extractLearnings 三信号 + clean→[];formatLearningEntry markdown block 含/略 prose 正确。纯函数。</done>
</task>

<task type="tdd">
  <name>Task 2: appendLearning + captureWorkflowLearnings(no-empty-write)</name>
  <files>src/checkpoint/learnings.ts, tests/checkpoint/learnings.test.ts</files>
  <red>
失败测试(tmp repo + chdir,sister state-store):
1. `learningsPath(repoRoot)` = `join(repoRoot,'.planning','LEARNINGS.md')`。
2. `appendLearning(repoRoot, entry)`:`.planning/` 不存在 → 创建 + 写文件(首次带 `# Learnings` header)+ entry;再次调 → APPEND(文件含两 entry,header 只一个,旧内容保留)。
3. `captureWorkflowLearnings(ledger, phase)`(用 repoKey(cwd)=chdir 的 tmp repo):ledger 有信号 → LEARNINGS.md 出现且含信号;**clean ledger → 不创建文件**(`existsSync` false)(D4 核心)。
  </red>
  <green>
`learnings.ts` 续(impure):
- `import { existsSync } from 'node:fs'`;`import { appendFile, mkdir, writeFile } from 'node:fs/promises'`;`import { dirname, join } from 'node:path'`;`import { repoKey } from './workflowStore.js'`。
- `export function learningsPath(repoRoot: string): string { return join(repoRoot, '.planning', 'LEARNINGS.md') }`
- `export async function appendLearning(repoRoot: string, entry: string): Promise<void>`:`const p = learningsPath(repoRoot); await mkdir(dirname(p), {recursive:true}); if(!existsSync(p)) await writeFile(p, '# Learnings\n\n> Append-only cross-session learning log (harnessed Phase 16). Newest at bottom.\n\n', 'utf8'); await appendFile(p, entry, 'utf8')`。
- `export async function captureWorkflowLearnings(ledger, phase): Promise<number>`:`const signals = extractLearnings(ledger); if(signals.length===0) return 0; const entry = formatLearningEntry({phase, signals, at: new Date().toISOString()}); await appendLearning(repoKey(), entry); return signals.length`。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/learnings.test.ts 2>&1 | tail -18</automated>
  </verify>
  <done>appendLearning 首建带 header + 后续 append 不截断;captureWorkflowLearnings 有信号写、clean 不创建文件(D4)。写目标 repoKey(cwd)。</done>
</task>

<task type="tdd">
  <name>Task 3: checkpoint complete auto-capture + harnessed learn CLI</name>
  <files>src/cli/checkpoint.ts, src/cli/learn.ts, src/cli.ts, tests/checkpoint/learnings.test.ts</files>
  <red>
失败测试:
1. **auto-capture(integration,isolated cwd=tmp repo)**:seed 一个含 failed/rejected sub 的 ledger → `checkpoint complete` 末 sub(allResolved)→ tmp repo 的 `.planning/LEARNINGS.md` 出现且含信号。**clean ledger 完成 → 不写文件**。
2. `registerLearn` 注册 `learn <lesson>`;action append prose learning 到当前 repo(`harnessed learn "X"` → LEARNINGS.md 含 `lesson: X`)。
  </red>
  <green>
- `src/cli/checkpoint.ts` complete 分支:在 `completePhase(...)` 后(L226 附近)、auto-compact 前或后,加:`if (allResolved && afterMark) { const { captureWorkflowLearnings } = await import('../checkpoint/learnings.js'); const n = await captureWorkflowLearnings(afterMark.sub_progress ?? [], sub); if (n>0) console.log(`[harnessed] captured ${n} learning(s) → .planning/LEARNINGS.md`) }`。surgical,不动既有 complete 逻辑。
- `src/cli/learn.ts`(NEW):`registerLearn(program)` → `program.command('learn <lesson>').description('Append a prose learning to this repo .planning/LEARNINGS.md').action(async (lesson: string) => { const { appendLearning, formatLearningEntry } = await import('../checkpoint/learnings.js'); const { repoKey } = await import('../checkpoint/workflowStore.js'); const entry = formatLearningEntry({ phase: '(manual)', signals: [], prose: lesson, at: new Date().toISOString() }); await appendLearning(repoKey(), entry); console.log('[harnessed] learning appended → .planning/LEARNINGS.md'); process.exit(0) })`。
  注意:`formatLearningEntry` 当 signals=[] 且有 prose 时须只出 prose 行(Task 1 已支持空 signals)。
- `src/cli.ts`:`import { registerLearn }` + `registerLearn(program) // 24th — Phase 16 learning capture`。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/learnings.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>checkpoint complete 在 allResolved+有信号时 auto-capture、clean 不写;`harnessed learn` append prose;既有 complete 逻辑无回归。</done>
</task>

<task type="auto">
  <name>Task 4: verify gate — 全测试 + biome + 真 repo 无污染</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. `corepack pnpm exec biome check --write` 目标文件 → 再 `biome check` clean。
2. `corepack pnpm exec tsc --noEmit` exit 0。
3. `corepack pnpm test` 全套 green,0 fail（≥1256 + 新增 learnings 测试）。
4. **污染检查(关键)**:`git status --short .planning/LEARNINGS.md` —— 真 repo 的 `.planning/LEARNINGS.md` **不应被测试套件创建/改动**(D4 + cwd 隔离生效)。若出现 → 测试 cwd 隔离漏了,STOP 修。
5. `git diff --stat` 仅目标文件(4 src + 1 test)+ 新 learn.ts。
  </action>
  <verify>
    <automated>cd /d/GitCode/harnessed && corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3 && (git status --short .planning/LEARNINGS.md | grep -q . && echo "POLLUTION!" || echo "no-pollution=PASS")</automated>
  </verify>
  <done>biome clean;tsc 0;全测试 green;真 repo `.planning/LEARNINGS.md` 无污染(D4);diff 仅目标文件。证据进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-16-01 | Tampering (data loss) | append 误截断既有 LEARNINGS.md | mitigate | 用 `appendFile` 非 `writeFile`(writeFile 仅首次 header);Task 2 append-不截断测试守门 |
| T-16-02 | Tampering (repo pollution) | 测试/clean 完成把 LEARNINGS.md 写进真 repo | mitigate | D4 no-empty-write(无信号不写）+ auto-capture 测试 chdir 隔离;Task 4 显式污染检查 |
| T-16-03 | Tampering | 写错目标(全局根而非 repo) | mitigate | 写目标硬编码 `repoKey()` repo root + `.planning/`;Task 2 tmp-repo 测试断言路径 |
| T-16-04 | Info Disclosure | learnings 落 git → 泄露敏感 sub 名/reason | accept | 内容来自用户自己的 ledger(sub 名/reason 本就用户产),append-only 用户可见可编辑;无新外部面 |
</threat_model>

<verification>
phase 级验收(= 16-CONTEXT.md Acceptance):
1. extractLearnings 三信号 + clean→[]。
2. formatLearningEntry dated markdown block,prose 可选。
3. appendLearning 首建带 header + append 不截断。
4. captureWorkflowLearnings 有信号写 / clean no-op 不建文件(D4)。
5. checkpoint complete allResolved+信号 auto-capture / clean 不写。
6. harnessed learn append prose。
7. 全 gate green;真 repo 无污染;biome clean。
</verification>

<success_criteria>
- workflow 完成自动把失败/loop/reject 信号 append 进 repo `.planning/LEARNINGS.md`;`harnessed learn` 补 prose;append-only 不改现有内容。
- 写 in-repo(git 共享,Trellis 对位)、repoKey(cwd) 目标、无信号不写(防污染)。
- 消费走标准 `.planning/` 读(loop 闭合的读半 + Phase 17 注入)。
- TDD red→green;全 gate green;KARPATHY-minimal。
</success_criteria>

<output>
完成后建 `.planning/phases/16-learning-loop/16-01-SUMMARY.md`:收录 RED→GREEN、extract/format/append 证据、no-empty-write(D4)证据、auto-capture integration、`harnessed learn` 跑通、真 repo 无污染检查、biome clean、tsc 0、全测试计数、`git diff --stat`。
</output>
