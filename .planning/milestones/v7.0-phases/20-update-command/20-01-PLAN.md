---
phase: 20-update-command
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/cli/lib/version-check.ts
  - src/cli/lib/check-update.ts
  - src/cli/lib/doctor-registry.ts
  - src/cli/update.ts
  - src/cli/lib/migration-report.ts
  - src/cli/install-base.ts
  - src/cli.ts
  - tests/cli/version-check.test.ts
  - tests/cli/update.test.ts
  - tests/cli/doctor.test.ts
  - tests/cli/doctor-fixtures.test.ts
autonomous: true
requirements:
  - "v7.0 follow-on Phase 20 acceptance (20-CONTEXT.md D1–D7) — no numeric REQUIREMENTS.md IDs"
user_setup: []

must_haves:
  truths:
    - "compareVersions returns current/behind/ahead/unknown correctly incl. prerelease + malformed→unknown"
    - "checkUpdate: behind→warn (X→Y + run-hint), current→pass, npm-unreachable→pass (fail-soft)"
    - "harnessed update --check reports installed/latest/status with no install"
    - "harnessed update self-updates via `npm i -g harnessed@latest` (asserted via mocked spawn — never really installs) + shows restart hint"
    - "harnessed update --upstreams|--all calls installBaseProfile after self-update"
    - "install-base behavior unchanged: registerInstallBase delegates to the extracted installBaseProfile; existing install-base tests stay green"
    - "doctor registry has 14 checks; summary stays 'pass' when current; full gate green; biome clean"
  artifacts:
    - path: "src/cli/update.ts"
      provides: "harnessed update [--check] [--upstreams|--all] — self + flagged upstream upgrade"
      contains: "registerUpdate"
    - path: "src/cli/lib/version-check.ts"
      provides: "compareVersions (pure) + fetchLatestVersion (impure, npm view, fail-soft)"
      contains: "compareVersions"
  key_links:
    - from: "checkUpdate doctor check + update --check"
      to: "version-check.ts compareVersions(pkg.version, npm latest)"
      via: "passive doctor surface + active --check (GSD-style update UX)"
      pattern: "compareVersions"
---

<objective>
给 harnessed 加 GSD 式 `update` 命令 + 被动 version-check:用户在 `harnessed doctor` 看到 "update available X→Y",跑 `harnessed update` 自升(`npm i -g harnessed@latest`),`--upstreams` 额外重跑 base manifests 升上游插件。补 `comet update` gap。

约束(20-CONTEXT.md D1–D7):self+upstreams flagged、doctor-check 被动 surface、`--check` 主动查、network fail-soft、install-base extract-and-delegate 不改行为、TDD red-first。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/20-update-command/20-CONTEXT.md
@src/cli/install-base.ts
@src/cli/lib/check-codegraph.ts
@src/cli/lib/doctor-registry.ts
@src/cli/compact-cmd.ts
</context>

<key_facts>
planner 已验证（直接用）：

- `pkg.version` via `import pkg from '../package.json' with { type: 'json' }`(cli.ts:3）。无 semver dep → 写最小 compareVersions。
- npm latest = spawn `npm view harnessed version`(win32 `npm.cmd`;sister `getNpmExecutable` in openspec/install code）。timeout + null on fail。
- `install-base.ts`:install loop 在 `registerInstallBase` action 闭包内(`listBaseManifests` private)。抽 `export async function installBaseProfile(opts): Promise<{installed,skipped,failed,...}>`,`registerInstallBase` 改为 delegate。
- doctor `CheckResult {name,status:'pass'|'warn'|'fail',message,fix?}`;`warn≠fail`(exit 0);CHECKS 13(Phase 18 后）→ 14。doctor.test.ts + doctor-fixtures.test.ts 各有 length 断言(13)+ per-check module-mock pattern(fs/network-using check 都 mock 成固定结果）。
- CLI 注册 sister:compact 22nd / workflows 23rd / learn 24th → update 25th。CHANGELOG.md(Keep-a-Changelog,root)取 top section 显示。
- 基线 1285 green。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1: version-check.ts — compareVersions(pure) + fetchLatestVersion(fail-soft)</name>
  <files>src/cli/lib/version-check.ts, tests/cli/version-check.test.ts</files>
  <red>
失败测试:
1. `compareVersions('4.4.0','4.5.0')→'behind'`;`('4.5.0','4.4.0')→'ahead'`;`('4.4.0','4.4.0')→'current'`;`('4.10.0','4.9.0')→'ahead'`(数值非字典序);prerelease `('4.5.0-rc.1','4.5.0')→'behind'`;malformed `('x','4.4.0')→'unknown'`、空→'unknown'。
2. `fetchLatestVersion`:mock spawn 返回 "4.5.0\n" → '4.5.0';spawn 抛/timeout/非 0 → null(fail-soft,no throw)。
  </red>
  <green>
`version-check.ts`(NEW):
- `compareVersions(installed, latest): 'current'|'behind'|'ahead'|'unknown'` —— 解析 `\d+\.\d+\.\d+`(+ 可选 `-prerelease`),任一不匹配 → 'unknown';逐段数值比;相等且一方有 prerelease → 有 prerelease 的更小(behind/ahead);全等 → 'current'。
- `fetchLatestVersion(timeoutMs = 8000): Promise<string|null>` —— `execFile`(getNpmExecutable, ['view','harnessed','version'], {timeout})；成功 trim stdout 且匹配 semver → 返回;否则 null。try/catch → null。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/version-check.test.ts 2>&1 | tail -14</automated>
  </verify>
  <done>compareVersions 4 态 + prerelease + malformed→unknown;fetchLatestVersion 成功/失败 fail-soft。</done>
</task>

<task type="tdd">
  <name>Task 2: checkUpdate doctor check + 注册(13→14)</name>
  <files>src/cli/lib/check-update.ts, src/cli/lib/doctor-registry.ts, tests/cli/check-update.test.ts, tests/cli/doctor.test.ts, tests/cli/doctor-fixtures.test.ts</files>
  <red>
失败测试:
1. `checkUpdate(installed, latestFetcher)`(注入 fetcher 便于测):behind → `{name:'update', status:'warn', message contains 'X→Y' or 'available', fix contains 'harnessed update'}`;current → status 'pass';fetcher 返回 null(unreachable)→ 'pass'(fail-soft)。
2. doctor.test/doctor-fixtures:`CHECKS.length===14`(现 13)+ expected names 含 'update'。先把断言 13→14 + 加 'update' name + module-mock check-update.js(固定 pass)。
  </red>
  <green>
- `check-update.ts`(NEW):`checkUpdate(installed = pkg.version, fetch = fetchLatestVersion): Promise<CheckResult>` —— `const latest = await fetch(); if(!latest) return {name:'update', status:'pass', message:'update check skipped (npm unreachable)'}; const cmp = compareVersions(installed, latest); if(cmp==='behind') return {name:'update', status:'warn', message:\`update available: ${installed} → ${latest}\`, fix:'run `harnessed update`'}; return {name:'update', status:'pass', message:\`up to date (${installed})\`}`。
- `doctor-registry.ts`:append `async () => (await import('./check-update.js')).checkUpdate()`(14th)。
- doctor.test.ts + doctor-fixtures.test.ts:length 13→14;expected names 加 'update';加 `vi.mock('check-update.js', { checkUpdate: ()=>({name:'update',status:'pass',message:'up to date'}) })`(sister codegraph mock,保 summary 'pass')。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/check-update.test.ts tests/cli/doctor.test.ts tests/cli/doctor-fixtures.test.ts 2>&1 | tail -12</automated>
  </verify>
  <done>checkUpdate behind→warn / current→pass / unreachable→pass;doctor 14 + summary pass;两 doctor 测试更新。</done>
</task>

<task type="tdd">
  <name>Task 3: install-base extract installBaseProfile + harnessed update 命令(含 --migration-report）</name>
  <files>src/cli/install-base.ts, src/cli/lib/migration-report.ts, src/cli/update.ts, src/cli.ts, tests/cli/update.test.ts</files>
  <red>
失败测试(mock `node:child_process` execFile/execFileSync + version-check fetch）：
1. `harnessed update --check`:打印 installed/latest/status,**不** spawn `npm i`(断言 install spawn 未调）。
2. `harnessed update`(behind,mock fetch + mock spawn）：调 `npm i -g harnessed@latest`(断言 spawn 收到该 args)+ 打印 restart hint;current 时不 spawn install,打印 "up to date"。
3. `harnessed update --upstreams`:self-update 后调 `installBaseProfile`(spy/mock 断言被调）。
4. **`collectMigrationReport(cwd, root)`**(D8,tmp dirs）：有 legacy `current-workflow.json` / backup dir / `_rogue-impl-reference/` → 各报一条 `{name, present:true, hint}`;无 → present:false。**断言无任何 fs 删除调用**(rm/unlink 未被 mock-spy 触发）。`update --migration-report` 打印该 inventory,**不** spawn npm i。
  </red>
  <green>
- `install-base.ts`:抽 `export async function installBaseProfile(opts: InstallProfileOpts): Promise<InstallProfileResult>`(把 action 闭包里 listBaseManifests→validate→install 循环移进去）；`registerInstallBase` action 改为 `await installBaseProfile(...)` + 现有输出。**行为不变**(现有 install-base 测试守）。
- `src/cli/lib/migration-report.ts`(NEW):`collectMigrationReport(cwd, root): MigrationItem[]` —— `existsSync` 盘点:`root/current-workflow.json`(legacy singleton,hint "post-multi-workflow rollback anchor; removable once store proven")、`cwd/.harnessed-backup`、`<homedir>/.harnessed/backups`、`cwd/.planning/phases/_rogue-impl-reference`。**只读,零 rm/unlink**。每项 `{name, present, path, hint}`。
- `src/cli/update.ts`(NEW):`registerUpdate(program)` → `update` 命令,`--check`/`--upstreams`/`--all`/`--migration-report` options。`--migration-report` → 打印 `collectMigrationReport` 表(不删不装)return。否则 `const latest = await fetchLatestVersion(); const cmp = compareVersions(pkg.version, latest ?? '')`;`--check` → 打印 installed/latest/status return;否则 behind → `execFileSync(npm, ['i','-g','harnessed@latest'], {stdio:'inherit'})` + CHANGELOG top + restart hint;else "up to date";若 `--upstreams||--all` → `await installBaseProfile({apply:true,...})`。fail-soft on fetch null。
- `cli.ts`:`registerUpdate(program) // 25th`。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/update.test.ts tests/cli/install-base.test.ts 2>&1 | tail -14</automated>
  </verify>
  <done>install-base delegate 无回归;`update --check` 只报;`update` behind 调 npm i -g(mock 断言)+ restart hint;`--upstreams` 调 installBaseProfile;`--migration-report` 只读盘点(零删除)。</done>
</task>

<task type="auto">
  <name>Task 4: verify gate — 全测试 + biome + diff scope</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. `corepack pnpm exec biome check --write` 目标 → clean。
2. `corepack pnpm exec tsc --noEmit` exit 0。
3. `corepack pnpm test` 全套 green 0 fail（≥1285 + 新增；install-base/doctor 无回归）。
4. **真升级安全确认**:测试**绝不真跑** `npm i -g`(全 mock spawn);grep 测试确认 spawn 是 mock。
5. `git diff --stat` 仅目标文件。
  </action>
  <verify>
    <automated>cd /d/GitCode/harnessed && corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3</automated>
  </verify>
  <done>biome clean;tsc 0;全测试 green;无真 npm 安装(全 mock);diff 仅目标文件。证据进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-20-01 | Availability (doctor false-fail) | npm unreachable 让 doctor fail | mitigate | fetchLatestVersion fail-soft → null;checkUpdate null→pass;never throw |
| T-20-02 | Tampering (test really installs) | 测试真跑 npm i -g 改环境 | mitigate | 全 mock `node:child_process`;Task 4 grep 确认无真 spawn |
| T-20-03 | Tampering (install-base regression) | extract 改了 install-base 行为 | mitigate | extract-and-delegate(registerInstallBase 调 installBaseProfile）；现有 install-base 测试守门 |
| T-20-04 | Tampering (wrong-scope upgrade) | 非 global 装却 npm i -g | mitigate | D4 假设 global(文档化 install 路径）；命令打印执行的 cmd,用户可见;非 global 用户提示自走 npm |
| T-20-05 | DoS (slow doctor) | npm view 每次 doctor 卡 | mitigate | timeout(8s)+ fail-soft;doctor 测试 mock 不真 spawn |
</threat_model>

<verification>
phase 级验收(= 20-CONTEXT.md Acceptance):
1. compareVersions 4 态 + prerelease + malformed→unknown。
2. checkUpdate behind→warn / current→pass / unreachable→pass。
3. update --check 只报。
4. update self-update 调 npm i -g(mock)+ restart hint。
5. update --upstreams 调 installBaseProfile。
6. doctor 14 + summary pass + 两 doctor 测试更新。
7. install-base 无回归;全 gate green;biome clean。
</verification>

<success_criteria>
- `harnessed update` GSD 式自升 + `--upstreams` 升上游;doctor 被动 surface "update available";补 comet update gap。
- network/spawn fail-soft;install-base extract 无回归;测试零真安装。
- TDD red→green;全 gate green;KARPATHY-minimal。
</success_criteria>

<output>
完成后建 `.planning/phases/20-update-command/20-01-SUMMARY.md`:RED→GREEN、compareVersions/checkUpdate/update 证据、install-base 无回归、doctor 14、零真安装确认、biome clean、tsc 0、全测试计数、`git diff --stat`。
</output>
