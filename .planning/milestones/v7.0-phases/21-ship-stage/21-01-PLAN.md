---
phase: 21-ship-stage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/cli/lib/release-preflight.ts
  - src/cli/release-preflight.ts
  - src/cli.ts
  - tests/cli/release-preflight.test.ts
  - workflows/ship/auto/SKILL.md
  - workflows/ship/auto/workflow.yaml
  - workflows/ship/preflight/SKILL.md
  - workflows/ship/preflight/workflow.yaml
  - workflows/judgments/stage-routing.yaml
  - README.md
autonomous: true
requirements:
  - "v7.0 follow-on Phase 21 acceptance (21-CONTEXT.md D1–D6)"
user_setup: []

must_haves:
  truths:
    - "collectPreflight flags an empty CHANGELOG [Unreleased] as fail; non-empty → pass"
    - "collectPreflight flags a dirty working tree and an already-existing v<version> tag as fail; clean+ready → all pass"
    - "release-preflight NEVER mutates git or remote (read-only: git status --porcelain, git tag -l)"
    - "harnessed release-preflight exits 1 if any check fails, 0 if all pass"
    - "the /ship master + ship/preflight sub validate against check-workflow-schema and load in the orchestrator; /ship slash command generates"
    - "ship stops at tag-ready — no push, no publish, no tag creation (publish stays in publish.yml CI)"
    - "full gate green; biome clean"
  artifacts:
    - path: "src/cli/lib/release-preflight.ts"
      provides: "collectPreflight read-only release-readiness checks (injectable git/fs deps)"
      contains: "collectPreflight"
    - path: "workflows/ship/auto/workflow.yaml"
      provides: "Stage ⑤ Ship master delegating to ship-preflight"
      contains: "delegates_to"
  key_links:
    - from: "harnessed release-preflight + /ship master"
      to: "CHANGELOG [Unreleased] + version + git-clean + tag-absence checks"
      via: "tag-ready gate before publish.yml CI (PR ready != release ready)"
      pattern: "collectPreflight"
---

<objective>
给 harnessed 加第 5 stage ship/release:CLI `harnessed release-preflight`(机检门:CHANGELOG Unreleased 非空 + version + git clean + tag 不存在,只读)+ `/ship` workflow master(编排 preflight,委派 PR/deploy 给 gstack /ship)。止于 tag-ready,publish 留 publish.yml CI。补 comet/Trellis/Claude-Harness 都有的 ship stage gap。

约束(21-CONTEXT.md D1–D6):两层、deploy 边界 tag-ready(无 push/publish/tag 创建)、preflight 只读 git(注入 runner)、lean master 委派 gstack、TDD red-first(Wave 1)、Wave 2 过 check-workflow-schema。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/21-ship-stage/21-CONTEXT.md
@CHANGELOG.md
@workflows/verify/auto/workflow.yaml
@workflows/verify/progress/workflow.yaml
@src/cli/reject.ts
</context>

<key_facts>
planner 已验证（直接用）：

- CHANGELOG.md Keep-a-Changelog;`## [Unreleased]` 段当前**空**(8 phase 未记)→ preflight 该 fail。version in package.json。
- `publish.yml` tag-push 触发 npm publish + GitHub release → ship 只到 tag-ready。
- git 只读:`git status --porcelain`(dirty?)+ `git tag -l v<version>`(tag 存在?)。注入 runner 便于测(sister version-check fetchLatestVersion injectable)。
- master `workflows/<stage>/auto/workflow.yaml`:`schema_version: harnessed.workflow.v3` + `workflow: <name>` + `disciplines_applied: [karpathy,output-style,language,operational,priority,protocols]` + `tools_available: [...]` + `delegates_to: [...]`(serial 必带 order;K9 invariant)。sub 加 `phases: [...]`。SKILL.md frontmatter: name/description/trigger_phrases。`check-workflow-schema` 校验。stage-routing.yaml 触发。generateCommands 自动出 /ship。
- CLI 注册 sister:update 25th → release-preflight 26th。ExitError + runCli pattern = tests/cli/reject.test.ts。
- 基线 1302 green。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1 (Wave 1): collectPreflight 只读机检 + CLI</name>
  <files>src/cli/lib/release-preflight.ts, src/cli/release-preflight.ts, src/cli.ts, tests/cli/release-preflight.test.ts</files>
  <red>
失败测试(注入 deps:`{changelog, version, gitStatus, gitTags}`）：
1. CHANGELOG `[Unreleased]` 空(只有 header 无条目）→ 该 check status 'fail';非空(有 `### Added` 等内容）→ 'pass'。
2. gitStatus 非空(dirty）→ 'fail';空(clean）→ 'pass'。
3. `v<version>` 在 gitTags → 'fail'(tag 已存在);不在 → 'pass'。
4. version 存在 → pass;缺 → fail。
5. **只读断言**:deps 的 git runner 是只读命令(测试注入 spy,断言无 push/tag-create/commit 调用)。
6. CLI `harnessed release-preflight`:全 pass → exit 0;任一 fail → exit 1。
  </red>
  <green>
- `release-preflight.ts`(NEW):
  - `interface PreflightDeps { changelog: string; version: string; gitStatus: string; gitTags: string[] }`
  - `collectPreflight(deps): PreflightCheck[]` —— 4 check:
    - changelog-unreleased:`[Unreleased]` 段(`## [Unreleased]` 到下个 `## `)trim 后非空(去掉空行/纯 header）→ pass else fail(hint "add entries under ## [Unreleased] before releasing")。
    - version-present:`deps.version` 非空 semver → pass。
    - git-clean:`deps.gitStatus.trim()===''` → pass else fail(hint "commit or stash before releasing")。
    - tag-absent:`!deps.gitTags.includes('v'+version)` → pass else fail(hint "version already tagged — bump package.json")。
  - `defaultDeps(cwd): PreflightDeps` —— 读 CHANGELOG.md + package.json + `execFileSync('git',['status','--porcelain'])` + `execFileSync('git',['tag','-l'])`（只读）。fail-soft。
- `src/cli/release-preflight.ts`(NEW):`registerReleasePreflight` → `release-preflight` 命令,`collectPreflight(defaultDeps(cwd))` 打印每 check + exit(any fail?1:0)。
- `cli.ts`:`registerReleasePreflight(program) // 26th`。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/release-preflight.test.ts 2>&1 | tail -14</automated>
  </verify>
  <done>collectPreflight 4 check(empty-Unreleased/dirty/tag-exists/version 正确)+ 只读 + CLI exit 0/1。</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 2): /ship master + ship/preflight sub + routing + 5-stage doc</name>
  <files>workflows/ship/auto/{SKILL.md,workflow.yaml}, workflows/ship/preflight/{SKILL.md,workflow.yaml}, workflows/judgments/stage-routing.yaml, README.md</files>
  <action>
镜像 verify master 结构:
- `workflows/ship/auto/workflow.yaml`:v3 master,`workflow: ship`,`disciplines_applied: [6]`,`tools_available: [gstack-ship, planning-with-files]`(+ release-preflight),`delegates_to: [{ workflow: ship-preflight, mode: serial, order: 1 }]`。SKILL.md frontmatter name/description(Stage ⑤)/trigger_phrases(ship/发布阶段/stage 5)。prose:跑 release-preflight → 委派 PR/deploy 给 gstack `/ship` → 指向 publish.yml CI 做 publish。
- `workflows/ship/preflight/workflow.yaml`:v3 sub `ship-preflight`,`phases: [{id:01-release-preflight, name:..., upstream:harnessed}]`。SKILL.md prose:跑 `harnessed release-preflight`,fail 则 block。
- `workflows/judgments/stage-routing.yaml`:加 ship 触发(stage=='ship' fire ship-preflight)。
- README:4-stage → 5-stage(hero loop + Key Differentiators 提 Ship 在 Verify 后 Retro 前)。
- **过 check-workflow-schema**:若 K9/schema 报错按报错调(serial order 必带等)。
  </action>
  <verify>
    <automated>corepack pnpm vitest run tests/scripts/check-workflow-schema.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>/ship master + ship-preflight sub 过 check-workflow-schema;stage-routing ship 触发;README 5-stage;/ship slash 生成。</done>
</task>

<task type="auto">
  <name>Task 3: verify gate — 全测试 + biome + 无 remote 突变</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. `corepack pnpm exec biome check --write` 目标 → clean。
2. `corepack pnpm exec tsc --noEmit` exit 0。
3. `corepack pnpm test` 全套 green 0 fail（≥1302 + 新增；check-workflow-schema 含新 workflow）。
4. **无 remote/git 突变确认**:grep 新代码无 `git push`/`npm publish`/`git tag <create>`/`git commit`(只读 status/tag -l 允许)。
5. **dogfood**:`node dist/cli.js release-preflight`(或 tsx）在本 repo 跑 —— **应 fail on empty CHANGELOG [Unreleased]**(证明 gate 有效,正好提示发 4.5.0 前要补 CHANGELOG)。
6. `git diff --stat` 仅目标文件。
  </action>
  <verify>
    <automated>cd /d/GitCode/harnessed && corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3</automated>
  </verify>
  <done>biome clean;tsc 0;全测试 green;新代码无 remote/git 突变;dogfood preflight fail-on-empty-CHANGELOG;diff 仅目标。证据进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-21-01 | Tampering (unwanted remote action) | ship 命令 push/publish/tag without approval | mitigate | D2 tag-ready 边界:新代码只读 git(status/tag -l);Task 3 grep 确认无 push/publish/tag-create;publish 留 CI |
| T-21-02 | Availability (preflight false-pass) | 空 CHANGELOG 漏过 → 发空 release | mitigate | changelog-unreleased check fail-on-empty;Task 3 dogfood 验证真 fail |
| T-21-03 | Tampering (schema/loader break) | 新 workflow 破坏 master 加载 / 路由 | mitigate | check-workflow-schema 守门;mirror verify master 结构;K9 invariant 遵守 |
| T-21-04 | Tampering (git mutation in preflight) | preflight 误改 git | mitigate | D5 只读 runner(status --porcelain / tag -l);注入测试断言无突变 |
</threat_model>

<verification>
phase 级验收(= 21-CONTEXT.md Acceptance):
1. collectPreflight empty-Unreleased/dirty/tag-exists → fail;clean+ready → pass。
2. release-preflight CLI exit 1 any fail / 0 all pass;无 git/remote 突变。
3. /ship master + ship-preflight sub 过 check-workflow-schema + 加载 + /ship 生成。
4. README 5-stage(Ship 在 Verify→Retro 间）。
5. 全 gate green;biome clean;无 remote/git 突变。
</verification>

<success_criteria>
- 第 5 stage ship:CLI release-preflight 机检门(空 CHANGELOG/dirty/tag 已存在 → fail)+ /ship master 编排委派 gstack。
- 止于 tag-ready,publish 留 CI,零 remote 突变。
- TDD red→green(Wave 1);Wave 2 过 schema;dogfood 证 gate 有效。
</success_criteria>

<output>
完成后建 `.planning/phases/21-ship-stage/21-01-SUMMARY.md`:Wave1 RED→GREEN、collectPreflight 4 check、只读证据、Wave2 schema-pass、dogfood preflight fail-on-empty-CHANGELOG、无 remote 突变、biome clean、tsc 0、全测试计数、git diff --stat。
</output>
