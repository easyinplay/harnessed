# Milestone v0.2.0: Sub-task Loop + Extension Installers

**Status:** ✅ SHIPPED 2026-05-16
**Phases:** 2.1 / 2.2 / 2.3 / 2.4 (4 phases)
**Timeline:** 2026-05-15 → 2026-05-16 (2 days, back-to-back ship)
**Git range:** `v0.1.0` tag → `v0.2.0` tag (~111+ atomic commits)
**Milestone tags:** v0.2.0-alpha.1-installers / v0.2.0-alpha.2-execute-task / v0.2.0-alpha.3-extension-mvp / v0.2.0-alpha.4-doctor → **v0.2.0**
**Audit:** `.planning/v0.2.0-MILESTONE-AUDIT.md` — passed (4/4 phase verified)

## Overview

打通子任务级编排（execute-task workflow + ralph-loop full SDK integration）+ 实装 4 个 phase-2.1 placeholder installer 凑齐 6 method 全覆盖 + design/content/testing 3 extension category MVP + karpathy SKILL-ONLY 注入 + doctor 完整版 + audit 完整版 + dashboard C 路径 + Win sentinel。`@anthropic-ai/claude-agent-sdk@0.3.142` INTRODUCE NOW（NOT v0.3+）— execute-task workflow 主流程必须 real spawn；4-layer dual-signal completion degradation-safe；A7 守恒 ADR 0001-0013 main body 0 diff 持续验证 across 4 phase。Wave 0 backlog 一次根治模式 promoted 为 standard phase-pattern。

## Phases

### Phase 2.1: 4 phase-2.1 placeholder installer 实装 + 6 method 全覆盖

**Goal**: 4 method (cc-plugin-marketplace / git-clone-with-setup / npx-skill-installer / mcp-http-add) runtime-ready；install_type 字段 enforcement
**Plans**: 25+ atomic 子任务 (Wave 0-6)

- [x] cc-plugin-marketplace + git-clone-with-setup + npx-skill-installer + mcp-http-add 实装
- [x] install_type 字段 enforcement (D1.2.5-12) + ADR 0007 errata sister extension
- [x] ui-ux-pro-max install path 实测 PATH_A+B 双 OK (D1.2.5-11)
- [x] ADR 0010 installer-schema-errata + adr-0010-accepted baseline tag

**Details:** Acceptance bar 8/8. tests 318+3 → 374+3 (+56). CI run 三平台全绿. installer 6 method 全覆盖 ship. Wave 0 backlog 6 项一次根治 (sister Phase 2.2/2.3 同模式起源)。

### Phase 2.2: execute-task workflow + ralph-loop full SDK integration

**Goal**: 主进程 spawn subagent 时自动 ralph-loop wrap + verbatim COMPLETE marker
**Plans**: 30+ atomic 子任务 (Wave 0-6)

- [x] `@anthropic-ai/claude-agent-sdk@0.3.142` INTRODUCE NOW + AgentDefinition 5-字段 SDK input
- [x] 9-字段 prompt-inject contract v1.2 reconcile
- [x] dual-signal completion 4-layer (L1 outer PRIMARY structured_output + L2 outer FALLBACK `<promise>` + L3 inner FALLBACK + L4 retry)
- [x] schemaVersion 7-surface 单一兼容门 (CD-5): `harnessed.{phases,routing,manifest,sdk,agent,checkpoint,deferred-items}.v1` 全 surface 走 `branchOnSchemaVersion<T>` helper
- [x] per-phase model tier (4 phase × opus/sonnet/sonnet/haiku)
- [x] CD-6 provenance gate hard fail BEFORE-W4 (scope 限 `.harnessed/{sessions,checkpoints,route-logs}/**`)
- [x] CD-4 Task Session 复用 conditional → SC4 PARTIAL → T4.4 DEFERRED → v0.3.0 (closure infra 三件套 ready)
- [x] transparency CI gate W3 ENFORCE=true (Phase 2.1 deferred 根治) — 连续 2 phase 复发反模式升级 CI gate level
- [x] ADR 0011 9 章节 errata accepted + adr-0011-accepted baseline tag

**Details:** F1-F8 8/8 acceptance bar. tests 374+3 → 432+3 (+58). 30/30 sample harness COMPLETE 100% mocked SDK round-trip. 11 ADR + 11 baseline tag (含 v0.1.0 9 个 + 0010 + 0011)。

### Phase 2.3: design / content / testing extension category MVP + karpathy SKILL-ONLY 注入

**Goal**: 3 category × 真实候选 install adapter + decision rule 实测 + karpathy-skills behavior-rule SKILL-ONLY 注入
**Plans**: 31+ atomic 子任务 (Wave 0-6)

- [x] 5 NEW manifest (frontend-design + anthropics-skills pptx/slide-deck + playwright-test + chrome-devtools-mcp) 装配 + Wave 1 dry-run e2e
- [x] **T0.10 always_active spike FAIL** → R2 A1 fallback locked: description-keyword + self-reflexive prompt SKILL.md body 首段（karpathy 4 心法 always-on via SDK skill discovery，0 frontmatter 字段）
- [x] karpathy SKILL-ONLY (D-02) — CLAUDE.md 不动；`harnessed install karpathy-skills` cp -R 干净；R02 § 6 behavior-rule "CLAUDE.md merge engine" 推 v0.2.4+
- [x] CD-3 negative-space (`do_not_use_when` + `if_rejected_use`) + arbitrateWithRedirect 升级 ~15L + per-manifest hint mirror
- [x] EE-5 5-question merge gate 双层 — L1 CLI `harnessed manifest-add` 78L + L2 plan-phase 模板 + gsd-plan-checker BLOCKER
- [x] 30 FRESH SAMPLES 30/30 = 100% (≥85% D-05 acceptance bar 远超 15% headroom)
- [x] DI-1 hotfix karpathy-skills.yaml schema-compliant (git_ref 40-hex SHA pin + install_type:git per ADR 0007 1:N closure)
- [x] ADR 0012 9 章节 errata accepted + adr-0012-accepted baseline tag

**Details:** Acceptance bar F1-F8 8/8. tests 432+3 → 492+3 (+60). 30/30 routing accuracy = 100% (远超 ≥85% bar 15% headroom). Wave 0 backlog 6 项一次根治 (sister Phase 2.4 5 项 promoted phase-pattern)。

### Phase 2.4: doctor 完整版 + EE-4 SSOT + dashboard C 路径 + audit hard-fail + Win sentinel

**Goal**: doctor MIN 5 check + EE-4 plan-checker 4 维 SSOT + dashboard C 路径 3 子 FULL absorb + audit 完整版 hard-fail + ralph-loop Win sentinel
**Plans**: 25+ atomic 子任务 (Wave 0-6)

- [x] doctor MIN 5 check (D-01) — `src/cli/doctor.ts` 4 → 5 check (+origin URL warn) + `--json` flag + 三档 status enum (pass/warn/fail) + helper `origin-check.ts` sister-share
- [x] EE-4 plan-checker 4 维 SSOT (D-02) — `routing/plan-review-schema.yaml` 4 维 + `scripts/run-plan-checker.mjs` walker + T2.0 RELAX baseline (3 阈值 1.0 → 0.8) + BLOCKER manual rerun (auto-spawn 推 v0.3.0)
- [x] dashboard C 路径 3 子功能 FULL absorb (D-04) — cc-hook-add 第 7 install method + SSE watcher (替 mtime polling, zero-dep) + 多项目支持 + 3 处 schema enum 同步加
- [x] audit 完整版 hard-fail (R2.4.7) — origin URL drift hard-fail (allowFork: false) + cmd injection SHELL_EVAL_MARKERS refinement + provenance cross-check
- [x] ralph-loop Win sentinel (F5) — 5 fixture (simple/multi-iter/max-iter/subagent-mock/structured_output) + `if: runner.os == 'Windows'` + `shell: bash`
- [x] README CI counter gate (D-03 B 路径) — 三计数一致 grep -cE 精度匹配 (SHIPPED == BARS == L44) + line-start + bold + Phase 2.[1-9] 精度排除 1.X 历史
- [x] Wave 0 backlog 5 项一次根治 — H3+T4 README counter / M1 RETRO dashboard polish round 1 / M2 schemaVersion 3rd consumer (check-provenance.mjs) / T2 dashboard absorb 决断 / T3 v0.3.0 prereq 注记
- [x] deferred-items #3 RESOLVED — EE-4 step reorder fix (move AFTER pnpm install) 1-line ci.yml 修
- [x] ADR 0013 9 章节 errata accepted + adr-0013-accepted baseline tag + v0.2.0-alpha.4-doctor + 🎯 **v0.2.0** 大 tag

**Details:** F1-F8 8/8 acceptance bar. tests 492+3 → 543+4 skipped (+51 cells across W0-W6). 13 ADR + 13 baseline tag iterate. A7 守恒 ADR 0001-0012 main body 0 diff verified (T6.2 ship-time `git diff adr-0012-accepted..HEAD -- "docs/adr/000[1-9]-*.md" "docs/adr/001[0-2]-*.md" | wc -l = 0`)。CI 3-OS final verify (T6.4 push 触发)。

---

## Milestone Summary

### Key Decisions

- **SDK 引入策略**：Phase 2.2 `@anthropic-ai/claude-agent-sdk@0.3.142` INTRODUCE NOW (NOT v0.3+) — execute-task workflow 主流程必须 real spawn；4-layer dual-signal completion degradation-safe
- **schemaVersion 7-surface 单一兼容门 (CD-5)**：harnessed.{phases,routing,manifest,sdk,agent,checkpoint,deferred-items}.v1 全 surface 走 `branchOnSchemaVersion<T>` helper
- **karpathy SKILL-ONLY 注入 (D-02)**：CLAUDE.md 不动 + cp -R 干净 install + skill metadata-driven inject；R02 § 6 behavior-rule "CLAUDE.md merge engine" 推 v0.2.4+ patch release
- **doctor 5 check + audit hard-fail policy**：origin URL drift hard-fail allowFork:false + cmd injection SHELL_EVAL_MARKERS + provenance cross-check = 完整 supply-chain defense
- **EE-4 plan-checker quantitative 量化 SSOT**：routing/plan-review-schema.yaml NEW yaml-only SSOT 4 维 + RELAX baseline 0.8 (T2.0 spike-driven) + BLOCKER manual rerun (auto-spawn 推 v0.3.0)
- **dashboard C 路径 3 子 FULL absorb**：cc-hook-add 7th installer + SSE watcher + 多项目 + 3 处 enum 同步 + localhost-only bind
- **A7 守恒** — ADR 0001-0013 main body 永久冻结；4 phase × ship-time double-verify (Wave 5 mid + Wave 6 ship-time) 累积 0 false positive 0 false negative
- **transparency CI gate ENFORCE=true** (Phase 2.2 W0) — 连续 2 phase 复发反模式升级到 CI gate level，warn-only 不够
- **13 ADR**：v0.1.0 9 个 + 0010 installer-schema-errata + 0011 execute-task-sdk-ralph + 0012 extension-mvp-karpathy-inject + 0013 doctor-ee4-dashboard-audit-win

### Issues Resolved

- ralph-loop completion-promise 不可靠 (issue #1429) — Phase 2.2 dual-signal 4-layer + Phase 2.4 Win sentinel 5 fixture 跨平台验证
- Windows native CI ralph-loop 兼容 — Phase 2.4 F5 `if: runner.os == 'Windows'` + `shell: bash`
- audit 完整版 (R02 P1#10) — Phase 2.4 origin URL hard-fail + signed_by 校验 + 4 步 fallback
- doctor health check 完整版 (R04 P1#8) — Phase 2.4 MIN 5 check + JSON flag + 三档 status
- Phase 2.1 transparency 反模式根治 (Phase 2.2 W0 ENFORCE=true)
- DI-1 karpathy-skills.yaml schema fix (Phase 2.3 同日 hotfix) + EE-4 yaml ERR_MODULE_NOT_FOUND (Phase 2.4 W6 T6.2 1-line fix) — deferred-items review cadence same-Wave-before-ship 验证

### Issues Deferred (→ v0.3.0 / v0.2.4+)

- T4.4 Task Session 复用 完整版 → v0.3.0 (closure infra 三件套 ready Phase 2.2 ship)
- EE-4 BLOCKER auto-spawn rerun → v0.3.0 (Phase 2.4 D-02 down-scope)
- karpathy-skills local-copy install method → v0.2.4+ (DI-1 Phase 2.3 deferred)
- phase-X.Y self-check ENFORCE=true flip → v0.3.0 W0 (Phase 2.4 deferred-items #2 self-referential inherent)
- 路由命中率 ≥ 85% 验收 (30 sample × Haiku/Sonnet/Opus 各 ≥ 8) → v0.3.0 R4.2 Phase 3.4 (Phase 2.3 30/30 = 100% 是 extension category 范围非 model-distributed full benchmark)
- R3.4 multi-source merge segment-by-source → v0.3.0+ (随 SDK 引入但 merge 模块未实装)

### Technical Debt Incurred

- doctor.ts 215L > 200 hard limit 5% tolerance (合理性论证后 accept；不预防性 split 制造 churn)
- dashboard.mjs 610L ≤ 650L default split trigger 未到 (T3.4 SKIP split 决断；proactive split trigger 触发再拆)
- `harnessed.phases.v1` schema 未加 `task_session_id?` field (推 v0.3.0 T4.4 consumer 接入时 schema bump)
- routing/plan-review-schema.yaml 量化阈值 RELAX 0.8 (spike-baseline lock；v0.3.0+ collected fixture 规模扩大后可重新 spike 收紧)

---

_For current project status, see `.planning/STATE.md` + `.planning/ROADMAP.md`_
_Requirements snapshot: `.planning/milestones/v0.2.0-REQUIREMENTS.md`_
_Audit verdict: `.planning/v0.2.0-MILESTONE-AUDIT.md`_
