---
milestone: v8.0
milestone_name: Frictionless Entry
audited: 2026-06-23
status: passed
scope_note: "v8.0 = Phase 24 + Phase 25 only (ROADMAP active milestone). gsd-tools phase_count=18 counts the whole phases/ dir (v5.1–v7.0 shipped phases 09–23 not yet archived to milestones/) — out of scope for this audit."
scores:
  requirements: 2/2   # ROADMAP milestone goals (no REQUIREMENTS.md traceability — see note)
  phases: 2/2
  integration: 1/1    # doc→feature link (Phase 25 quickstart ↔ Phase 24 zero-arg harnessed)
  flows: 1/1          # install → first workflow → you-are-here → resume (documented + shipped)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 24-resume-entry
    items:
      - "Deferred (PLAN D4 accepted): populated output emits two synonymous pointer lines (buildRecoverLines `→ next: harnessed prompt <sub>` + here `→ run: harnessed prompt <sub>`). De-dup deferred to a future ergonomics pass."
  - phase: 25-quickstart
    items:
      - "Deferred → v10.0 i18n surface: README-cn full-body refresh (L106 4-stage / L153 25-workflow / missing Ship ⑤ — README-cn lags EN by one milestone). Only the positioning sentence was synced this milestone (D4)."
      - "Deferred → v10.0 i18n surface: 7 other translations (tw/ja/ko/pt-BR/tr/ru/vi/th) positioning-sentence + quickstart sync."
  - phase: milestone
    items:
      - "Non-phase follow-on: adoption (stars / demo gif / DeepWiki) — marketing, ~0 ★ vs comet 1.5k / Trellis 10.9k (ROADMAP Adoption tier)."
      - "Doc hygiene: REQUIREMENTS.md is a frozen 2026-06-05 snapshot (self-reports active v6.0 / published v4.3.0); v7.0–v8.0 not reconciled into its traceability table. Acceptance tracked per-phase (CONTEXT Acceptance + PLAN must_haves) instead — works, but the formal table is stale."
      - "Doc hygiene: shipped milestones v5.1–v7.0 phase dirs (09–23) still live in active phases/ rather than milestones/<ver>-phases/ — inflates gsd-tools phase_count. Archive at v8.0 complete-milestone."
---

# v8.0 Frictionless Entry — Milestone Audit

> Audited 2026-06-23. Scope = **Phase 24 + Phase 25** (ROADMAP active milestone). Lightweight 2-phase milestone (quick-wins tier of the 3-milestone split: v8.0 quick-wins → v9.0 cross-harness → v10.0 i18n). Execution was main-session hand-controlled (STATE methodology lesson — GSD plan/execute agent chain overreaches this host), so verification evidence lives inline in each phase SUMMARY rather than a separate VERIFICATION.md.

## Definition of Done (from ROADMAP active milestone)

v8.0 = turn the 2026-06-23 comet/Trellis re-comparison's low-risk gaps into shipped wins:
1. **Single-command resume entry** (comet `/comet` analog; mechanism already shipped, only zero-arg entry + ergonomics missing) → **Phase 24**.
2. **Legible value-prop + <5min quickstart** (comet's instantly-graspable positioning) → **Phase 25**.

Both big bets (cross-harness reach, i18n surface) explicitly deferred to v9.0 / v10.0.

## Phase Results

| Phase | Status | Shipped | Evidence (SUMMARY) |
|-------|--------|---------|--------------------|
| 24 single-command resume entry | ✅ passed | `c988bc8` | TDD RED→GREEN; zero-arg `harnessed` you-are-here dashboard + `NEXT: auto\|manual\|done` + `--json`; pure aggregation (`readCurrentWorkflow` + `buildRecoverLines` + `resolveNext`), read-only, explicit `parseBareInvocation` (no misfire, T-24-01); 1352 tests green, tsc=0, biome clean; dogfood 4 forms (bare→dashboard, `--json`, `bogus`→unknown-cmd, `--help`/`--version` intact). Acceptance 1–5 met. |
| 25 value-prop + quickstart legibility | ✅ passed | `58a6900` | EN README result-oriented main sentence lifted to top (wedge → secondary line, preserved) + TL;DR reworked to mechanism + `First 5 Minutes` linear quickstart (install → `/auto` → bare `harnessed` you-are-here → resume, Phase-24 spine) + bare `harnessed` CLI row; README-cn positioning sentence synced (ZH body drift → v10.0). docs-only; diff = README.md + README-cn.md only; 7 translations untouched; mermaid/badge intact; TDD skip recorded (D5). Acceptance 1–6 met. |

## Cross-Phase Integration (checked inline — see scope note)

Integration check done **inline** rather than via a spawned gsd-integration-checker: this is a 2-phase milestone whose only cross-phase link is documentation→feature, with no API routes / service wiring / E2E orchestration to verify, and the host-overreach lesson argues against an extra agent chain.

- **Phase 25 docs ↔ Phase 24 feature**: the `First 5 Minutes` quickstart (step 3) and the `harnessed` (no args) CLI row describe zero-arg `harnessed` = you-are-here dashboard + `NEXT:` + `--json` machine-readable + no-active→onboarding. This matches Phase 24's shipped behavior (SUMMARY dogfood: bare→dashboard+NEXT, `--json` valid JSON, no-active→onboarding). **No doc/feature drift.** ✓

## End-to-End Flow

The advertised newcomer flow — `install → /auto → bare harnessed (you-are-here) → resume` — is both documented (Phase 25 README) and shipped (Phase 24 CLI). Each hop is real: `harnessed setup` (existing), `/auto` (existing master), bare `harnessed` (Phase 24, c988bc8), `harnessed resume` (existing). ✓

## Requirements Coverage

v8.0 carried **no REQUIREMENTS.md traceability entries** (that file is a frozen 2026-06-05 snapshot, not reconciled past v6.0 — flagged as tech debt). DoD was tracked per-phase via CONTEXT Acceptance + PLAN must_haves, both fully verified in SUMMARYs. Mapping the two ROADMAP milestone goals:

| ROADMAP goal | Phase | Satisfied |
|--------------|-------|-----------|
| Single-command resume entry (comet `/comet` analog) | 24 | ✅ (c988bc8, Acceptance 1–5) |
| Legible value-prop + <5min quickstart | 25 | ✅ (58a6900, Acceptance 1–6) |

No unsatisfied requirements, no orphans.

## Verdict

**status: passed** — both phases shipped and verified, the advertised E2E flow is real, no critical gaps. Tech debt is all deferred-by-design (ZH/translation i18n → v10.0; pointer-line de-dup → ergonomics backlog; adoption → marketing follow-on) plus two doc-hygiene items (stale REQUIREMENTS.md snapshot; un-archived shipped phase dirs) to address at complete-milestone.

Next: v8.0 closed **lightweight** (matching the v5.1–v7.0 precedent — phase dirs stay in `phases/NN/` with a pointer, no `-ROADMAP`/`-REQUIREMENTS` archive, no git tag per the codename≠npm-version convention, REQUIREMENTS.md left untouched). Then v9.0 Cross-Harness opens with `/plan-eng-review`.

## Milestone Discuss Decisions (carried forward 2026-06-23)

Preserved here at close — the ROADMAP active block collapses to a one-line shipped-index row, so these are the durable rationale:

1. **Posture = 守宽做深 (B)** — keep the heterogeneous-upstream-arbitration moat (capabilities.yaml + priority.yaml + ADR de-confliction); do NOT narrow to a single pairing like comet's OpenSpec×Superpowers.
2. **Fact correction (verified)** — the earlier "守宽 conflicts with cross-platform" premise was WRONG. gstack + ECC are already cross-harness; cross-platform work collapses to abstracting harnessed's OWN hardcoded `~/.claude/` (`setup.ts:120/194` + state + inject) onto the industry `.agents/` + per-platform convention with auto-detect. No generator; no CC-degrade. → v9.0.
3. **State-machine + evidence-guard are NOT gaps** — harnessed already has a two-tier state machine (envelope 3-state activate/pause/complete + ledger 5-state) + a fail-closed evidence guard (`checkpoint complete` → checkArtifacts + checkPlanningSync, BLOCKED unless `--force`). Earlier compare drafts mis-attributed these to comet; corrected.
4. **Remaining real comet/Trellis gaps** = (1) cross-harness reach [→ v9.0], (2) single-command resume [v8.0 Phase 24 ✓], (3) i18n skill surface [→ v10.0], (4) adoption/legibility [v8.0 Phase 25 ✓].
5. **Milestone split** — quick-wins first (this v8.0), then cross-harness (v9.0) + i18n (v10.0) each as a dedicated milestone.
