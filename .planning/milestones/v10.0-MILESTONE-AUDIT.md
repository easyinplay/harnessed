---
milestone: v10.0
milestone_name: i18n Surface
audited: 2026-06-25
status: passed
scope_note: "v10.0 = Phases 29–33 (ROADMAP active milestone). Extends i18n from the shipped CLI message layer to the skill/workflow + user-facing yaml surface. Execution main-session hand-controlled (STATE methodology lesson — GSD plan/execute agent chain overreaches this host); translation prose via fan-out subagents, mechanism via main-session TDD; verification evidence inline in each phase SUMMARY (no separate VERIFICATION.md). Local commits only, NOT pushed; npm unpublished (codename≠npm-version)."
scores:
  requirements: 6/6   # REQ-v100-{resolve-layer,sync-guard,translation,cli-gap,yaml-i18n,validation}
  phases: 5/5
  integration: 1/1    # locale-resolve → sync-guard → translation → yaml loader chain unbroken; en-default never breaks
  flows: 2/2          # en byte-identical (zero blast) + zh surface end-to-end (SKILL.md body + CLI msg + role/discipline prompt)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 33-yaml-i18n
    items:
      - "Locale source split: the yaml loader (resolveLocaleYaml) + skill body pick use getLocale() (HARNESSED_LANG chain), while the prompt language directive (buildLanguageSection) uses env.HARNESSED_USER_LANG (set by `harnessed setup` Step D). On a zh user configured only via HARNESSED_USER_LANG (no HARNESSED_LANG), getLocale() falls through to LC_ALL/LANG/Intl — usually still zh on a zh host, but the two signals CAN diverge. e2e pins via explicit HARNESSED_LANG. Future: unify (have getLocale() also consult HARNESSED_USER_LANG) if a divergence bug surfaces."
      - "language.yaml left in its current mixed zh/en base (NOT bilingualized) — deliberate: grep-proved its rule.description is never surfaced (buildDisciplinesSection filters it; enforcement hooks read no description text). If a future change un-filters language from the disciplines section, it must be bilingualized then (the en-default bug would reappear)."
  - phase: milestone
    items:
      - "T33.5 yaml parity guard uses the `yaml` package (CI step post-pnpm-install, sister check-workflow-schema), a deviation from the PLAN's dep-free (pre-install) note — regex-parsing yaml structure would itself be a bug source. Logged in phases/33-yaml-i18n/findings.md."
      - "Doc hygiene (carried v8.0→v9.0→v10.0): shipped phase dirs 09–33 still live in active phases/ not milestones/<ver>-phases/ (de-facto convention — trust ROADMAP active section for scope, not the gsd-tools phase count). REQUIREMENTS.md forward-scope WAS reconciled for v10.0 (6 REQ-v100-* entries, traceability 6/6) — better than v9.0's frozen snapshot."
      - "3-OS CI green is PENDING push (local gate green: biome+tsc clean, vitest 1446/0). Verified on the eventual release-pass push."
---

# v10.0 i18n Surface — Milestone Audit

> Audited 2026-06-25. Scope = **Phases 29–33** (ROADMAP active milestone). The i18n tier of the 3-milestone split (v8.0 frictionless ✓ → v9.0 cross-harness ✓ → **v10.0 i18n**). Execution main-session hand-controlled (STATE methodology lesson — GSD plan/execute agent chain overreaches this host); mechanism via main-session TDD, translation prose via fan-out subagents with independent re-verification (structural guard + e2e). Strategy SoT: `v10.0-i18n-surface-DESIGN.md` (office-hours full-scope user override + ceo-review release-before-v10.0 re-sequence → released as v4.7.0).

## Definition of Done (from ROADMAP active milestone + design doc)

Extend i18n from the shipped CLI message layer (`src/i18n/`, en/zh-Hans) to the **skill/workflow + user-facing yaml surface** — comet model: ship EN+中文 skills. **Full bilingual scope** (user override of the office-hours narrow finish-14-CLI-keys recommendation) + **Approach A parallel sibling files** (`SKILL.md` + `SKILL.zh-Hans.md`; `<base>.yaml` + `<base>.zh-Hans.yaml`; locale-aware resolve; CI sync-guard makes dual-maintenance a checkable hard constraint). Additive-only — en default byte-identical, claude default install byte-identical, no architecture change (reuses the v9.0 resolve layer + messages/{en,zh} file pattern).

## Phase Results

| Phase | Status | Shipped | Evidence (SUMMARY) |
|-------|--------|---------|--------------------|
| 29 locale-resolve-layer | ✅ passed | `f28e89e`/`7c892cf`/`0d28810` | TDD; `resolveSkillBody` locale body picker + `renderSkillFile`/`renderAllSkills` wiring + setup threads resolved locale; OPEN-2 = render-step, dest single `SKILL.md`, sibling stripped. en byte-identical. 1416 tests (+22). 详: `phases/29-locale-resolve-layer/29-01-SUMMARY.md`. |
| 30 skill-i18n sync-guard | ✅ passed | `dbaa181` + `3e7bbe0` (ci) | TDD; `scripts/check-skill-i18n-parity.mjs` dep-free structural-parity checker (frontmatter keys / `{{capabilities.X}}` placeholders / heading-level shape) + pre-install hard-fail ci step. OPEN-1 = structural parity. drift-only. 1423 tests. 详: `phases/30-skill-i18n-sync-guard/30-01-SUMMARY.md`. |
| 31 SKILL.md translation | ✅ passed | `d1d2834` | 26 `SKILL.zh-Hans.md` siblings (~10,132 words) via 7 parallel translation subagents; guard exit 0 (structural parity), en byte-identical, prose TDD-skip. yaml split → Phase 33. 1423/0. 详: `phases/31-skill-translation/31-01-SUMMARY.md`. |
| 32 CLI message gap | ✅ passed | `f2f58af` | TDD; `messages/zh-Hans.json` 80→94 full key parity with en.json + `tests/unit/i18n-parity.test.ts` (both-direction). 16 `uninstall.unified.*` added, 2 dead keys removed (count 14→16 reconciled). en byte-identical. 1426/0. 详: `phases/32-cli-message-gap/32-01-SUMMARY.md`. |
| 33 user-facing yaml i18n | ✅ passed | `5e0b2de` | TDD mechanism + subagent translation; `resolveLocaleYaml` loader (src/i18n/localeYaml.ts) + `loadRolePrompts`/`buildDisciplinesSection` locale-aware; `role-prompts.zh-Hans.yaml` (24 roles) + 5 discipline zh siblings; **fixed pre-existing en-default bug** (4 Chinese-source bases→English, zh siblings HEAD byte-identical); `language` excluded (never surfaced); guard `check-yaml-i18n-parity.mjs` + ci. e2e en→English / zh→Chinese. 1446/0 (+20). 详: `phases/33-yaml-i18n/33-01-SUMMARY.md`. |

## Cross-Phase Integration

The 5 phases form one chain (each a layer of the same end-to-end locale switch), verified by the e2e + the structural guards rather than a separate integration-checker:

- **resolve (29) → guard (30) → translation (31)** — the render-step body picker selects `SKILL.zh-Hans.md` under a zh locale; the dep-free guard enforces structural parity of every sibling pair (green at 26 siblings); en bodies byte-identical. Chain proven: guard exit 0 + en-default render unchanged.
- **CLI messages (32)** — independent `t()` layer brought to 94/94 key parity, pinned both-direction by `i18n-parity.test.ts`; en.json byte-identical.
- **yaml loader (33)** — `resolveLocaleYaml` serves `role-prompts.zh-Hans.yaml` + discipline zh siblings under a zh locale, base under en; surfaced via `harnessed prompt` (role body + `## Disciplines`) + `commands/*.md` generation. e2e: `HARNESSED_LANG=en prompt task-code` → English; `zh-Hans` → Chinese. The pre-existing en-default bug (English users receiving Chinese discipline text) is fixed; zh users byte-identical to today (siblings = HEAD verbatim).

No blocker-class gaps. Two non-blocker follow-ons recorded as tech debt (locale-source split HARNESSED_LANG vs HARNESSED_USER_LANG; language.yaml left mixed-but-never-surfaced).

## End-to-End Flows

1. **en incumbent (zero blast)** — an English-locale user gets byte-identical CLI messages, byte-identical `SKILL.md` bodies, byte-identical `role-prompts.yaml` + `doc-discipline.yaml` (English source); and now CORRECT English for the 4 previously-Chinese disciplines (the bug fix). Proven: en.json/SKILL.md byte-identical assertions across phases + e2e English disciplines.
2. **zh surface end-to-end** — a zh-Hans user gets the translated `SKILL.zh-Hans.md` body (resolve layer), the zh CLI messages (`t()`), and the zh role/discipline prompt text (`resolveLocaleYaml`), with discipline behavior byte-identical to today (siblings = pre-change Chinese verbatim). Proven: guard parity + e2e Chinese disciplines + `git show HEAD:… | diff` clean.

## Requirements Coverage

v10.0 carries full REQUIREMENTS.md traceability (reconciled at milestone-open):

| Requirement | Phase | Satisfied |
|-------------|-------|-----------|
| REQ-v100-resolve-layer | 29 | ✅ (1416 tests, en byte-identical) |
| REQ-v100-sync-guard | 30 | ✅ (drift-only hard gate) |
| REQ-v100-translation | 31 | ✅ (26 zh siblings, guard exit 0) |
| REQ-v100-cli-gap | 32 | ✅ (zh 94/94 + parity test) |
| REQ-v100-yaml-i18n | 33 | ✅ (loader + siblings + en bug fix + guard) |
| REQ-v100-validation | 29–33 | ✅ (biome+tsc clean, vitest 1446/0, additive, en byte-identical) |

Coverage **6/6**, no orphans.

## Verdict

**status: passed** — all 5 phases shipped + independently re-verified; the end-to-end locale switch chain is unbroken (resolve → guard → translation → CLI → yaml loader); both E2E flows real; the en incumbent is provably byte-identical (zero blast) AND a latent en-default bug was fixed (English users no longer receive Chinese discipline text). Two CI hard-gates (skill structural parity pre-install + yaml structural parity post-install) make the dual-maintenance tax a checkable constraint. All tech debt is non-blocking (locale-source unification; language.yaml mixed-but-never-surfaced; carried doc-hygiene; 3-OS CI pending the release push).

Next: v10.0 closed **lightweight** (5th use of the pattern, v5.1–v9.0 precedent — phase dirs 29–33 stay in `phases/NN/`, no `-ROADMAP`/`-REQUIREMENTS` archive, no git tag per codename≠npm-version; ROADMAP + MILESTONES collapse to one-line index rows). Then a **release-pass decision** — whether v10.0 ships as a new npm semver (e.g. 4.8.0) since 4.7.0 (CHANGELOG `## [X.Y.Z]` header required before tag; publish.yml auto gh-release). Per the release lesson (don't let closed milestones pile up unreleased), lean toward releasing.

## Milestone Discuss Decisions (carried forward — durable rationale)

Preserved here at close (the ROADMAP active block collapses to a one-line shipped-index row):

1. **Full bilingual scope (user override)** — the office-hours narrow recommendation (finish the 14 CLI keys only) was overridden by the user toward full skill+yaml bilingual surface. Logged as a defended-premise founder signal (forward-looking Chinese-audience bet, not observed-demand-driven); not re-litigated.
2. **Approach A parallel sibling files** — chosen over B (string-table extend `t()`, anti-karpathy for ~12k words of prose) and C (build-time split). Reuses the v9.0 resolve layer + messages/{en,zh} file pattern; the CI sync-guards make dual maintenance a hard constraint.
3. **Release-before-v10.0 re-sequence (ceo-review)** — the CEO audit caught v8.0+v9.0 built-but-unpublished (npm stuck at 4.6.0); a release pass shipped them as v4.7.0 BEFORE v10.0 work, so v10.0 builds on a published base.
4. **Phase 33 split from Phase 31 (research-driven)** — Phase-31 research found most yaml is internal (capabilities/judgments descriptions never surfaced at runtime); only `role-prompts.yaml` + surfaced `disciplines/*.yaml` need a locale-aware loader = a separate mechanism, so yaml i18n split into its own Phase 33 (roadmap 4→5). Phase 33 then surfaced + fixed the pre-existing en-default bug.
