# T1.0 CC CLI BDL Artifact — Wave 1 A1/A2 Assumption Resolve

**Date**: 2026-05-19
**Verdict**: PASS — both cmds exist; syntax partially differs from inferred (scope handling)

---

## Verified cmd syntax (verbatim from `--help`)

### `claude mcp remove` (mcpStdioAdd + mcpHttpAdd uninstallers)

```
Usage: claude mcp remove [options] <name>

Remove an MCP server

Options:
  -h, --help           Display help for command
  -s, --scope <scope>  Configuration scope (local, user, or project)
                       - if not specified, removes from whichever scope it exists in
```

**Invocation for T1.2**: `['mcp', 'remove', name]`
**Note**: `--scope project` NOT required for remove (transport-agnostic; removes from whichever scope). Differs from install which hardcodes `--scope project`. mcpStdioAdd + mcpHttpAdd share identical remove cmd.

### `claude plugin uninstall` (ccPluginMarketplace uninstaller)

```
Usage: claude plugin uninstall|remove [options] <plugin>

Uninstall an installed plugin

Options:
  -h, --help           Display help for command
  --keep-data          Preserve plugin persistent data
  --prune              Also remove auto-installed dependencies
  -s, --scope <scope>  Uninstall from scope: user, project, or local (default: "user")
  -y, --yes            Skip --prune confirmation prompt
```

**Invocation for T1.2**: `['plugin', 'uninstall', pluginAtMkt]`
**Note**: scope defaults to "user" (matches D-12 install behavior). Alias `remove` also valid.

---

## A1/A2 resolution

| Assumption | Inferred | Actual | Delta |
|------------|----------|--------|-------|
| A1: `claude mcp remove <name> --scope project` | YES | `claude mcp remove <name>` (no scope needed) | MINOR — drop `--scope project` |
| A2: `claude plugin uninstall <slug>` | YES | `claude plugin uninstall <plugin>@<marketplace>` | MATCH |

**Impact on T1.2**: mcpStdioAdd + mcpHttpAdd uninstallers use `['mcp', 'remove', name]` (no scope flag). ccPluginMarketplace uses `['plugin', 'uninstall', pluginAtMkt]`.
