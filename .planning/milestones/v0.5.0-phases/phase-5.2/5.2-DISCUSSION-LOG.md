# Phase 5.2 Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `5.2-CONTEXT.md` — this log preserves alternatives considered.

**Date**: 2026-05-19
**Phase**: 5.2 — R10.3 harnessed uninstall (#BV) + R10.4 path traversal regex hardening (#AH)
**Mode**: discuss 4 HIGH deliberate (Q1/Q2/Q3/Q4) + 4 MED/LOW batch按推荐 (Q5/Q6/Q7/Q8); project memory dual-feedback (three-layer-stack-strict + workflow-cadence) 严格遵守
**gstack governance gate**: inline 6-role briefing 实现 sister Phase 5.1 cadence延袭 (CEO + Eng + Designer + Paranoid Staff Eng + QA + CSO)

---

## Meta-decision LOCKED upfront (sister Phase 5.1 cadence延袭)

**Decision**: M-01 PhaseClass = ARCHITECTURAL (NOT R-5 publish-only)

| Alternative | Verdict |
|-------------|---------|
| R-5 publish-only mode | ❌ REJECT — NEW src/cli/uninstall.ts + 7 NEW uninstallers + MODIFY src/manifest/security.ts = architectural change |
| ARCHITECTURAL full cadence (ADR 0022 + ci.yml A7 iter 0021→0022 + tag) | ✅ LOCK — sister Phase 5.1 + v0.3.0/v0.4.0 close cadence延袭 |

---

## Q1 (HIGH, deliberate): R10.3 uninstall dispatch architecture

**Decision**: D-01 per-method 7 NEW src/uninstallers/*.ts (sister installers/ pattern symmetric延袭)

### Alternatives considered

| Option | Verdict |
|--------|---------|
| per-method 7 NEW uninstall*.ts in src/uninstallers/ + index.ts dispatch | ✓ SELECTED |
| 单 uninstall.ts switch 7 case | asymmetric with installers/ 7-file pattern + ~150L approaches Karpathy 200L |
| Reverse-by-install ledger (.harnessed/installed.json) | over-engineering + install-time co-mod + state.ts lock 互动 |
| Hybrid per-method + ledger | 2 architectures simultaneously over-engineering |

**User's choice**: per-method 7 NEW (Recommended)
**Notes**: 7 file each ≤30L sister install precedent; symmetric clarity + Karpathy isolation

---

## Q2 (HIGH, deliberate): R10.3 ephemeral install (npx --yes) uninstall semantic

**Decision**: D-02 no-op + warn user

### Alternatives considered

| Option | Verdict |
|--------|---------|
| no-op + warn `'ephemeral install: nothing to uninstall'` exit 0 | ✓ SELECTED Unix idempotent + UX clarity |
| error + exit 1 | UX 弱 用户必先验知 ephemeral |
| auto-detect + silent skip | implicit logic 用户不明 |
| ledger lookup (Q1 Hybrid prereq) | Q1 NOT Hybrid 不适用 |

**User's choice**: no-op + warn (Recommended)
**Notes**: detection via manifest spec.install.cmd includes "npx --yes" / "npx -y"

---

## Q3 (HIGH CSO, deliberate): R10.4 path traversal attack vector coverage

**Decision**: D-03 5 vector minimal MVP

### Alternatives considered

| Option | Verdict |
|--------|---------|
| 5 minimal MVP (../+..\+null byte+URL-encoded+double-encoded) OWASP A1 top 5 | ✓ SELECTED |
| Comprehensive 10+ vectors (+ symlink + UNC + Unicode NFD/NFC + Win 8.3 + ADS) | 超 Karpathy YAGNI Phase 5.2 1-day target |
| regex 黑名单 + path.resolve() 白名单双防 | defense-in-depth 优秀 但 fs.realpath + Win symlink edge case + 2x test surface |
| 仅加 1 vector ../ | 违 R10.4 spec verbatim "5+ vectors" 不接受 |

**User's choice**: 5 vector minimal MVP (Recommended)
**Notes**: pre-compile RegExp module-level sister Phase 5.1 R10.1 redact 5 patterns symmetric design

---

## Q4 (HIGH, deliberate): R10.4 hardening 集成位点

**Decision**: D-04 resolveAlias + manifest path 2 点 minimal scope

### Alternatives considered

| Option | Verdict |
|--------|---------|
| resolveAlias + manifest path 2 user-controlled sites | ✓ SELECTED minimal real attack surface |
| + src/audit/log.ts JSONL path | hardcoded NOT user-controlled YAGNI |
| + uninstall.ts explicit verify | transitive resolveAlias 已 covers (redundant) |
| Comprehensive fs.* full API audit | 超 Phase 5.2 scope (sister BB path NOT v1.0 gate) |

**User's choice**: resolveAlias + manifest path 2 点 (Recommended)
**Notes**: minimal attack surface; sister CSO Paranoid Staff Eng 视角 align surface area minimal

---

## Q5-Q8 (MED/LOW, batch按推荐 lock)

**Decision**: All 4 confirmed per recommended defaults (sister Phase 5.1 batch cadence延袭)

### Q5 (MED) D-05 DryRunFlag: --dry-run default ON

| Option | Verdict |
|--------|---------|
| --dry-run default ON; --apply explicit | ✓ SELECTED sister install ADR 0004 contract 1 延袭 |
| --apply default (immediate execute) | destructive default 不可接受 |
| Force interactive y/N every time | UX 弱 CI 阻塞 |

### Q6 (MED) D-06 ConfirmPrompt: --yes bypass interactive y/N

| Option | Verdict |
|--------|---------|
| Interactive y/N default No + --yes bypass for CI | ✓ SELECTED sister install --non-interactive double-flag 延袭 |
| No prompt always (--apply enough) | 单层保护不足 destructive op |
| Always prompt (no --yes bypass) | CI 自动化阻塞 |

### Q7 (LOW) D-07 BackupOption: NO --keep-backup flag

| Option | Verdict |
|--------|---------|
| NO --keep-backup; use `harnessed backup` independent | ✓ SELECTED single responsibility + sister backup workflow ship |
| --keep-backup default ON (move-to-backup) | 混淆 backup 职责 + disk space accumulation |
| --keep-backup opt-in flag | over-engineering YAGNI |

### Q8 (LOW CSO) D-08 ErrorMessageSafety: NOT leak attempted path

| Option | Verdict |
|--------|---------|
| Generic 'path traversal attempt detected' message + internal log only | ✓ SELECTED CSO defense + sister R10.4 redact symmetric |
| Echo back attempted path | attack reconnaissance leakage CSO veto |
| Silent fail no error | UX 弱 + audit log missing |

**User's choice**: confirm 按推荐 batch lock (Recommended)
**Notes**: 4 MED/LOW implementation detail level (NOT strategic); sister Phase 5.1 4 HIGH + 4 MED batch pattern延袭

---

## Claude's Discretion (留 planner)

- src/uninstallers/index.ts dispatch table 命名 (vs consolidate)
- Uninstaller fixture TDD coverage 10-14 cells 7 method × edge case (ephemeral / dry-run / --yes / Win rm -rf)
- R10.4 5 RegExp pre-compile site (`src/manifest/security.ts` co-locate vs NEW `src/manifest/lib/path-guard.ts` extract)
- PathTraversalError class location (recommend co-locate security.ts YAGNI)
- ADR 0022 outline sister 0021 9-section format延袭
- ci.yml A7 step iter 0021→0022 (single extend NOT retroactive — Phase 5.1 W2 T2.8 已 retroactive fix 0019/0020 + 0021)
- W0 sub-batch #BH validateFlags.ts + #BI dry-run abstract ABSORB boundary
- STATE.md ≤150L verify post-W0.1 D2 iter 6 trim

---

## Deferred Ideas (logged for v0.6+ / Phase 5.3+)

- comprehensive 10+ attack vectors — v0.6+ if signal
- regex + path.resolve() double-defense — v0.6+ if signal
- reverse-by-install ledger — REJECT permanent (over-eng)
- --keep-backup flag — REJECT (use independent backup workflow)
- uninstall undo / time-machine — v0.7+ if signal
- comprehensive fs.* full API audit — v0.6+ supply-chain
- error message path echo back — REJECT permanent CSO veto
- #BJ #BK Phase 4.2 LOW cosmetic — Phase 5.3+ defer

---

## Carry-forward backlog inventory (Phase 5.1 → Phase 5.2 W0)

11 items inventoried (full disposition in 5.2-CONTEXT.md "Phase 5.2 W0 backlog" table):

- ✅ DISCHARGED this discuss: #BV (R10.3 PRIMARY) + #AH (R10.4 PRIMARY)
- ✅ PRE-DISCHARGED Phase 5.1: #BU (R10.2 state lock SHIPPED)
- W0.1 FIRST TASK conditional: #BA SIZE_LIMIT round 3 165→150 + #BS D2 iter 6 same task
- W0 sub-batch ABSORB recommend: #BH validateFlags.ts MED + #BI dry-run abstract MED
- DEFER Phase 5.3+: #BJ LOW cosmetic + #BK LOW rare
- DEFER PERMANENT: #BL/#BM F40-2 lock
- DEFER conditional: #BC/#BD/#BE/#BN no Phase 5.2 signal
