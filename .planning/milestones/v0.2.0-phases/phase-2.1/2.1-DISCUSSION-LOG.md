# Phase 2.1: 4 installer methods 实装 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 2.1-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-15
**Phase:** 2.1 — 4 installer methods 实装
**Areas discussed:** npx-skill-installer scope / anthropics/skills license policy / Wave 0 packaging / v0.2.0 ADR strategy
**Mode:** interactive (4 gray areas presented, all decided in one round — research refresh had already resolved most HOW questions)

---

## npx-skill-installer scope

| Option | Description | Selected |
|--------|-------------|----------|
| minimal-pin 一个工具实装 | 选一个工具 minimal pin 实装,verify best-effort。保证 6 install method 全覆盖 | ✓ |
| 实装 stub + 标记 partial | 实装接口骨架,核心装载逻辑标 partial(类 semanticRouter L2 stub) | |
| 完整实装(含目录桥接) | 完整实装 + 解决 `~/.agents/` vs `~/.claude/skills/` 目录桥接 | |

**User's choice:** minimal-pin 一个工具实装
**Notes:** research 标 npx-skill-installer 为 MEDIUM/soft-defer(无单一官方工具 + 目录约定冲突)。用户选不让它拖累 6-method 覆盖,但目录桥接(SessionStart sync-hook)不在 Phase 2.1 引入 — 作为已知 limitation 记 progress.md finding(D-02)。

---

## anthropics/skills license policy

| Option | Description | Selected |
|--------|-------------|----------|
| 加 anthropics-official carve-out | whitelist 加 `anthropics-official` license class 专门放 anthropics/skills 来源 | ✓ |
| 加 license_source 审计字段 + warn | 不加专门 class,加 `license_source` 字段记来源,none 时 warn 不 block | |
| 推迟 —— Phase 2.3 再决 | Phase 2.1 只做 installer 机制,license policy 推 Phase 2.3 | |

**User's choice:** 加 anthropics-official carve-out
**Notes:** `frontend-design`/`document-skills`/`webapp-testing` 来自 `anthropics/skills`(无 LICENSE 文件)。项目已隐性接受(decision_rules.yaml 已含)。决策写进 ADR 0010 § Decision,同时加 `license_source` audit 字段(两个 option 的元素都吸收)。baoyu-skills D1.2.5-10 research 已 resolve 为 MIT-0 — 一起 fold 进 Wave 0。

---

## Wave 0 packaging

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 2.1 Wave 0 任务批 | 独立任务批,在 installer 实装 wave 之前。progress.md 单独追踪 | ✓ |
| fold 进各 installer 任务 | Wave 0 项分散 fold 进相关 installer 任务 | |
| 拆出 Phase 2.0.5 | insert 一个 Phase 2.0.5 专门处理 | |

**User's choice:** Phase 2.1 Wave 0 任务批
**Notes:** phase 1.5 sister review deferred(H3/H4/M1 + transparency checklist)+ research refinement #8/#10/#11 打包为 Phase 2.1 Wave 0 独立任务批,沿袭 phase 1.5 8-wave 模式。Wave 0 必须在 installer wave 之前 —— license whitelist / bundle-install schema / checkCmdString carve-out 都是 installer 实装的前置(7 项见 CONTEXT D-07)。

---

## v0.2.0 ADR strategy

| Option | Description | Selected |
|--------|-------------|----------|
| 拆分 —— 0010 Phase 2.1 schema + 0011 Phase 2.2 SDK | 按 phase 分,谱系清晰 | ✓ |
| 单 ADR 0010 覆盖 v0.2.0 全部 | 一个 ADR 覆盖所有决策 | |
| Phase 2.1 只动 errata,ADR 0010 推 Phase 2.2 | Phase 2.1 走现有 ADR errata 路径 | |

**User's choice:** 拆分 —— ADR 0010 = Phase 2.1 schema errata,ADR 0011 = Phase 2.2 SDK
**Notes:** ADR 0010 = license whitelist 扩展 + bundle-install schema + install_type enforcement + H3 agentDefinition budget errata + H4 substring false-positive transparency(Wave 0 起草)。ADR 0011 = Claude Agent SDK 引入 + ralph-wiggum keep-vs-switch(推 Phase 2.2)。ADR 0010 走 errata 路径(A7 守恒),ship 时加 adr-0010-accepted tag,CI A7 iter 1-9 → 1-10。

## Claude's Discretion

- 各 installer 的 lib helper 复用/新增结构(7 个现有 helper 是否够用)
- bundle-install schema 的具体字段形态
- transparency verify checklist 的具体落地形态(markdown checklist vs CI grep gate)
- npx-skill-installer 选哪个具体工具 minimal-pin

## Deferred Ideas

- npx-skill-installer 完整目录桥接 → 后续 phase
- Claude Agent SDK + ralph-wiggum keep-vs-switch → Phase 2.2 / ADR 0011
- execute-task workflow + ralph-loop full integration → Phase 2.2
- design/content/testing extension category MVP → Phase 2.3
- `--force` flag for idempotent_check → phase 2.1+ 评估
