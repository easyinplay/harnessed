# T4.4 DEFERRED — v0.3.0 接手 onboarding (1 页)

> **Status**: DEFERRED → v0.3.0 checkpoint 完整版
> **Trigger**: T1.2 SC4 PARTIAL — SDK 0.3.142 session resume API 仅走 `@alpha` bridge/assistant 子系统, NOT main `query()` (research § 1.3 ralph-loop 用 query()); 违 Karpathy simplicity for v0.2.0 stable → B-35 fallback branch triggered
> **Source**: D-18 / B-35+B-36 / ADR 0011 § 9 / intel CD-4 (L139-147)

## v0.3.0 接手者最大 1 commit 就 land 的 3 件事

### 1. Closure infra 三件套（已铺好，0 改动）

| File | Line | Symbol | 状态 |
|------|------|--------|------|
| `src/routing/lib/sdkSpawn.ts` | ~30-40 | `onSessionId?: (id: string) => void` callback option | ready |
| `src/routing/lib/ralphLoop.ts` | ~20-30 | `resumeSessionId` closure variable across iterations | ready |
| `src/routing/engine.ts` | ~120-150 | `capturedSessionId` closure in `wrappedSpawn` | ready |

→ Wave 4 T4.1/T4.2 已实现完整 capture-and-resume 闭环。当 SDK session resume API 出 `@alpha` 标记 (or 重新设计为 main `query()` API exposure) 后，**仅需 1 commit** 连接 schema + UI 即可激活。

### 2. v0.3.0 接手 1-commit 实施步骤

```diff
// src/workflow/schema/phases.ts
 export const PhaseEntry = Type.Object({
   id: PhaseId,
   model: ModelTier,
+  task_session_id: Type.Optional(Type.String()),
 }, { additionalProperties: false })
```

```diff
// src/routing/engine.ts (or 自定义 spawn wrapper)
   const wrappedSpawn = async (resumeSessionId?: string) => {
-    return sdkSpawn({...sdkOpts, onSessionId: (id) => capturedSessionId = id})
+    const taskSessionId = phase.task_session_id ?? resumeSessionId
+    return sdkSpawn({...sdkOpts, onSessionId: (id) => capturedSessionId = id, resumeSessionId: taskSessionId})
   }
```

```diff
// tests/routing/sdk-spawn.test.ts → 加 cell
+  it('task_session_id propagates to options.resume when phase.task_session_id set', async () => { ... })
```

### 3. v0.3.0 检查清单（接手前）

- [ ] `npm view @anthropic-ai/claude-agent-sdk version` → 是否 ≥ 0.4 (假设 0.3 系列 ship 后 0.4 移走 `@alpha` 标记 OR 改 expose 路径)
- [ ] 验 `node_modules/@anthropic-ai/claude-agent-sdk/dist/sdk.d.ts` 是否 `query()` 直接接 `resume?: string` option (非走 bridge/assistant)
- [ ] 若新 SDK 直接 expose: 走 1-commit path 上文
- [ ] 若仍走 bridge/assistant @alpha: 评估是否值得 v0.3.0 即上 OR 继续 deferred (重审 Karpathy simplicity)

### 4. 相关引用

- ADR 0011 § 9 Task Session 复用 conditional (full spec)
- ASSUMPTIONS B-35 + B-36 (fallback branch + conditional schema 字段)
- `.planning/intel/omc-comparison.md` L139-147 (CD-4 omo 来源 + 原理)
- `.planning/phase-2.2/task_plan.md` T4.4 (DEFERRED block + spec 保留作 v0.3.0 参考)
- Wave 4 commits: 60e1241 (sdkSpawn closure) / bad8be1 (engine.ts capturedSessionId)
