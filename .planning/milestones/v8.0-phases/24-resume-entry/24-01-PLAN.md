---
phase: 24-resume-entry
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/checkpoint/nextStep.ts
  - src/cli/next.ts
  - src/cli/lib/here.ts
  - src/cli.ts
  - messages/en.json
  - messages/zh-Hans.json
  - tests/cli/here.test.ts
  - tests/checkpoint/nextStep.test.ts
autonomous: true
requirements:
  - "v8.0 Phase 24 acceptance (24-CONTEXT.md D1–D6 + Acceptance 1–5)"
user_setup: []

verified_refs:
  - "readCurrentWorkflow in src/checkpoint/state.js (exists — next.ts:21 / status.ts:20 import)"
  - "resolveNext in src/checkpoint/nextStep.ts:15 (exists)"
  - "resolveAutoTransition in src/cli/next.ts:7 (exists, private — MIGRATE+export; no test covers it, grep empty)"
  - "buildRecoverLines in src/cli/status.ts:59 (exists, export, async)"
  - "repoKey in src/checkpoint/workflowStore.ts:60 (exists, export)"
  - "src/cli/lib/here.ts (NEW — sister src/cli/lib/release-preflight.ts)"
  - "cli.ts bare-dispatch before program.parse L112 (NEW)"
  - "here.no_workflow / here.no_workflow.hint i18n keys (NEW, en.json + zh-Hans.json)"
  - "tests/checkpoint/nextStep.test.ts (exists — add resolveAutoTransition cases)"

must_haves:
  truths:
    - "parseBareInvocation([]) → {here:true,json:false}; (['--json']) → {here:true,json:true}; (['--lang','zh']) → {here:true,json:false}; (['status']|['bogus']|['--help']) → {here:false}"
    - "buildHereView(null,…) → onboarding line(s) + json {active:false}; populated → lines contain phase + per-sub markers + a NEXT: auto|manual|done line; json contains phase/status/next/sub"
    - "resolveAutoTransition: env true/false overrides envelope; unset → envelope ?? true"
    - "bare `harnessed` reports the active workflow (or onboarding); `harnessed --json` prints valid JSON; `harnessed bogus` still errors unknown-command; `--help`/`--version` unchanged"
    - "the entry is read-only — no state/git/remote mutation"
    - "full gate green; biome clean; tsc 0"
  artifacts:
    - path: "src/cli/lib/here.ts"
      provides: "parseBareInvocation + buildHereView + runHere — zero-arg you-are-here aggregation"
      contains: "parseBareInvocation"
    - path: "src/checkpoint/nextStep.ts"
      provides: "exported resolveAutoTransition (migrated from next.ts, de-dup)"
      contains: "resolveAutoTransition"
  key_links:
    - from: "bare `harnessed` (cli.ts dispatch)"
      to: "readCurrentWorkflow + buildRecoverLines + resolveNext"
      via: "parseBareInvocation → runHere → buildHereView (no-spawn report, comet /comet analog)"
      pattern: "buildHereView"
---

<objective>
给 harnessed 加零参数入口:裸 `harnessed` = auto-detect 当前 repo 的 active workflow（`readCurrentWorkflow`，已按 repoKey 路由）+ 打印 you-are-here 仪表盘（复用 `buildRecoverLines`）+ `NEXT: auto|manual|done` 契约行 + run hint；`--json` 给机器读；无 active → onboarding 提示。补 comet `/comet` 的单命令 resume ergonomics gap。机制全在，仅缺零参数 dispatch + 聚合视图。

约束（24-CONTEXT.md D1–D6）：human 默认 + `--json` + NEXT hint（D1）；no-spawn report，不 auto-exec prompt（D2）；显式 `parseBareInvocation` 只认 `[]` / `['--json']`，不破坏 subcommand/`--help`/`--version`/unknown（D3）；复用 buildRecoverLines（D4）；聚合在 `src/cli/lib/here.ts`（D5）；TDD red-first（D6）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/24-resume-entry/24-CONTEXT.md
@src/cli/next.ts
@src/cli/status.ts
@src/checkpoint/nextStep.ts
@src/checkpoint/workflowStore.ts
@src/cli.ts
</context>

<key_facts>
planner 已验证（直接用）：

- `readCurrentWorkflow()`（state.js）返回 cwd-repo envelope 或 null；字段 `phase`/`status`/`started_at`/`sub_progress`(ledger)/`auto_transition?`。next.ts:23 + status.ts:114 已用法证明。
- `buildRecoverLines(wf, ledger)`（status.ts:59，async，export）= you-are-here 主体：`workflow:` 行 + 每 sub `statusMarker` + `← next` + `→ next: harnessed prompt <sub>` + drift。空 ledger 优雅降级（"no ledger — run gates + start"）。直接复用做 human 主体。
- `resolveNext(ledger, auto)`（nextStep.ts:15）→ `{next:'auto'|'manual'|'done', sub?, hint?}`。`NEXT:` 行沿用 next.ts:27 字面风格（非 t()）。
- `resolveAutoTransition`（next.ts:7，私有，**无测试**）：`HARNESSED_AUTO_TRANSITION` env > envelope > default true。迁到 nextStep.ts export，next.ts 改 import（无环：nextStep 只 import ledger）。
- cli.ts:42-53 已有 `--lang` 预解析循环（在 parse 前消费）；故 `parseBareInvocation` 检测时也 strip `--lang`/`--lang=`。cli.ts:112 是裸 `program.parse(process.argv)` —— 在它之前插 dispatch。
- ESM（package.json type module）→ top-level await 可用；或用 `void runHere(...).catch(...)`。
- i18n key 风格 `"here.no_workflow": "...{{path}}"`（en.json L80 sister）。zh-Hans.json 加同 key。
- CLI test pattern：ExitError + spyExit + silence + runCli（tests/cli/install.test.ts L30-68 / next.test.ts）。
- 基线：post-ECC-batch 1342 green，tsc/biome clean。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1 (Wave 1): resolveAutoTransition 迁移 + here.ts 聚合 + cli.ts dispatch</name>
  <files>src/checkpoint/nextStep.ts, src/cli/next.ts, src/cli/lib/here.ts, src/cli.ts, messages/en.json, messages/zh-Hans.json, tests/cli/here.test.ts, tests/checkpoint/nextStep.test.ts</files>
  <red>
失败测试：

`tests/checkpoint/nextStep.test.ts`（追加）：
1. `resolveAutoTransition(undefined)` env unset → true；`(false)` env unset → false；`(true)` → true。
2. `HARNESSED_AUTO_TRANSITION='false'` → false（override envelope true）；`='true'` → true（override envelope false）。

`tests/cli/here.test.ts`（NEW）：
3. `parseBareInvocation([])` → `{here:true,json:false}`。
4. `parseBareInvocation(['--json'])` → `{here:true,json:true}`。
5. `parseBareInvocation(['--lang','zh'])` → `{here:true,json:false}`；`(['--lang=zh'])` → 同。
6. `parseBareInvocation(['status'])` / `(['bogus'])` / `(['--help'])` / `(['--version'])` → `{here:false,json:false}`。
7. `parseBareInvocation(['--json','extra'])` → `{here:false}`（多 token 非裸）。
8. `buildHereView(null, [], true)` → lines 含 onboarding（非空）+ `json.active===false`。
9. `buildHereView(wf, ledger, false)`（wf={phase:'phase-24',status:'active',started_at:'…'}，ledger=[1 done,1 pending]）→ lines 含 `'phase-24'` + 某 `← next` + 一行以 `NEXT:` 开头（值 ∈ auto/manual/done）；`json.phase==='phase-24'` + `json.next` 存在 + `json.sub` 存在。
10. `buildHereView(wf, [allDone], true)` → `NEXT: done`；`json.next==='done'`。
  </red>
  <green>
- `src/checkpoint/nextStep.ts`：移入 `export function resolveAutoTransition(envelopeValue?: boolean, env = process.env.HARNESSED_AUTO_TRANSITION): boolean`（env==='true'→true / 'false'→false / else envelopeValue ?? true）。
- `src/cli/next.ts`：删本地 `resolveAutoTransition`，改 `import { resolveNext, resolveAutoTransition } from '../checkpoint/nextStep.js'`。
- `src/cli/lib/here.ts`（NEW）：
  - `export function parseBareInvocation(argvRest: string[]): { here: boolean; json: boolean }` —— strip `--lang <code>`/`--lang=…` 后：`tokens.length===0` → `{here:true,json:false}`；`length===1 && tokens[0]==='--json'` → `{here:true,json:true}`；else `{here:false,json:false}`。
  - `export interface HereJson { active: boolean; phase?: string; status?: string; started_at?: string; next?: 'auto'|'manual'|'done'; sub?: string; hint?: string; sub_progress?: unknown[] }`
  - `export async function buildHereView(wf, ledger, auto): Promise<{ lines: string[]; json: HereJson }>`：
    - wf null → `lines=[t('here.no_workflow',{path: harnessedFile('workflows.json')}), t('here.no_workflow.hint')]`；`json={active:false}`。
    - else → `const body = await buildRecoverLines(wf, ledger)`（import 自 status.js）；`const step = resolveNext(ledger, auto)`；`lines=[...body, \`NEXT: ${step.next}\`, ...(step.sub ? [\`→ run: harnessed prompt ${step.sub}\`] : [])]`；`json={active:true, phase:wf.phase, status:wf.status, started_at:wf.started_at, next:step.next, sub:step.sub, hint:step.hint, sub_progress:ledger}`。
  - `export async function runHere(opts: { json?: boolean }): Promise<void>`：lazy-import `readCurrentWorkflow`（state.js）；`const wf = await readCurrentWorkflow(); const ledger = wf?.sub_progress ?? []; const auto = resolveAutoTransition(wf?.auto_transition); const view = await buildHereView(wf, ledger, auto);` json ? `console.log(JSON.stringify(view.json,null,2))` : `for (const l of view.lines) console.log(l)`；`process.exit(0)`。
- `src/cli.ts`：在 `program.parse(process.argv)` 之前插：
  ```
  import { parseBareInvocation, runHere } from './cli/lib/here.js'
  const bare = parseBareInvocation(process.argv.slice(2))
  if (bare.here) { void runHere({ json: bare.json }); } else { program.parse(process.argv) }
  ```
  （`--lang` 预解析循环保留在前；runHere 内 process.exit，错误走既有 unhandledRejection handler。）
- `messages/en.json`：`"here.no_workflow": "no active workflow for this repo ({{path}} has no entry for cwd)"`，`"here.no_workflow.hint": "  to start: run a stage (e.g. \`harnessed gates discuss\`) or \`harnessed run <workflow>\`"`。
- `messages/zh-Hans.json`：对应中文（保留命令/路径英文）。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/here.test.ts tests/checkpoint/nextStep.test.ts 2>&1 | tail -16</automated>
  </verify>
  <done>parseBareInvocation 7 形态正确；buildHereView null→onboarding / populated→phase+marker+NEXT；resolveAutoTransition env 优先级；迁移不破坏 next。</done>
</task>

<task type="auto">
  <name>Task 2: verify gate — 全测试 + biome + tsc + dogfood + 无突变</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. `corepack pnpm exec biome check --write` 目标文件 → clean。
2. `corepack pnpm exec tsc --noEmit` exit 0。
3. `corepack pnpm test` 全套 green 0 fail（≥1342 + 新增）；含 `tests/cli/next.test.ts`（迁移后仍绿）。
4. **dogfood**（build 后 `node dist/cli.mjs`）：
   - 本 repo（无 active workflow）裸 `harnessed` → onboarding 提示 exit 0。
   - `harnessed --json` → 合法 JSON（`{"active":false}` 或当前 envelope）。
   - `harnessed bogus` → 仍报 unknown command（非仪表盘）。
   - `harnessed --help` / `--version` → 不变。
5. **无突变确认**：grep 新路径无 `git `/`writeFile`/`writeCurrentWorkflow`/`process.env` 写（here.ts 只读 readCurrentWorkflow + buildRecoverLines）。
6. `git diff --stat` 仅 8 个目标文件。
  </action>
  <verify>
    <automated>corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3</automated>
  </verify>
  <done>biome clean；tsc 0；全测试 green（含 next.test.ts 迁移后）；dogfood 4 形态正确；无突变；diff 仅目标。证据进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-24-01 | Tampering (dispatch misfire) | 零参数 dispatch 误吞 `harnessed bogus` / `--help` / subcommand | mitigate | D3 显式 `parseBareInvocation` 只认 `[]`/`['--json']`；测试 case 6/7 断言其余 → `{here:false}` 走 program.parse |
| T-24-02 | Tampering (unintended state/git write) | 入口误改 workflow/状态 | mitigate | here.ts 只 readCurrentWorkflow + buildRecoverLines（buildRecoverLines 的 detectDrift 只 re-hash，不写）；Task2#5 grep 确认无写 |
| T-24-03 | Information disclosure (JSON shape drift) | `--json` 输出与现有 next/status 契约不一致 | mitigate | HereJson 显式 interface；测试 case 9/10 断言字段；复用 resolveNext 的 next/sub/hint 语义 |
| T-24-04 | DoS (drift re-hash on huge ledger) | buildRecoverLines detectDrift 对大 evidence 集慢 | accept | 沿用 status --recover 既有行为；ledger 规模有界（单 phase sub 数）；无新增风险面 |
</threat_model>

<verification>
phase 级验收（= 24-CONTEXT.md Acceptance）：
1. parseBareInvocation 7 argv 形态正确（含 `--lang` strip、unknown 不 misfire）。
2. buildHereView null→onboarding+`{active:false}`；populated→phase+markers+`← next`+`NEXT:` 行；json 含 phase/status/next/sub。
3. resolveAutoTransition env 优先级（true/false override；unset→envelope??true）。
4. dogfood：裸 `harnessed`（active / 无 active）、`--json`、`bogus`、`--help`/`--version` 全符合。
5. 全 gate green；biome clean；tsc 0；无 state/git/remote 突变。
</verification>

<success_criteria>
- 零参数 `harnessed` = 单命令 you-are-here + NEXT（comet `/comet` analog），human 默认 + `--json`，无 active→onboarding。
- 纯聚合既有件（readCurrentWorkflow + buildRecoverLines + resolveNext），无新机制、无 spawn、只读。
- 显式 dispatch 不破坏任何现有 subcommand / `--help` / `--version` / unknown-command。
- TDD red→green（Wave 1）；dogfood 证 4 形态；全 gate green。
</success_criteria>

<output>
完成后建 `.planning/phases/24-resume-entry/24-01-SUMMARY.md`：RED→GREEN、parseBareInvocation 7 形态、buildHereView 复用 buildRecoverLines、resolveAutoTransition 迁移 + 首测覆盖、cli.ts dispatch、i18n 2 key、dogfood 4 形态证据、无突变、biome clean、tsc 0、全测试计数、git diff --stat。
</output>
