---
milestone: v12.0
milestone_name: Forward Continuation
audited: 2026-06-30
status: passed
scope_note: "v12.0 = Phases 37–40. Closes the horizontal-advance gap: after a task/phase finishes, derive + advance to the next (cross-task/phase), vs manual re-invocation. Design SoT = specs/2026-06-30-forward-continuation-design.md (5-repo comparative fusion: gsd-pi/gsd-core/Trellis/comet/oh-my-pi → unanimous 'derive next from disk SoT, no queue'). Strategy gate /office-hours transparent-skipped (direction pre-locked by the 5-repo research + explicit user decision — not a fresh 'should we build' question). OQ-1/2/3 locked at discuss-phase (advance print-only / phase-level floor / phases/NN-*/ dir scan SoT). Execution main-session hand-controlled, per-wave TDD red→green. Bundled the issue #2 fix. Published npm 4.10.0 (provenance) + gh release v4.10.0, CI green 3-OS."
scores:
  requirements: 9/9   # AC1–AC9 (BDD acceptance, spec §5)
  phases: 4/4
  integration: 1/1    # deriveNext (W1) → harnessed next/advance surface (W2) → per-turn NEXT-UNIT pointer (W3) → docs/verify (W4); one disk-derived forward seam
  flows: 2/2          # cross-phase advance (AC2/AC3/AC4) end-to-end + backward-compat sub semantics unchanged (AC9)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 39-perturn-pointer
    items:
      - "D5 (optional gsd_run query reuse) DEFERRED — per-turn surface is a plain-JS bin (no subprocess/import by design); adding a gsd_run path would diverge TS-vs-bin on GSD-compatible projects (breaks the ts↔bin `toBe` parity gate), add hot-path subprocess latency per turn, and couple to gsd-tools' ROADMAP schema (spec §3.2 forbids). native scan is the complete floor (AC8). Revisit only if the `next`/`advance` CLI path (not per-turn) wants the acceleration."
  - phase: 38-next-advance
    items:
      - "Task-level CLI surface unwired (OQ-2 lean) — deriveNext + describeUnit fully support `kind:'task'`, and scanPlanning has `includeTasks`, but the CLI surface ships at phase-level granularity. Wiring task-level is a clean follow-on (flip includeTasks + surface the task UNIT line); no redesign."
  - phase: milestone
    items:
      - "Post-release docs deferred: harnessed-site reference/cli.md next/advance (en+zh) + the 9 localized READMEs (which are stale at the 4-stage era, needing a full re-sync not a backfill) + README forward-continuation marketing. Tracked in STATE post-release follow-ups."
      - "Doc hygiene (carried v8.0→v12.0): shipped phase dirs 09–40 still live in active phases/ (de-facto convention — trust ROADMAP/MILESTONES shipped-index for scope)."
---

# v12.0 Forward Continuation — Milestone Audit

> Audited 2026-06-30. Scope = **Phases 37–40**. Closes the horizontal-continuation gap surfaced by the user: harnessed's singleton ledger tracked only one workflow invocation's subs, so finishing a task/phase advanced nothing. Now `harnessed next`/`advance` derive the next unit from `.planning/` disk state. Design = a 5-repo comparative study; all converge on **derive-next-from-disk, never maintain a queue**.

## Definition of Done (spec §5, BDD AC1–AC9)

- **AC2 phase-boundary** · **AC3 resume-first-incomplete** · **AC4 mid-insertion** · **AC5 done** · **AC6 advance-gate** · **AC8 no-GSD graceful** · **AC9 backward-compat sub** — all covered (deriveNext + CLI tests). AC1 task-level + AC7 per-turn pointer covered (resolver + injectState).
- Phase complete ⇔ every `NN-*-PLAN.md` has a matching `NN-*-SUMMARY.md` (artifact-derived). Stateless first-incomplete pointer. Additive; existing `harnessed next` sub semantics byte-identical (AC9).

## Phase Results

- **P37 deriveNext core (W1)** — `deriveNext.ts` (pure `NextUnit` resolver) + `planningScan.ts` (impure disk reader, graceful). 13 tests. ✅
- **P38 next/advance CLI (W2)** — `forwardStep.ts` (testable seam) + `harnessed next` exit-code fall-through + `harnessed advance` (print-only + advance-gate + `--json`). ✅
- **P39 per-turn pointer (W3)** — `NEXT-UNIT` breadcrumb in `injectState.ts` + `bin/harnessed-inject-state.mjs` (ts↔bin parity). D5 deferred. ✅
- **P40 docs + verify (W4)** — full gate green (vitest 1673/0 · tsc 0 · biome · i18n-parity · workflow-schema) + CHANGELOG. Site/localized docs ride post-release. ✅

## Verdict

**PASSED 9/9 + 4/4 + 1/1 + 2/2.** Shipped + published npm 4.10.0 (CI `28406331261` → publish `28406547619`). Lightweight-closed: ROADMAP + MILESTONES shipped-index rows; phase dirs 37–40 stay in `phases/`. Next: no active milestone.
