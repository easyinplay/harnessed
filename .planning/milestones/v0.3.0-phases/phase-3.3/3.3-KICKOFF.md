# Phase 3.3: aliases.yaml + deprecation marker + known-good 版本组合 — KICKOFF

**Phase**: 3.3 (v0.3.0 milestone 第 3 phase, 4 phase 中第 3)
**Started**: 2026-05-17
**Status**: discuss-phase 完成 → 待 plan-phase 启动

---

## 1. ROADMAP / REQUIREMENTS 锚定

**ROADMAP § v0.3.0 Phase 拆分 L163-165**:

> **Phase 3.3：aliases.yaml + deprecation marker + known-good 版本组合**
> - manifest 重定向；doctor warning；release 流程冻结 known-good lock
> - 验收：模拟上游改名场景 install 通过

**REQUIREMENTS R7.5 + R7.6** (`.planning/REQUIREMENTS.md` L281-291):

- **R7.5 manifest deprecation marker + aliases.yaml**: 上游改名时 `manifests/aliases.yaml` 重定向；deprecation_marker 字段在 doctor / install 阶段显式提示
  - 验收：模拟上游改名场景 install 通过 + 提示
- **R7.6 "known good" 版本组合**: 每个 harnessed 版本冻结一组通过 e2e 的上游版本 lock
  - 验收：`harnessed install --known-good` reproducible

---

## 2. Carry-forward from prior phases (locked, 不重新讨论)

| 来源 | 决策 | 本 phase 应用 |
|------|------|---------------|
| **Phase 2.4 D-01 doctor MIN 5-check + Phase 3.2 6th check** | doctor `--json` flag + 三档 status enum (pass/warn/fail) + helper sister-share | doctor deprecation warning 加 7th check (`checkDeprecations`); sister probe-gstack.ts helper pattern |
| **Phase 2.2 CD-5 schemaVersion single 兼容门** | `branchOnSchemaVersion<T>()` helper, 11 surfaces | aliases.v1 + known-good.v1 加 2 surface (12th + 13th) |
| **Phase 2.2 TypeBox + Value.Check pattern** | runtime validate | aliases.yaml + known-good.yaml schema 同 pattern |
| **Phase 1.2/2.1 manifests/ pattern** | `manifests/{tools,skill-packs}/<name>.yaml` | aliases.yaml 在 `manifests/aliases.yaml` 顶层 (sister manifest 平级) |
| **Karpathy 4 心法 always-on** | YAGNI / Surgical / Simplicity / Goal-Driven | RICH schema 但只加真需 fields; DOCTOR-ONLY-WARN; YAML lock 沿袭已有 yaml convention |

---

## 3. v0.3.0 milestone 在本 phase 的位置

v0.3.0 = plan-feature workflow + checkpoint (4 phase):
- Phase 3.1 ✅ SHIPPED (checkpoint 引擎 + harnessed resume + compact 协议)
- Phase 3.2 ✅ SHIPPED (gstack 前缀探测 + workflow 变量插值 + plan-feature reference 实装)
- **Phase 3.3** (本 phase): aliases.yaml + deprecation marker + known-good 版本组合 — **upstream rename + version lock 适配层**
- Phase 3.4: 路由命中率 ≥ 85% 验收 + token budget 监控

Phase 3.3 ship 后, v0.3.0 进度 13/17 = 76.5% (3/4 milestone progress)。

---

## 4. v0.3.0 W0 backlog (Phase 3.2 ship sister carry-forward — 必修)

从 STATE.md L23a + Phase 3.2 deferred-items.md 拉入, 一次 W0 根治 (沿袭 Phase 2.3+2.4+3.1+3.2 W0 4-phase cadence):

1. **W0.1 (Phase 3.2 sister H3 STATE dual-SSOT 5th recurrence 结构性根治)**: 删 `.planning/STATE.md` L4 frontmatter Status: 行 (5-recurrence pattern terminus: README L9 / README L44 / PROJECT-SPEC / STATE freshness scope / STATE 当前位置块). "当前位置"块 作 single SSOT. Freshness gate 扩 scope 到 "当前位置"块 pattern check (sister Phase 2.4 H2 gate 2nd 硬化 round 2 模式). Acceptance: `! grep -q "^> Status:" .planning/STATE.md` + freshness gate `check-transparency-verdicts.mjs` `STATE_POSITION_RE = /\*\*Phase [1-9]\.[0-9]+ SHIPPED\*\*/` 加 + sister 5th recurrence terminus 记录. ≤30L delta.
2. **W0.2 (Phase 3.2 deferred #2 dashboard-sse 4 cell flaky 一次根治)**: 选 3 fix path candidate 之 (a) random ephemeral port + retry helper (sister Node `net.createServer with port 0` pattern). 修 `tests/scripts/dashboard-sse.test.ts` 4 cell setup. Acceptance: `corepack pnpm test -- --run tests/scripts/dashboard-sse.test.ts 2>&1 | tail -3` shows 4/4 pass. ≤40L delta.
3. **W0.3 (Phase 3.3 own prereq)**: schemaVersion 12th + 13th surface decision doc — aliases.v1 (D-01 RICH) + known-good.v1 (D-03 YAML manifest). Sister Phase 3.2 W0.3 pattern. ~50L decision doc.

**Phase 3.1 deferred-items.md** 仅含 #1 cli-audit, 已 W0.1 b41a84a RESOLVED (M2 verify 已 mark).
**DEFERRED #2 userSpawn niche** 仍 defer Phase 3.4+ (real userSpawn usage statistics dogfooding 后定).

---

## 5. 不在本 phase

- **路由命中率 ≥ 85% 验收 + token budget 监控** → Phase 3.4
- **plan-feature 真接外部 gsd-* spawn** → Phase 3.3+ dogfood (Phase 3.2 D-03 WIRED stub mode, Phase 3.4 dogfood 时 transition)
- **EE-4 BLOCKER auto-spawn rerun** → Phase 3.4 后 evaluate
- **userSpawn session_id capture** → Phase 3.4+ (DEFERRED #2 Phase 3.1 carry)
- **install --known-good 真 e2e 上游版本 lock seed data** → Phase 3.4 dogfood 时填充 (本 phase MVP 仅 infra + seed empty/初版)

---

## 6. discuss-phase 4 gray area 决策 (详 CONTEXT.md)

| ID | 主题 | Locked |
|----|------|--------|
| D-01 | aliases.yaml schema 形态 | RICH (含 reason + since_version + deprecation_date + removal_date?) |
| D-02 | deprecation marker 可见性策略 | DOCTOR-ONLY-WARN (install 静默重定向) |
| D-03 | known-good 版本锁格式 | YAML manifest `versions/<harnessed-ver>-known-good.yaml` |
| D-04 | H3 STATE dual-SSOT 根治 fix path | (b) COLLAPSE (删 L4 frontmatter Status, "当前位置"块 single SSOT) |

---

*Phase 3.3 — aliases.yaml + deprecation marker + known-good 版本组合*
*Discuss-phase: 2026-05-17*
