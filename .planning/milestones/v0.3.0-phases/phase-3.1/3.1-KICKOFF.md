# Phase 3.1: checkpoint 引擎 + harnessed resume + compact 协议 — KICKOFF

**Phase**: 3.1 (v0.3.0 milestone 第 1 phase, 4 phase 中第 1)
**Started**: 2026-05-16
**Status**: discuss-phase 完成 → 待 plan-phase 启动

---

## 1. ROADMAP / REQUIREMENTS 锚定

**ROADMAP § v0.3.0 Phase 拆分 L156-158** (`.planning/ROADMAP.md`):

> **Phase 3.1：checkpoint 引擎 + harnessed resume + compact 协议**
> - 摘要 < 1k token；archive 完整；`current-workflow.json` 状态机
> - 验收：人为中断 session 后从 03 phase 续跑成功

**REQUIREMENTS R7.2 + R7.3** (`.planning/REQUIREMENTS.md` L263-273):

- **R7.2 checkpoint 分层（摘要 vs archive）**: `.harnessed/checkpoints/`（摘要 < 1k token，进 context）+ `.harnessed/archive/`（完整原始产出，不进 context，仅供回溯）
  - 验收：单 checkpoint < 1k token；后续 phase 只读 checkpoint 不读 archive
- **R7.3 `harnessed resume`**: session 中断从 `.harnessed/current-workflow.json` 找到当前 phase，重读 checkpoint 续跑
  - 验收：人为中断 session 后 resume 不丢上下文

**Canonical refs**:
- `.planning/ROADMAP.md` L156-158 (Phase 拆分)
- `.planning/REQUIREMENTS.md` L255-303 (R7 整段，含 R7.1-R7.6 plan-feature workflow + checkpoint cluster)
- `docs/PROJECT-SPEC.md` § 12 (checkpoint mech 立项参数)
- `docs/WORKFLOWS-MVP.md` § "跨 workflow 共享设计 4" (resume + reload 设计)

---

## 2. Carry-forward from prior phases (locked, 不重新讨论)

| 来源 | 决策 | 本 phase 应用 |
|------|------|---------------|
| **Phase 2.2 CD-5 ⭐⭐⭐** | schemaVersion 7-surface 单一兼容门 (`harnessed.routing-snapshot/handoff-doc/phases-yaml/manifest-state/installer-state/route-decision-log/checkpoint.v1`) | `checkpoint.v1` schema 是 7 surface 之一, 直接复用. 新 checkpoint write 走 `branchOnSchemaVersion<T>()` helper |
| **Phase 2.2 T4.4 closure infra (ready, 未 wire)** | `sdkSpawn.onSessionId` / `ralphLoopWrap.resumeSessionId` / `engine.wrappedSpawn capturedSessionId` 三件套 closure infra | 本 phase D-04 决策 WIRE-IN: session_id 写 checkpoint, resume 时 SDK redirect 续 session |
| **Phase 2.4 D-04 dashboard SSE watcher** | localhost-only bind + `text/event-stream` HTTP + browser-native EventSource reconnect | checkpoint write 触发可触发 dashboard SSE push (Wave 0 backlog 评估) |
| **Karpathy 4 心法 always-on** | YAGNI / Surgical / Simplicity / Goal-Driven | 状态机 3 state 不开 FSM (D-02 锁); 摘要 zero LLM call (D-01 锁); resume 不偷袭用户 (D-03 锁) |

---

## 3. v0.3.0 milestone 在本 phase 的位置

v0.3.0 = plan-feature workflow + checkpoint (3-4 周，4 phase):
- **Phase 3.1** (本 phase): checkpoint 引擎 + `harnessed resume` + compact 协议 — **infra 底座**
- Phase 3.2: gstack 前缀探测 + workflow 变量插值 + plan-feature reference 实装
- Phase 3.3: aliases.yaml + deprecation marker + known-good 版本组合
- Phase 3.4: 路由命中率 ≥ 85% 验收 + token budget 监控

Phase 3.1 ship 后, Phase 3.2-3.4 都依赖 checkpoint infra (workflow 暂停时存 checkpoint + resume 续跑).

---

## 4. v0.3.0 W0 prereq backlog (Phase 2.4 ship sister M+L+T defer)

从 STATE.md L23a 拉入, 一次 W0 根治 (沿袭 Phase 2.4 W0 5 项根治 pattern):

1. **M1**: audit "Line budget deviations accepted" 透明节加 — `v0.2.0-MILESTONE-AUDIT.md` 加 1 节登记 deferred-items #1 (`run-plan-checker.mjs` 130L vs spec ≤100L +30% biome blocking ACCEPTED) + #2 (`plan-checker-quant.test.ts` 103L vs spec ≤80L +29% ACCEPTED). Karpathy 透明性纪律.
2. **M2+M3**: dashboard polish round 2 commit attribution 复盘 — handoff intel L482 草案"独立 commit"未采纳 (executor 合理 absorb 进 W3 cf00d17/008f9ab). RETRO 加 1 节登记 advisory 允许 absorb 路径 (避免 future "advisory rejected" 暗示).
3. **T4**: README completeness check — counter gate 现 consistency-only (SHIPPED==BARS==L44 三计数一致). 加 completeness check: STATE.md SHIPPED count vs README SHIPPED count, 不等 fail. Phase 2.4 ship sister T4 advisory.
4. **T5**: phase-2.4/task_plan.md self-check fr ≥ 0.80 end-state verify — `node scripts/run-plan-checker.mjs .planning/phase-2.4/` real-run pass 验证 deferred-items #2 self-referential 解决.

**T1+T3 不再 defer**: 已被 commit `800e459` inline 修 (H1 5+1 README stale + H2 freshness gate 2 sub-bug root-cause 修).

---

## 5. 不在本 phase

- **plan-feature workflow 跑通** → Phase 3.2 (依赖本 phase checkpoint infra)
- **gstack 前缀探测** → Phase 3.2 (workflow 变量插值用)
- **aliases.yaml + deprecation marker + known-good** → Phase 3.3
- **路由命中率 ≥ 85% 验收 + token budget 监控** → Phase 3.4
- **EE-4 BLOCKER auto-spawn rerun** → Phase 3.4 之后 evaluate (Phase 2.4 down-scope DEFERRED)
- **EE-2 ECC 9-field manifest schema** → v0.3+ 评估
- **V-1 动态 model routing** → v0.3+ discuss
- **V-2 Manual fallback bundle export** → v0.4+

---

## 6. discuss-phase 4 gray area 决策 (详 CONTEXT.md)

| ID | 主题 | Locked |
|----|------|--------|
| D-01 | checkpoint 摘要生成策略 | TEMPLATE (机械 extract 固定 fields, zero LLM call) |
| D-02 | `current-workflow.json` 状态机 | KARPATHY 极简 3 state (active / paused / complete) |
| D-03 | `harnessed resume` 行为 | RELOAD (reload checkpoint 进 context, 用户手动续跑) |
| D-04 | T4.4 Task Session closure infra wiring | WIRE-IN (session_id 写 checkpoint, resume SDK redirect 续 session) |

---

*Phase 3.1 — checkpoint 引擎 + harnessed resume + compact 协议*
*Discuss-phase: 2026-05-16*
