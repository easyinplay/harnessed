---
phase: 5.2
plan: W0
subsystem: planning/cli-extract
tags: [d2-cadence, state-trim, size-limit, validateFlags, runOrPreview, wave0]
dependency_graph:
  requires: [phase-5.1]
  provides: [validateFlags-extract, runOrPreview-extract, state-trim-iter6, size-limit-150]
  affects: [src/cli/install.ts, src/cli/install-base.ts, src/cli/research.ts, src/cli/manifest-add.ts, src/cli/execute-task.ts]
tech_stack:
  added: [src/cli/lib/validateFlags.ts, src/uninstallers/lib/runOrPreview.ts]
  patterns: [extract-refactor, d2-archive-cadence, size-limit-flip]
key_files:
  created:
    - src/cli/lib/validateFlags.ts
    - src/uninstallers/lib/runOrPreview.ts
  modified:
    - .planning/STATE.md
    - .planning/RETROSPECTIVE.md
    - scripts/check-state-archive-stale.mjs
    - src/cli/install.ts
    - src/cli/install-base.ts
    - src/cli/research.ts
    - src/cli/manifest-add.ts
    - src/cli/execute-task.ts
    - tests/scripts/check-state-archive-stale.test.ts
decisions:
  - D2 cadence iter 6 REINFORCE — implicit-standing-process graduation (no explicit iter count Phase 5.3+)
  - "#BA SIZE_LIMIT FLIP 165→150 (post-trim STATE 150L ≤ 150L, FLIP PATH active)"
  - "#BH validateFlags extract: uninstall.ts --yes/--apply variant stays inline (YAGNI 1-caller)"
  - "#BI runOrPreview in src/uninstallers/lib/ NOT src/installers/lib/ (uninstaller-only scope)"
metrics:
  duration: "~45 minutes"
  completed: "2026-05-19"
  tasks_completed: 4
  tasks_total: 4
  files_modified: 9
  files_created: 2
---

# Phase 5.2 Wave 0 Summary

**One-liner**: D2 cadence iter-6 STATE trim (164L→150L) + SIZE_LIMIT FLIP 165→150 + #BH validateFlags.ts extract (5-file H1 gate dedup) + #BI runOrPreview.ts extract (uninstaller dry-run gate); 733 tests PASS no regression.

## Tasks Completed

| Task | Name | Commit | Result |
|------|------|--------|--------|
| T0.1 | STATE.md D2 iter 6 trim + RETROSPECTIVE archive | 838a457 | STATE 164L→150L; RETRO +47L Phase 5.1 archive section |
| T0.2 | STATE size verify + #BA SIZE_LIMIT round 3 FLIP | ab508bc | SIZE_LIMIT 165→150 FLIP; both CI gates PASS |
| T0.3 | #BH validateFlags.ts NEW + #BI runOrPreview.ts NEW | e1cefd0 | 2 NEW files; 5 import sites updated; 733 tests PASS |
| T0.4 | Wave 0 baseline gate verify | eb11333 | All 4 gates GREEN; Wave 1 ready |

## Key Outputs

### T0.1 — STATE.md trim + RETROSPECTIVE archive

- **STATE.md**: 164L → 150L (-14L net). Trims: condense 当前位置 D-decision list, 待办 P0 tag merge, remove resolved #BA/#BB DEFERRED entries, condense milestone table rows, merge P2 items, condense 未决问题.
- **RETROSPECTIVE.md**: Appended `§ ARCHIVED FROM STATE — Phase 5.1` (~47L) with Phase 5.1 ship decisions, W0 backlog resolutions, test counts, recovery instructions.
- **HTML archive marker**: `<!-- Phase 5.1 narrative archived ... iter 6 -->` added to STATE.md.
- **D2 cadence**: iter 6 REINFORCE = formally institutionalized as implicit-standing-process. Phase 5.3+ executes without explicit iter annotation.

### T0.2 — SIZE_LIMIT FLIP

- **Decision tree verdict**: Post-trim STATE 150L ≤ 150L → FLIP PATH active.
- **scripts/check-state-archive-stale.mjs**: `SIZE_LIMIT = 165 → 150` (15L safety headroom cadence pattern; sister Phase 4.3 W0.2 200→175延袭 tighter as confidence grows).
- **tests/scripts/check-state-archive-stale.test.ts**: Updated Rule 1 fixture assertion `250L > 165L → 250L > 150L` (Rule 1 auto-fix).
- **2 CI gates PASS**: transparency-verdicts + state-archive-stale.

### T0.3 — #BH validateFlags.ts + #BI runOrPreview.ts

**#BH validateFlags.ts** (`src/cli/lib/validateFlags.ts`, 23L):
- Extracts `validateNonInteractiveFlags(raw, cmdName)` from 5 CLI files.
- Import sites: install.ts + install-base.ts + research.ts + manifest-add.ts + execute-task.ts.
- Each site: inline 4-6L block → single 1L call. Net: -15L across 5 files + 23L new file.
- uninstall.ts (Wave 1 T1.1) keeps its `--yes`/`--apply` variant inline per YAGNI (1 caller, different flag name).

**#BI runOrPreview.ts** (`src/uninstallers/lib/runOrPreview.ts`, 26L):
- Extracts `dryRunGate(ctx)` for 7 uninstaller files (Wave 1 T1.2 reuse).
- Creates `src/uninstallers/` + `src/uninstallers/lib/` directories (Wave 1 scaffold).
- Minimal `DryRunCtx` interface inline; `src/uninstallers/lib/types.ts` (T1.1) will formalize.
- NOT used by installer files — installer files unchanged Phase 5.2 per scope boundary.

### T0.4 — Gate Verification

| Gate | Result |
|------|--------|
| `biome check .` | PASS — 220 files, no fixes needed |
| `tsc --noEmit` | PASS — 0 type errors |
| `vitest run` | PASS — 733 tests, 4 skipped, 0 failures |
| `check-transparency-verdicts.mjs` | PASS |
| `check-state-archive-stale.mjs` | PASS (SIZE_LIMIT=150, STATE=150L) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Test fixture assertion mismatch after SIZE_LIMIT flip**
- **Found during:** T0.4 gate verify (tests run after T0.2 FLIP)
- **Issue:** `tests/scripts/check-state-archive-stale.test.ts` expected `250L > 165L` but SIZE_LIMIT changed to 150
- **Fix:** Updated test assertion to `250L > 150L` + describe block title to reflect round 3 FLIP
- **Files modified:** `tests/scripts/check-state-archive-stale.test.ts`
- **Commit:** e1cefd0 (absorbed into T0.3 commit)

**2. [Rule 1 - Bug] STATE.md split-line count 151 vs wc -l 150**
- **Found during:** T0.2 — initial SIZE_LIMIT=150 gate failed (script uses `split(/\r?\n/)` counting trailing newline)
- **Fix:** Merged last 2 lines of 框架治理路由 section (Tavily/Exa + ctx7 → single line)
- **Files modified:** `.planning/STATE.md`
- **Commit:** ab508bc

## Known Stubs

None — Wave 0 is pure extract refactor + docs. `src/uninstallers/lib/runOrPreview.ts` uses minimal inline type stubs (DryRunCtx, AbortResult) that will be replaced by `src/uninstallers/lib/types.ts` in Wave 1 T1.1. This is intentional scaffolding, not a functional stub.

## Self-Check: PASSED

- `src/cli/lib/validateFlags.ts` exists: VERIFIED (23L)
- `src/uninstallers/lib/runOrPreview.ts` exists: VERIFIED (26L)
- `.planning/RETROSPECTIVE.md` § ARCHIVED FROM STATE — Phase 5.1: VERIFIED
- `scripts/check-state-archive-stale.mjs` SIZE_LIMIT=150: VERIFIED
- Commits 838a457, ab508bc, e1cefd0, eb11333: VERIFIED (git log confirmed)
- 733 tests PASS: VERIFIED
- Both CI gates PASS: VERIFIED
