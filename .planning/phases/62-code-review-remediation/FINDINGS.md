---
phase: 62
name: Code Review Report Remediation
status: complete
created: 2026-09-16
completed: 2026-09-16
source: external review report (Qwen workspace, 2026-09-16), 39 findings — 7 high / 16 medium / 16 low
method: every finding verified against this tree before being called real; a fix
  counts only with a test that fails on the pre-fix code
---

# Phase 62 — Code Review Report Remediation

Verdicts: **REAL** (reproduced / confirmed by reading) · **PARTIAL** (real, but the
report's framing or fix is wrong in a way that matters) · **NOT REPRODUCED** ·
**NOT A DEFECT** · **NOT CHANGED** (real, but the fix is a product decision).

## Outcome

- All 39 findings + the report's 2 comment/implementation notes (C1, C2) have a verdict.
- Fixed with a falsifying test: every REAL / PARTIAL item except the three below.
- **Maintainer decisions (2026-09-16)**: L11 → delete the unreachable hook (done);
  L16 and M6 residual → keep as is (L16 can only force more review; `harnessed run`
  is the execution path that needs `Bash`).
- **Not a defect**: C2 second half (`git_ref` is required for cc-plugin-marketplace).
- **Found beyond the report**: the `.platform` pin was written where
  `detectPlatform()` never reads it (fixed with L1); `codex plugin list` lists
  not-installed plugins (fixed with M15); `initialPrompt` dropped the role prompt
  (fixed with L14).
- **Verified on the host (maintainer approved a real install, then removed it)**:
  `manifests/tools/superpowers.yaml` codex override was BROKEN on codex-cli 0.154.0 —
  `codex plugin add superpowers@openai-curated` fails ("plugin `superpowers` was not
  found in marketplace `openai-curated`"); `@openai-api-curated` installs and writes
  `[plugins."superpowers@openai-api-curated"]`. The M15 probe returned true while
  installed, false after `codex plugin remove`. Manifest cmd + both shell checks fixed
  (checks now match the STATUS column); guarded by a test on the shipped manifest.
- Commits: 97e62a7 … 1d80dc9 (batches 7–25 + CI fixes), CI green on 3 OS at 1d80dc9.
  Not released (4.41.0 is still `latest`).

## High

| # | finding | verdict | fix | falsified |
|---|---|---|---|---|
| H1 | `checkpoint reopen` writes a stale snapshot back, undoing itself | **REAL** — on exactly the `status==='complete'` path reopen exists for | `mutateWorkflow` single-lock RMW; reopen + status flip in one write | yes: old code under a faithful whole-replace mock → `Expected "pending" / Received "done"` |
| H2 | Windows `npm.cmd` spawned shell-less → EINVAL → `update` silently broken | **REAL** — reproduced on Node 24.19.0 (`spawn EINVAL`; with shell → `11.17.0`) | Windows path runs the fixed string `npm view harnessed version` through `exec` (no injection surface, no DEP0190) | dogfood: doctor update check now reports `up to date (4.41.0)` |
| H3 | eval `--filter` pre-filter has an empty body → `--update-golden` rewrites every golden | **PARTIAL** — bug real; the report's fix (bare `continue` on dir name) would drop scenarios matched only by NAME, which lives inside the yaml | read the scenario name before running; skip only when neither dir nor name matches | yes: old runner overwrites a sentinel golden in a scenario outside the filter |
| H4 | rollback `unlink`s a directory for git-clone installs | **REAL** — clone target is a dir; ENOENT and EISDIR both recorded `backup:''`; rollback single-file `unlink` → EPERM/EISDIR → exit 1. The report's "deletes user data" does NOT happen today (unlink cannot remove a dir) — but a naive `rm -rf` fix would make it happen | backup records `sentinel: 'created' \| 'preexisting-dir'`; rollback removes `created` recursively, LEAVES `preexisting-dir`, and never deletes a directory from legacy metadata on a guess | yes: 3 cells fail on the old code, with `unlink` made to reject on a directory the way the real one does |
| H5 | `ccHookAdd` crashes on `settings.json` containing `null` | **REAL** — also a number, string, array (all valid JSON) | non-object top level → `settings-json-malformed` structured error, file untouched | yes: old code `TypeError: Cannot read properties of null (reading 'hooks')` |
| H6 | Windows `cmd.exe /c` arg re-parsing; `& \| < > ^` not screened | **REAL, and worse than reported** — besides `&` injection, the SHIPPED args `tavily-mcp@^0.2.0` / `exa-mcp-server@^3.2.0` already lose their `^` (cmd's escape char): a semver range silently became an exact pin on every Windows install. So the report's "add metachar detection to checkCmdString" is wrong twice: `&&` is legitimate in manifest cmd strings on every platform, and failing closed on `^` would break tavily/exa | new `lib/winSpawn.ts`: resolve PATH+PATHEXT; `.exe/.com` spawned DIRECTLY with no shell (native Claude Code / Codex installers ship these); only a `.cmd/.bat` shim goes through `cmd.exe /d /s /c` with cross-spawn's quote + double caret-escape (the shim's `%*` re-parses a second time) | yes, on a REAL cmd.exe + `%*` shim: caret, `&` URL, pipes, `%PATH%`, spaces, quotes arrive byte-identical; an injection stays one literal arg and writes no marker file; a control cell proves the old shape mangles the caret |
| H7 | security gate skips `hook_command` and `harness_overrides.*.cmd` | **REAL** | 3 paths added to `cmdPaths`; `ccHookAdd` re-screens `hook_command` at runtime (defense in depth) | yes: 5 malicious cases pass the old gate |

## Medium

| # | finding | verdict | notes |
|---|---|---|---|
| M1 | unlocked read → locked whole write, lost updates | **REAL, fixed** | every site now a single-lock RMW via `mutateWorkflow`: `pause()`, `complete()`, `checkpoint complete`'s verify_mode write (metrics still computed outside the lock — they shell out to git — but the write touches only verify_mode), the ship_ready/retro_due merge, and `compactWorkflow` (cheap unlocked pre-check keeps its documented no-write contract; the compaction itself is recomputed inside the lock). **Not individually falsified**: a deterministic race needs an injection hook between read and write; the lock semantics are covered by H1's falsified test |
| M8 | `gc` keeps the lexicographically-last bin-backup | **REAL** — `'4.10.0' < '4.9.0'` as strings, so the NEWER backup was deleted; the existing fixture only used 0.8.0/0.9.0 where the two orders agree | fixed: `byVersion` via `compareVersions` (string fallback for non-semver names); falsified |
| M16 | `findPhaseContextExcerpt` substring-matches phase numbers | **REAL, and wider than reported** — phase "16" hit dir "1-"; ALSO the reverse: phase "1" never matched zero-padded dir "01-" (`"1".includes("01")` is false), so the standard GSD layout with a short phase string injected nothing | fixed: first numeric token of the phase string compared numerically to the dir number; both directions falsified |
| M9 | `prompt <sub>` joins without a traversal guard | **REAL** — `harnessed run` has had the guard since R10.4 | `checkSafeSegment(sub)`, exit 2; falsified |
| M10 | `rollback <timestamp>` joins without a guard | **REAL, and the existing guard would NOT have caught it** — `checkPathSafe` blocks `../` and `..\` but not a bare `..`, which is all a single segment needs (`rollback ..` → parent of the backup root → a foreign metadata.json whose `files[].target` rollback then writes/unlinks) | new `checkSafeSegment` (non-empty, not `.`/`..`, no separators, plus every checkPathSafe vector); falsified |
| M11 | `manifest-add --category/--name` joins into a written path | **REAL** | `checkSafeSegment` on both, before any prompt or write; falsified |
| M4 | `runWorkflow` lacks the documented phases-or-delegates fail-fast | **REAL, plus a second shape** — also a yaml carrying `delegates_to` under a NON-master name falls through to the phases path and "completes" with its delegates silently ignored | assert after the master check: no/empty phases → throw, naming which case; falsified (3 cells) |
| M7 | fallback handlers call `process.exit()` in library code | **REAL** — reachable in production: `workflows/task/deliver` declares `fallback.max_iterations_exceeded`; under the master's `Promise.allSettled` fan-out one sub's halt killed every running sibling and skipped checkpoint flushes | handlers print the same UX text then throw `WorkflowHaltError(exitCode)`; `harnessed run` / `research` map it to the yaml exit code; the master's parallel aggregate keeps the highest halt code instead of degrading to exit 1. Falsified (3 cells: handler no longer exits, sibling runs to completion + code preserved, CLI exits with the code) |
| M2 | parallel subs race on the global checkpoint state | **REAL (record half); checkpoint-file half LATENT** — `activate()` replaces the ONE `current-workflow.json` per phase, so the 8 parallel verify subs clobbered each other's record, and whichever sub finished first flipped the whole chain `complete` while siblings ran. Per-phase checkpoint file collision needs two parallel subs sharing a phase id; the shipped verify subs' ids are all distinct today | `RunWorkflowOpts.subOf`: `defaultSpawnDriver` marks every spawned run as a sub; subs never activate or complete the global record (they still write their own checkpoint envelopes); a top-level master activates once under its own name and completes after the fan-out. Falsified (sub cell + driver cell; nested-master cell is a regression guard) |
| M3 | every phase completion flips the workflow complete; failure writes nothing | **REAL, both halves** | `transitionWorkflowComplete` only on the last phase of a record-owning run; a failed dispatch writes a `FAILED: phase <id>: …` record (the `checkpoint fail` convention) with no transition. Falsified (M3a, M3b) |
| M5 | `sdkSpawn` has no timeout | **REAL** — `for await` over the SDK stream is the only exit; ralph-loop bounds iterations, not time | `AbortController` on the query plus a race against a wall-clock timer (a stream that never yields again cannot hang past it): `SpawnTimeoutError`, which the dispatch catch turns into the existing fail-soft `{status:'fail'}`. Default 1 h, `HARNESSED_SPAWN_TIMEOUT_MS` overrides, `0` disables. Falsified |
| M6 | `permissionMode` never reaches the SDK | **REAL as a defect class, not exploitable today** — the prompt runs as the query's MAIN thread, but `tools` / `disallowedTools` / `maxTurns` / `permissionMode` were only registered on the `agents` entry or rendered as prompt text, so any declared limit was decorative. In the shipped path `buildAgentDef` declares none of them, so nothing is being ignored right now | declared limits now land on the query options; `bypassPermissions` is never forwarded (it would also need `allowDangerouslySkipPermissions`). Falsified. **Not changed**: a def that declares no tools still gets the default set incl. `Bash` + `Task` — `harnessed run` is the headless execution path (task-code writes and tests code); narrowing that is a product decision, not a bug fix |
| M12 | `git clone` dest parser skips 2 tokens per unknown flag | **REAL, latent for shipped manifests** — the shipped clone cmds use only `--depth` / `--branch`, which were special-cased; any value-less flag (`--quiet`, `--single-branch`, …) in a user or future manifest slid the walk so the URL was read as `<dest>`, rev-parse ran in a non-existent dir and the SHA pin degraded to a warning. Two copies of the walk existed (installer + idempotency probe) | one shared `parseGitCloneDest` classifying git's value / bare flags; an UNKNOWN flag now returns null, which the installer already reports as a clear preflight error (no guessing). Falsified through the installer's exported `isSelfCleaningCloneCmd` |
| M13 | timeout kill leaves orphan process trees | **REAL — reproduced on Windows 11**: `spawn('cmd.exe', ['/c', 'node', f])` then `child.kill('SIGKILL')` left the node grandchild running; `taskkill /T /F` removed it. POSIX has the same shape for any compound cmd (`/bin/sh -c 'a && b'` forks) | `killProcessTree` at all three timeout sites (installer spawn, harness CLI args, git rev-parse): Windows `taskkill /PID <pid> /T /F` (falls back to `kill`), POSIX snapshot descendants via `pgrep -P`, kill the root first (so a shell cannot start the next command of a chain), then the snapshot. Falsified with a real shell + grandchild through `spawnCmd`. **Not done**: resolving only after `close` — once the tree is dead the pipes close anyway, and waiting on `close` would reintroduce a hang if a kill ever fails |
| M14 | verify fs errors escape; `runInstall` has no catch-all | **REAL, narrower than reported** — `setup`'s `runOne` already wraps `runInstall` in try/catch, so setup does not crash; but `install`, `install-base` (one throw aborted the rest of its batch), the L4 rescue and the optional offer had no catch | catch-all in `runInstall` → structured `{ok:false, keyword:'installer-exception'}` carrying the errno code; installers keep re-throwing deliberately (no silent swallowing), every caller now gets a per-component failure. Falsified |
| M15 | codex cc-plugin idempotency probe always fails | **REAL, and wider** — (a) `isPluginRegistered` knew only Claude Code's JSON registries; codex keeps installed plugins as `[plugins."<p>@<m>"]` tables in `~/.codex/config.toml` (host-verified), so the probe was always false and every setup re-ran `codex plugin add`; (b) the codex VERIFY matched `codex plugin list` stdout by substring, but that list also prints plugins that are NOT installed (host-verified: `superpowers@openai-api-curated  not installed`), so a failed install passed verify; the manifests' shell `idempotent_check` (a text search over `codex plugin list`) has the same false positive. The probe regex also only knew `plugin install` (harmless today only because both codex plugin names equal `metadata.name`) | `isPluginInToml` + codex branch in `isPluginRegistered`; codex verify uses it instead of the stdout match; probe regex accepts `plugin add`. Falsified (verify false-positive cell, registry cells). **Observation, not changed**: `manifests/tools/superpowers.yaml` codex override adds `superpowers@openai-curated`, while this host's `codex plugin list` shows the marketplace as `openai-api-curated` — unverified whether codex aliases the name; needs a real `codex plugin add` to confirm |

## Low

| # | finding | verdict | notes |
|---|---|---|---|
| L2 | `spawnSync('npm')` shell-less on Windows always ENOENT | **PARTIAL** — NOT reproduced here (fnm provides an extensionless shim, status=0); real on a standard Node install that ships only `npm.cmd` | hardened with the same fixed-string-through-shell approach as H2 |
| L6 | reopen's follow-up read sees the rolled-back state | **REAL** — downstream of H1 | fixed by H1; cell 8 of the reopen suite fails on the old code |
| L7 | hook bins hard-code `~/.claude/harnessed` | **REAL** — the CLI resolves the root through `detectPlatform()` (env, `.platform` pin, auto-probe); under a codex pin the CLI wrote `~/.codex/harnessed` and both hooks read the claude root, so injection went silently empty (the in-code comment calling the pin "orthogonal" was wrong) | dependency-free `hookStateRoot()` replicating detectPlatform's precedence for `stateRoot`, shared by both hook mains; bins rebuilt. Falsified against the OLD shipped bin; 9 parity cells pin it to `detectPlatform(home).stateRoot` |
| L4 | `audit-log --filter` has no EPIPE handling | **REAL** — reproduced as vitest's `Unhandled Error: write EPIPE` | stdin `error` listener: EPIPE (jq exited early on a bad filter) is expected and absorbed so jq's own message + exit code stand; any other stdin error rejects. Falsified |
| L8 | STALE ledger judged by the shared store's mtime | **REAL** — `workflows.json` holds every repo's slot, so any repo's write refreshed it | per-slot `updated_at` stamped at the single slot-write choke point (`writeCurrentWorkflowUnlocked`); the hook ages the ledger from it, falling back to the file mtime for pre-stamp records; eval goldens strip it like the other timestamps. Falsified end to end against the OLD bin; bin rebuilt |
| L11 | before-commit hook is a no-op | **REAL, and stronger: unreachable** — `triggers_commit` was never assigned on any production dispatch path (only the type and the test stub); even when reached, `changedFiles: []` made every rule inert, and biome auto-fix / STATE.md read targeted the harnessed package root, not the user repo | **deleted on the maintainer's decision** (same treatment as the 4.34.0 after-output hook): call site, `triggers_commit`, `before-commit.ts` and its test. Commit-time rules still reach the model via the discipline block in `harnessed prompt`; STATE.md length via `harnessed check-docs`. Follow-on: the discipline yaml field `auto_fix_cmd` (operational biome, output-style strip/replace) now has no evaluator — the discipline schema is outside `check-schema-consumers.mjs` coverage, so the gate does not flag it |
| L14 | `initialPrompt` injected twice | **REAL, and wider (latent)** — with `initialPrompt` set, the base prompt WAS `initialPrompt`: the role prompt was dropped entirely and `initialPrompt` appeared twice. No shipped caller sets it | base is always `def.prompt`; `initialPrompt` is appended once as its own section. Falsified |
| L15 | eval runner: overrides before try; file step path escape | **REAL, both** — the pre-fix falsification run actually wrote the escaped file into the OS temp dir | env / chdir / dynamic import moved inside the try (finally always restores); file step path must stay inside the temp repo, else ERROR. Falsified (escape cell) |
| L16 | `user_overrides` substring match forces gates | **REAL as imprecision, not changed** — deliberate "用户明示 → 覆盖判据" mechanism; the bypass can only force a gate to FIRE (more review), never skip one, so a quoted keyword costs extra work, not safety. Tightening the grammar is a product decision |
| C1 | `confirm.ts` header vs code on non-interactive dry-run | **REAL contradiction; the CODE is right** — dry-run yields `proceed:false` → `aborted: user-cancel` (exit 2), which the install integration tests depend on; the comment claimed `proceed:true` | comment corrected; no behaviour change |
| C2 | `ccHookAdd` idempotent path: no receipt; `ccPluginMarketplace` records `git_ref` as version | **first half REAL** — Phase 59 missed this installer, and it also returned the plain success shape, so setup reported "installed" on every re-run; **second half NOT a defect** — `git_ref` is REQUIRED for `cc-plugin-marketplace` (SHA / SemVer pattern) and is the pin authority | idempotent path records the receipt and returns `alreadyInstalled: true`. Falsified |
| L12 | atomic write leaks `.tmp` on persistent rename failure; sync retry has no backoff | **REAL** | unique temp removed (best-effort) before the error propagates; sync variant gets the async variant's linear backoff via `Atomics.wait`. Falsified (directory blocking the target, sync + async) |
| L13 | stop directives ignore terminal status | **REAL** — all three are documented as "stop respawning" conditions, yet a sub that failed 3x and then passed / was skipped / rejected was told to stop every turn | `isSettled()`; the per-turn injector emits the three directives only for subs that can still be respawned. `detectLoop` itself stays a status-blind history detector (compaction G6 test relies on it; the CLI calls it right after fail / reopen, when status is failed / pending). Falsified (3 settled statuses); bin rebuilt |
| L9 | aliases / known-good located via `process.cwd()` | **REAL, zero current impact** — shipped `aliases.yaml` is an empty table; `versions/` is not in package.json `files` and holds only a 0.3.0 pin, so `--known-good` is unreachable in the published package regardless | both resolve lazily via `getAssetsRoot()` (same as install.ts resolves target manifests); 8 cwd-dependent test files moved to `HARNESSED_ASSETS_OVERRIDE`. Falsified |
| L10 | install_type closure table lacks `hook` | **REAL** — an unmapped install_type is treated as unconstrained, so `install_type: hook` accepted any method | `hook: ['cc-hook-add']`; all shipped manifests still validate. Falsified |
| L1 | `setup --dry-run` persists the `.platform` pin | **REAL, plus an unreported defect underneath** — setup wrote the pin to the CHOSEN platform's stateRoot (`~/.codex/harnessed/.platform`) while `detectPlatform()` reads it only at the claude stateRoot; each side had a test pinning its own half. On any machine that also has `~/.claude`, a codex pin never took effect | dry-run sets the env for the run only; the pin is always written to `claudeDescriptor().stateRoot`. Falsified (write-path cell, dry-run cell) |
| L3 | `research` lacks the nested-session guard | **REAL** — adding it immediately tripped the existing research tests inside this CC session | guard extracted to `cli/lib/nestedHarness.ts`, shared by `run` and `research`. Falsified |
| L5 | `--max-iterations` not validated | **REAL** — `abc` → NaN, and NaN / 0 were dropped by the `raw.maxIterations ?` spread | `parseMaxIterations`: integer in [1, 100] else commander `InvalidArgumentError`. Falsified (7 invalid values) |

## Why the H1 bug survived its own test suite

`tests/cli/checkpoint-reopen.test.ts` mocked `writeCurrentWorkflow` as a **no-op**.
The real function replaces the record wholesale, so the rollback needed that write
to overwrite the ledger the other mock had just changed — which a no-op can never
do. The mock was kinder than the thing it stood in for, and hid exactly the defect
it existed to catch. The suite now models one shared record with a real
whole-replace.
