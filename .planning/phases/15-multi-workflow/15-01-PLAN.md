---
phase: 15-multi-workflow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/schemaVersion.ts
  - src/checkpoint/workflowStore.ts
  - src/checkpoint/state.ts
  - src/cli/workflows.ts
  - src/cli.ts
  - tests/checkpoint/workflowStore.test.ts
  - tests/checkpoint/state-store.test.ts
autonomous: true
requirements:
  - "v7.0 Phase 15 acceptance (15-CONTEXT.md D1–D8) — no numeric REQUIREMENTS.md IDs mapped"
user_setup: []

must_haves:
  truths:
    - "readCurrentWorkflow/writeCurrentWorkflow/mutateSubProgress keep their signatures; the 17 call sites are NOT modified"
    - "state is keyed by repo-root (nearest ancestor with .git, else normalized cwd) via a pure fs walk-up — no git subprocess"
    - "a workflow under repoKey A and one under repoKey B coexist; reading from A's cwd never returns or clobbers B's"
    - "a legacy current-workflow.json migrates verbatim into workflows.json under repoKey(cwd) on first write; legacy file retained; migration is idempotent"
    - "mutateSubProgress remains a single-lock read-modify-write over the resolved slot (no lost-update, no double-lock)"
    - "every write dual-writes the active repo's envelope back to legacy current-workflow.json (rollback anchor, D7)"
    - "CurrentWorkflowV1 envelope schema is unchanged; full test gate green; biome clean"
  artifacts:
    - path: "src/checkpoint/workflowStore.ts"
      provides: "WorkflowStoreV1 schema + repoKey(cwd) + readStoreRaw/writeStoreRaw lock-free kernels + listWorkflows"
      contains: "repoKey"
    - path: "src/checkpoint/state.ts"
      provides: "readCurrentWorkflow/writeCurrentWorkflow/mutateSubProgress rewired to the per-repo store slot + dual-write"
      contains: "workflowStore"
    - path: "tests/checkpoint/workflowStore.test.ts"
      provides: "per-repo isolation + migration + repoKey TDD coverage"
      contains: "isolation"
  key_links:
    - from: "readCurrentWorkflow()/writeCurrentWorkflow()/mutateSubProgress()"
      to: "workflowStore.ts repoKey-resolved slot"
      via: "behind-API multi-store (signatures unchanged)"
      pattern: "workflowStore"
---

<objective>
把全局 singleton `current-workflow.json` 换成 per-repo multi-workflow store（arch A，behind-API），让并行项目不再互相 clobber checkpoint 状态 —— 不动 17 个 call site、零丢旧数据、保锁语义。key = repo-root（纯 fs walk-up 找 `.git`，无 git 子进程），active 隐式按 cwd 解析（`cd` 即切）。store = 单文件 `workflows.json`。compat-read 迁移旧 singleton + dual-write 一 release rollback 窗口。

约束（15-CONTEXT.md D1–D8）：arch A 签名不变；key=repo-root 非 cwd+phase；无显式 active 指针 / 无 `resume --switch`；envelope schema 不变；单文件 store；迁移零丢失 + 留旧文件；mutateSubProgress 仍单锁 RMW；TDD red-first。修掉 rogue F4（store 即唯一 SoT 非旁挂）+ F5（单写路径无 stale index）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/15-multi-workflow/15-CONTEXT.md
@src/checkpoint/state.ts
@src/checkpoint/schema/currentWorkflow.v1.ts
@src/installers/lib/harnessedRoot.ts
@src/types/schemaVersion.ts
@src/checkpoint/atomicWrite.ts

# 参考（已知缺陷 F4/F5，禁止盲抄）
@.planning/phases/_rogue-impl-reference/phase15-workflowstore-063b5a9.patch
</context>

<key_facts>
planner 已验证（直接用）：

- `state.ts`：`statePath() = harnessedFile('current-workflow.json')`（唯一文件名 literal）。`readCurrentWorkflow()` = read statePath → `branchOnSchemaVersion` → `CurrentWorkflowV1` check。`writeCurrentWorkflowUnlocked(s)` = validate + `mkdir` + `writeFileAtomic`（lock-FREE 内核）。`writeCurrentWorkflow(s) = withLock(unlocked)`。`mutateSubProgress(fn)` = **单 `withLock`** read→fn→writeUnlocked（pre-v4 C5 lost-update fix；禁止 double-lock，proper-lockfile 非 reentrant）。
- `harnessedRoot.ts`：`harnessedFile(name)` → `~/.claude/harnessed/<name>`。store 用 `harnessedFile('workflows.json')`。
- `atomicWrite.ts`：`writeFileAtomic(path, data)` 已有，store 复用。
- envelope `CurrentWorkflowV1` 形状不变（store 的 value 直接复用它）。`SCHEMA_VERSIONS.currentWorkflow = 'harnessed.current-workflow.v1'`。
- 17 consumers 全走那 3 个函数；改内部即可，签名不动。
- 测试 setup 模式：`HARNESSED_ROOT_OVERRIDE` + tmpdir（sister `tests/checkpoint/ledger-state.test.ts`）。基线 = post-Phase-14 全绿（1242）。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1: schemaVersion + WorkflowStoreV1 schema + repoKey(cwd) 纯函数</name>
  <files>src/types/schemaVersion.ts, src/checkpoint/workflowStore.ts, tests/checkpoint/workflowStore.test.ts</files>
  <red>
失败测试：
1. `repoKey(dir)`：给一个含 `.git` 子目录的临时 dir 树，从其深层子目录调用 → 返回该 `.git` 所在的 root（walk-up 命中）；无 `.git` → 返回 normalize(cwd)。纯函数，无 subprocess（断言不 spawn）。
2. `Value.Check(WorkflowStoreV1, {...})`：`{ schemaVersion:'harnessed.workflow-store.v1', workflows: { '/repo/a': <envelope> } }` → true；缺 `workflows` → false。
当前无 `workflowStore.ts` → red。
  </red>
  <green>
- `schemaVersion.ts`：加 `workflowStore: 'harnessed.workflow-store.v1'`。
- `workflowStore.ts`（NEW）：
  - `WorkflowStoreV1 = Type.Object({ schemaVersion: Type.Literal(SCHEMA_VERSIONS.workflowStore), workflows: Type.Record(Type.String(), CurrentWorkflowV1) }, { additionalProperties:false })` + `WorkflowStoreV1Type`。
  - `repoKey(cwd: string = process.cwd()): string` —— 从 cwd 向上 `existsSync(join(dir,'.git'))` 逐级走，命中返回该 dir；到根未命中返回 `resolve(cwd)`。纯 fs，无 `child_process`。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/workflowStore.test.ts -t "repoKey|schema" 2>&1 | tail -15</automated>
  </verify>
  <done>`repoKey` walk-up + fallback 正确、纯无 subprocess；`WorkflowStoreV1` schema check 通过；schemaVersion 常量加好。</done>
</task>

<task type="tdd">
  <name>Task 2: readStoreRaw / writeStoreRaw lock-free 内核 + 迁移 fallback + listWorkflows</name>
  <files>src/checkpoint/workflowStore.ts, tests/checkpoint/workflowStore.test.ts</files>
  <red>
失败测试（HARNESSED_ROOT_OVERRIDE + tmpdir）：
1. `readStoreRaw()`：`workflows.json` 存在 → parse 返回；损坏/缺失 → 返回 empty store `{schemaVersion, workflows:{}}`（no-throw）。
2. **迁移 fallback（in-memory，D5）**：无 `workflows.json` 但有 legacy `current-workflow.json` → `readStoreRaw()` 返回的 store 在 `workflows[repoKey(cwd)]` 处含 legacy envelope（逐字段相等）；**且此时不写文件**（断言 `workflows.json` 仍不存在 —— 读无副作用）。
3. `writeStoreRaw(store)`：原子写 `workflows.json`；回读相等。
4. idempotent：写出 `workflows.json` 后，即便 legacy 还在，`readStoreRaw()` 只认 `workflows.json`（不再 fold legacy）。
5. `listWorkflows()`：返回每个 slot 的 `{ key, phase, status }`。
  </red>
  <green>
`workflowStore.ts` 续写（全部 lock-FREE，sister `writeCurrentWorkflowUnlocked`）：
- `storePath() = harnessedFile('workflows.json')`；`legacyPath() = harnessedFile('current-workflow.json')`。
- `readStoreRaw(): WorkflowStoreV1Type` —— 读 storePath：存在且 `Value.Check` → 返回；JSON/校验失败 → empty。**不存在**时：读 legacyPath，若是合法 `CurrentWorkflowV1` → 返回 in-memory `{schemaVersion, workflows:{[repoKey()]: legacy}}`（不写）；否则 empty store。
- `writeStoreRaw(store)`：`Value.Check` → `mkdir` → `writeFileAtomic(storePath, json)`。
- `listWorkflows()`：`Object.entries(readStoreRaw().workflows).map(([key,wf]) => ({key, phase:wf.phase, status:wf.status}))`。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/workflowStore.test.ts 2>&1 | tail -18</automated>
  </verify>
  <done>store 读写 + 迁移 in-memory fallback（读无副作用）+ idempotent + listWorkflows 全 green。</done>
</task>

<task type="tdd">
  <name>Task 3 (CORE): state.ts 三函数改走 store slot + per-repo 隔离 + dual-write</name>
  <files>src/checkpoint/state.ts, tests/checkpoint/state-store.test.ts</files>
  <red>
失败测试（HARNESSED_ROOT_OVERRIDE + 两个 tmp repo dir，各含 `.git`，用 `process.chdir` 或给函数注入 cwd 切换）：
1. **per-repo 隔离（核心反 clobber）**：在 repoA 下 `writeCurrentWorkflow(envA)`、repoB 下 `writeCurrentWorkflow(envB)` → repoA 下 `readCurrentWorkflow()` 得 envA、repoB 下得 envB；互不覆盖；`workflows.json` 同时含两 key。
2. **null 语义**：当前 repo 无 slot → `readCurrentWorkflow()` 返回 `null`（和今天一致）。
3. **迁移持久化**：只有 legacy `current-workflow.json` 的 fixture → 首次 `readCurrentWorkflow()`（当前 repo）得 legacy envelope；首次 `writeCurrentWorkflow` 后 `workflows.json` 出现且 `workflows[repoKey]` = 最新；legacy 文件仍在。
4. **mutateSubProgress 单锁 RMW**：seed 后并发两次 mutate 不 lost-update（sister 现有 C5 测试）；slot 未 seed 时 no-op。
5. **dual-write（D7）**：`writeCurrentWorkflow(env)` 后 legacy `current-workflow.json` 内容 = 当前 repo 的 env（rollback anchor）。
  </red>
  <green>
改 `state.ts` 内部（**签名全不变**，import `workflowStore.js`）：
- `readCurrentWorkflow()` = `const store = readStoreRaw(); return store.workflows[repoKey()] ?? null`（保留 envelope `Value.Check` 防御）。
- `writeCurrentWorkflowUnlocked(s)`（lock-free 内核）：validate envelope → `const store = readStoreRaw(); store.workflows[repoKey()] = s; writeStoreRaw(store)` → **dual-write**：`writeFileAtomic(legacyPath(), JSON.stringify(s,null,2))`（D7 anchor）。
- `writeCurrentWorkflow(s) = withLock(() => writeCurrentWorkflowUnlocked(s))`（不变）。
- `mutateSubProgress(fn)`：**单 `withLock`** 内：`readStoreRaw()` → `slot = store.workflows[repoKey()]` → 若无 slot 则 no-op return（sister 今天"无 active record"）→ `slot.sub_progress = fn(slot.sub_progress ?? [])` → `store.workflows[repoKey()]=slot` → `writeStoreRaw(store)` + dual-write legacy。**保持单锁**（禁止在锁内再调 withLock 的 read/write —— 直接用 lock-free 的 readStoreRaw/writeStoreRaw/writeFileAtomic）。
- `statePath()` 保留给 legacy dual-write 用（`legacyPath`）。`activate`/`pause`/`complete` 等 transition 不改（它们调 read/write，自动走 store）。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/state-store.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>per-repo 隔离（反 clobber）、null 语义、迁移持久化、mutateSubProgress 单锁 RMW 不 lost-update、dual-write anchor 全 green；3 函数签名未变。</done>
</task>

<task type="tdd">
  <name>Task 4: harnessed workflows CLI + 注册</name>
  <files>src/cli/workflows.ts, src/cli.ts, tests/checkpoint/workflowStore.test.ts</files>
  <red>
失败测试：`registerWorkflows` 注册 `workflows` 子命令；action 调 `listWorkflows()` 打印每行 `key phase [status]`（空时打印 "no active workflows"）。
  </red>
  <green>
- `src/cli/workflows.ts`（NEW）：`registerWorkflows(program)` → `program.command('workflows').description('List all in-flight workflows (one per repo)').action(async()=>{ const { listWorkflows } = await import('../checkpoint/workflowStore.js'); const ws = await listWorkflows(); if(!ws.length){console.log('[harnessed] no active workflows'); return} for(const w of ws) console.log(`${w.key}  ${w.phase} [${w.status}]`) })`。
- `src/cli.ts`：`import { registerWorkflows }` + `registerWorkflows(program)`（23rd，注释 Phase 15）。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/checkpoint/workflowStore.test.ts -t "workflows CLI|listWorkflows" 2>&1 | tail -12</automated>
  </verify>
  <done>`harnessed workflows` 列出所有 slot（key/phase/status），空时友好提示；注册为 23rd 子命令。</done>
</task>

<task type="auto">
  <name>Task 5: verify gate — 全 17 consumer green + biome + diff scope</name>
  <files>（无新改动 — 验证 + SUMMARY 证据）</files>
  <action>
跑完整验收 gate，证据进 SUMMARY：
1. `corepack pnpm exec biome check --write src/ tests/` → 再 `biome check` 确认 clean。
2. `corepack pnpm exec tsc --noEmit` exit 0。
3. `corepack pnpm test` 全套 green，0 fail —— **重点确认现有 checkpoint/resume/next/status/inject/compact 测试全过**（证明 17 consumer behind-API 无回归）。
4. **反 clobber 复核**：Task 3 的 per-repo 隔离测试 + 迁移持久化测试在全套里 green。
5. `git diff --stat` 仅 7 目标文件（5 src + 2 test）+ 新 `workflowStore.ts`/`workflows.ts`；无意外（尤其没碰 17 consumer 文件）。
任一 fail → 不输出完成,回对应 task。
  </action>
  <verify>
    <automated>cd /d/GitCode/harnessed && corepack pnpm exec biome check src/ tests/ 2>&1 | tail -4 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3</automated>
  </verify>
  <done>biome clean；tsc 0；全测试 green（0 fail，17 consumer 无回归）；反 clobber + 迁移测试 green；diff 仅目标文件（17 consumer 未触碰）。证据进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-15-01 | Tampering / Info Disclosure (data loss) | 迁移旧 singleton 丢数据 | mitigate | in-memory fallback 逐字段保真 + 首写持久化 + **不删 legacy** + dual-write anchor；Task 3 迁移持久化测试守门 |
| T-15-02 | Tampering (lost-update) | store 单文件写并发覆盖 | mitigate | mutateSubProgress 保持单 `withLock` RMW（lock-free 内核读写 store，不 double-lock）；sister C5 并发测试 |
| T-15-03 | Tampering (cross-repo clobber 回归) | repoKey 解析错把两 repo 归一 key | mitigate | repoKey walk-up 单测 + per-repo 隔离测试；key=repo-root 非 phase |
| T-15-04 | DoS / footgun (rollback 丢进度) | breaking on-disk 变更回退丢迁移后数据 | mitigate | D7 dual-write 一 release 窗口：reverted readers 仍从 legacy 读到当前数据 |
| T-15-05 | Tampering | 误改 17 consumer / envelope schema | mitigate | 签名不变（behind-API）；Task 5 `git diff --stat` 强制仅目标文件 + 全 consumer 测试 green |
</threat_model>

<verification>
phase 级验收（= 15-CONTEXT.md Acceptance）：
1. `repoKey` walk-up + fallback，纯无 subprocess。
2. per-repo 隔离：A/B slot 共存，按 cwd 解析，互不 clobber。
3. 迁移：legacy-only fixture → 读得 verbatim、首写持久化 workflows.json、legacy 保留、idempotent。
4. mutateSubProgress 单锁 RMW 不 lost-update、未 seed no-op。
5. dual-write：legacy current-workflow.json = 当前 repo envelope（anchor）。
6. `harnessed workflows` 列全部 slot；17 consumer 全绿（签名未变）。
7. envelope schema 不变；全 gate green；biome clean；diff 仅目标文件。
</verification>

<success_criteria>
- 全局 singleton → per-repo store（arch A behind-API），17 call site 零改动，反 clobber 成立。
- key=repo-root 纯 fs walk-up；active 隐式按 cwd；无 stale-index（修 rogue F4/F5）。
- 迁移零丢失 + 留 legacy + dual-write rollback 窗口。
- mutateSubProgress 锁语义保留（单锁 RMW，无 lost-update / double-lock）。
- TDD red→green；全 gate green；KARPATHY-minimal surgical diff。
</success_criteria>

<output>
完成后建 `.planning/phases/15-multi-workflow/15-01-SUMMARY.md`：收录 RED→GREEN、per-repo 隔离测试输出、迁移持久化 + idempotent 证据、mutateSubProgress 并发不 lost-update、dual-write anchor 验证、17 consumer 全绿证据、biome clean、tsc 0、`git diff --stat`。
</output>
