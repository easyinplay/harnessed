---
phase: 27-config-resolvers
plan: 01
type: tdd
wave: 1
depends_on: ["26-platform-descriptor-seam"]
files_modified:
  - src/installers/lib/platform.ts
  - src/cli/lib/settingsWriter.ts
  - src/cli/lib/enableAgentTeamsInSettings.ts
  - src/cli/lib/enableUserLangInSettings.ts
  - src/installers/ccHookAdd.ts
  - src/installers/lib/readClaudeConfig.ts
  - src/cli/setup.ts
  - src/cli/lib/capabilityResolver.ts
  - src/installers/lib/idempotent.ts
  - tests/installers/platform.test.ts
  - tests/cli/settingsWriter.test.ts
autonomous: true
requirements:
  - "v9.0 Phase B acceptance (27-CONTEXT.md Acceptance 1-4) + architecture §3 resolver layer + §7 D-C"
user_setup: []

verified_refs:
  - "platform.ts: claudeDescriptor()/detectPlatform() (Phase 26, exist; extend with optional home — D2)"
  - "enableAgentTeamsInSettings.ts settingsPath L26/backupOriginal L83/atomicWrite L98/createFreshSettings L71 + env key CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS (Read-confirmed)"
  - "enableUserLangInSettings.ts near-identical L35/L129/L144/L114 + key HARNESSED_USER_LANG + detectUserLang (KEEP) (Read-confirmed)"
  - "ccHookAdd.ts:46 inline settingsPath; Installer pipeline writes hooks (NOT folded — swap path only) (Read-confirmed)"
  - "readClaudeConfig.ts getUserClaudeJsonPath L35 / isPluginRegistered plugins L112 + legacy settings L129 + claude.json L130 (Read-confirmed)"
  - "setup.ts:120 skillsBase / :194 commandsBase (Read-confirmed)"
  - "capabilityResolver.ts:84 readInstalledPlugins(homedirOverride?) / :119 readInstalledUserSkills(homedirOverride?) — preserve override param via D2 (Read-confirmed)"
  - "idempotent.ts:96 indicator + :144 npm-cli single .claude (swap); :120-128 ['.claude','.agents'] dual-probe (LEAVE) (Read-confirmed)"
  - "src/cli/lib/settingsWriter.ts (NEW)"

must_haves:
  truths:
    - "platform.ts: claudeDescriptor(home=homedir()) + detectPlatform(home=homedir()) optional base; 5 resolvers getSettingsPath/getSkillsDir/getCommandsDir/getPluginsRegistry/getMcpConfigPath(home?) → detectPlatform(home).<field>"
    - "Phase 26 no-arg callers (getHarnessedRoot + platform.test) unchanged green"
    - "settingsWriter.mergeSettingsEnvKey reproduces 3-case (create/idempotent/backup+merge) + atomicWrite tmpPath+rename + backup to harnessedSubdir('backups')"
    - "enableAgentTeams/enableUserLang keep public signatures + result-type unions + idempotency/lang policy (thin callers)"
    - "ccHookAdd settingsPath→getSettingsPath only; hooks pipeline untouched"
    - "readClaudeConfig/setup/capabilityResolver swap to resolvers; capabilityResolver homedirOverride preserved via D2"
    - "idempotent :96/:144 swap; :120-128 dual-probe LEFT AS-IS"
    - "full existing suite green UNCHANGED (regression proof); tsc 0; biome clean"
  artifacts:
    - path: "src/installers/lib/platform.ts"
      provides: "5 config resolvers + optional home base (D1/D2)"
      contains: "getSettingsPath"
    - path: "src/cli/lib/settingsWriter.ts"
      provides: "shared env-key merge writer (D3 fold)"
      contains: "mergeSettingsEnvKey"
  key_links:
    - from: "~7 scattered ~/.claude config call sites"
      to: "detectPlatform().<field> via resolvers"
      via: "platform.ts resolvers (D1) + settingsWriter fold (D3); zero behavior change"
      pattern: "getSettingsPath"
---

<objective>
中心化 ~7 个散落的 `~/.claude/`-config call site 经 descriptor-backed resolver,并把两个近乎重复的 settings env-key writer fold 进一个共享 writer(D-C)。纯 refactor,claude default 输出与今日完全一致。Tier-2 of v9.0;auto-probe + `.agents/` 是 Phase 28。

约束(27-CONTEXT.md D1-D4):resolver 在 platform.ts(D1);descriptor 加可选 home 基保留 capabilityResolver test param(D2);settings fold 仅 2 个 env-key writer,ccHookAdd 不 fold 只 swap path(D3);idempotent dual-probe 留着(D4)。零行为变化 = 全现有套件不变绿。TDD red-first。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/27-config-resolvers/27-CONTEXT.md
@.planning/v9.0-cross-harness-ARCHITECTURE.md
@src/installers/lib/platform.ts
@src/cli/lib/enableAgentTeamsInSettings.ts
@src/cli/lib/enableUserLangInSettings.ts
@src/installers/lib/readClaudeConfig.ts
</context>

<key_facts>
verified (直接用,勿重新假设):
- platform.ts (Phase 26): PlatformDescriptor 8 字段已定义; claudeDescriptor()/detectPlatform() 当前无参。D2 扩展加 `home = homedir()` 默认参 (additive)。
- 2 个 settings writer 结构近同: read→parse→3-case(create fresh / idempotent / backup+merge)→atomicWrite(tmpPath+rename); backup→harnessedSubdir('backups')/settings.json.{ISO-ts}.bak。差异仅 env key + (lang writer 多 detectUserLang/already-set 策略)。
- ccHookAdd 是 Installer (preflight/confirmAt L3/backup()/renderDiff/R7 verify),写 settings.hooks 嵌套,直接 writeFile。**不 fold**,仅 settingsPath(L46)→getSettingsPath()。
- capabilityResolver readInstalledPlugins(homedirOverride?)/readInstalledUserSkills(homedirOverride?) 有 test 注入参 → D2 让 resolver 收 home? 透传。
- idempotent :120-128 是 ['.claude','.agents'] 双探测,**不动**;仅 :96(indicator)+:144(npm-cli)单 .claude swap。
- ESM (.js import 后缀)。基线: Phase 26 后 1357 tests green。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1 (Wave 1): platform.ts 5 resolvers + optional home (D1/D2) + 机械 swap 站点</name>
  <files>src/installers/lib/platform.ts, src/cli/setup.ts, src/cli/lib/capabilityResolver.ts, src/installers/lib/readClaudeConfig.ts, src/installers/ccHookAdd.ts, tests/installers/platform.test.ts</files>
  <red>
追加 `tests/installers/platform.test.ts`:
1. `claudeDescriptor('/home/x')` → homeDir==='/home/x/.claude'; 所有字段以 '/home/x' 为基; `claudeDescriptor()` 默认仍 homedir()。
2. `detectPlatform('/home/x').skillsDir` === join('/home/x','.claude','skills'); 无参 detectPlatform() 与 Phase 26 一致。
3. `getSettingsPath()` === detectPlatform().settingsPath; `getSkillsDir()`/`getCommandsDir()`/`getPluginsRegistry()`/`getMcpConfigPath()` 各 === 对应 field; 带 home 参透传 (e.g. `getSkillsDir('/home/x')`)。
4. `HARNESSED_ROOT_OVERRIDE` 只影响 stateRoot,不影响 getSkillsDir/getSettingsPath (orthogonal)。
  </red>
  <green>
- `platform.ts`:
  - `claudeDescriptor(home: string = homedir())` — 用 `home` 替换内部 `homedir()`;`mcpConfigPath = join(home, '.claude.json')` (注意: 现在基于 home 参,sibling 仍正确)。
  - `detectPlatform(home: string = homedir())` — 调 `claudeDescriptor(home)`;HARNESSED_ROOT_OVERRIDE 仍只覆盖 stateRoot。
  - 加 `export function getSettingsPath(home?: string) { return detectPlatform(home).settingsPath }`,同样 5 个 (skills/commands/pluginsRegistry/mcpConfig)。
- `setup.ts:120` → `const skillsBase = getSkillsDir()`;`:194` → `const commandsBase = getCommandsDir()` (import from platform.js;删除 unused homedir/resolve 若无其它用)。
- `capabilityResolver.ts:84` → `const path = getPluginsRegistry(homedirOverride)`;`:119` → `const skillsRoot = getSkillsDir(homedirOverride)` (透传 override 经 D2)。
- `readClaudeConfig.ts`: `getUserClaudeJsonPath()` body → `return getMcpConfigPath()` (保留导出名,内部委托;或直接让调用方用 getMcpConfigPath — 保 export 兼容 test);`isPluginRegistered` L112 → `getPluginsRegistry()`;L129 → `getSettingsPath()`;L130 → `getMcpConfigPath()`。
- `ccHookAdd.ts:46` → `const settingsPath = getSettingsPath()` (hooks pipeline 其余不动)。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/installers/platform.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>5 resolver + optional home; 机械 swap (setup/capabilityResolver/readClaudeConfig/ccHookAdd) 经 resolver; capabilityResolver override 保留; idempotent dual-probe 未动 (本 task 不碰 idempotent — 见 Task 2 的单 .claude swap, 或并入此 task)。</done>
</task>

<task type="tdd">
  <name>Task 2: settingsWriter fold (D3) + idempotent 单 .claude swap</name>
  <files>src/cli/lib/settingsWriter.ts, src/cli/lib/enableAgentTeamsInSettings.ts, src/cli/lib/enableUserLangInSettings.ts, src/installers/lib/idempotent.ts, tests/cli/settingsWriter.test.ts</files>
  <red>
`tests/cli/settingsWriter.test.ts` (NEW, HARNESSED_ROOT_OVERRIDE + tmp home 隔离):
1. `mergeSettingsEnvKey('K','v')` file 缺 → 创建 `{env:{K:'v'}}` (status created)。
2. 已有 `env.K==='v'` → idempotent no-op (status already / 调用方策略)。
3. 已有 file 缺 K → backup 原文件到 harnessedSubdir('backups') + merge K,atomicWrite。
4. enableAgentTeamsInSettings() 行为不变 (现有测试绿);enableUserLangInSettings() 行为不变 (含 already-set/override 策略)。
  </red>
  <green>
- NEW `src/cli/lib/settingsWriter.ts`: 抽出共享 `mergeSettingsEnvKey(key, value, opts?: { skipIfPresent?: (existing) => boolean })` — settingsPath via `getSettingsPath()`; backupOriginal→harnessedSubdir('backups'); atomicWrite(tmpPath+rename); createFresh; 返回判别 union (created/already/enabled/warn) 供调用方映射。保留 3-case + atomic + backup 语义 byte-for-byte。
- `enableAgentTeamsInSettings.ts`: 改为调 `mergeSettingsEnvKey('CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS','1', { skipIfPresent: v => v === '1' })`;保留 export 名 + EnableResult union 映射 (already-enabled/created/enabled/warn)。删本地 settingsPath/backupOriginal/atomicWrite/createFreshSettings。
- `enableUserLangInSettings.ts`: 保留 detectUserLang/safeIntlLocale + already-set(override===undefined) 策略;写经 mergeSettingsEnvKey('HARNESSED_USER_LANG', detected, { skipIfPresent: ... });保留 EnableUserLangResult union 映射。
- `idempotent.ts:96` → `getSkillsDir()` 拼 indicator;`:144` → `getSkillsDir()` 拼 name。**:120-128 ['.claude','.agents'] dual-probe 不动**。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/cli/settingsWriter.test.ts "tests/cli/enable*" 2>&1 | tail -12</automated>
  </verify>
  <done>settingsWriter fold; 2 writer 瘦身为调用方,public API + union 不变; ccHookAdd 非 fold; idempotent 单 .claude swap, dual-probe 留。</done>
</task>

<task type="auto">
  <name>Task 3: regression gate — 全测试不变绿 + biome + tsc + 零行为变化</name>
  <files>（验证 + SUMMARY）</files>
  <action>
1. `corepack pnpm exec biome check --write src/ tests/` → clean。
2. `corepack pnpm exec tsc --noEmit` → 0。
3. `corepack pnpm test` 全套 green 0 fail。**回归证明**: 现有 settings/installer/capabilityResolver/setup/idempotent 测试全部不变绿 (零行为变化)。新增 settingsWriter + platform resolver 测试通过。
4. **零行为变化确认**: grep 确认无 call site 残留硬编码 `join(homedir(), '.claude'` (除 idempotent dual-probe :120-128 + platform.ts claudeDescriptor 内部基) — `grep -rn "homedir(), '\.claude'" src/ | grep -v platform.ts` 应只剩 dual-probe。
5. `git diff --stat` = 计划内文件 (不含 README.md)。
  </action>
  <verify>
    <automated>corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3</automated>
  </verify>
  <done>biome clean; tsc 0; 全测试 green (现有不变 + 新增); 无残留硬编码 (除 dual-probe); diff 计划内。证据进 SUMMARY。</done>
</task>

</tasks>

<verification>
phase 级验收 (= 27-CONTEXT.md Acceptance):
1. 5 resolver + optional home (D1/D2); Phase 26 no-arg 调用不变。
2. settingsWriter 3-case fold; 2 writer public API/union/policy 不变。
3. ~7 swap 经 resolver; idempotent dual-probe + ccHookAdd pipeline 未动。
4. 全现有套件不变绿 (回归证明); tsc 0; biome clean。
</verification>

<success_criteria>
- 散落 ~/.claude config 访问中心化经 descriptor resolver; 2 个 env-key writer DRY fold (D-C)。
- 零行为变化 (claude default = byte-identical); HARNESSED_ROOT_OVERRIDE orthogonal。
- ccHookAdd 非 fold (Installer pipeline 保留); idempotent dual-probe 保留 (Phase 28 reconcile)。
- KARPATHY-minimal; TDD red→green; 全 gate green = 接缝透明回归证明。
</success_criteria>

<output>
完成后建 `.planning/phases/27-config-resolvers/27-01-SUMMARY.md`: 5 resolver + optional home (D2) + settingsWriter fold (2 writer before/after 行数) + ccHookAdd/readClaudeConfig/setup/capabilityResolver/idempotent swap 清单 + 无残留硬编码 grep 证据 (除 dual-probe) + 全现有套件不变绿 (回归证明计数) + biome clean + tsc 0 + git diff --stat + Phase 28 衔接指针。
</output>
