---
phase: 25-quickstart
plan: 01
type: docs
wave: 1
depends_on: ["24-resume-entry"]
files_modified:
  - README.md
  - README-cn.md
autonomous: true
requirements:
  - "v8.0 Phase 25 acceptance (25-CONTEXT.md D1–D5 + Acceptance 1–6)"
user_setup: []

verified_refs:
  - "README.md H1 + 副标题 L5–6 (exists — 主句改对象)"
  - "README.md TL;DR L18–24 'Best-practice orchestration for Harness Engineering' (exists — 重组对象)"
  - "README.md Quick Install L59–65 + Quick Start 3 Options L79–115 (exists — quickstart 整合点, 3-Options 保留)"
  - "README.md Operational Commands CLI table L402–426 (exists — 加 bare harnessed 行; sister harnessed next L408 / resume L414)"
  - "zero-arg harnessed = Phase 24 shipped c988bc8, src/cli/lib/here.ts (exists — quickstart demo 主角)"
  - "README-cn.md 一句话定位段 L16–18 '做 Claude Code 上 Harness Engineering...' (exists — ZH 主句改对象); wedge 副标题 L5–6 保留"
  - "README-cn.md 正文整体 stale: L106 4-stage / L153 25-workflow / 缺 Ship ⑤ (Read-confirmed — NOT touched, D4; → v10.0)"

must_haves:
  truths:
    - "README.md H1 下方含结果导向主句 (D1); package-manager wedge 仍在副标题 (两者共存, grep 确认)"
    - "README.md 含一条 First 5 Minutes linear quickstart: install → 第一个 workflow → 裸 harnessed you-are-here → resume; step 3 显式零参数 harnessed (Phase 24)"
    - "3-Options (Auto/Stage/Surgical) 分层保留, 不删"
    - "Operational Commands CLI 表含一行 bare harnessed (zero-arg you-are-here + NEXT)"
    - "README-cn.md 一句话定位段同步 D1 结果导向主句中文版 (保留命令/术语英文); wedge 副标题保留"
    - "ZH 仅改主句段 — README-cn quickstart/CLI/4-stage/25-workflow 整体 drift 不动 (→ v10.0)"
    - "无 broken link / 锚点; mermaid / badge 不破坏 (EN + ZH)"
    - "git diff 仅 README.md + README-cn.md 两文件; 其余 7 个翻译版未动"
  artifacts:
    - path: "README.md"
      provides: "结果导向定位主句 + First 5 Minutes linear quickstart + bare harnessed CLI 行"
      contains: "Turn raw Claude Code into a disciplined senior engineering team"
    - path: "README-cn.md"
      provides: "一句话定位段同步 D1 结果导向主句中文版 (仅主句; wedge 副标题保留)"
      contains: "纪律严明"
  key_links:
    - from: "first-time reader (README.md top)"
      to: "install → first workflow → zero-arg harnessed (Phase 24 you-are-here)"
      via: "结果导向主句 (D1) + First 5 Minutes linear quickstart (D2/D3)"
      pattern: "First 5 Minutes"
---

<objective>
把 README.md 门面改得 instantly-graspable (对标 comet): ① H1 下方放结果导向定位主句 (用户读完一句知道「它给我什么」), package-manager wedge 降为副标题副线保留; ② Quick Install 之后加一条「First 5 Minutes」最短线性路径 (install → 跑第一个 workflow → 裸 `harnessed` 看 you-are-here + NEXT → resume), 把 Phase 24 的零参数 `harnessed` 作为 quickstart 的 spine; ③ Operational Commands CLI 表加一行 bare `harnessed`; ④ `README-cn.md` 一句话定位段同步同一结果导向主句中文版 (仅主句). 现有 3-Options / mermaid / FAQ / badge / 9-语言链接全保留. EN 全套 + `README-cn.md` 仅主句; ZH quickstart/整体 drift → v10.0.

约束 (25-CONTEXT.md D1–D5): 结果导向主句 (D1, verbatim 或微调); linear quickstart demo zero-arg harnessed (D2/D3); EN 全套 + ZH 仅主句 (D4 — README-cn 正文落后一个 milestone, 整体刷新留 v10.0); TDD 跳过 — docs-only, zero-arg 行为是 Phase 24 已测 (D5); KARPATHY-minimal 重组不重写, ZH 只动主句段.
</objective>

<context>
@.planning/phases/25-quickstart/25-CONTEXT.md
@README.md
@README-cn.md
@.planning/phases/24-resume-entry/24-01-SUMMARY.md
</context>

<key_facts>
- 定位主句 EN (D1 锁定, AskUserQuestion): "**Turn raw Claude Code into a disciplined senior engineering team.** One install wires governance, planning, TDD, and review into a single Discuss→Ship workflow where progress and evidence persist on disk, not in chat."
- 定位主句 ZH (D4, 保留命令/术语英文): "**把原始的 Claude Code 变成一支纪律严明的资深工程团队。** 一次安装,就把治理、规划、TDD、审查织进一条 Discuss→Ship 工作流 —— 进度和证据落在磁盘上,不消散在对话里。" (措辞可微调, 关键词 `纪律严明` 供 grep)
- 副标题副线保留 (wedge): EN "AI coding harness package manager + composition orchestrator"; ZH "AI coding harness 包管理器 + composition orchestrator".
- 现有 EN TL;DR (L18–24) `Best-practice orchestration for Harness Engineering on Claude Code` → 重组为「它怎么做到」展开 (operating loop / 三层栈机器化), 不再当第一印象主句. (ZH 一句话定位段 L16–18 直接换成结果导向主句.)
- zero-arg `harnessed` (Phase 24): 裸 `harnessed` = you-are-here dashboard (复用 buildRecoverLines) + `NEXT: auto|manual|done` + run hint; `--json` 机器读; 无 active → onboarding. 只读无 spawn.
- 现有 EN 数字: 27 workflows / 5-stage (`/auto` + 5 master + 19 sub + 2 standalone). quickstart 用 current EN 数字.
- README-cn 整体 stale (L106 4-stage / L153 25-workflow / 缺 Ship ⑤) — Phase 25 ZH **仅改主句段**, 正文 drift NOT touch (D4); 整体刷新 v10.0.
- 现有 Quick Install one-liner: `npm install -g harnessed && harnessed setup` (+ PowerShell 5.x `;` note).
</key_facts>

<tasks>

<task type="docs">
  <name>Task 1 (Wave 1): README.md 主句 + quickstart + bare 行 + README-cn.md ZH 主句</name>
  <files>README.md, README-cn.md</files>
  <action>
1. **EN 定位主句 (D1)**: `README.md` H1 (`# harnessed`) + 语言链接 + 副标题 block 之后, 在 badge/NOTICE 附近上方插结果导向主句 (D1 verbatim, blockquote 风格). 副标题两行 (package-manager wedge + Machine-executes) 保留作副线.
2. **EN TL;DR 重组 (D1)**: `## ✨ TL;DR` 段 (`Best-practice orchestration for Harness Engineering...`) 改为承接主句的「它怎么做到」展开 — 不再重复定位, 转述 composition/no-vendoring 机制 + operating loop 引子. 现有 operating loop mermaid 保留.
3. **EN First 5 Minutes (D2/D3)**: `## 📦 Quick Install` 之后、`## 🚀 Quick Start — 3 Options` 之前, 新增 `### ⏱️ First 5 Minutes` linear 路径:
   - step 1 install (复用 one-liner)
   - step 2 跑第一个 workflow: `/auto "your first requirement"` (一行说明 newcomer 默认)
   - step 3 **裸 `harnessed`** → you-are-here dashboard + `NEXT:` (一句话点明: 不用记 status/next/resume, 一个命令看「我在哪 + 下一步」, comet `/comet` analog)
   - step 4 resume: 裸 `harnessed` / `harnessed resume` 随时续
   - 收尾一句: 想更细控制 → 下方 3-Options.
   3-Options section 标题/内容保留不动 (改为「想分层控制」的进阶).
4. **EN bare harnessed CLI 行 (D3)**: Operational Commands CLI table (`### CLI Commands`) 在 `harnessed next` 行附近加一行 bare `harnessed` (no args): Zero-arg you-are-here = active workflow dashboard + `NEXT: auto|manual|done` + run hint; `--json` machine-readable; no active → onboarding (comet `/comet` analog, read-only).
5. **ZH 主句 (D4)**: `README-cn.md` 一句话定位段 (`## ✨ 一句话定位` `做 Claude Code 上 Harness Engineering 的最佳编排实践...`) 同步为 D1 结果导向主句中文版 (key_facts ZH 措辞; 保留命令/术语英文); wedge 副标题 (`AI coding harness 包管理器...`) 保留. **ZH 仅改主句段** — quickstart/CLI/4-stage/25-workflow/缺-Ship 整体 drift NOT touched (→ v10.0).
6. **不动** (EN + ZH): badge / NOTICE / comparison 链接 / operating loop + 5-stage mermaid / 27-workflow table / Usage Flow / Architecture / FAQ / License / 9-语言链接 / README-cn 正文其余.
  </action>
  <verify>
    <automated>grep -n "disciplined senior engineering team" README.md && grep -n "First 5 Minutes" README.md && grep -n "纪律严明" README-cn.md && grep -n "package manager" README.md | head -1 && grep -n "包管理器" README-cn.md | head -1</automated>
  </verify>
  <done>EN 主句在顶部 (D1) + wedge 副标题共存; First 5 Minutes linear quickstart 含 install→workflow→裸 harnessed→resume 且 step 3 显式零参数 (D2/D3); 3-Options 保留; CLI 表含 bare harnessed 行; README-cn 主句同步 D1 中文版 + wedge 保留 (D4); ZH 正文 drift 未动; 无 broken link / mermaid 破坏.</done>
</task>

<task type="auto">
  <name>Task 2: verify gate — 锚点 grep + 链接完整 + 渲染 sanity + SUMMARY</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. **锚点 grep** (Acceptance 1–3,6): EN 主句 verbatim 在场; `AI coding harness package manager` 副标题仍在 (共存); `First 5 Minutes` section 存在; `/auto` + 裸 `harnessed` + `harnessed resume` 三 step 在 quickstart 内; Operational Commands 含 bare `harnessed` 行; README-cn `纪律严明` 主句 + `包管理器` wedge 共存.
2. **3-Options 保留**: `Quick Start — 3 Options` + Auto/Stage/Surgical 三 subsection 仍在.
3. **链接/渲染 sanity** (Acceptance 4): 9-语言切换链接 L3 不变 (EN + ZH); 内部锚点 (`docs/comparison.md` / `docs/WORKFLOW.md` / `#faq` 等) 不破坏; mermaid fence 完整 (配对); badge block 不变.
4. **diff 范围** (D4 Acceptance 6): `git diff --stat` 仅 `README.md` + `README-cn.md` 两文件 (ZH 仅主句段改动); 其余 7 个翻译版未动.
5. **TDD 跳过记录** (D5): docs-only 无自动化测试 — SUMMARY 显式声明跳过理由 (非核心逻辑, zero-arg 行为 Phase 24 已测).
  </action>
  <verify>
    <automated>grep -c "mermaid" README.md && grep -n "Quick Start — 3 Options" README.md && git diff --stat README.md README-cn.md && git status --short | grep -E "README-(tw|ja|ko|pt-BR|tr|ru|vi|th)" || echo "translations untouched"</automated>
  </verify>
  <done>全锚点在场 (EN 主句+quickstart+bare 行; ZH 主句+wedge 共存); 3-Options 保留; 链接/mermaid/badge 不破坏; diff 仅 README.md + README-cn.md; 7 翻译版未动; TDD 跳过已记. 证据进 SUMMARY.</done>
</task>

</tasks>

<verification>
phase 级验收 (= 25-CONTEXT.md Acceptance):
1. EN 结果导向主句在 README 顶部; package-manager wedge 仍在副标题 (共存).
2. First 5 Minutes linear quickstart (install → 第一个 workflow → 裸 `harnessed` you-are-here → resume), step 3 显式零参数 (Phase 24); 3-Options 保留.
3. Operational Commands 含 bare `harnessed` 行.
4. 无 broken link/锚点; mermaid/badge 不破坏 (EN + ZH).
5. TDD 跳过理由记录; docs-only 无自动化测试.
6. README-cn 一句话定位同步 D1 主句中文版 + wedge 保留; ZH 整体 drift 不动 (→ v10.0); diff 仅 README.md + README-cn.md.
</verification>

<success_criteria>
- README 顶部一句话定位 instantly-graspable (结果导向, 对标 comet); wedge 副线保留 (EN + ZH).
- First 5 Minutes linear quickstart 以 Phase 24 零参数 `harnessed` 为 spine, install→see-where-I-am <5min.
- 3-Options / mermaid / FAQ / 全结构保留 (KARPATHY-minimal 重组, 非重写).
- EN 全套 + README-cn 仅主句同步; diff README.md + README-cn.md; ZH quickstart/整体 drift 留 v10.0.
</success_criteria>

<output>
完成后建 `.planning/phases/25-quickstart/25-01-SUMMARY.md`: EN 主句 D1 落地 (before/after) + TL;DR 重组 + First 5 Minutes quickstart (4 step, zero-arg spine) + bare harnessed CLI 行 + ZH 主句同步 (D4, before/after) + 3-Options 保留 + 锚点 grep 证据 (EN 主句/quickstart/bare; ZH 纪律严明/包管理器) + 链接/mermaid sanity + TDD 跳过声明 (D5) + git diff --stat (仅 README.md + README-cn.md) + 7 翻译版未动确认.
</output>
