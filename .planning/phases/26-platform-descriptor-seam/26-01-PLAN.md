---
phase: 26-platform-descriptor-seam
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/installers/lib/platform.ts
  - src/installers/lib/harnessedRoot.ts
  - tests/installers/platform.test.ts
autonomous: true
requirements:
  - "v9.0 Phase A acceptance (26-CONTEXT.md Acceptance 1-4) + architecture doc §3 Phase-A subset"
user_setup: []

verified_refs:
  - "getHarnessedRoot in src/installers/lib/harnessedRoot.ts:65-69 (exists — Read-confirmed; override check + join(homedir,'.claude','harnessed'))"
  - "harnessedSubdir L78 / harnessedFile L88 (exist — call getHarnessedRoot, inherit routing, NOT touched)"
  - "migrateLegacyHarnessedRoot L114 (exists — NOT touched)"
  - "HARNESSED_ROOT_OVERRIDE env precedence (harnessedRoot.ts:66 — preserve exactly)"
  - "src/installers/lib/platform.ts (NEW)"
  - "tests/installers/platform.test.ts (NEW — check existing harnessedRoot test stays green: grep -rl harnessedRoot tests/)"
  - ".planning/v9.0-cross-harness-ARCHITECTURE.md §3 (design SoT, committed 374b3ff)"

must_haves:
  truths:
    - "PlatformDescriptor interface exported, 8 fields: id, homeDir, stateRoot, settingsPath, skillsDir, commandsDir, pluginsRegistry, mcpConfigPath"
    - "claudeDescriptor() returns claude paths; mcpConfigPath = join(homedir(),'.claude.json') (SIBLING, not under homeDir)"
    - "detectPlatform(): default → claudeDescriptor(); HARNESSED_ROOT_OVERRIDE set → claude descriptor with stateRoot=override verbatim, other fields claude-default"
    - "detectPlatform has NO auto-probe / NO .agents fallback / NO HARNESSED_PLATFORM (Phase C scope)"
    - "getHarnessedRoot() body = return detectPlatform().stateRoot; output byte-identical to current for default + override"
    - "full existing suite green UNCHANGED (regression proof); tsc 0; biome clean"
  artifacts:
    - path: "src/installers/lib/platform.ts"
      provides: "PlatformDescriptor type + claudeDescriptor() + detectPlatform() (the seam)"
      contains: "detectPlatform"
    - path: "src/installers/lib/harnessedRoot.ts"
      provides: "getHarnessedRoot routed through detectPlatform().stateRoot (override check moved into detectPlatform)"
      contains: "detectPlatform"
  key_links:
    - from: "getHarnessedRoot() (6 root consumers via harnessedSubdir/File)"
      to: "detectPlatform().stateRoot"
      via: "PlatformDescriptor seam — claude default = byte-identical, zero behavior change"
      pattern: "detectPlatform"
---

<objective>
建 PlatformDescriptor seam,**零行为变化**:把 `getHarnessedRoot()` 路由经新的 `detectPlatform()`,后者返回与当前硬编码路径完全相同的 Claude-Code descriptor。这是 v9.0 refactor-first 地基(相位 A);auto-probe + `.agents/` 第二平台是相位 C(28),散落 config resolver 是相位 B(27)。相位 A 只造接缝并证明全套现有测试不变绿(接缝透明的回归证明)。

约束(26-CONTEXT.md + 架构 doc §3 Phase-A subset):descriptor 8 字段全定义但仅 claude 填充 + 仅 stateRoot 被消费; detectPlatform 仅 OVERRIDE > claude-default 两档,无 probe/agents/HARNESSED_PLATFORM(anti-stale,第二平台不存在前不建检测分支); HARNESSED_ROOT_OVERRIDE 优先级精确保留; byte-identical 输出。TDD red-first。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/26-platform-descriptor-seam/26-CONTEXT.md
@.planning/v9.0-cross-harness-ARCHITECTURE.md
@src/installers/lib/harnessedRoot.ts
</context>

<key_facts>
verified (直接用):
- getHarnessedRoot 当前 body(harnessedRoot.ts:65-69): `const override = process.env.HARNESSED_ROOT_OVERRIDE; if (override !== undefined && override !== '') return override; return join(homedir(), '.claude', 'harnessed')`。
- harnessedSubdir/harnessedFile 调 getHarnessedRoot,自动继承路由,不改。
- migrateLegacyHarnessedRoot 不改。
- ESM (package.json type module); import 用 .js 后缀风格 (现有 harnessedRoot.ts import 'node:os'/'node:path')。
- 测试基线:v8.0 ship 后全套绿 (1352)。Phase A 不应新增任何行为 → 现有计数 + platform.test.ts 新增。
- mcpConfigPath 是 §7 D-A 不规则字段: CC 为 ~/.claude.json (homedir sibling, 非 homeDir 下)。
</key_facts>

<tasks>

<task type="tdd">
  <name>Task 1 (Wave 1): platform.ts seam + getHarnessedRoot 路由</name>
  <files>src/installers/lib/platform.ts, src/installers/lib/harnessedRoot.ts, tests/installers/platform.test.ts</files>
  <red>
失败测试 `tests/installers/platform.test.ts` (NEW):
1. `claudeDescriptor()` → `id==='claude'`; `homeDir` 以 `.claude` 结尾; `stateRoot` === `join(homeDir,'harnessed')`; `settingsPath` === `join(homeDir,'settings.json')`; `skillsDir`/`commandsDir`/`pluginsRegistry` 正确; `mcpConfigPath` === `join(homedir(),'.claude.json')` (sibling, NOT under homeDir)。
2. `detectPlatform()` (env 无 override) → deep-equals `claudeDescriptor()`。
3. `HARNESSED_ROOT_OVERRIDE='/tmp/x'` → `detectPlatform().stateRoot === '/tmp/x'`; `detectPlatform().homeDir` 仍 claude-default (= 仅 stateRoot 被 override)。
4. `getHarnessedRoot()` (default) === `detectPlatform().stateRoot` === `join(homedir(),'.claude','harnessed')`。
5. `getHarnessedRoot()` (override set) === override verbatim (回归: 与当前行为一致)。
  </red>
  <green>
- `src/installers/lib/platform.ts` (NEW):
  - `export interface PlatformDescriptor { id: 'claude'|'agents'|'cursor'|'gemini'|'copilot'; homeDir: string; stateRoot: string; settingsPath: string; skillsDir: string; commandsDir: string; pluginsRegistry: string; mcpConfigPath: string }`
  - `export function claudeDescriptor(): PlatformDescriptor`:
    ```
    const home = join(homedir(), '.claude')
    return { id: 'claude', homeDir: home,
      stateRoot: join(home, 'harnessed'),
      settingsPath: join(home, 'settings.json'),
      skillsDir: join(home, 'skills'),
      commandsDir: join(home, 'commands'),
      pluginsRegistry: join(home, 'plugins', 'installed_plugins.json'),
      mcpConfigPath: join(homedir(), '.claude.json') }
    ```
  - `export function detectPlatform(): PlatformDescriptor`:
    ```
    const base = claudeDescriptor()
    const override = process.env.HARNESSED_ROOT_OVERRIDE
    if (override !== undefined && override !== '') return { ...base, stateRoot: override }
    return base
    ```
    (注释说明: Phase A 仅 override + claude-default; auto-probe / .agents / HARNESSED_PLATFORM = Phase C。)
- `src/installers/lib/harnessedRoot.ts`:
  - import `{ detectPlatform } from './platform.js'`。
  - `getHarnessedRoot()` body → `return detectPlatform().stateRoot`。删除本地 override 检查 (已移入 detectPlatform)。更新 doc-comment: 标注路由经 detectPlatform seam (v9.0 Phase A),behavior 不变。
  - harnessedSubdir/harnessedFile/migrateLegacyHarnessedRoot 不动。
  </green>
  <verify>
    <automated>corepack pnpm vitest run tests/installers/platform.test.ts 2>&1 | tail -12</automated>
  </verify>
  <done>descriptor 8 字段正确; detectPlatform override+default 两档; getHarnessedRoot byte-identical (default + override); 无 probe/agents 分支。</done>
</task>

<task type="auto">
  <name>Task 2: regression gate — 全测试不变绿 + biome + tsc + 无行为变化确认</name>
  <files>（无新改动 — 验证 + SUMMARY）</files>
  <action>
1. `corepack pnpm exec biome check --write src/ tests/` → clean。
2. `corepack pnpm exec tsc --noEmit` → exit 0。
3. `corepack pnpm test` 全套 green 0 fail。**关键回归证明**: 现有 harnessedRoot/state/checkpoint/audit/workflowStore 测试**全部不变绿** (seam 透明)。`grep -rl harnessedRoot tests/` 确认现有覆盖仍过。
4. **零行为变化确认**: getHarnessedRoot 在 default + HARNESSED_ROOT_OVERRIDE 两态输出与 pre-change 一致 (Task1 测试 4/5 已断言)。
5. `git diff --stat` 仅 3 个目标文件 (platform.ts new + harnessedRoot.ts M + platform.test.ts new)。
  </action>
  <verify>
    <automated>corepack pnpm exec biome check src/ tests/ 2>&1 | tail -3 && corepack pnpm exec tsc --noEmit && echo "tsc=0" && corepack pnpm test 2>&1 | grep -iE "Test Files|Tests " | tail -3</automated>
  </verify>
  <done>biome clean; tsc 0; 全测试 green (现有不变 + platform.test.ts 新增); 零行为变化; diff 仅 3 文件。证据进 SUMMARY。</done>
</task>

</tasks>

<verification>
phase 级验收 (= 26-CONTEXT.md Acceptance):
1. PlatformDescriptor 8 字段 + claudeDescriptor 正确 (incl. mcpConfigPath sibling)。
2. detectPlatform default→claude / override→stateRoot 替换。
3. getHarnessedRoot === detectPlatform().stateRoot, byte-identical (default + override)。
4. 全现有套件不变绿 (回归证明 seam 透明); tsc 0; biome clean。
</verification>

<success_criteria>
- PlatformDescriptor seam 就位,零行为变化 (claude default = byte-identical)。
- getHarnessedRoot 路由经 detectPlatform; HARNESSED_ROOT_OVERRIDE 精确保留。
- KARPATHY-minimal (descriptor + 1 detect function, 无 framework, 无 probe 分支)。
- TDD red→green; 全 gate green; 现有测试不变 = 接缝透明回归证明。
</success_criteria>

<output>
完成后建 `.planning/phases/26-platform-descriptor-seam/26-01-SUMMARY.md`: RED→GREEN + PlatformDescriptor 8 字段 + detectPlatform 两档 (override/default) + getHarnessedRoot 路由 (byte-identical 证据) + 全现有套件不变绿 (回归证明) + biome clean + tsc 0 + git diff --stat (仅 3 文件) + Phase B/C 衔接指针。
</output>
