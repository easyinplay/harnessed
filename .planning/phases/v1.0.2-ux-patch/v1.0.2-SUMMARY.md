---
phase: v1.0.2
plan: ux-patch
subsystem: cli/setup + workflows/plan-feature + docs
tags: [ux-redesign, one-shot-onboarding, gap-fix, namespace-fix, non-expert]
dependency_graph:
  requires: [v1.0.1]
  provides: [plan-feature-SKILL.md, setup-one-shot-onboarding, namespace-correct-docs]
  affects: [src/cli/setup.ts, workflows/plan-feature/SKILL.md, README.md, docs/WORKFLOW.md]
tech_stack:
  added: []
  patterns: [install-base-reuse, dry-run-opt-in-UX-flip]
key_files:
  created:
    - workflows/plan-feature/SKILL.md
  modified:
    - src/cli/setup.ts
    - tests/cli/setup.test.ts
    - README.md
    - docs/WORKFLOW.md
    - package.json
    - CHANGELOG.md
    - .planning/STATE.md
decisions:
  - "T1.3+T1.4 combined into single commit (same file, atomic change)"
  - "Manifest count 16 (not 17 as spec claimed) — 7 tools + 9 skill-packs; spec list matched actual files"
  - "Tests remain 764 (5 new setup cells replaced 5 old ones, net-zero change)"
metrics:
  duration: ~8min
  completed: 2026-05-20
  tasks_completed: 7
  files_changed: 8
---

# v1.0.2 UX Patch Summary

**One-liner:** `harnessed setup` flipped to immediate-install default + install-base chain; `/plan-feature` SKILL.md created; namespace docs corrected.

---

## Tasks Completed

| Task | Commit | Description | Files |
|------|--------|-------------|-------|
| T1.1 | 2e2f805 | NEW `workflows/plan-feature/SKILL.md` 56L Gap B fix | `workflows/plan-feature/SKILL.md` |
| T1.2 | 161a374 | README + docs/WORKFLOW.md namespace correction | `README.md`, `docs/WORKFLOW.md` |
| T1.3+T1.4 | 7f49d02 | `src/cli/setup.ts` default --apply + chain install-base | `src/cli/setup.ts` (93→166L) |
| T1.5 | da514b4 | `tests/cli/setup.test.ts` 5-cell v1.0.2 coverage | `tests/cli/setup.test.ts` |
| T2.1 | 4a156b0 | `package.json` 1.0.1 → 1.0.2 | `package.json` |
| T2.2 | 371fea1 | CHANGELOG [1.0.2] release entry | `CHANGELOG.md` |
| T2.3 | 9c9b72e | STATE.md update + LOCAL CREATE v1.0.2 annotated tag | `.planning/STATE.md` |

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Test readdir mock path separator mismatch**
- **Found during:** T1.5 first test run (2 of 5 cells failed)
- **Issue:** Mock checked `String(p).includes('manifests/tools')` but `resolve()` on Windows produces backslash paths (`manifests\tools`), so the manifest readdir branches never matched → `runInstall` called 0 times
- **Fix:** Changed path checks to use `ps.includes('tools')` and `ps.includes('skill-packs')` (substring of the dir name, not the full slash-separated path)
- **Files modified:** `tests/cli/setup.test.ts`
- **Commit:** da514b4 (included in T1.5 commit)

**2. [Observation] T1.3 + T1.4 combined into single commit**
- Both tasks modify the same file (`src/cli/setup.ts`); combining is cleaner per Karpathy atomic-change principle. No deviation from plan intent — separate task IDs preserved in commit message.

**3. [Observation] Manifest count = 16, not 17 as spec stated**
- Actual: 7 tools + 9 skill-packs = 16 manifests. Spec listed all 16 files explicitly but mentioned "17 manifests" — off-by-one in the count. No action needed; install-base chain processes all files found by auto-glob regardless of expected count.

---

## Known Stubs

None — all implemented functionality is wired end-to-end. `setup.ts` Step B calls real `runInstall` + `validateManifestFile` (mocked only in tests).

---

## Threat Flags

None — no new network endpoints, auth paths, or schema changes. `setup.ts` writes to `~/.claude/skills/` (same trust boundary as existing `cp` in v1.0.1).

---

## Self-Check

## Self-Check: PASSED

| Item | Status |
|------|--------|
| `workflows/plan-feature/SKILL.md` | FOUND |
| `src/cli/setup.ts` | FOUND |
| `tests/cli/setup.test.ts` | FOUND |
| commit 2e2f805 (T1.1) | FOUND |
| commit 161a374 (T1.2) | FOUND |
| commit 7f49d02 (T1.3+T1.4) | FOUND |
| commit da514b4 (T1.5) | FOUND |
| commit 4a156b0 (T2.1) | FOUND |
| commit 371fea1 (T2.2) | FOUND |
| commit 9c9b72e (T2.3) | FOUND |
| v1.0.2 annotated tag | FOUND |
| 764 tests PASS | VERIFIED |
