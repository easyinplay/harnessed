# Phase 2.4: doctor 完整版 + EE-4 ABSORB + dashboard C 路径 + README CI gate - Context

**Gathered:** 2026-05-16
**Status:** Ready for planning
**Discuss mode:** interactive (4 D-decisions D-01~D-04, AskUserQuestion locked)

<domain>
## Phase Boundary

把 v0.2.0 收尾末 phase 装配为 **`doctor` 健康检查完整版 ship + EE-4 plan-checker 4 维量化阈值 ABSORB + dashboard C 路径 3 子功能 FULL absorb + README CI counter gate（B 路径）+ ralph-loop Windows native 兼容验收 sentinel**。 5 大主题 sister cluster 一次 ship，作为 v0.2.0 milestone 4/4 close（v0.2.0 alpha cycle 收官）。

**doctor 完整版 MIN 5 check**（D-01）— Node ≥ 22 / MCP scope / jq present / Win bash flavor / origin URL 校验。 现 `src/cli/doctor.ts` ~38L 扩到 ~150L。 沿袭 ROADMAP P2.4 原承诺核心项（jq/Git Bash 探测 + Win bash flavor + origin URL 篡改检）。 weekly cron / upstream_health / plugin uninstall 4 步 fallback 推 v0.3+。

**EE-4 ABSORB-2.4**（D-02）— Phase 2.4 doctor 完整版 sister cluster 拉入。 落地为 `gsd-plan-checker` agent prompt + 新 `routing/plan-review-schema.yaml` SSOT + ~30L plan-checker 量化输出（4 维硬标准：100% file refs / ≥80% reference sources / ≥90% concrete acceptance / 0 business-logic assumption）+ 不达标自动触发 plan-phase 重跑。 与 doctor 项目健康度 sister cluster 语义一致（doctor 查项目，plan-checker 查 plan）。

**README CI counter gate B 路径**（D-03）— Wave 0 加 CI step 三计数一致才 PASS（`grep "Phase 2.X shipped"` × `grep "Acceptance bar X1-X8"` × L44 counter），~15L yaml。 沿袭 Phase 2.2 T0.4 freshness gate pattern，补 counter 维度。 README 用户面 enumeration 保留 visible（非激进 SSOT 收敛 a 路径）。

**dashboard C 路径 FULL absorb**（D-04）— intel § 3 全 ~160L 进 doctor sister cluster：3.1 SessionStart hook auto-install ~30L（`src/installers/cc-hook-installer.ts` NEW + dispatch 接入）+ 3.2 STATE.md watcher + WebSocket push ~50L（增 `scripts/dashboard.mjs`）+ 3.3 多项目支持 ~80L（左栏 nav + URL routing）。 doctor + dashboard 完整 sister ship — heptagent ADD 形态 ancestor prototype。

**Wave 0 必办**（STATE.md Phase 2.4 Prereq Notes L601+ 一次根治）— H3 README CI counter gate B 路径 / M1 RETRO dashboard polish 历史 cluster 一句补 / M2 schemaVersion 7 surface land 1 consumer / T3 v0.3.0 backlog 预启动注记（不 land）/ deferred-items review 节强化（Phase 2.3 T5 沿袭）。 T2 dashboard C-path D-04 absorb 不重复。

**不在本 phase**:
- T4.4 Task Session 复用 完整版 → v0.3.0 checkpoint（closure infra ready）
- T1.1 dual-signal 4-layer real-API integration cell → v0.3.0 prep（需 ANTHROPIC_API_KEY）
- EE-2 ECC 9-field manifest schema → v0.3+ 评估（Phase 2.4 manifest schema 仅加 `install_type: cc-hook` MIN 扩）
- EE-1 role-router scoring 中间层 → v0.3+ 路由命中率验收
- CD-1 handoff 四字段完整模板 → v0.3.0 checkpoint
- V-1 动态 model routing → v0.3+ discuss
- V-2 Manual fallback bundle export → v0.4+
- plan-feature workflow + checkpoint + 路由命中率验收 + gstack 前缀探测 → v0.3.0
- intel 第 2 条 resolved_routing 快照冻结 → v0.3+ evaluation

</domain>

<decisions>
## Implementation Decisions

### D-01: doctor 主流程 scope = MIN

**Selected**: MIN（5 check：Node ≥ 22 / MCP scope / jq present / Win bash flavor / origin URL 校验）

**Locked rationale**: Karpathy YAGNI — ROADMAP P2.4 承诺 5 项核心（jq/Git Bash 探测 + Win bash flavor + origin URL 篡改检 + plugin uninstall 4 步 fallback + upstream_health weekly cron），weekly cron + plugin uninstall 4 步 v0.3+ 评估（真实需求出现再扩）。 MIN 5 check 已覆盖 R02 红旗（Win 上 jq + bash 未声明依赖 doctor 不通过则拒绝执行）+ origin URL 篡改检（audit sister 共享 helper），核心承诺兑现。

**Implementation hint**: `src/cli/doctor.ts` 现 ~38L 扩到 ~150L，每 check 独立 method 返回 `{ name, status: 'pass'|'warn'|'fail', message, fix? }`。 `spawnSync('node', ['--version'])` / `spawnSync('jq', ['--version'])` / `bash --version` 字符串解析 Git Bash vs MSYS2 vs WSL / `git config --get remote.origin.url` 比对 manifest 期望值。 `--json` flag + exit code 0/1（任一 fail/warn exit 1）。 Karpathy hard limit `doctor.ts` ≤200L。

**decision_source**: 用户首轮决策 MIN

### D-02: EE-4 plan 4 维量化阈值 timing = ABSORB-2.4

**Selected**: ABSORB-2.4（Phase 2.4 doctor 完整版 sister cluster 拉入）

**Locked rationale**: 与 doctor 项目健康度 sister cluster 语义一致（doctor 查项目环境 + audit 查 origin 完整性 + plan-checker 查 plan 量化健康度 — 三者 sister "项目级 health check"）。 不 defer Phase 2.5 独立，避免 v0.2.0 末 phase 后再开小 phase 拖延 v0.3.0 启动。 intel EE-4 ⭐⭐ omo Metis + Momus 4 维硬标准（100% file references verified / ≥80% reference sources / ≥90% concrete acceptance / 0 business-logic assumption）formalize 落地。

**Implementation hint**: 新 `routing/plan-review-schema.yaml` 4 维 SSOT（yaml 单一来源 + TypeBox runtime validate）。 `gsd-plan-checker` agent prompt 加 ~30L 量化输出节，输出 4 维 score + 三档（4/4 PASS / 3/4 WARNING / ≤2/4 BLOCKER 强制重跑 plan-phase）。 CI 加 step `node scripts/run-plan-checker.mjs .planning/phase-X.Y/ > plan-check.json`，BLOCKER exit 1。 Wave A R2-2 research 跑 1-2 现存 plan 试跑（如 phase-2.3/task_plan.md）出 baseline 分布 + 阈值校准候选。

**decision_source**: 用户首轮决策 ABSORB-2.4

### D-03: README counter integrity = B 路径 CI counter gate

**Selected**: B 路径（Wave 0 加 CI step 三计数一致才 PASS，~15L yaml）

**Locked rationale**: README 用户面 enumeration 保留 visible（用户读 README 看 phase shipped 进度）— 非 SSOT 收敛激进路径 a（删 README 冗余收敛 STATE.md）。 沿袭 Phase 2.2 T0.4 freshness gate yaml pattern，补 counter 维度。 三计数一致校验机械可靠（grep -c），CI fail 第一时间发现 README 漂移（H3 sister review root-cause-class 第 3 次复发 — Phase 2.2 / Phase 2.3 W3-prereq / Phase 2.3 ship 后）。

**Implementation hint**: `.github/workflows/ci.yml` 加 step `README counter integrity gate`：
```yaml
- name: README counter integrity gate
  run: |
    SHIPPED=$(grep -c "Phase 2\..*shipped" README.md)
    BARS=$(grep -c "Acceptance bar [A-Z][0-9]" README.md)
    L44=$(sed -n '44p' README.md | grep -oE '[0-9]+/4' | cut -d/ -f1)
    if [ "$SHIPPED" != "$BARS" ] || [ "$SHIPPED" != "$L44" ]; then
      echo "README counter drift: shipped=$SHIPPED bars=$BARS L44=$L44"
      exit 1
    fi
```
exact regex 由 plan-phase 实占（grep pattern 校准 — 当前 README 行格式 sample 后 lock）。 Wave 0 ship。

**decision_source**: 用户首轮决策 B 路径

### D-04: Dashboard C 路径 absorb = FULL

**Selected**: FULL（intel § 3 全 ~160L 进 doctor sister cluster）

**Locked rationale**: doctor + dashboard 完整 sister ship — heptagent ADD 形态 ancestor prototype（一个 ADD wave 装多个 sister 功能，避免 Phase 2.5+ 再开小 phase 拖延 v0.3.0）。 dashboard C 路径 3 子功能（hook + watcher + 多项目）三者强 coupling — hook 触发 dashboard 首启自动 install，watcher 即时 push 状态更新，多项目 nav 是 cross-project 入口，拆开 ship 失去 sister 语义 + 引重维护。

**Implementation hint**:
- **3.1 SessionStart hook auto-install** ~30L：新 `src/installers/cc-hook-installer.ts`（沿袭 Phase 2.1 6 installer pattern + provenance 4 字段）+ dispatch table 加第 7 entry + manifest `install_type: cc-hook` TypeBox enum 加项 + dashboard 启动时检 hook 不在 → 提示 + `--install-hook` flag
- **3.2 STATE.md watcher + WebSocket push** ~50L 增 `scripts/dashboard.mjs`：`fs.watch('.planning/STATE.md')` + debounce 500ms + WebSocket broadcast（ws npm 包 或 原生 http upgrade，planner 决）+ 前端 event-driven 不轮询
- **3.3 多项目支持** ~80L：左栏 nav 列 `~/.claude/harnessed-projects.json` 项目列表 + URL routing `?project=<path>` + 项目切换无 reload（client-side route，原生 history.pushState）

3 子功能均 Wave 3 内 ship（拓扑可并行 OR 串行 — Wave A R2-1 research 给推荐）。 dashboard 与 doctor sister cluster 一并 ship（doctor 启 dashboard 自动 install hook）。

**decision_source**: 用户首轮决策 FULL

### Claude's Discretion

- 5 check 各自实现 method exact（`spawnSync('node', ['--version'])` 解析 semver vs `process.version` 内建 vs 等）— planner 决（D-01 5 check scope 已锁，method 细节留 planner 选最低复杂度）
- `plan-review-schema.yaml` exact 4 维 schema 形态（intel L74-82 原型，planner formalize：yaml top-level keys + threshold 数值类型 + score 计算 formula）
- README CI gate 具体 regex（grep pattern 校准 — 当前 README 行格式 sample 后 lock，避免误判）— planner 决
- dashboard C 路径 3 子功能 wave 拓扑（能否 Wave 3 内 clean split 为 3 sub-task 并行 OR 必须串行 hook → watcher → 多项目）— Wave A R2-1 research 给推荐，planner 决
- ralph-loop Win 兼容 acceptance bar exact scope（CI matrix Windows native runner 跑哪些 e2e — MIN 1 spawn vs 完整 30 sample）— Wave A R2-3 research 给推荐，planner 决（推 MIN 走 5 fixture sample，不重做 Phase 2.3 30 sample）
- audit 完整版 scope（origin URL 校验 + 模拟恶意 fork 测试 + provenance 完整性）— planner 决（与 doctor check 5 共享 helper）
- ADR 编号 — Phase 2.4 plan-phase 实占（沿袭 intel § 0 SSOT 引用纪律，**不预占 0013**）
- Wave 0 prereq backlog 5 项 absorption 内部排序（H3 CI gate / M1 RETRO entry / M2 schemaVersion long-tail / T3 v0.3.0 prep / deferred-items review）— planner 决依赖关系
- `cc-hook-installer.ts` install/uninstall 路径细则（hook 文件位置 `~/.claude/hooks/` vs `~/.config/claude/hooks/` 跨平台 path） — Wave A R1 PATTERNS 给推荐
- M2 schemaVersion 7 surface 5 待 land consumer 中选哪 1（handoff-doc / phases-yaml / manifest-state / installer-state / route-decision-log / checkpoint / agent-definition-factory）— planner 决（选最低 friction surface）

### Folded Todos
None — 无 pending todos 跨 phase reference 进 Phase 2.4 scope。

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 2.4 核心实装依据
- `.planning/intel/omc-comparison.md` — **EE-4 主依据** — L74-82（omo Metis + Momus 4 维硬标准原型）+ § 0 SSOT 引用纪律 + § v0.2.0+ actionable table（CD-2/CD-5/CD-6 IMPL Phase 2.2 + CD-3/EE-5 IMPL Phase 2.3 + EE-4 PENDING Phase 2.4 absorb candidate）
- `.planning/intel/dashboard-handoff-2026-05-16.md` § 3 — **C 路径主依据** — ~160L 全 absorb（3.1 SessionStart hook auto-install + 3.2 STATE.md watcher + 3.3 多项目支持）
- `.planning/ROADMAP.md` L113-115 — Phase 2.4 必含项（jq / Git Bash 探测；upstream_health weekly cron；origin URL 校验；plugin uninstall 4 步 fallback）+ 验收（Windows native CI 跑过 ralph-loop 完整链路；audit 检测出 origin 篡改 + 模拟恶意 fork）
- `.planning/STATE.md` L601+ — Phase 2.4 Prereq Notes（Phase 2.3 SHIPPED post-mortem sister review 后 explicit 启动 prereq：H3 README counter / M1 dashboard polish RETRO / M2 schemaVersion long-tail / T2 dashboard C-path 已 D-04 absorb / T3 v0.3.0 prep）
- `.planning/phase-2.3/2.3-CONTEXT.md` D-06 + Deferred Ideas — EE-4 DEFER-2.4 决议依据；ADR 编号 plan-phase 实占（**不预占 0013**）

### Phase 2.4 Wave 0 必办来源
- `.planning/STATE.md` Phase 2.4 Prereq Notes H tier（H3） + M tier（M1/M2） + T tier（T3 + deferred-items review 强化沿袭 Phase 2.3 T5）
- `.planning/phase-2.3/RETROSPECTIVE.md` — Phase 2.3 ship 后 lessons（H3 root-cause-class README counter 第 3 次复发分析 + dashboard polish round 1 cluster）
- `scripts/check-transparency-verdicts.mjs` + `scripts/check-provenance.mjs` — Phase 2.2/2.3 ship gate — Phase 2.4 Wave 0 沿用 + audit 完整版 共享 provenance helper

### v0.2.0 ship 接口契约（Phase 2.4 直接消费 — frozen）
- `src/routing/engine.ts` — main-process-driven routing engine + SDK query() + ralph-loop wrap（Phase 2.2 ship；Phase 2.4 不动接口）
- `src/routing/decisionRules.ts` — arbitrate + CD-3 do_not_use_when + if_rejected_use（Phase 2.3 ship；Phase 2.4 不动）
- `src/routing/agentDefinition.ts` — `AgentDefinition` v1.2 contract（Phase 2.2 14 字段 reconcile ship）
- `src/installers/index.ts` — 6 install method dispatch table（Phase 2.1 ship + Phase 2.3 extension category 5 NEW adapter；Phase 2.4 加第 7 method `cc-hook`）
- `src/routing/lib/ralphLoop.ts` — `ralphLoopWrap` ≤50L wedge（D1.4-3 lock 永久架构；Phase 2.4 Win sentinel 验证不动接口）
- `src/cli/doctor.ts` — 现 ~38L baseline（Phase 2.4 扩 ~150L MIN 5 check）
- `scripts/dashboard.mjs` — 0b4e76d NEW + 161621c polish baseline（Phase 2.4 加 STATE watcher + WebSocket + 多项目 ~130L 增量）
- `docs/AGENT-DEFINITION-FACTORY-CONTRACT.md` + `docs/INSTALLER-CONTRACT.md` — A7 守恒不动 main body

### Schema 资源
- `routing/decision_rules.yaml` v2 + 三 category 段（Phase 2.3 ship；Phase 2.4 不动）
- `src/manifest/schema/spec.ts` — TypeBox manifest schema（Phase 2.4 加 `install_type: cc-hook` enum 项 MIN 扩）
- 新 `routing/plan-review-schema.yaml` — EE-4 4 维 SSOT（yaml + TypeBox runtime validate）

### 里程碑上下文
- `.planning/ROADMAP.md` § v0.2.0 Phase 2.4 — Goal + 必含项 5 项（jq/Git Bash + weekly cron + origin URL + uninstall 4 步 + Win ralph-loop）+ 验收（Win native CI + audit origin/恶意 fork）+ 关键风险 L120 R02（Win jq+bash 红旗）
- `.planning/phase-2.3/RETROSPECTIVE.md` — Phase 2.3 ship 后 lessons + Phase 2.4 启动 cadence
- `.planning/ROADMAP.md` § v0.3.0 — plan-feature workflow + checkpoint + 路由命中率验收 + gstack 前缀探测（Phase 2.4 ship 后启动）
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **Routing engine（Phase 1.4-2.3 ship）**: `src/routing/engine.ts` 主流程编排 + SDK query() + ralph-loop wrap + CD-3 negative-space consume — Phase 2.4 不动
- **6 installer（Phase 2.1 ship）+ extension category 5 NEW adapter（Phase 2.3 ship）**: dispatch table 全 runtime-ready — Phase 2.4 加第 7 method `cc-hook` for dashboard SessionStart hook auto-install
- **doctor baseline ~38L**: `src/cli/doctor.ts` 现状 — Phase 2.4 扩 ~150L MIN 5 check
- **dashboard baseline**: `scripts/dashboard.mjs` 0b4e76d NEW + 161621c polish round 1 — Phase 2.4 加 STATE watcher + WebSocket + 多项目 ~130L
- **transparency infra（Phase 2.2 ship）**: `scripts/check-transparency-verdicts.mjs` ENFORCE=true + freshness check + `scripts/check-provenance.mjs` — Phase 2.4 audit 完整版 共享 helper
- **gsd-plan-checker agent**（全局 `~/.claude/agents/`）: BLOCKER/WARNING/SUGGESTION 三档 — Phase 2.4 加 ~30L 量化输出节（4 维 score + 阈值判定）
- **CI A7 step**: ci.yml iter 1-12（Phase 2.3 ship）— Phase 2.4 ship 时 1-12 → 1-N（N 实占）

### Established Patterns
- **A7 conservation + errata-only ADR path** — Phase 1.1-2.3 11+ phase 一致践行；Phase 2.4 新 ADR 走 errata，A7 baseline tag iterate 1-12 → 1-N
- **TypeBox schema (`@sinclair/typebox`)** — 所有 schema 改动用 `Type.*`，沿袭 ADR 0009/0010/0011/0012 errata 注释块 fence 模式；`install_type: cc-hook` 字段 TypeBox enum 加项；`plan-review-schema.yaml` yaml SSOT + TypeBox runtime validate
- **Karpathy hard limits** — engine ≤200L / agentDefinition ≤200L / systemPrompt ≤80L / ralphLoop ≤50L / promiseExtract ≤50L；新 `doctor.ts` ≤200L / `cc-hook-installer.ts` ≤100L
- **Anti-stall executor protocol** — gsd-* agent 易 stall；Phase 2.4 plan-phase Wave A/B/C 沿袭 phase 1.5/2.1/2.2/2.3 anti-stall 约束
- **Sister review tiering** — Phase 2.4 ship 后预期 sister review（reviewer 已建立连续追溯，Phase 1.4 T1 / 1.5 H1+M1 / 2.1 H1+H2 / 2.2 H1+H2+H3 / 2.3 H1+H2+H3+M1+M2+T3 全闭环）
- **Wave 0 prereq backlog 一次根治** — phase 2.1/2.2/2.3 沿袭模式；Phase 2.4 Wave 0 absorb STATE.md L601+ 5 项

### Integration Points
- **doctor 5 check + audit 共享 helper** — origin URL check（doctor #5 + audit 完整版）共用 `git config --get remote.origin.url` 解析 + manifest 期望值比对 helper（~15L 抽 `src/cli/lib/origin-check.ts`）
- **plan-review-schema.yaml + gsd-plan-checker 改造** — schema yaml 单一 SSOT；plan-checker agent prompt 加 ~30L 量化输出节读 yaml threshold；CI run-plan-checker.mjs 加 step BLOCKER exit 1
- **dashboard C 路径 sister cluster** — 3 子功能（hook + watcher + 多项目）Wave 3 整批 ship；hook 触发 dashboard 启动自动 install，watcher push 即时状态更新，多项目 nav cross-project 入口
- **cc-hook installer** — `src/installers/cc-hook-installer.ts` NEW 沿袭 6 installer pattern + install_type + provenance 4 字段约束；dispatch table 加第 7 entry；manifest schema enum 加 `cc-hook`
- **README CI counter gate** — `.github/workflows/ci.yml` 加 step ~15L yaml；Wave 0 ship；与 freshness gate 同一 yaml 段（Phase 2.2 T0.4 pattern 沿袭）
- **ralph-loop Win matrix sentinel** — CI matrix `os: [ubuntu-latest, macos-latest, windows-latest]` Windows native runner 加 step 跑 `npm run test:e2e:ralph-loop`（5 fixture sample MIN scope，planner 决具体 sample 数）
- **CI A7 step**: ci.yml iter 1-12 → 1-N（Phase 2.4 ship 加新 ADR 时，N 实占）
- **CI Wave 0 加固 step**: README counter gate / schemaVersion long-tail land 1 consumer / ralph-loop Win sentinel — ci.yml 加 ~30-50L 增量

</code_context>

<specifics>
## Specific Ideas

- **doctor 5 check return schema**:
  ```ts
  type CheckResult = {
    name: string;
    status: 'pass' | 'warn' | 'fail';
    message: string;
    fix?: string;   // 可选 fix 提示（e.g. "Install jq: brew install jq"）
  };
  ```
  doctor 整体 return `{ checks: CheckResult[], summary: 'pass' | 'warn' | 'fail' }`，CLI `--json` flag print JSON OR human-readable colored output。
- **plan-review-schema.yaml 4 维 formalize**（intel L74-82 原型, planner 实占 exact yaml shape）:
  ```yaml
  thresholds:
    file_references_verified: 1.0    # 100%
    reference_sources_real: 0.8       # ≥80%
    concrete_acceptance: 0.9          # ≥90%
    business_logic_assumptions: 0     # exact 0
  scoring:
    pass_threshold: 4    # 4/4 维达标
    warning_threshold: 3 # 3/4 维达标
    blocker_threshold: 2 # ≤2/4 维达标 BLOCKER 强制 plan-phase 重跑
  ```
- **README CI counter gate exact yaml**（D-03 B 路径, planner regex 校准）:
  ```yaml
  - name: README counter integrity gate
    run: |
      SHIPPED=$(grep -cE "Phase 2\.[0-9]+ shipped" README.md)
      BARS=$(grep -cE "Acceptance bar [A-Z][0-9]" README.md)
      L44=$(sed -n '44p' README.md | grep -oE '[0-9]+/4' | head -1 | cut -d/ -f1)
      if [ "$SHIPPED" != "$BARS" ] || [ "$SHIPPED" != "$L44" ]; then
        echo "README counter drift: shipped=$SHIPPED bars=$BARS L44=$L44"; exit 1
      fi
  ```
- **cc-hook-installer.ts skeleton**（D-04 3.1，沿袭 6 installer pattern + provenance 4 字段）:
  ```ts
  // src/installers/cc-hook-installer.ts (≤100L)
  export async function installCcHook(manifest: HookManifest): Promise<InstallResult> {
    // 1. resolve hook target path（~/.claude/hooks/ vs ~/.config/claude/hooks/ 跨平台 — Wave A R1 推荐）
    // 2. write hook file（hook script + manifest declared hook config）
    // 3. provenance 4 字段（installed_at / installed_by / source_url / install_type: 'cc-hook'）
    // 4. idempotent check（hook 已存 + content 一致 skip）
  }
  export async function uninstallCcHook(manifest: HookManifest): Promise<UninstallResult> {
    // rm hook 文件 + manifest state 撤销（沿袭 Phase 2.1 install_type uninstall pattern）
  }
  ```
- **STATE.md watcher + WebSocket**（D-04 3.2，~50L 增 `scripts/dashboard.mjs`）:
  ```js
  // scripts/dashboard.mjs (增量 ~50L)
  import { watch } from 'node:fs';
  import { WebSocketServer } from 'ws';
  const wss = new WebSocketServer({ port: 0 /* dynamic */ });
  let debounceTimer = null;
  watch('.planning/STATE.md', () => {
    clearTimeout(debounceTimer);
    debounceTimer = setTimeout(() => {
      wss.clients.forEach(c => c.send(JSON.stringify({ type: 'state-changed' })));
    }, 500);
  });
  ```
- **多项目支持**（D-04 3.3，~80L）:
  - `~/.claude/harnessed-projects.json` 项目列表（`[{ name, path, lastAccessed }]`）
  - 左栏 nav 渲染列表 + 项目切换 client-side route（`history.pushState` `?project=<path>`）
  - dashboard.mjs serve `/api/projects` endpoint（read JSON file + return list）+ `/api/project/<id>` endpoint（read 项目 STATE.md content）
  - 项目切换无 reload（fetch new STATE + re-render）

</specifics>

<deferred>
## Deferred Ideas

- **T4.4 Task Session 复用 完整版** → **v0.3.0 checkpoint**（closure infra 三件套 ready：sdkSpawn.onSessionId / ralphLoopWrap.resumeSessionId / engine.wrappedSpawn capturedSessionId，consumer 接入 + `harnessed.phases.v1` schema bump 加 `task_session_id?` field 即可，~1 commit 见 `.planning/phase-2.2/T4.4-DEFERRED-onboarding.md`）
- **T1.1 dual-signal 4-layer real-API integration cell** → **v0.3.0 prep**（需 ANTHROPIC_API_KEY env，real-API e2e）
- **EE-2 ECC 9-field manifest schema** → **v0.3+ 评估**（Phase 2.4 manifest schema 仅加 `install_type: cc-hook` MIN 扩展，不动 9-field — 与 Phase 2.4 MIN scope 不冲突）
- **EE-1 role-router scoring 中间层** → **v0.3+ 路由命中率验收**（30 sample → 100+ sample × multi-model 验收时）
- **CD-1 handoff 四字段完整模板** → **v0.3.0 checkpoint 完整版**（Decided / Rejected / Risks / Files 进 `.harnessed/checkpoints/`；Phase 2.2/2.3/2.4 已用模板格式，完整机制 v0.3.0）
- **V-1 动态 model routing** → **v0.3+ discuss**（static model tier Phase 2.2 ship 跑通后再考虑 signal→rules→scorer 升级）
- **V-2 Manual fallback bundle export** → **v0.4+**（边界 case wedge 风险警示，不在 v0.2 主线）
- **intel 第 2 条 resolved_routing 快照冻结** → **v0.3+ evaluation**（karpathy YAGNI，真实 mid-session drift 出现后再做）
- **plan-feature workflow + checkpoint + 路由命中率验收 + gstack 前缀探测** → **v0.3.0**（Phase 2.4 ship 后启动；v0.3.0 alpha cycle 主线）
- **doctor weekly cron + upstream_health + plugin uninstall 4 步 fallback** → **v0.3+ 评估**（ROADMAP P2.4 原承诺，Phase 2.4 MIN 5 check 已兑现核心，cron + 4 步 fallback 真实需求出现再扩）
- **gstack 前缀探测 + workflow 变量插值** → **v0.3.0 P3.2**（plan-feature workflow 时落地）
- **`--force` flag for install idempotent_check** → 真实需求出现时

### Reviewed Todos (not folded)
None — 无 pending todos cross-reference 到本 phase。

</deferred>

---

*Phase: 2.4 — doctor 完整版 + EE-4 ABSORB + dashboard C 路径 + README CI gate + ralph-loop Win sentinel*
*Context gathered: 2026-05-16*
*Discuss-phase: 4 D-decisions D-01~D-04 locked（interactive AskUserQuestion）；EE-4 ABSORB-2.4 absorbed；dashboard C 路径 FULL absorb；STATE.md backlog 5 项 Wave 0 一次根治*
*v0.2.0 末 phase — ship 后 v0.3.0 plan-feature workflow + checkpoint 启动*
