# Phase 3.2: gstack 前缀探测 + workflow 变量插值 + plan-feature reference 实装 — KICKOFF

**Phase**: 3.2 (v0.3.0 milestone 第 2 phase, 4 phase 中第 2)
**Started**: 2026-05-16
**Status**: discuss-phase 完成 → 待 plan-phase 启动

---

## 1. ROADMAP / REQUIREMENTS 锚定

**ROADMAP § v0.3.0 Phase 拆分 L161-163** (`.planning/ROADMAP.md`):

> **Phase 3.2：gstack 前缀探测 + workflow 变量插值 + plan-feature reference 实装**
> - 三选一探测；`invokes` 字段插值；governance 层 pause + on_veto
> - 验收：用户 gstack 装哪种前缀都能跑通

**REQUIREMENTS R7.1 + R7.4** (`.planning/REQUIREMENTS.md` L257-279):

- **R7.1 5-phase 流水 (reference implementation)**: gstack-decision (governance) → brainstorm (execution，UI 任务时 +ui-ux-pro-max) → gsd-discuss (orchestration) → gsd-plan → persist (planning-with-files)
  - 验收：30 plan-feature 真实场景全跑通；CEO veto 时 halt_workflow 不再续
- **R7.4 gstack 命令前缀探测**: doctor 探测三选一 (默认 `/office-hours` / `--no-prefix` / `--prefix gstack-`) 写入 `.harnessed/config.json`；workflow `invokes` 字段变量插值
  - 验收：用户三种前缀场景任一都跑通

**Canonical refs**:
- `.planning/ROADMAP.md` L161-163 (Phase 拆分)
- `.planning/REQUIREMENTS.md` L257-279 (R7.1 + R7.4)
- `docs/WORKFLOWS-MVP.md` § Workflow 3 (plan-feature 5-phase 流水设计原型)
- `docs/PROJECT-SPEC.md` § 10 (plan-feature reference scope)
- `docs/PROJECT-SPEC.md` § 12 (checkpoint mech, Phase 3.1 W4 已 implement)

---

## 2. Carry-forward from prior phases (locked, 不重新讨论)

| 来源 | 决策 | 本 phase 应用 |
|------|------|---------------|
| **Phase 3.1 D-01 TEMPLATE 摘要** | checkpoint 摘要 zero LLM call | plan-feature 5-phase 桩消费 Phase 3.1 checkpoint engine (writeCheckpoint + state machine) |
| **Phase 3.1 D-02 KARPATHY 3-state** | active/paused/complete | governance veto = pause state (R7.4 verify) |
| **Phase 3.1 D-03 RELOAD resume** | 不偷袭用户 | governance veto 后 user 调 `harnessed resume` 续跑 (sister cadence) |
| **Phase 3.1 D-04 WIRE-IN T4.4 closure infra** | session_id 写 checkpoint | plan-feature workflow 5-phase 之间 sessionId 传递 (consume Phase 3.1 wired engine.ts) |
| **Phase 2.4 D-04 dashboard SSE** | localhost-only bind + EventSource | governance veto signal 可触发 dashboard SSE push (本 phase 未必需) |
| **Karpathy 4 心法 always-on** | YAGNI / Surgical / Simplicity / Goal-Driven | doctor PROBE zero-dep + JINJA template str.replace + WIRED 中庸 + PUSH file-based |

---

## 3. v0.3.0 milestone 在本 phase 的位置

v0.3.0 = plan-feature workflow + checkpoint (4 phase):
- Phase 3.1 ✅ SHIPPED (checkpoint 引擎 + harnessed resume + compact 协议)
- **Phase 3.2** (本 phase): gstack 前缀探测 + workflow 变量插值 + plan-feature reference — **plan-feature 5-phase 流水 + 用户 gstack 前缀适配层**
- Phase 3.3: aliases.yaml + deprecation marker + known-good 版本组合
- Phase 3.4: 路由命中率 ≥ 85% 验收 + token budget 监控

Phase 3.2 ship 后, Phase 3.3+ 可 dogfood plan-feature workflow 设计 ship 自己 (consume Phase 3.1 checkpoint + Phase 3.2 workflow infra).

---

## 4. v0.3.0 W0 backlog (Phase 3.1 ship sister carry-forward — **W0 first task 必修**)

从 STATE.md L23a + Phase 3.1 deferred-items.md 拉入, 一次 W0 根治:

1. **W0.1 (Phase 3.1 deferred #1 priority bump — 必修 first task)**: `tests/unit/cli-audit.test.ts` env-dependent CI red fix. Phase 3.1 ship 后 CI 全 3-OS red on `pnpm test` step (b6a0feb baseline 也 fail local 但 CI 当时 green). Root cause: Phase 2.4 W4 audit runtime-layer 加 (origin-check + install.cmd injection + provenance gate) 后 unit test mocks 未同步, Phase 3.1 W3 engine.ts MODIFY 间接改变 import chain → CI 复现. **Fix path locked**: 拆 `tests/unit/cli-audit.test.ts` 仅 manifest-layer (mock readdir/readFile/validate only) + add vi.mock spawnSync/readFileSync. ≤30L delta, ≤30 tool uses. **CI 全绿是 Phase 3.2 W0 first acceptance bar**.
2. **W0.2 (DEFERRED #1 W0.3 README completeness ENFORCE flip)**: STATE.md / README.md per-phase shipped marker format normalize (`**Phase X.Y SHIPPED**` 与 `- **Phase X.Y shipped** ✅` 统一); ci.yml `continue-on-error: true` 删除 (ENFORCE flip → fail-on-drift). Phase 3.1 W0.3 注册 prereq, Phase 3.2 W0 兑现.
3. **W0.3 (Phase 3.2 own prereq)**: doctor PROBE 实装 + `.harnessed/config.json` schema 加 `gstack_prefix` field (TypeBox + branchOnSchemaVersion 9th surface? 或 复用 current-workflow.v1 schema?) — Wave A R2 research 决.

**T1+T3 不再 defer**: Phase 2.4 ship sister T1+T3 已被 800e459 inline 修.
**DEFERRED #2 userSpawn niche**: 仍 defer Phase 3.4+ (real userSpawn usage statistics dogfooding 后定).

---

## 5. 不在本 phase

- **plan-feature workflow 真接 gsd-discuss/plan/execute spawn** → Phase 3.3+ dogfood (本 phase WIRED 中庸, 不接外部)
- **aliases.yaml + deprecation marker + known-good 版本组合** → Phase 3.3
- **路由命中率 ≥ 85% 验收 + token budget 监控** → Phase 3.4
- **EE-4 BLOCKER auto-spawn rerun** → Phase 3.4 后 evaluate (Phase 2.4 down-scope DEFERRED)
- **userSpawn session_id capture** → Phase 3.4+ (DEFERRED #2 Phase 3.1 carry)
- **dashboard SSE governance veto push integration** → 可选 (Phase 3.2 D-04 PUSH locked is file-based, dashboard 仅可读 governance.json watcher)

---

## 6. discuss-phase 4 gray area 决策 (详 CONTEXT.md)

| ID | 主题 | Locked |
|----|------|--------|
| D-01 | gstack 命令前缀探测策略 | PROBE (doctor 运行时探三选一 + fallback 用户选) |
| D-02 | workflow `invokes` 变量插值机制 | JINJA-like `{{ prefix }}` 模板替换 |
| D-03 | plan-feature reference 实装范围 | WIRED (接 governance gate + 5 phase 桩不接外) |
| D-04 | CEO veto halt_workflow 触发 mechanism | PUSH (gstack veto 写 .harnessed/governance.json → workflow next-phase 读) |

---

*Phase 3.2 — gstack 前缀探测 + workflow 变量插值 + plan-feature reference 实装*
*Discuss-phase: 2026-05-16*
