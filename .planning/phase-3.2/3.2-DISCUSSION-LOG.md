# Phase 3.2: gstack 前缀探测 + workflow 变量插值 + plan-feature reference 实装 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 3.2-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-16
**Phase:** 3.2 — gstack 前缀探测 + workflow 变量插值 + plan-feature reference 实装
**Areas discussed:** gstack 命令前缀探测策略 / workflow invokes 变量插值机制 / plan-feature reference 实装范围 / CEO veto halt_workflow 触发 mechanism
**Mode:** interactive (4-question batch AskUserQuestion, 1 call)

---

## Probe — gstack 命令前缀探测策略

| Option | Description | Selected |
|--------|-------------|----------|
| PROBE | doctor 运行时 spawn `which`/`command -v` 探三选一 + fallback prompt 用户三选一. zero-friction; sister Phase 2.4 doctor `--json` CI-friendly 经验. | ✓ |
| CONFIG | 读用户 .claude/settings.json explicit 设置. Zero spawn 但需用户提前知道字段, 默认用户跳不过取值 step. | |
| INTERACTIVE | doctor 首跑 prompt 三选一. 明确意图但 CI/headless env 跑不动 (R7.4 "doctor 探测" 语义不符). | |

**User's choice:** PROBE (Recommended option)
**Notes:** R7.4 验收"用户三种前缀场景任一都跑通"明指 runtime 自动检测. fallback prompt 兜底 0/2 响应 ambiguity case.

---

## Interpolate — workflow `invokes` 变量插值机制

| Option | Description | Selected |
|--------|-------------|----------|
| JINJA | `{{ prefix }}` 模板 str.replace 替换. Zero-dep (Karpathy YAGNI); cross-OS 一致; 用户直观可读. | ✓ |
| BASH | `${PREFIX}` envsubst-like. Shell 原生但 Win Git Bash 与 Linux env 行为差; workflow 是 node spawn 语义不符. | |
| SCHEMA | structured `invokes: { command, prefix_token }`. 类型安全但 over-engineering, Karpathy YAGNI 违反. | |

**User's choice:** JINJA (Recommended option)
**Notes:** zero-dep + cross-OS + 用户直观可读. 沿袭 Phase 2.4 dashboard template parse 模式. regex `/\{\{\s*(\w+)\s*\}\}/g` 实现.

---

## Reference — plan-feature reference 实装范围

| Option | Description | Selected |
|--------|-------------|----------|
| WIRED | 5 phase 桩实装 + governance gate 接 + 不接 gsd-discuss/plan/execute 真 spawn. 中庸: 验 workflow runtime 行为, Phase 3.3+ dogfood 换真. | ✓ |
| SKELETON | 仅 yaml + skill 桩, workflow 不跑. 最少工作但难验 R7.1 "30 场景跑通" 验收. | |
| FULL | 5 phase 全实装含 ralph-loop + checkpoint + 真 gsd-* spawn. Scope 太大一 phase 装不下; Phase 3.4 dogfooding 才合理. | |

**User's choice:** WIRED (Recommended option)
**Notes:** SKELETON 不验, FULL scope 大; WIRED 沿袭 Karpathy 中庸 + Phase 3.1 D-02 KARPATHY 3-state YAGNI. integration test 5 phase 跑通 + veto halt 2 fixture 验.

---

## Veto — CEO veto halt_workflow 触发 mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| PUSH | gstack veto 写 .harnessed/governance.json → workflow next-phase 读. Zero coordination; lazy read 不是定时 polling; 沿袭 Phase 3.1 current-workflow.json file-based state machine. | ✓ |
| POLL | workflow 每 5s poll governance state file. Sister Phase 2.4 SSE polling 反 anti-pattern (已 root-cause defer 改 EventSource). | |
| EVENT | gstack veto 触 SIGUSR1 signal. OS 原生但 cross-OS (Win signal 不同), workflow 需 install signal trap, 调试难. | |

**User's choice:** PUSH (Recommended option)
**Notes:** zero coordination + file-based state (Phase 3.1 sister) + lazy read (overhead 仅 5 phase × 1 read). checkpoint mid-flight 保留 → user `harnessed resume` (D-03 RELOAD sister cadence).

---

## Claude's Discretion (留 planner)

- doctor PROBE Win shell flavor 兼容性 (`which` vs `where.exe` choice)
- JINJA template error handling (未定义 var fallback empty OR throw)
- 5 skill stub mock 输出格式 metadata 多少
- governance.json schemaVersion 9th surface 复用 OR 新增决策 (Wave A R2)
- plan-feature workflow.yaml DSL 形态 (sister `workflows/execute-task/phases.yaml` reference)
- W0.2 README format normalize SSOT 选哪个 (推荐 README format 作 SSOT)
- dashboard SSE governance.json watcher 集成评估 (推荐 SKIP 本 phase, Phase 3.3+ 集成)

## Deferred Ideas (写 CONTEXT.md `<deferred>` 节)

- plan-feature 真接 gsd-* spawn → Phase 3.3+ dogfood
- aliases.yaml + deprecation + known-good → Phase 3.3
- 路由命中率 ≥ 85% 验收 → Phase 3.4
- userSpawn session_id capture → Phase 3.4+ (DEFERRED #2 Phase 3.1 carry)
- CONFIG / INTERACTIVE / BASH / SCHEMA / SKELETON / FULL / POLL / EVENT 路径全 evaluated rejected

---

*Discussion: 4 questions, 1 batch AskUserQuestion call, all 4 Recommended options accepted by user (decisive cadence consistent with established workflow, sister Phase 3.1 同 pattern)*
