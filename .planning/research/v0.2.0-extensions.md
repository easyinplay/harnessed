# v0.2.0 Phase 2.3 — Extension Category Install Candidates (RESEARCH REFRESH)

> **Researched:** 2026-05-15 (refresh of 2026-05-11 roadmap)
> **Scope:** design / content / testing extension category — current repo state + install method mapping + license + gotchas
> **Method:** WebSearch + `gh api` license/metadata verification (Tavily/Exa MCP unavailable this session — fell back to WebSearch + gh CLI per env)
> **Downstream:** v0.2.0 requirements re-confirmation + Phase 2.3 plan-phase
> **harnessed 6 install methods:** cc-plugin-marketplace / git-clone-with-setup / npx-skill-installer / cli-npm / mcp-stdio-add / mcp-http-add
> **License whitelist:** MIT / Apache-2.0 / BSD-3-Clause / ISC / 0BSD

---

## § 1 — design category

### 1.1 ui-ux-pro-max — ⚠️ UPSTREAM MOVED (was midwayjs/midway)

| Field | Value |
|-------|-------|
| **Current canonical repo** | `github.com/nextlevelbuilder/ui-ux-pro-max-skill` — dedicated standalone repo, ~71k stars, **NOT** `midwayjs/midway` |
| **License** | MIT (verified via `gh api` 2026-05-15) ✅ whitelist-OK |
| **Last push** | 2026-04-03 — active |
| **Install method** | **cc-plugin-marketplace** (primary): `/plugin marketplace add nextlevelbuilder/ui-ux-pro-max-skill` → `/plugin install ui-ux-pro-max@ui-ux-pro-max-skill`. Alt: **cli-npm** `npm i -g uipro-cli` → `uipro init --ai claude` |
| **Confidence** | **HIGH** (repo + license + install both verified) |

**GOTCHA — manifest divergence:** The current `manifests/skill-packs/ui-ux-pro-max.yaml` points at `midwayjs/midway` v4-next branch via `git-clone-with-setup` (pinned ref `e89d70e4`). Phase 1.3's PATH_A+B test validated *that* path. But the skill now has a **dedicated upstream repo** with first-class plugin-marketplace support — the midwayjs/midway `.codex/skills/` vendoring was a workaround that is now obsolete. Phase 2.3 should **re-point the manifest** to `nextlevelbuilder/ui-ux-pro-max-skill` and switch `install.method` from `git-clone-with-setup` → `cc-plugin-marketplace`. This is a clean win: removes the brittle deep-copy-from-monorepo cmd.

**GOTCHA — Python search tool:** ships `scripts/search.py` (queries local CSV, no network). harnessed `audit` / install-time `print full hook text` policy (ROADMAP key decisions) should surface this to the user. Not a blocker but flag in manifest `notice:`.

**GOTCHA — v2.x design-system-generator:** upstream added a "Design System Persistence (Master + Overrides)" feature in 2026 — writes state files into the project. Verify idempotent_check still holds (`SKILL.md` presence is fine; no change needed).

### 1.2 frontend-design — ⚠️ NOW FIRST-PARTY ANTHROPIC

| Field | Value |
|-------|-------|
| **Current canonical repo** | `anthropics/skills` → `skills/frontend-design/` (also mirrored in `anthropics/claude-code` → `plugins/frontend-design/`). 277k+ installs as of 2026-03 — this is the **official** one |
| **License** | source-available (Anthropic) — **NOT** OSI-approved, **NOT** on whitelist ⚠️ see gotcha |
| **Last push** | `anthropics/skills` 2026-05-09 — active |
| **Install method** | **cc-plugin-marketplace**: `/plugin marketplace add anthropics/skills` → install the relevant plugin. Alt: **npx-skill-installer** `npx skills add anthropics/claude-code --skill frontend-design` |
| **Confidence** | **HIGH** (repo verified; license needs decision) |

**GOTCHA — license whitelist conflict:** `anthropics/skills` has **no LICENSE file** (`gh api .../license` → 404); README states skills are "source-available, not open source." This fails harnessed's MIT/Apache/BSD/ISC/0BSD whitelist. **Decision needed:** harnessed already special-cases anthropics official skills (decision_rules.yaml routes `anthropics-skills-pptx`, `anthropics-skills-skill-creator` etc.). Phase 2.3 should add an explicit **`anthropics-official` license-class exception** (same treatment content-category pptx/docx/xlsx/pdf will need — see § 2.1). Treat as a known, documented carve-out, not a silent pass.

**GOTCHA — community alt exists:** `Ilm-Alan/frontend-design` (MIT ✅, last push 2026-05-02) is a clean-license community skill, 8 aesthetic anchors, installs via `git clone … ~/.claude/skills/frontend-design` (**git-clone-with-setup**). If the team wants to avoid the anthropics license carve-out for the design category entirely, this is a whitelist-clean fallback. Tradeoff: far less battle-tested than the 277k-install official one.

---

## § 2 — content category

### 2.1 anthropics/skills — pptx / docx / xlsx / pdf — ⚠️ LICENSE CARVE-OUT NEEDED

| Field | Value |
|-------|-------|
| **Current canonical repo** | `github.com/anthropics/skills` → `skills/{pptx,docx,xlsx,pdf}/` — confirmed all 4 present 2026-05-15 |
| **License** | source-available (Anthropic), **no LICENSE file** — same as § 1.2. Not whitelist-OK without carve-out ⚠️ |
| **Last push** | 2026-05-09 — active |
| **Install method** | **cc-plugin-marketplace**: `/plugin marketplace add anthropics/skills` → `/plugin install document-skills@anthropic-agent-skills` (the 4 doc skills are bundled as one **`document-skills`** plugin, NOT 4 separate plugins) |
| **Confidence** | **HIGH** (repo + structure + plugin bundling all verified) |

**GOTCHA — bundled plugin, not per-skill:** harnessed's decision_rules.yaml currently has a rule `pptx-file-task → anthropics-skills-pptx` as if pptx is an independently-installable unit. It is **not** — installing gets you all 4 (pptx+docx+xlsx+pdf) via the single `document-skills` plugin. Phase 2.3 manifest should model this as **one manifest = one plugin = 4 skills**, and the routing rule's `primary_expert` should reference the bundle. Upstream issue #675 (open, 2026-03) confirms the bundling + a discoverability complaint — naming won't change soon.

**GOTCHA — license:** identical carve-out to § 1.2. Resolve once, apply to both design `frontend-design` and content `document-skills`.

**GOTCHA — sandbox/network:** API-side these skills run sandboxed with no network. For Claude Code local install via plugin marketplace this is not a constraint, but the skills shell out to Python libs (python-docx, openpyxl, etc.) — doctor health check should verify Python availability (ties into Phase 2.4 doctor work).

### 2.2 baoyu-skills (baoyu-slide-deck) — ✅ LICENSE RESOLVED (was "None warn")

| Field | Value |
|-------|-------|
| **Current canonical repo** | `github.com/JimLiu/baoyu-skills` → `skills/baoyu-slide-deck/` (confirmed present; repo has 22 skills total) |
| **License** | **MIT-0** for skills published to ClawHub registry (per repo README.zh.md + README.md — "skills published to ClawHub are distributed under MIT-0"). Root repo has **no top-level LICENSE file** (`gh api` returned no spdx_id) — the MIT-0 grant is README-declared, registry-scoped |
| **Last push** | 2026-05-14 — very active |
| **Install method** | **npx-skill-installer**: `npx skills add https://github.com/jimliu/baoyu-skills --skill baoyu-slide-deck` |
| **Confidence** | **MEDIUM** — install path HIGH; license MEDIUM (MIT-0 is README-declared not LICENSE-file-declared; MIT-0 itself IS whitelist-compatible in spirit — it's MIT minus attribution — but harnessed whitelist lists "MIT" not "MIT-0" explicitly) |

**GOTCHA — D1.2.5-10 resolution:** decision_rules.yaml rule `chinese-content-deck` carries `warn: 'license: None — D1.2.5-10 pending license verify'`. **This research resolves it:** license is **MIT-0 (README/registry-declared)**, not None. Phase 2.3 should: (a) add `MIT-0` to harnessed's license whitelist (it's strictly more permissive than MIT — zero downside), (b) update the manifest `upstream.license: MIT-0`, (c) **remove the `warn:` from the routing rule**. Minor caveat: because it's README-declared rather than a LICENSE file, note `license_source: README` in the manifest for audit transparency.

**GOTCHA — skill name in routing:** routing rule uses `jimliu-baoyu-skills-baoyu-slide-deck` — verify this matches harnessed's `<org>-<repo>-<skill>` naming convention; repo owner cases as `JimLiu` on GitHub but `jimliu` in the npx URL — keep manifest lowercase-normalized.

---

## § 3 — testing category

### 3.1 webapp-testing — ⚠️ IT'S AN ANTHROPIC SKILL (not a standalone repo)

| Field | Value |
|-------|-------|
| **Current canonical repo** | `github.com/anthropics/skills` → `skills/webapp-testing/` — confirmed present 2026-05-15. **NOT** a standalone repo, NOT in obra/superpowers (obra's testing dir has only `condition-based-waiting` / `test-driven-development` / `testing-anti-patterns`) |
| **License** | source-available (Anthropic), no LICENSE file — same carve-out as § 1.2 / § 2.1 ⚠️ |
| **Last push** | 2026-05-09 — active |
| **Install method** | **cc-plugin-marketplace**: `/plugin marketplace add anthropics/skills` → install `example-skills@anthropic-agent-skills` plugin (webapp-testing ships in the example-skills bundle). Alt: **npx-skill-installer** `npx skills add anthropics/skills --skill webapp-testing` |
| **Confidence** | **HIGH** (location verified via `gh api` repo tree) |

**GOTCHA — sourcing assumption was wrong:** the user's CLAUDE.md references `webapp-testing` skill (Python e2e, `tests/e2e/*.py`, `python scripts/with_server.py`) as if it were obra/superpowers-adjacent. It is actually an **Anthropic first-party skill** in `anthropics/skills`. Phase 2.3 manifest must source from `anthropics/skills`, same license carve-out applies. This means **3 of the testing/content/design candidates (`frontend-design`, `document-skills`, `webapp-testing`) all share the SAME anthropics license carve-out** — resolve it once as an `anthropics-official` license class.

**GOTCHA — Python dependency:** webapp-testing setup directly calls Python backend (Tortoise ORM / pandas). doctor health check (Phase 2.4) must verify Python + the relevant libs.

### 3.2 playwright-cli — ✅ MICROSOFT OFFICIAL, CLEAN

| Field | Value |
|-------|-------|
| **Current canonical repo** | `github.com/microsoft/playwright-cli` — Microsoft official "CLI for common Playwright actions" |
| **License** | **Apache-2.0** (verified via `gh api`) ✅ whitelist-OK |
| **Last push** | 2026-05-07 — active |
| **Install method** | **npx-skill-installer**: `npx skills add https://github.com/microsoft/playwright-cli --skill playwright-cli`. Note: usable WITHOUT skill install (point agent at the CLI, it self-discovers via `--help`) — but for harnessed manifest consistency, use the skill-installer path |
| **Confidence** | **HIGH** (repo + license + install all verified) |

**GOTCHA — name collision:** there's a legacy `microsoft/playwright-cli` history and multiple community `playwright-skill` repos (`lackeyjb/playwright-skill`, `willmarple/playwright-skill`). The CURRENT official one IS `microsoft/playwright-cli` (Apache-2.0, active 2026-05). harnessed routing rule `ai-explore-debug → playwright-cli` should pin to `microsoft/playwright-cli` explicitly in the manifest `upstream.source`.

**GOTCHA — runtime dep:** requires Playwright + browsers installed (`npx playwright install`). doctor should check.

### 3.3 chrome-devtools-mcp — ✅ CHROME OFFICIAL, CLEAN

| Field | Value |
|-------|-------|
| **Current canonical repo** | `github.com/ChromeDevTools/chrome-devtools-mcp` — npm package `chrome-devtools-mcp` |
| **License** | **Apache-2.0** (verified via `gh api`) ✅ whitelist-OK |
| **Last push** | 2026-05-14 — very active |
| **Install method** | **mcp-stdio-add**: `claude mcp add chrome-devtools --scope user npx chrome-devtools-mcp@latest`. Alt: **cc-plugin-marketplace** `claude plugin marketplace add ChromeDevTools/chrome-devtools-mcp` → `claude plugin install chrome-devtools-mcp` (plugin bundles MCP + helper skills) |
| **Confidence** | **HIGH** (repo + npm + license + install all verified) |

**GOTCHA — two install shapes:** MCP-only (`claude mcp add`) vs plugin (MCP + skills). harnessed's 6-method model maps the MCP-only path to **mcp-stdio-add** and the plugin path to **cc-plugin-marketplace**. Recommend **mcp-stdio-add** for the manifest — it's the minimal, well-defined shape; matches harnessed's "MCP changes must use `claude mcp add/remove` CLI" key decision (ROADMAP). Per CLAUDE.md `claude mcp add/remove` is mandatory anyway.

**GOTCHA — requirements:** Node ≥ 20.19, Chrome stable+. The "connect to real browser" `--browserUrl` / `--autoConnect` flags involve Chrome 136+ remote-debugging security restrictions — keep the manifest install on the DEFAULT isolated-Chrome mode (no flags); document the advanced path in `notice:` only.

**GOTCHA — obra/superpowers-chrome is a different thing:** `obra/superpowers-chrome` (MIT, zero-dep, CDP-direct) is a *separate* candidate that overlaps functionally. Not in scope for the 3 named candidates — but worth a note: if harnessed ever wants a zero-dependency MIT browser-control option, that's it. For v0.2.0 stick with `chrome-devtools-mcp` (it's what CLAUDE.md mandates for perf/a11y/memory diagnostics).

---

## § 4 — v0.2.0 Phase 2.3 Recommendations

### Summary table

| # | Candidate | Current repo | Install method | License | Status |
|---|-----------|-------------|---------------|---------|--------|
| 1 | ui-ux-pro-max | `nextlevelbuilder/ui-ux-pro-max-skill` ⚠️moved | cc-plugin-marketplace | MIT ✅ | **READY** (re-point manifest) |
| 2 | frontend-design | `anthropics/skills` skills/frontend-design | cc-plugin-marketplace | source-available ⚠️ | **READY-with-carve-out** |
| 3 | document-skills (pptx/docx/xlsx/pdf) | `anthropics/skills` (bundled plugin) | cc-plugin-marketplace | source-available ⚠️ | **READY-with-carve-out** |
| 4 | baoyu-slide-deck | `JimLiu/baoyu-skills` | npx-skill-installer | MIT-0 (README-declared) ✅ | **READY** (resolves D1.2.5-10) |
| 5 | webapp-testing | `anthropics/skills` skills/webapp-testing | cc-plugin-marketplace | source-available ⚠️ | **READY-with-carve-out** |
| 6 | playwright-cli | `microsoft/playwright-cli` | npx-skill-installer | Apache-2.0 ✅ | **READY** |
| 7 | chrome-devtools-mcp | `ChromeDevTools/chrome-devtools-mcp` | mcp-stdio-add | Apache-2.0 ✅ | **READY** |

### READY (no blockers — implement in Phase 2.3)

- **ui-ux-pro-max** — but **MUST re-point the manifest** from `midwayjs/midway` git-clone-with-setup → `nextlevelbuilder/ui-ux-pro-max-skill` cc-plugin-marketplace. The phase-1.3 PATH_A+B test validated the *old* path; the new path is cleaner (first-class plugin support). Treat the re-point as a small Phase 2.3 task, not a blocker.
- **baoyu-slide-deck** — **D1.2.5-10 RESOLVED**: license is MIT-0 (registry-declared). Add `MIT-0` to whitelist, update manifest, drop the `warn:` from `chinese-content-deck` routing rule.
- **playwright-cli** — `microsoft/playwright-cli`, Apache-2.0, npx-skill-installer. Clean.
- **chrome-devtools-mcp** — `ChromeDevTools/chrome-devtools-mcp`, Apache-2.0, mcp-stdio-add. Clean. Matches CLAUDE.md mandate.

### READY-with-carve-out (one shared prerequisite)

- **frontend-design + document-skills + webapp-testing** — all 3 come from `anthropics/skills`, all 3 are source-available with **no LICENSE file** → fail the current MIT/Apache/BSD/ISC/0BSD whitelist. **Single prerequisite for Phase 2.3:** add an explicit **`anthropics-official` license class** to harnessed's license policy — a documented, audited carve-out (harnessed already routes anthropics skills in decision_rules.yaml, so the project has implicitly accepted them). This is a policy decision, not research-blocked. Once done, all 3 are READY.

### SHOULD-WAIT / flags

- **Nothing should-wait.** All 7 candidates have verified-current repos and working install paths as of 2026-05-15.
- **Flag — manifest schema impact:** `document-skills` is **one plugin = 4 skills**, not 4 installable units. decision_rules.yaml rule `pptx-file-task` references `anthropics-skills-pptx` as if atomic. Phase 2.3 must model bundle-installs in the manifest schema (one manifest → multiple exposed skills). This may need an `ManifestSpec` field — coordinate with the Phase 2.1 installer work.
- **Flag — moved upstreams since 2026-05-11:** `ui-ux-pro-max` (midwayjs/midway → nextlevelbuilder dedicated repo) and `webapp-testing` / `frontend-design` (sourcing assumption was non-anthropic → confirmed anthropics first-party). Both are *improvements* (cleaner sources) but require manifest edits.
- **License-policy action items for Phase 2.3:** (1) add `MIT-0` to whitelist, (2) add `anthropics-official` license class / carve-out, (3) add `license_source: README` audit field for README-declared licenses (baoyu).

### Confidence roll-up

| Area | Confidence | Note |
|------|-----------|------|
| design (ui-ux-pro-max, frontend-design) | HIGH | repos + licenses + install all gh-verified |
| content (document-skills, baoyu) | HIGH / MEDIUM | anthropics HIGH; baoyu MEDIUM (license README-declared) |
| testing (webapp-testing, playwright-cli, chrome-devtools-mcp) | HIGH | all 3 repos + licenses gh-verified |
| install-method mapping | HIGH | each mapped to exactly one of harnessed's 6 methods |

> **Tool-call note:** Tavily/Exa MCP not available in this session; used WebSearch for discovery + `gh api` for authoritative license/metadata/repo-tree verification. License facts (MIT, Apache-2.0, MIT-0, source-available) are `gh api`-verified or repo-README-verified — HIGH confidence. The `anthropics/skills` "no LICENSE file" is confirmed via `gh api .../license` → 404.
