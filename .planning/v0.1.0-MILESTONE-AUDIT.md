---
milestone: 0.1.0
audited: 2026-05-15
status: passed
status_history:
  - "2026-05-15 initial: gaps_found (3 unsatisfied: R2.1/R4.3/R4.4 — ADR 0006 traceability 未回填)"
  - "2026-05-15 reconciled: passed — REQUIREMENTS.md traceability 回填 ADR 0006 re-scope (path A);0 unsatisfied"
reconciliation: "Path A applied — REQUIREMENTS.md traceability 表 + R1.3/R2.1/R3.4/R4.1/R4.3/R4.4 条目回填 ADR 0006 re-scope。R2.1 Re-scoped / R4.3+R4.4 Superseded / R4.1 Adjusted / R1.3+R3.4 Partial(有明确 phase 去向)。0 unsatisfied 残留。"
scores:
  requirements: 20/20 reconciled (14 Done + 2 Partial + 2 Superseded + 1 Re-scoped + 1 Adjusted — 0 unsatisfied)
  phases: 6/6 self-report passed
  integration: 5/5 wired seams OK (1 unwired capability — DAG resolver no production caller, R1.3 Partial 已记)
  flows: 2/3 fully wired (research E2E dry-run-only by design — R3.4 Partial 已记)
gaps:
  requirements:
    - id: "R2.1"
      status: "unsatisfied"
      phase: "1.2 (命令骨架计划但未实装)"
      verification_status: "not surfaced by phase 1.2 VERIFICATION.md"
      evidence: "无 `harnessed setup` 子命令 — cli.ts 注册 install/install-base/doctor/audit/rollback/status/backup-list/gc/research 9 个,无 registerSetup。版本自检 + dry-run + confirm + backup 能力散落在各 installer 内,未聚合为 setup 命令。"
    - id: "R4.3"
      status: "unsatisfied"
      phase: "(无 phase 实装)"
      verification_status: "orphaned — 不在任何 phase VERIFICATION.md"
      evidence: "src/ 无 hook 子系统。grep `hook` 仅命中注释。无 B 层 soft_hint 降级代码路径。"
    - id: "R4.4"
      status: "unsatisfied"
      phase: "(无 phase 实装)"
      verification_status: "orphaned — 不在任何 phase VERIFICATION.md"
      evidence: "src/ 无 hook 脚本;无 install 时 print 全文 / 1-key uninstall for hooks。"
    - id: "R1.3"
      status: "partial"
      phase: "1.5"
      verification_status: "passed (D1) — 但 REQ 原文要求未完全满足"
      evidence: "dag.ts Kahn + cycle detect 实装 + 14 unit cell,wired into engine.route 签名。但:(1) 无 manifest dependency 字段(depends_on/install_after)— spec.ts 无;(2) 无 production caller — install-base.ts 用 alphabetical glob 非拓扑序;(3) 循环依赖 reject 在 resolveDag 运行时,非 REQ 原文要求的 schema 校验阶段。"
    - id: "R3.4"
      status: "partial"
      phase: "1.4"
      verification_status: "passed (C4) — v0.1 dry-run 范围"
      evidence: "routing 选源 OK,但 segment-by-source merge 模块不存在 — 随真实 spawn (F40-2 SDK dep) 一起 deferred v0.2+。"
    - id: "R4.1"
      status: "partial"
      phase: "1.3 / 1.5"
      verification_status: "passed (B4/D3) — 但 SSOT 形态与 REQ 原文不符"
      evidence: "decision_rules.yaml v2 + Ajv 校验 loader 存在。但 REQ 原文要求 routing/*.md SSOT + B 层/C 层共享 yaml frontmatter — 项目无 routing/*.md 文件,yaml 是 SSOT 不是 .md;B/C 双层结构被 ADR 0006 main-process-driven engine 取代。"
  integration:
    - "DAG resolver (dag.ts) wired into engine.route 签名但无 production caller — opts.dagNodes 无任何代码传入;capability 是 interface seam 非 active data flow"
  flows:
    - "research workflow E2E: dry-run 路径完整,--apply 真实 spawn 是 placeholder throw (F40-2 SDK dep deferred v0.2+) — 设计内 deferred"
tech_debt:
  - phase: "1.2"
    items:
      - "4 installer methods (cc-plugin-marketplace / git-clone-with-setup / npx-skill-installer / mcp-http-add) → phase 2.1"
      - "CLAUDE.md merge engine → phase 2.2"
      - "6-month stale check → phase 2.4"
      - "--force flag for idempotent_check → phase 2.1+"
  - phase: "1.4"
    items:
      - "@anthropic-ai/claude-agent-sdk runtime dep (blocks real query() spawn) → v0.2+"
      - "F42 array semantic match → 1.5 partial done"
  - phase: "1.5"
    items:
      - "Semantic Router L2 = return null stub (real ML embedding → v0.2+;lib/embedding.ts 17L placeholder interface)"
      - "ralph-loop full integration → phase 2.2"
      - "--add-plugin ralph-wiggum 切换 → phase 2.1+"
  - phase: "cross-cutting"
    items:
      - "Phase 1.5 sister review H3/H4/M1 + transparency verify checklist → phase 2.0 Wave 0 (已记 STATE.md)"
---

# v0.1.0 Milestone Audit — Gaps Found

> Audited: 2026-05-15 | Auditor: gsd-audit-milestone (adapted to project structure) + gsd-integration-checker
> Milestone scope: 6 phases (1.1 / 1.2 / 1.2.5 / 1.3 / 1.4 / 1.5)
> Build state: `pnpm typecheck` clean / `pnpm test` 318 passed + 3 skipped / CI 三平台全绿

## TL;DR

v0.1.0 工程质量高 — 6/6 phase 自验 8/8 acceptance bar 全 passed,build 三平台全绿,318+3 tests,9 ADR baseline tag A7 守恒。**已 wired 的跨 phase 集成全部端到端正确**。

但里程碑审计发现 **per-phase verification 未浮现的 requirements-vs-reality 偏差** — 核心是 **ADR 0006 架构修订(phase 1.2.5)后,REQUIREMENTS.md traceability 表从未回填重新对齐**:

- **3 个 v0.1-tagged requirements UNSATISFIED**:R2.1(`harnessed setup` 命令从未实装)、R4.3 + R4.4(hook 子系统在 src/ 完全不存在)
- **3 个 PARTIAL**:R1.3(DAG resolver 实装但无 production caller + cycle reject 不在 schema 层)、R3.4(merge 随 SDK deferred)、R4.1(SSOT 是 yaml 非 routing/*.md,B/C 双层被 main-process engine 取代)

按 audit-milestone FAIL gate:任何 unsatisfied requirement 强制 `gaps_found`。

**关键判断**:这些 gap 大概率是 **ADR 0006 架构修订的 traceability 残留**,不是"忘了做"。ADR 0006 把项目从"装配主义包管理器"重定位为"完整三层栈方法论的可执行 engine" — C 层 UserPromptSubmit hook 被 main-process-driven routing engine 取代(D1.2.5-3),`setup` 单体命令被 `install-base` + per-installer dry-run/confirm/backup 取代。但 ROADMAP v3 重排时 **REQUIREMENTS.md 的 traceability 表没同步更新**,R2.1/R4.3/R4.4 仍标 v0.1 Pending。

## § 1 Per-Phase Status

| Phase | 自验状态 | Critical Gaps | Tech Debt / Deferred |
|-------|---------|---------------|---------------------|
| 1.1 schema v1 frozen | passed (A1-A8 8/8) | none | F5/F16/F20 ~3 deferred |
| 1.2 installers + setup/doctor 骨架 | passed (B1'-B9' 9/9) | none | 8 deferred(4 installer + CLAUDE.md merge + stale + --force) |
| 1.2.5 architecture revision | shipped (discuss-phase 8 支柱 lock) | none | docs-only;ADR 0006 重定位 |
| 1.3 base profile + categorization | passed (B1-B8 8/8) | none | F38 perf relax + H1a/M2 deferred |
| 1.4 routing engine v1 | passed (C1-C8 8/8) | none | F40-2 SDK dep + F42 array match deferred |
| 1.5 DAG + Semantic L2 stub + 23 招式 | passed (D1-D8 8/8) | none | Semantic L2 = stub;SDK / ralph-wiggum / array match → v0.2+ |

6/6 phase 自验全 passed,无 phase 报 critical gap。**审计层发现的 gap 是 per-phase verification 范围之外的里程碑级 traceability 偏差**。

## § 2 Cross-Phase Integration

| Seam | 状态 | Evidence |
|------|------|----------|
| manifest schema → installers + routing | ✅ OK | spec.ts SpecSchema → validate.ts → runInstall;category/install_type/phase/triggers 全在 schema |
| installers → CLI subcommands | ✅ OK | runInstall dispatched by install.ts + install-base.ts;6 method dispatch table(2 real + 4 placeholder) |
| routing engine → rules/dag/semantic/factory/ralphLoop/skillInstall | ✅ OK (wired) | engine.ts import + 调用全部组件 |
| CLI root → 9 subcommands | ✅ OK | cli.ts 注册 install/install-base/research/doctor/audit/rollback/status/backup-list/gc |
| barrel routing/index.ts | ✅ OK | 全组件 re-export;research.ts 经 barrel 消费 |
| **DAG resolver → production caller** | ⚠️ UNWIRED | resolveDag 仅在 engine.runRouting 收 opts.dagNodes 时可达 — **无任何代码传 dagNodes**;无 manifest depends_on 字段;install-base.ts 用 sorted glob 非拓扑序 |

Build:`pnpm typecheck` clean / `pnpm test` 318 passed + 3 skipped / CI 三平台全绿。

## § 3 E2E Flows

| Flow | 状态 | 说明 |
|------|------|------|
| research workflow (`harnessed research`) | ⚠️ dry-run only | CLI → runRouting → DAG pre-check → arbitrate L1 → semanticMatch L2 stub null → L3 fallback → createAgent → ralphLoopWrap → isComplete。**--apply 真实 spawn 是 placeholder throw**(F40-2 SDK dep deferred v0.2+)— dry-run 完整,real spawn 设计内未 ship |
| install-base (`harnessed install-base`) | ✅ wired | CLI → glob manifests → validateManifestFile → runInstall dispatch → 2 real + 4 placeholder("deferred phase 2.1" skipped)。**无 DAG resolve step** — install 顺序是 alphabetical glob 非拓扑序。v0.1 功能完整 |
| routing decision (query → engine.route) | ✅ wired | resolveDag pre-check → arbitrate L1 → semanticRouter.match L2(stub:return matched:false)→ fallbackSupervisor L3。三层兜底契约完整 |

## § 4 Requirements Coverage (v0.1 milestone — 20 reqs)

| REQ | 状态 | Evidence | Phase |
|-----|------|----------|-------|
| R1.1 schema v1 frozen | ✅ satisfied | spec.ts + metadata.ts 全字段;ADR 0001 baseline-tag + CI A7 守恒 | 1.1 |
| R1.2 4 type + cli-npm/mcp-stdio | ✅ satisfied | installers/index.ts npm-cli + mcp-stdio-add real,4 placeholder | 1.2 |
| R1.3 DAG resolver Day 1 | ⚠️ **partial** | dag.ts Kahn + cycle detect + 14 cell,但无 manifest dep 字段 + 无 production caller + cycle reject 不在 schema 层 | 1.5 |
| R1.4 strict JSON Schema validation | ✅ satisfied | additionalProperties:false + Ajv strict + negative tests | 1.1 |
| R2.1 `harnessed setup` self-check | ❌ **unsatisfied** | 无 setup 子命令;能力散落 installer 内未聚合 | — |
| R2.2 `harnessed doctor` (v0.1 basics) | ✅ satisfied | doctor.ts Node≥22 + MCP scope + jq + Win bash 4 check | 1.2 |
| R3.1 multi-source research routing | ✅ satisfied (v0.1 dry-run) | research.ts → routing engine;decision_rules.yaml search category | 1.4 |
| R3.2 MCP installer --scope project | ✅ satisfied | mcpStdioAdd.ts hardcode --scope project;doctor 检查 | 1.2 |
| R3.3 Windows npx cmd /c | ✅ satisfied | spawn.ts:76 + mcpStdioAdd.ts:46 win32 分支 | 1.2 |
| R3.4 multi-source merge v1 | ⚠️ partial | routing 选源 OK,segment-by-source merge 模块不存在 — 随 SDK deferred | 1.4 |
| R4.1 routing/*.md SSOT schema | ⚠️ partial | decision_rules.yaml v2 + Ajv loader,但无 routing/*.md;B/C 双层被 main-process engine 取代 | 1.3/1.5 |
| R4.3 hook failure degradation | ❌ **unsatisfied** | src/ 无 hook 子系统;无 B 层 soft_hint 降级路径 | — |
| R4.4 hook security constraints | ❌ **unsatisfied** | 无 hook 脚本;无 install 时 print / 1-key uninstall | — |
| R5.1 CI matrix mac+linux+win | ✅ satisfied | CI 三平台全绿(phase 1.4/1.5 VERIFICATION 实证) | 1.1+ |
| R5.2 npx cross-platform wrapper | ✅ satisfied | spawn.ts platform 分支 | 1.2 |
| R9.1 single-maintainer mitigation | ✅ satisfied | YAML 数据格式 + 9 ADR baseline-tag + CI A7 automation + signed_by | all |
| R9.2 transparency | ✅ satisfied | matched_rule 打印;ADR for 非常规决策;placeholder 返回 explicit "deferred" 非静默 skip | all |
| R9.3 security | ✅ satisfied | install dry-run default + diff + confirm;checkCmdString shell-escape gate;license whitelist | 1.1/1.2 |
| R9.4 upstream breaking change (v0.1 basics) | ✅ satisfied | upstream_health 字段(stability/last_check/fallback_action)| 1.1 |
| R9.5 scope creep defense | ✅ satisfied | RX.1-RX.6 reject list locked;ADR 0009 §9 边界 | all |

**14 satisfied / 3 partial (R1.3/R3.4/R4.1) / 3 unsatisfied (R2.1/R4.3/R4.4)**

## § 5 Root Cause — ADR 0006 架构修订 traceability 未回填

3 个 unsatisfied + 部分 partial 的共同根因:**phase 1.2.5 ADR 0006 架构修订把项目重定位,但 REQUIREMENTS.md 的 traceability 表(line 321-343)从未同步更新**:

| REQ | 原 v0.1 设计 | ADR 0006 后实际 | traceability 现状 |
|-----|-------------|----------------|------------------|
| R2.1 `harnessed setup` | 单体 setup 命令(版本自检 + dry-run + confirm + backup)| `install-base` + per-installer dry-run/confirm/backup 拆分 | 仍标 v0.1 Pending — 未记 re-scope |
| R4.3 hook 降级 | C 层 UserPromptSubmit hook 降级回 B 层 | main-process-driven routing engine(D1.2.5-3)取代 hook 机制 | 仍标 v0.1 Pending — 未记 superseded |
| R4.4 hook 安全 | hook 脚本审计 + print + 1-key uninstall | 同上 — 无 hook 子系统 | 仍标 v0.1 Pending — 未记 superseded |
| R4.1 routing/*.md SSOT | routing/*.md + B/C 双层 yaml frontmatter | decision_rules.yaml v2 单一 SSOT(main-process engine 消费)| 标 v0.1 — SSOT 形态已变 |

这正是 phase 1.5 sister review H1 强调的 **"文档即真相" 护城河** 在 traceability 层的同类问题 — 架构变了,需求追溯表没跟上。

## § 6 Recommended Resolution

3 条路径(milestone close 前需决策):

**A. Reconcile REQUIREMENTS.md traceability(推荐)** — 不是"补做",是"对齐真相":
- R2.1 → 标 "re-scoped by ADR 0006:setup 单体命令拆分为 install-base + per-installer dry-run/confirm/backup;能力已 v0.1 ship,命令形态变更"
- R4.3 + R4.4 → 标 "superseded by ADR 0006:C 层 hook 机制被 main-process-driven routing engine 取代(D1.2.5-3);hook 子系统不再是 v0.1 范围"
- R4.1 → 标 "adjusted by ADR 0006:SSOT 从 routing/*.md B/C 双层 改为 decision_rules.yaml v2 单一 SSOT"
- R1.3 → 标 "v0.1 partial:Kahn resolver + cycle detect ship;manifest depends_on 字段 + production caller + schema-层 cycle reject 推 phase 2.x(execute-task workflow 真实多 manifest 依赖图出现时)"
- R3.4 → 标 "v0.1 partial:随 F40-2 SDK dep deferred v0.2+"
- 估 ~20 min,docs-only,然后 milestone 可正常 close(passed)

**B. `/gsd-plan-milestone-gaps`** — 若认为 setup/hook 是真实缺失的 v0.1 能力(而非 re-scoped),创建 gap-closure phase 实装。**不推荐** — ADR 0006 已明确 re-scope,实装 hook 子系统违反 ADR 0006 的 main-process-driven 决策。

**C. Proceed anyway(accept as known gaps)** — milestone close 时在 archive 记 `### Known Gaps`,R2.1/R4.3/R4.4 列为 ADR 0006 re-scope 残留,不阻塞 v0.2.0。

---

## § 7 Reconciliation Applied (2026-05-15 — Path A)

用户选择 **Path A — Reconcile REQUIREMENTS.md traceability**。已回填:

- **traceability 表**:20 个 v0.1 需求逐条标注 status + ADR 0006 备注;加 "v0.1.0 milestone 结算" 汇总行
- **R2.1** → 🔄 Re-scoped(install-base + per-installer 拆分;version staleness check 推 phase 2.4)
- **R4.3 + R4.4** → 🔄 Superseded(main-process-driven routing engine 取代 hook 机制,D1.2.5-3;security gate 由 checkCmdString + ADR 0004 6 契约承接)
- **R4.1** → 🔧 Adjusted(decision_rules.yaml v2 单一 SSOT 取代 routing/*.md B/C 双层)
- **R1.3** → ⚠️ Partial(Kahn + cycle detect ship;depends_on 字段 + caller + schema-层 reject 推 phase 2.x)
- **R3.4** → ⚠️ Partial(选源 OK;merge 随 F40-2 SDK dep deferred v0.2+)
- REQUIREMENTS.md 头部加 "ADR 0006 re-scope 说明" 段

**0 unsatisfied 残留** — Partial/Re-scoped/Superseded/Adjusted 均有明确去向。

---

**Verdict: passed (post-reconciliation)** — 工程质量高(6/6 phase 8/8 + build 三平台全绿 + 318+3 tests + 9 ADR A7 守恒);初始 gaps_found 的 3 unsatisfied 是 ADR 0006 架构修订的 traceability 残留,Path A reconcile 后归零。milestone v0.1.0 可正常 close。
