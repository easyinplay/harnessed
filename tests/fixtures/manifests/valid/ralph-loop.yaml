# yaml-language-server: $schema=../../schemas/manifest.v1.schema.json
#
# SCHEMA FIXTURE ONLY — harnessed does NOT ship this manifest. `manifests/tools/
# ralph-loop.yaml` was deleted in 4.36.0 when the completion guarantee was
# internalized (`harnessed checkpoint complete` / `checkpoint fail`); this file
# stays purely as the `type: cc-plugin` × `install_type: skill` ×
# `method: cc-plugin-marketplace` shape under tests/fixtures/manifests/valid/,
# which is loaded by directory glob (tests/unit/manifest-validate.fixtures.test.ts
# + the perf suite's ≥10-fixture floor). Same precedent as karpathy-skills.yaml,
# whose manifest was likewise dropped while its fixture stayed.
apiVersion: harnessed/v1
kind: Manifest
metadata:
  name: ralph-loop
  display_name: ralph-loop
  description: Third-party sub-task delivery loop (Anthropic-official plugin). Schema fixture only — not shipped by harnessed.
  upstream:
    source: ralph-loop
    homepage: https://github.com/anthropics/claude-plugins-official/tree/main/plugins/ralph-loop
    repository: https://github.com/anthropics/claude-plugins-official.git
    license: MIT
    notice: ralph-loop maintained by Anthropic in claude-plugins-official. Retained here only as a schema fixture; harnessed no longer installs or depends on it.
spec:
  type: cc-plugin
  component_type: command
  category: engineering
  install_type: skill
  install:
    method: cc-plugin-marketplace
    cmd: "/plugin install ralph-loop@claude-plugins-official"
    git_ref: 45896c8f2fe68e68ca2a8e3554d4441f48475431
    idempotent_check: "/plugin list | grep -q ralph-loop"
  verify:
    cmd: "/plugin list | grep -q ralph-loop"
    timeout_ms: 5000
  uninstall:
    cmd: "/plugin uninstall ralph-loop@claude-plugins-official"
  upstream_health:
    stability: beta
    last_check: "2026-05-11"
    last_known_good_version: main
    fallback_action: warn
  signed_by: easyinplay
  platforms:
    - linux
    - darwin
    - win32
