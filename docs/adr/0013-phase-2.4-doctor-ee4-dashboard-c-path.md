# ADR 0013: Phase 2.4 — doctor MIN 5 check + EE-4 plan-checker 量化 + dashboard C 路径 FULL absorb + README CI counter gate + audit 完整版 + Win sentinel (9 章节 errata 合并)

## Status

**Draft (phase 2.4 W0 sketch — 2026-05-16; → Accepted (Wave 6 fill-out at phase 2.4 ship))** — phase 2.4 plan-phase Wave 0 sketch (T0.2 9 章节 skeleton draft) → Wave 6 fill-out (T6.1 detailed expand) → Accepted at phase 2.4 ship。

> Phase 2.4 是 v0.2.0 milestone **末 phase (4/4)**，把 v0.2.0 alpha cycle 收尾装配为 **doctor 健康检查完整版 + EE-4 plan-checker 4 维量化阈值 absorb + dashboard C 路径 3 子功能 FULL absorb + README CI counter gate B 路径 + audit 完整版扩 + ralph-loop Windows native 兼容 sentinel**。5 大主题 sister cluster 一次 ship，作为 v0.2.0 milestone close 的最后一块拼图 — 沿袭 ADR 0008 / 0009 / 0010 / 0011 / 0012 多决策合并 errata 模式 (B-35 + B-36 lock)。**本 ADR Wave 0 阶段仅 sketch (9 章节 skeleton only)；Wave 6 T6.1 详细 fill-out，每章节 expand 至与 ADR 0011 / 0012 9 章节同等深度**。

## Context

Phase 2.4 把 v0.2.0 收尾末 phase 装配为 doctor 健康检查完整版 ship + EE-4 plan-checker 4 维量化阈值 ABSORB + dashboard C 路径 3 子功能 FULL absorb + README CI counter gate B 路径 + ralph-loop Windows native 兼容验收 sentinel + audit 完整版扩。5 大主题 sister cluster 一次 ship，作为 v0.2.0 milestone 4/4 close。

并办 STATE.md L601+ Phase 2.4 Prereq Notes 5 项 backlog 一次根治 — H3+T4 README counter integrity CI gate (sister review root-cause-class 第 3 次复发根治) / M1 RETRO dashboard polish round 1 entry / M2 schemaVersion 7 surface long-tail land 1 consumer / T2 dashboard 进 doctor 完整版 absorb 决策 / T3 v0.3.0 backlog 预启动注记 — 沿袭 Phase 2.1 / 2.2 / 2.3 Wave 0 一次根治模式。

### A7 守恒约束 (ADR 0001-0012 main body 不可改)

phase 2.4 沿袭 ADR 0003 / 0005 / 0007 / 0008 / 0009 / 0010 / 0011 / 0012 errata 风格 — **不动 ADR 0001-0012 main body** (A7 守恒)。 baseline tag iterate 12 → 13 (Wave 6 T6.2 ship 时 push `adr-0013-accepted` tag，ci.yml A7 step iter `1-12` → `1-0013`)。 本 ADR 0013 起 phase 2.4 ship 时刻 frozen；v0.2.4+ 演化走 ADR 0014+ errata。

## Decisions

### 1. doctor 5 check MIN scope (B-01 ~ B-07 / D-01) — 152→~210L 容忍超 hard limit 5%

`src/cli/doctor.ts` 现 4 check (Node ≥ 22 / MCP scope / jq present / Win bash flavor) 扩到 5 check，加 **#5 origin URL drift check** (warn mode 容忍 fork 合法用例)。 升级 `CheckResult` interface 从 `{ok: boolean}` 到 `{status: 'pass' | 'warn' | 'fail'}` 三档；加 `--json` flag 支持 machine-readable output；exit code policy: warn = 0 (advisory)，fail = 1 (blocking) per B-06。 helper `src/cli/lib/origin-check.ts` (≤ 80L) sister-share 给 audit 完整版 (allowFork 二态切换)。

> **Wave 6 T6.1 fill expansion**: 详 implementation breakdown + rationale tree + sister precedent table (Phase 2.2 dual-signal 4-layer pattern analog)。

### 2. EE-4 plan-review-schema.yaml 4 维量化阈值 SSOT (B-08 ~ B-15 / D-02)

EE-4 (omo intel ⭐⭐ — Phase 2.3 推 Phase 2.4 absorb) 装配 `schemas/plan-review-schema.yaml` 4 维 SSOT：file_refs (≥ 1.0 ratio) / source_refs (≥ 0.8 ratio) / acceptance (≥ 0.9 ratio) / weasel (== 0 后白名单 carve-out)。 `scripts/run-plan-checker.mjs` walker 跑 task_plan.md 输出 PASS/WARNING/BLOCKER verdict。 project-local agent override 允许 `.harnessed/agents/plan-checker.md` 在 repo-local 自定义 (overrides 全局 `~/.claude/agents/`)。

> **Wave 6 T6.1 fill expansion**: spike outcome (T2.0 phase-2.3 + phase-2.2 task_plan baseline 4 维 score) + 阈值校准 evidence + walker pseudocode + project-local override 优先级树。

### 3. EE-4 BLOCKER auto-rerun DOWN-SCOPE (B-12 / O1) — CI fail + manual rerun (NOT auto-spawn)

EE-4 BLOCKER verdict 当前 down-scope 为 CI fail + manual rerun (NOT auto-spawn subagent loop)。 auto-spawn rerun loop 推 v0.3.0 feature (依赖 ralph-loop full integration 稳态 + subagent budget gating)。 Phase 2.4 ship 时 BLOCKER 只产生 CI red + structured error report，开发者手动修正 + 重 push 触发。

> **Wave 6 T6.1 fill expansion**: down-scope rationale + v0.3.0 实施 plan 锚定 (closure infra 三件套 reference + budget gating contract)。

### 4. dashboard C 路径 3 子功能 FULL absorb (B-18 ~ B-22 / D-04)

`.planning/intel/dashboard-handoff-2026-05-16.md` § 3 C 路径 advisory FULL absorb 进 Phase 2.4 Wave 3：

- **§ 3.1 SessionStart hook auto-install** — `harnessed install dashboard-watcher` (新 method) 注册 SessionStart hook 自动 spawn dashboard server on Claude Code session start
- **§ 3.2 STATE.md SSE watcher** — `scripts/dashboard.mjs` 加 SSE endpoint `/events` (zero-dep + native EventSource 自动 reconnect)，前端 mtime polling 替换为 push-driven 更新
- **§ 3.3 多项目支持** — `dashboard.mjs` 加 `--project-dir <path>` flag + 前端 sidebar project switcher，跨 repo dashboard 单 server 多 project 视图

并 `cc-hook-add` 装配第 7 install method (sister Phase 2.1 6 method dispatch 扩到 7)。 3 子功能并行 ship in Wave 3，3 atomic sub-task。

> **Wave 6 T6.1 fill expansion**: 详 cc-hook schema 3 处 enum 加 (InstallType + TypeEnum + install.method) + SSE 替 WebSocket rationale (B-23 / D2.4-9) zero-dep + 单向 push + native auto-reconnect。

### 5. SSE 替 WebSocket rationale (B-23 / D2.4-9) — zero-dep + 单向 push + native auto-reconnect

dashboard C 路径 § 3.2 selection SSE over WebSocket: (a) zero-dep 沿袭 dashboard.mjs Node-stdlib-only 哲学，无需 ws / socket.io 依赖；(b) STATE.md → frontend 单向 push 语义 SSE 天然 match (WebSocket bidirectional 是 over-engineering)；(c) native browser EventSource 自动 reconnect (失连续传 lastEventId) 替手写 ws reconnect helper。

> **Wave 6 T6.1 fill expansion**: 三层 rationale 详 + production SSE corner cases (proxy buffering / Cloudflare timeout) sister handoff doc。

### 6. cc-hook 3 处 schema enum 加 (B-22 — R2 critical finding 修正 KICKOFF wording)

`schemas/manifest.v1.schema.json` + `src/manifest/schema/spec.ts` 三处 enum 同步 additive add `cc-hook-add` (沿袭 ADR 0007 categorization 3 字段 errata pattern)：
- **InstallType enum** 加 `cc-hook` (sister installType: `cc-plugin` / `mcp-stdio` / `cli-npm` / `bundle-install` / ...)
- **TypeEnum** 加 `cc-hook` (sister type: `tool` / `skill-pack` / ...)
- **install.method enum** 加 `cc-hook-add` (sister method: `cc-plugin-marketplace-add` / `mcp-stdio-add` / `npx-skill-installer` / ...)

R2 critical finding 修正 KICKOFF wording 把原 "新 installer" 表述明确改为 "新 install method" + "现有 installType 的 1:N 扩"。

> **Wave 6 T6.1 fill expansion**: TypeBox additive schema diff snippet + 3 处 enum 同步 verify gate (Phase 2.2 RETROSPECTIVE Lesson 6 schema-vs-source drift 防护沿袭)。

### 7. README CI counter gate B 路径 (B-16 + B-17 / D-03) — 三计数一致 grep -cE + Wave 0 pre-flight calibration

`.github/workflows/ci.yml` 加 `README counter integrity gate` step ~15L yaml。 沿袭 Phase 2.2 T0.4 freshness gate pattern + Phase 2.3 W0 T0.5 schemaVersion grep gate sister，三计数一致才 PASS：
- `SHIPPED` = `^- \*\*Phase 2\.[1-9] shipped\*\* ✅` 行数 (line-start + bold + Phase 2.X 限当前 v0.2.0 cycle 排除 1.X 历史)
- `BARS` = `^- \*\*Acceptance bar [A-Z]1-[A-Z]8 8/8 \(Phase 2\.[1-9]\)\*\* ✅` 行数
- `L44` = L44 行 "Phase 2.1+2.2+2.3+2.4/2.4 = N/4 完成" enumeration numerator

三者必须 align (`SHIPPED == BARS == L44`)，否则 CI exit 1。 **B1 plan-check fix path** (regex 精度提升 + pre-flight FIX) — 实测当前 (Phase 2.3 ship 后) SHIPPED=3, BARS=3, L44=3 三者 align ✅ (无 drift)，T0.3 直接 ship CI gate，无 pre-flight FIX 必需。

> **Wave 6 T6.1 fill expansion**: root-cause-class 第 3 次复发原因 + 候选 (a) STATE.md 单 SSOT 收敛 vs (b) CI gate B 路径选定 rationale + future Phase 2.5+ 自动 SHIPPED entry append helper 设想。

### 8. audit 完整版扩 + Win sentinel (B-28 + B-29 + B-30)

`src/cli/audit.ts` 现 125L 基线 → 扩 origin URL drift hard-fail check (sister-share `src/cli/lib/origin-check.ts` with `allowFork: false`) + 跨 OS 调用 path normalization helper split (≤ 60L sister helper)。 audit 是 NOT NEW (R2 critical finding 修正原"新 audit 命令"为"扩 audit.ts 现有命令")。 Win sentinel 5 fixture MIN scope (sister Phase 2.3 T1.3 Win pwsh provenance sentinel 模式) 覆盖 ralph-loop 跨平台兼容 corner case (Git Bash / cmd.exe / pwsh / WSL / cygwin)。

> **Wave 6 T6.1 fill expansion**: audit 完整版 diff snippet + 5 sentinel fixture matrix table + Win bash flavor detection prior-art (Phase 1.1 H4 dual-layer mock-bin pattern 复用)。

### 9. Wave 0 backlog 5 项一次根治 (B-32 + B-33 + STATE.md L601+ Prereq Notes)

Wave 0 一次根治 STATE.md L601+ Phase 2.4 Prereq Notes 5 项 backlog (沿袭 Phase 2.2 / 2.3 Wave 0 atomic-batch 模式)：
- **H3 + T4 README counter integrity CI gate** (root-cause-class 第 3 次复发根治) — T0.3 ship B 路径 ~15L CI step
- **M1 dashboard polish round 1 RETRO entry** (跨 phase tooling reference) — T0.6 加 RETROSPECTIVE 历史 cluster 一句
- **M2 schemaVersion 7 surface long-tail** (current consumer = 2，每 phase land 1) — T0.4 `scripts/check-provenance.mjs` land 1 consumer (3rd surface)
- **T2 dashboard 进 doctor 完整版 absorb 决策** — Phase 2.4 discuss-phase 已决断 absorb (D-04 FULL absorb)，Wave 3 实施
- **T3 v0.3.0 backlog 预启动注记** — T0.5 加 STATE.md `## v0.3.0 启动 prereq` 节 (closure infra 三件套 + plan-feature workflow + checkpoint cadence + 路由命中率验收 + gstack 前缀探测 + EE-4 BLOCKER auto-spawn rerun 推 v0.3.0)

并 R2.4.1~R2.4.7 7 子 requirement ID expand verify (B2 plan-check fix path) + tests/installers + tests/scripts 目录预创建 (W1 plan-check fix path)。

> **Wave 6 T6.1 fill expansion**: 5 项 backlog 详 absorb evidence + each commit hash + sister cross-phase 沿袭 evidence table。

## A7 Conservation

ADR 0001-0012 main body untouched; baseline tag iterate `1-12 → 1-0013`; ci.yml A7 step iter `1-12 → 1-0013` (Wave 6 T6.2 implementation);
docs/AGENT-DEFINITION-FACTORY-CONTRACT.md + docs/INSTALLER-CONTRACT.md main body 不动 (B-37)。

## Consequences

> **Wave 6 T6.1 fill expansion**: positive consequence (v0.2.0 milestone 4/4 close + doctor 完整版 ship + EE-4 量化 absorb + dashboard C 路径 FULL ship) + negative consequence (EE-4 BLOCKER auto-rerun 推 v0.3.0 短期 manual cost + cc-hook 第 7 method 增 dispatch surface 复杂度) + mitigation strategy。

## Compliance

> **Wave 6 T6.1 fill expansion**: F1-F8 8/8 acceptance bar verify evidence + 3-OS CI 全绿 evidence (run-id reference) + ADR 0001-0012 main body diff wc = 0 verify。

## Errata-path note

> **Wave 6 T6.1 fill expansion**: Phase 2.4 ship 起 frozen；v0.2.4+ 演化走 ADR 0014+ errata；本 ADR 0013 是 v0.2.0 末 ADR (沿袭 ADR 0009 / 0010 / 0011 / 0012 errata 模式)。

## Adoption-confirmation

> **Wave 6 T6.1 fill expansion**: doctor --json output sample + EE-4 walker pseudocode + dashboard C 路径 3 子功能 smoke test fixture + cc-hook schema 3 enum verify gate + README counter gate green run reference。

## References

- `.planning/phase-2.4/{KICKOFF,2.4-CONTEXT,2.4-DISCUSSION-LOG,PATTERNS,RESEARCH,ASSUMPTIONS,PLAN,task_plan,PLAN-CHECK,PLAN-CHECK-DELTA}.md`
- `.planning/intel/omc-comparison.md` L74-82 (EE-4 4 维原型) + L286 (EE-4 PENDING)
- `.planning/intel/dashboard-handoff-2026-05-16.md` § 3 (C 路径 3 子功能 + SSE rationale)
- `.planning/ROADMAP.md` L113-115 (Phase 2.4 必含项 + 验收)
- `.planning/STATE.md` L601+ (Phase 2.4 Prereq Notes 5 项 backlog)
- ADR 0007 (categorization-schema-extension errata — 3 字段 additive sister) / ADR 0010 (installer-schema-extension errata — 6 install method 1:N closure sister) / ADR 0011 (execute-task-sdk-ralph 9 章节 errata sister) / ADR 0012 (extension-mvp-karpathy-inject 9 章节 errata sister)
